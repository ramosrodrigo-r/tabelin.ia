---
phase: 15-export-ux-migration-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(workspace)/workspace/layout.tsx
  - apps/web/src/features/unified-chat/unified-chat-tool.tsx
  - apps/web/tests/table-clarifier.test.ts
autonomous: false
requirements: [UNI-07]
must_haves:
  truths:
    - "A rota raiz /workspace NAO renderiza o ToolNav (bottomNav removido do mount raiz)"
    - "Os tools continuam alcancaveis: a Sidebar com links /workspace/sql, /workspace/regex etc. esta montada no workspace layout"
    - "buildTableSpec retorna spec deterministica e valida quando OPENAI_API_KEY esta ausente"
  artifacts:
    - path: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      provides: "Sidebar montada no layout do workspace (acesso aos tools pos-remocao do ToolNav)"
      contains: "Sidebar"
    - path: "apps/web/src/features/unified-chat/unified-chat-tool.tsx"
      provides: "ToolNav removido do mount raiz (prop bottomNav + import orfao)"
    - path: "apps/web/tests/table-clarifier.test.ts"
      provides: "Cobertura do branch fixture de buildTableSpec sem OPENAI_API_KEY"
      contains: "buildTableSpec"
  key_links:
    - from: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      to: "apps/web/src/components/app/sidebar.tsx"
      via: "import Sidebar + render"
      pattern: "Sidebar"
    - from: "apps/web/tests/table-clarifier.test.ts"
      to: "buildTableSpec fixture branch"
      via: "delete process.env.OPENAI_API_KEY + tableSpecPayloadSchema.safeParse"
      pattern: "OPENAI_API_KEY"
---

<objective>
Concluir a migracao de UX (success criterion 3): tornar a Sidebar o mecanismo de navegacao entre tools na raiz e remover o ToolNav do mount raiz do chat unificado — sem regredir o acesso aos tools. Tambem cobrir com teste o fixture fallback ja existente de buildTableSpec.

Purpose: O chat unificado ja e o entry point default (Phase 12). O que falta e tirar o ToolNav da raiz mantendo os tools acessiveis. ACHADO DE PLANEJAMENTO CRITICO: a Sidebar (apps/web/src/components/app/sidebar.tsx) ja tem links para todos os tools mas NAO esta montada em lugar nenhum — o ToolNav e hoje a unica navegacao visivel. Por isso, montar a Sidebar no layout e pre-requisito de remover o ToolNav (Pitfall 5 do RESEARCH). O fixture fallback ja existe (table-clarifier.ts:254); aqui so se garante cobertura de teste.
Output: layout com Sidebar montada, ToolNav removido do mount raiz, teste de fixture mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-export-ux-migration-hardening/15-CONTEXT.md
@.planning/phases/15-export-ux-migration-hardening/15-RESEARCH.md
@.planning/phases/15-export-ux-migration-hardening/15-PATTERNS.md

<interfaces>
<!-- Estado atual confirmado por grep durante o planejamento. -->

apps/web/src/components/app/sidebar.tsx:
  export function Sidebar(): JSX.Element
  Ja contem TOOLS com href: /workspace, /workspace/scripts, /workspace/sql,
  /workspace/regex, /workspace/templates, /workspace/file-analysis, /workspace/ocr.
  Usa usePathname() para data-active. NAO esta montada em nenhum layout atualmente.

apps/web/src/features/unified-chat/unified-chat-tool.tsx:
  linha 20:  import { ToolNav } from "@/components/app/tool-nav";   <- import a remover se orfao
  linha 455: bottomNav={<ToolNav />}                                <- prop a remover
  chat-input.tsx:18  bottomNav?: React.ReactNode  (opcional)
  chat-input.tsx:81  { bottomNav ? ... : null }   (guardado — remover a prop nao renderiza nada)

apps/web/src/app/(workspace)/workspace/layout.tsx:
  server component; monta WorkspaceShell > Topbar > main > div.workspace-center > {children}
  Sidebar NAO presente — adicionar.

apps/web/src/server/ai/table-clarifier.ts:
  export async function buildTableSpec(...): Promise<TableSpecPayload>
  linha 254: if (!process.env.OPENAI_API_KEY) { return { title: "Controle de Gastos", ...
             coluna formula "=SOMA(C{row};-D{row})" ... } }   <- branch a cobrir
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Montar Sidebar no workspace layout + remover ToolNav do mount raiz</name>
  <files>apps/web/src/app/(workspace)/workspace/layout.tsx, apps/web/src/features/unified-chat/unified-chat-tool.tsx</files>
  <read_first>
    - apps/web/src/app/(workspace)/workspace/layout.tsx (estrutura atual: WorkspaceShell > Topbar > main > workspace-center)
    - apps/web/src/components/app/sidebar.tsx (componente Sidebar pronto, com links de tools)
    - apps/web/src/features/unified-chat/unified-chat-tool.tsx (linhas 20 e 455)
    - apps/web/src/components/app/tool-nav.tsx (confirmar que ToolNav e Sidebar cobrem o mesmo conjunto de links — evitar duplicacao)
  </read_first>
  <action>
    (a) No layout: importar a Sidebar de @/components/app/sidebar e renderizar Sidebar dentro do WorkspaceShell, como irmao do bloco .workspace-page (a esquerda do conteudo), de modo que apareca em TODAS as rotas do workspace, inclusive a raiz. Manter Topbar, main e children intactos. Ajustar o wrapper (ex.: container flex horizontal) apenas o necessario para a Sidebar coexistir com o conteudo; reusar classes CSS existentes do shell se houver, caso contrario envolver em um container com layout horizontal.
    (b) No unified-chat-tool.tsx: remover a prop bottomNav={<ToolNav />} (linha 455). Se ToolNav nao tiver mais nenhum outro uso no arquivo, remover tambem o import orfao da linha 20. Apos a remocao, a contagem de ocorrencias de ToolNav no arquivo deve ser 0. Os tools permanecem acessiveis pela Sidebar montada em (a) e pelos deep links (/workspace/sql etc., rotas ja existentes).
  </action>
  <verify>
    <automated>cd apps/web && grep -q "Sidebar" "src/app/(workspace)/workspace/layout.tsx" && [ "$(grep -c 'ToolNav' src/features/unified-chat/unified-chat-tool.tsx)" = "0" ] && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx importa e renderiza Sidebar (grep "Sidebar" casa)
    - unified-chat-tool.tsx nao contem mais "ToolNav" (grep -c == 0): prop e import removidos
    - pnpm --filter web typecheck sai com exit 0 (sem import quebrado)
  </acceptance_criteria>
  <done>Sidebar montada no workspace layout; ToolNav removido do mount raiz; typecheck verde; tools acessiveis via sidebar + deep links.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cobrir fixture fallback de buildTableSpec sem OPENAI_API_KEY</name>
  <files>apps/web/tests/table-clarifier.test.ts</files>
  <read_first>
    - apps/web/tests/table-clarifier.test.ts (convencao de fixture-mode test: REAL_OPENAI_API_KEY, beforeEach delete process.env.OPENAI_API_KEY, afterAll restore — linhas 12-25; import de tableSpecPayloadSchema linha 10)
    - apps/web/src/server/ai/table-clarifier.ts (branch fixture linhas 254-281: title "Controle de Gastos", coluna formula "=SOMA(C{row};-D{row})")
  </read_first>
  <behavior>
    - Com OPENAI_API_KEY deletado, buildTableSpec retorna uma TableSpecPayload deterministica (nao lanca, nao chama OpenAI)
    - O retorno passa tableSpecPayloadSchema.safeParse (success === true)
    - O spec contem title "Controle de Gastos" e ao menos uma coluna com formula contendo "=SOMA"
  </behavior>
  <action>
    Se ja existir um describe cobrindo o fixture de buildTableSpec, apenas garantir os tres asserts de behavior acima; se nao existir, adicionar describe("buildTableSpec — fixture mode") reusando o mesmo padrao de isolamento de env var do arquivo (const REAL_OPENAI_API_KEY = process.env.OPENAI_API_KEY; beforeEach delete; afterAll restore condicional). Importar buildTableSpec de ../src/server/ai/table-clarifier e tableSpecPayloadSchema de @tabelin/shared (ja usado no arquivo). NAO reimplementar o fixture — apenas testar o branch existente. Nomes de teste em pt-BR. Ver memoria [[fixture-mode-sem-openai-key]].
  </action>
  <verify>
    <automated>cd apps/web && pnpm --filter web test -- table-clarifier 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter web test -- table-clarifier exit 0
    - O teste deleta OPENAI_API_KEY em beforeEach e restaura em afterAll
    - Ha assert de tableSpecPayloadSchema.safeParse(...).success === true sobre o retorno do fixture
  </acceptance_criteria>
  <done>Branch fixture de buildTableSpec coberto por teste verde; env var isolado e restaurado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao humana da navegacao pos-migracao</name>
  <files>(verificacao manual — nenhum arquivo modificado)</files>
  <what-built>Sidebar montada no workspace layout e ToolNav removido do mount raiz do chat unificado.</what-built>
  <action>Verificacao humana apos a automacao das Tasks 1-2 (nao substitui a automacao). Seguir os passos de how-to-verify.</action>
  <how-to-verify>
    1. Rodar pnpm --filter web dev e abrir http://localhost:3000/workspace
    2. Confirmar que o chat unificado aparece como entry point e que NAO ha mais o ToolNav (a barra de navegacao por aba) embaixo do input
    3. Confirmar que a Sidebar (Formula, Scripts, SQL, Regex, Templates, File Analysis, OCR) esta visivel
    4. Clicar em "SQL" na Sidebar -> deve navegar para /workspace/sql e abrir o tool de SQL
    5. Voltar para /workspace -> chat unificado novamente
  </how-to-verify>
  <verify><human-check>Navegacao confirmada: chat unificado na raiz sem ToolNav, Sidebar funcional, deep links OK</human-check></verify>
  <done>Usuario confirmou que /workspace mostra o chat sem ToolNav e que os tools sao alcancaveis pela Sidebar.</done>
  <resume-signal>Digite "approved" ou descreva o que esta faltando/quebrado na navegacao</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> rota /workspace | Mudanca de renderizacao client-side (qual nav exibir); nenhum input externo novo cruza fronteira |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Denial of Service (regressao de navegacao) | remocao do ToolNav sem alternativa | mitigate | Montar Sidebar no layout ANTES/junto da remocao do ToolNav; verify exige Sidebar presente e ToolNav ausente na mesma task (Pitfall 5). Deep links das rotas por-tool permanecem |
| T-15-07 | Information Disclosure | fixture fallback le OPENAI_API_KEY | accept | A fase nao altera nem renomeia a env var; apenas testa o branch existente. Fixture e deterministico, sem dado sensivel |
| T-15-SC | Tampering | npm/pnpm installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- pnpm --filter web typecheck verde (layout + unified-chat-tool sem import quebrado)
- grep -c ToolNav em unified-chat-tool.tsx == 0
- Sidebar presente no workspace layout
- pnpm --filter web test -- table-clarifier verde
- pnpm --filter web test (suite completa) verde
</verification>

<success_criteria>
- /workspace mostra o chat unificado SEM ToolNav, com a Sidebar dando acesso aos tools
- Deep links /workspace/sql, /workspace/regex etc. continuam funcionando
- Fixture fallback de buildTableSpec coberto por teste verde
</success_criteria>

<output>
Create `.planning/phases/15-export-ux-migration-hardening/15-03-SUMMARY.md` when done
</output>
