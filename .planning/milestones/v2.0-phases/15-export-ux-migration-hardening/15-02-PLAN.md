---
phase: 15-export-ux-migration-hardening
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/web/src/features/unified-chat/components/table-grid-panel.tsx
  - apps/web/tests/table-grid-panel.test.tsx
autonomous: true
requirements: [EXP-01, EXP-02]
must_haves:
  truths:
    - "Botão 'Exportar CSV' aparece no toolbar do TableGridPanel"
    - "Botão 'Exportar XLSX' aparece no toolbar do TableGridPanel"
    - "Clicar Exportar CSV chama buildCsv/downloadCsv com displayRows + columns atuais"
    - "Clicar Exportar XLSX chama buildXlsx/downloadXlsx com displayRows + columns atuais"
  artifacts:
    - path: "apps/web/src/features/unified-chat/components/table-grid-panel.tsx"
      provides: "Dois botões de export no slot reservado (linha 446), handlers que passam displayRows"
      contains: "buildCsv"
    - path: "apps/web/tests/table-grid-panel.test.tsx"
      provides: "Teste de presença + clique dos botões de export (download mockado)"
      contains: "Exportar"
  key_links:
    - from: "apps/web/src/features/unified-chat/components/table-grid-panel.tsx"
      to: "apps/web/src/features/unified-chat/lib/table-export.ts"
      via: "import { buildCsv, buildXlsx, downloadCsv, downloadXlsx }"
      pattern: "from.*lib/table-export"
    - from: "handlers de export"
      to: "displayRows (fórmulas calculadas)"
      via: "passar displayRows, nunca historyState.present.rows"
      pattern: "displayRows"
---

<objective>
Plugar a utilidade pura do Plan 01 em dois botões "Exportar CSV" / "Exportar XLSX" no toolbar do `TableGridPanel`, no slot já reservado (linha 446), e estender o teste de componente para cobrir presença e disparo.

Purpose: É a entrega visível de EXP-01/EXP-02 — o usuário clica e baixa o arquivo. Os handlers passam `displayRows` (valores calculados, Pitfall 3) e `historyState.present.columns` para os builders puros, e disparam o download. Separador CSV fixo em `;` por decisão de CONTEXT.
Output: edição em `table-grid-panel.tsx` + extensão de `table-grid-panel.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-export-ux-migration-hardening/15-PATTERNS.md
@.planning/phases/15-export-ux-migration-hardening/15-RESEARCH.md
@.planning/phases/15-export-ux-migration-hardening/15-01-SUMMARY.md

<interfaces>
<!-- Contratos da util criada no Plan 01 (Wave 1). Usar direto. -->

De apps/web/src/features/unified-chat/lib/table-export.ts:
```typescript
export function buildCsv(columns: TableColumn[], rows: RowData[]): string;
export function buildXlsx(columns: TableColumn[], rows: RowData[]): XLSX.WorkBook;
export function downloadCsv(content: string, filename: string): void;
export function downloadXlsx(wb: XLSX.WorkBook, filename: string): void;
```

Estado vivo do grid (table-grid-panel.tsx):
```typescript
// displayRows  — RowData[] com fórmulas CALCULADAS (linha 121, derivado de useFormulaEngine) → exportar ISTO
// historyState.present.columns — TableColumn[] (cabeçalho)
// spec.title   — usar como slug do arquivo (linha 420/422)
// slot de export: linhas 446-447  {/* Slot reservado para export Phase 15 */}
// botão analog (linhas 436-444): <button className="ghost-button" type="button" aria-label="..." onClick={...}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender teste de componente (RED) — botões de export</name>
  <files>apps/web/tests/table-grid-panel.test.tsx</files>
  <read_first>
    - apps/web/tests/table-grid-panel.test.tsx (convenção existente: @testing-library/react, fireEvent, SPEC_FIXTURE — linhas 1-37)
    - apps/web/src/features/unified-chat/components/table-grid-panel.tsx (toolbar linhas 425-447; displayRows linha 121)
  </read_first>
  <behavior>
    - Renderizar TableGridPanel com SPEC_FIXTURE mostra um botão com aria-label "Exportar CSV" e outro "Exportar XLSX"
    - Clicar "Exportar CSV" invoca o caminho de download CSV exatamente uma vez (downloadCsv mockado via vi.mock de ../src/features/unified-chat/lib/table-export)
    - Clicar "Exportar XLSX" invoca o caminho de download XLSX exatamente uma vez (downloadXlsx/XLSX.writeFile mockado)
  </behavior>
  <action>
    Estender o arquivo existente (não recriar). Adicionar `vi.mock("../src/features/unified-chat/lib/table-export", ...)` retornando spies para `buildCsv`/`buildXlsx`/`downloadCsv`/`downloadXlsx` — assim o teste não toca DOM/fs (Pitfall 4). Usar `screen.getByLabelText("Exportar CSV")` / `"Exportar XLSX"` e `fireEvent.click`. Assertar que o spy de download correspondente foi chamado 1x. Nomes de teste em pt-BR. Rodar e confirmar RED (botões ainda não existem).
  </action>
  <verify>
    <automated>cd apps/web && pnpm --filter web test -- table-grid-panel 2>&1 | grep -qiE "fail|Exportar|Unable to find" && echo "RED ok"</automated>
  </verify>
  <acceptance_criteria>
    - O teste referencia aria-labels "Exportar CSV" e "Exportar XLSX"
    - downloadCsv/downloadXlsx são mockados (sem efeito DOM real)
    - Suíte table-grid-panel está RED por ausência dos botões antes da Task 2
  </acceptance_criteria>
  <done>Teste de componente cobre presença + clique dos dois botões; falha por botões ausentes (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar botões Exportar CSV/XLSX ao toolbar (GREEN)</name>
  <files>apps/web/src/features/unified-chat/components/table-grid-panel.tsx</files>
  <read_first>
    - apps/web/src/features/unified-chat/components/table-grid-panel.tsx (slot linha 446; botão analog 436-444; displayRows 120-125; spec.title 420)
    - apps/web/src/features/unified-chat/lib/table-export.ts (assinaturas de buildCsv/buildXlsx/downloadCsv/downloadXlsx)
    - .planning/phases/15-export-ux-migration-hardening/15-PATTERNS.md (seção table-grid-panel: import a adicionar, padrão de botão)
  </read_first>
  <action>
    Adicionar `import { buildCsv, buildXlsx, downloadCsv, downloadXlsx } from "../lib/table-export";` (seguir estilo de import da linha 12). No slot reservado (linhas 446-447), substituir o spacer por dois `<button className="ghost-button" type="button">` com `aria-label="Exportar CSV"` e `aria-label="Exportar XLSX"` (manter o `table-grid-toolbar-spacer` antes ou depois conforme layout). Criar dois handlers (`useCallback`): `handleExportCsv` chama `downloadCsv(buildCsv(historyState.present.columns, displayRows), slug + ".csv")`; `handleExportXlsx` chama `downloadXlsx(buildXlsx(historyState.present.columns, displayRows), slug + ".xlsx")`. Passar SEMPRE `displayRows` (linha 121 — fórmulas calculadas), NUNCA `historyState.present.rows` (templates — Pitfall 3). Derivar `slug` de `spec.title` (normalizar para nome de arquivo seguro: minúsculas, espaços→`-`, remover acentos/chars inválidos). O separador CSV é `;` por decisão de CONTEXT (já fixo dentro de buildCsv — não passar override). Rodar a suíte até GREEN.
  </action>
  <verify>
    <automated>cd apps/web && pnpm --filter web test -- table-grid-panel 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test -- table-grid-panel` exit 0 (GREEN)
    - `grep -qE 'aria-label="Exportar CSV"' apps/web/src/features/unified-chat/components/table-grid-panel.tsx`
    - `grep -qE 'aria-label="Exportar XLSX"' apps/web/src/features/unified-chat/components/table-grid-panel.tsx`
    - `grep -q 'displayRows' apps/web/src/features/unified-chat/components/table-grid-panel.tsx` nos handlers de export (não historyState.present.rows)
    - `pnpm --filter web test` (suíte completa) verde
  </acceptance_criteria>
  <done>Dois botões de export no toolbar disparam download de displayRows; suíte completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler de UI → util de export → arquivo | O handler repassa o estado vivo do grid (input do usuário) ao gerador de arquivo; a fronteira de execução de fórmula é mitigada na util do Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering / Elevation of Privilege | handlers handleExportCsv/handleExportXlsx | mitigate | Reusam `buildCsv`/`buildXlsx` do Plan 01 que já sanitizam por célula (`'` prefix) e gravam `{t:"s"}`; o handler não reintroduz caminho não-sanitizado |
| T-15-05 | Information Disclosure | conteúdo exportado | mitigate | Exporta `displayRows` (o que o usuário já vê), estado efêmero client-side; nenhum dado vai ao servidor (export 100% browser, decisão de privacidade v2.0) |
| T-15-SC | Tampering | npm/pnpm installs | accept | Nenhum pacote novo; reuso de `xlsx@0.18.5` via util do Plan 01 |
</threat_model>

<verification>
- `pnpm --filter web test -- table-grid-panel` verde
- `pnpm --filter web test` (suíte completa) verde
- `pnpm --filter web typecheck` + `pnpm --filter web lint` sem erros
- Botões presentes com aria-labels corretos; handlers passam displayRows
</verification>

<success_criteria>
- Usuário vê "Exportar CSV" e "Exportar XLSX" no toolbar do grid
- Clicar dispara o download dos valores calculados atuais
- Nenhum import órfão; suíte completa verde
</success_criteria>

<output>
Create `.planning/phases/15-export-ux-migration-hardening/15-02-SUMMARY.md` when done
</output>
