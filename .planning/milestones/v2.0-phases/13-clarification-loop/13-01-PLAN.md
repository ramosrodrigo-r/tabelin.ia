---
phase: 13-clarification-loop
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/tests/table-clarifier.test.ts
  - apps/web/tests/unified-schema.test.ts
  - apps/web/src/features/unified-chat/components/clarification-card.tsx
  - apps/web/src/features/unified-chat/components/confirmation-card.tsx
autonomous: true
requirements: [CLAR-01, CLAR-02, CLAR-03, CLAR-04, CLAR-05]

must_haves:
  truths:
    - "Arquivo de teste table-clarifier.test.ts existe e executa sem erro de parse"
    - "Arquivo de teste unified-schema.test.ts tem cases para table_clar_question e table_spec"
    - "ClarificationCard exporta a função com props corretas (payload, onAnswer, onSkip)"
    - "ConfirmationCard exporta a função com props corretas (payload, onConfirm)"
    - "pnpm --filter web test suite passa sem regressão"
  artifacts:
    - path: "apps/web/tests/table-clarifier.test.ts"
      provides: "Testes de contrato para table-clarifier (fixture mode, pergunta única, spec final)"
      min_lines: 40
    - path: "apps/web/src/features/unified-chat/components/clarification-card.tsx"
      provides: "Componente stub ClarificationCard com interface tipada"
      exports: ["ClarificationCard"]
    - path: "apps/web/src/features/unified-chat/components/confirmation-card.tsx"
      provides: "Componente stub ConfirmationCard com interface tipada"
      exports: ["ConfirmationCard"]
  key_links:
    - from: "apps/web/tests/table-clarifier.test.ts"
      to: "apps/web/src/server/ai/table-clarifier.ts"
      via: "import (arquivo ainda inexistente — testes devem falhar com import error)"
      pattern: "table-clarifier"
    - from: "apps/web/src/features/unified-chat/components/clarification-card.tsx"
      to: "@tabelin/shared"
      via: "import type TableClarQuestionPayload"
      pattern: "TableClarQuestionPayload"
---

<objective>
Wave 0: criar os scaffolds de teste e os stubs de componentes que plans posteriores preencherão. Isso evita o anti-padrão "scavenger hunt" — o executor do Plan 02 recebe contratos já definidos.

Purpose: VALIDATION.md exige que estes arquivos existam antes da implementação para garantir feedback contínuo de testes a cada commit.
Output: 4 arquivos criados — 2 test scaffolds (falham graciosamente), 2 componentes stub (tipados, renderam sem erro).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-clarification-loop/13-RESEARCH.md
@.planning/phases/13-clarification-loop/13-PATTERNS.md
@.planning/phases/13-clarification-loop/13-VALIDATION.md
@.planning/phases/12-intent-classifier-unified-route/12-02-SUMMARY.md
@.planning/phases/12-intent-classifier-unified-route/12-03-SUMMARY.md

<interfaces>
<!-- Extraído de packages/shared/src/unified-chat/schema.ts (Phase 12) -->
<!-- Tipos ainda inexistentes que serão criados no Plan 02 — usar como contratos alvo -->

Tipos a serem exportados pelo Plan 02 de packages/shared/src/unified-chat/schema.ts:
  TableClarQuestionPayload = {
    kind: "table_clar_question";
    question: string;
    turnIndex: number;      // 0-based
    totalTurns: number;     // 2
    spec?: Record<string, unknown>;
    canSkip: boolean;
  }

  TableSpecPayload = {
    kind: "table_spec";
    title: string;
    columns: Array<{ name: string; type: string }>;
    rowCount: number;
    format?: string;
  }

Padrão de componente "use client" do projeto (de table-intent-stub.tsx):
  "use client";
  import { useState } from "react";
  import type { ... } from "@tabelin/shared";
  export function ComponentName({ ... }: PropsType) { ... }

Padrão CSS do projeto (de render-dispatcher.tsx):
  wrapper: className="assistant-card" aria-label="..."
  conteúdo: className="output-box" data-status="complete"
  botão secundário: className="ghost-button" type="button"
  header: className="output-header"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffolds de teste — table-clarifier e unified-schema extensions</name>
  <files>
    apps/web/tests/table-clarifier.test.ts,
    apps/web/tests/unified-schema.test.ts
  </files>
  <read_first>
    apps/web/tests/unified-route.test.ts (padrão de vi.hoisted mocks e estrutura describe/it),
    apps/web/tests/unified-schema.test.ts (arquivo existente a estender com novos cases),
    apps/web/tests/intent-classifier.test.ts (padrão de teste de fixture mode),
    apps/web/vitest.config.ts (config de teste),
    .planning/phases/13-clarification-loop/13-VALIDATION.md (mapa de requisitos → testes)
  </read_first>
  <behavior>
    table-clarifier.test.ts (novo arquivo — import vai falhar com "cannot find module" até Plan 02):
    - Descreve: "askClarificationQuestion — fixture mode" → quando OPENAI_API_KEY ausente, retorna string para turnIndex 0, string diferente para turnIndex 1
    - Descreve: "askClarificationQuestion — retorna exatamente uma pergunta" → resultado é string, não array
    - Descreve: "buildTableSpec — fixture mode" → retorna objeto com kind="table_spec", title string, columns array com pelo menos 1 elemento, rowCount >= 1
    - Descreve: "buildTableSpec — schema válido" → resultado passa em tableSpecPayloadSchema.safeParse (CLAR-01, CLAR-02)
    unified-schema.test.ts (estender arquivo existente):
    - Adicionar describe "tableClarQuestionPayloadSchema" → parse de payload válido com turnIndex=0, totalTurns=2, canSkip=true; rejeitar payload sem question
    - Adicionar describe "tableSpecPayloadSchema" → parse de payload válido com kind="table_spec"; rejeitar rowCount > 200
  </behavior>
  <action>
    Criar apps/web/tests/table-clarifier.test.ts seguindo o padrão de vi.hoisted de unified-route.test.ts.
    Mockar process.env.OPENAI_API_KEY como undefined nos casos de fixture.
    Importar askClarificationQuestion e buildTableSpec de "../src/server/ai/table-clarifier" (módulo ainda não existe — o import será resolvido no Plan 02; o arquivo de teste pode ser criado agora, os testes falharão com "cannot find module" até lá).
    Importar tableSpecPayloadSchema de "@tabelin/shared" (também inexistente até Plan 02 — o mesmo se aplica).
    Para unified-schema.test.ts: ler o arquivo existente completamente, adicionar os novos describes SEM remover nenhum existente, mantendo a estrutura de import existente do arquivo.
    Não usar fenced code blocks na action — a estrutura exata é derivada dos padrões documentados em PATTERNS.md e no campo behavior acima.
  </action>
  <verify>
    <automated>pnpm --filter web test -- tests/unified-schema.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/tests/table-clarifier.test.ts existe com pelo menos 4 describe blocks (fixture pergunta, unicidade, buildTableSpec fixture, schema válido)
    - apps/web/tests/unified-schema.test.ts tem ao menos um describe "tableClarQuestionPayloadSchema" e um "tableSpecPayloadSchema"
    - pnpm --filter web test -- tests/unified-schema.test.ts passa completamente (os novos cases já testam schemas que serão criados no Plan 02 — se os schemas ainda não existem, esses cases devem estar condicionados a skip ou o describe separado pode falhar graciosamente)
    - Sem regressão: pnpm --filter web test -- tests/unified-schema.test.ts retorna 0 failures nos tests existentes de schema
  </acceptance_criteria>
  <done>Scaffolds de teste existem; tests existentes de unified-schema não regrediram; table-clarifier.test.ts pronto para ser executado quando o módulo for criado no Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: Stubs de componentes — ClarificationCard e ConfirmationCard</name>
  <files>
    apps/web/src/features/unified-chat/components/clarification-card.tsx,
    apps/web/src/features/unified-chat/components/confirmation-card.tsx
  </files>
  <read_first>
    apps/web/src/features/unified-chat/components/table-intent-stub.tsx (padrão de componente "use client" mais simples),
    apps/web/src/features/unified-chat/components/render-dispatcher.tsx (padrão de CSS classes e estrutura JSX),
    .planning/phases/13-clarification-loop/13-PATTERNS.md (seções ClarificationCard e ConfirmationCard com estrutura JSX exata)
  </read_first>
  <action>
    Criar apps/web/src/features/unified-chat/components/clarification-card.tsx:
    - Diretiva "use client" no topo
    - Importar useState de "react"
    - Importar type TableClarQuestionPayload de "@tabelin/shared" (type-only import — o tipo ainda não existe; usar `// @ts-expect-error wave-0-stub` ou definir um tipo local temporário com os mesmos campos para evitar erro de TypeScript no CI — preferir tipo local temporário: `type TableClarQuestionPayload = { kind: "table_clar_question"; question: string; turnIndex: number; totalTurns: number; spec?: Record<string, unknown>; canSkip: boolean }`)
    - Props: payload: TableClarQuestionPayload, onAnswer: (answer: string) => void, onSkip: () => void
    - Estado local: answer via useState("")
    - JSX: div.assistant-card aria-label="Pergunta de clarificação", div.output-header com p.clarification-counter "Pergunta {turnIndex+1} de {totalTurns}", div.output-box data-status="complete" com p do payload.question, input controlado para resposta, button primary para confirmar resposta, button.ghost-button type="button" onClick={onSkip} "Gerar mesmo assim"
    - Exportar como named export ClarificationCard

    Criar apps/web/src/features/unified-chat/components/confirmation-card.tsx:
    - Diretiva "use client" no topo
    - Importar useState de "react"
    - Tipo local temporário: `type TableSpecPayload = { kind: "table_spec"; title: string; columns: Array<{ name: string; type: string }>; rowCount: number; format?: string }`
    - Props: payload: TableSpecPayload, onConfirm: (spec: TableSpecPayload) => void
    - Estado local: editedSpec via useState(payload)
    - JSX: div.assistant-card aria-label="Confirmar especificação da tabela", div.output-header com h2 "Confirme os detalhes da tabela", div.output-box data-status="complete" com: p para título (editável), lista de colunas (renderizar editedSpec.columns.map(c => c.name)), p para rowCount, button.ghost-button type="button" onClick={() => onConfirm(editedSpec)} "Confirmar e Gerar"
    - Exportar como named export ConfirmationCard

    Decisão (per planner_open_questions #3): streaming da pergunta de clarificação usa `complete` direto (mais simples no MVP), conforme recomendação do research. Confirmar no ConfirmationCard aciona novo POST com overrideGenerate=true (per planner_open_questions #1 — é a geração real, debita cota). Spec vazia + "Gerar mesmo assim" usa defaults do system prompt (per planner_open_questions #2).
  </action>
  <verify>
    <automated>pnpm --filter web typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/features/unified-chat/components/clarification-card.tsx existe e exporta ClarificationCard
    - apps/web/src/features/unified-chat/components/confirmation-card.tsx existe e exporta ConfirmationCard
    - Ambos os arquivos têm "use client" como primeira linha
    - Ambos os arquivos têm button com texto "Gerar mesmo assim" / "Confirmar e Gerar" respectivamente
    - pnpm --filter web typecheck passa sem erros novos (warnings em tipos temporários são aceitáveis se necessários)
    - grep -r "ClarificationCard" apps/web/src/features/unified-chat/components/clarification-card.tsx retorna ao menos 1 match (export)
    - grep -r "ConfirmationCard" apps/web/src/features/unified-chat/components/confirmation-card.tsx retorna ao menos 1 match (export)
  </acceptance_criteria>
  <done>ClarificationCard e ConfirmationCard existem com props tipadas e estrutura JSX correta; typecheck passa; Plan 04 pode importá-los diretamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→componente stub | Dados passados via props — apenas renderização; nenhuma chamada de rede neste plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | npm/pip install | accept | Phase 13 não instala novos pacotes — Package Legitimacy Audit marca como N/A na RESEARCH.md |
| T-13-01 | Tampering | clarification-card.tsx props | accept | Wave 0 — apenas stubs de renderização; sem lógica de negócio; sem chamadas de rede; mitigações aplicadas no Plan 04 (route) |
| T-13-02 | Tampering | confirmation-card.tsx props | accept | Wave 0 — estado local apenas; a re-validação server-side da spec editada é implementada no Plan 03 (route.ts) |

</threat_model>

<verification>
pnpm --filter web typecheck
pnpm --filter web test -- tests/unified-schema.test.ts
</verification>

<success_criteria>
- 4 arquivos criados: table-clarifier.test.ts, clarification-card.tsx, confirmation-card.tsx, e unified-schema.test.ts estendido
- TypeScript compila sem erros novos
- Testes de unified-schema existentes não regridem
- ClarificationCard tem props (payload, onAnswer, onSkip) e botão "Gerar mesmo assim"
- ConfirmationCard tem props (payload, onConfirm) e botão "Confirmar e Gerar"
</success_criteria>

<output>
Criar .planning/phases/13-clarification-loop/13-01-SUMMARY.md quando concluído
</output>
