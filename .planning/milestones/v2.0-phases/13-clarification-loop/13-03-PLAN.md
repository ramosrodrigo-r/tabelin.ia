---
phase: 13-clarification-loop
plan: 03
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - apps/web/src/app/api/chat/unified/route.ts
  - apps/web/tests/unified-route.test.ts
autonomous: true
requirements: [CLAR-01, CLAR-02, CLAR-03, CLAR-05]

must_haves:
  truths:
    - "POST /api/chat/unified com intent=tabela e clarTurnCount=0 emite complete com kind=table_clar_question"
    - "POST /api/chat/unified com intent=tabela e clarTurnCount=0 chama releaseToolUse e NÃO chama confirmToolUse"
    - "POST /api/chat/unified com intent=tabela e clarTurnCount>=2 emite complete com kind=table_spec (generation path)"
    - "POST /api/chat/unified com intent=tabela e overrideGenerate=true emite complete com kind=table_spec (escape hatch)"
    - "POST /api/chat/unified com intent=tabela e clarTurnCount>=2 chama confirmToolUse (debita cota)"
    - "clarTurnCount é derivado do histórico PostgreSQL — campo client-side ignorado"
    - "overrideGenerate validado no servidor com z.literal('true').optional()"
    - "specOverride (spec editada do client) lida do body via asString e re-validada com tableSpecPayloadSchema.safeParse"
  artifacts:
    - path: "apps/web/src/app/api/chat/unified/route.ts"
      provides: "Bifurcação do case unified_table em clarification path e generation path"
      contains: "releaseToolUse"
    - path: "apps/web/tests/unified-route.test.ts"
      provides: "Tests de CLAR-01, CLAR-02, CLAR-03, CLAR-05 (clarification e generation paths)"
      contains: "confirmToolUse"
  key_links:
    - from: "apps/web/src/app/api/chat/unified/route.ts"
      to: "apps/web/src/server/ai/table-clarifier.ts"
      via: "import { askClarificationQuestion, buildTableSpec }"
      pattern: "table-clarifier"
    - from: "apps/web/src/app/api/chat/unified/route.ts"
      to: "apps/web/src/server/usage/quota-service.ts"
      via: "releaseToolUse no clarification path, confirmToolUse no generation path"
      pattern: "releaseToolUse"
    - from: "apps/web/tests/unified-route.test.ts"
      to: "route.ts mock de confirmToolUse"
      via: "expect(routeMocks.confirmToolUse).not.toHaveBeenCalled()"
      pattern: "not.toHaveBeenCalled"
---

<objective>
Wave 2: bifurcar o case `unified_table` em `route.ts` — o coração da Phase 13. Implementar os dois sub-caminhos determinísticos (clarification path e generation path) com quota correta, contagem de turns via PostgreSQL e `overrideGenerate`/`specOverride` validados. Cobrir CLAR-05 com assertions negativas nos tests.

Purpose: Este plano implementa a lógica de servidor que garante que a cota nunca é debitada em turns de clarificação (CLAR-05 — maior risco de correção da fase). A geração da spec final (ConfirmationCard) é o único ponto de débito.
Output: route.ts bifurcado com logic determinística; tests cobrindo todos os cenários de quota (calls positivos e negativos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-clarification-loop/13-RESEARCH.md
@.planning/phases/13-clarification-loop/13-PATTERNS.md
@.planning/phases/12-intent-classifier-unified-route/12-02-SUMMARY.md

<interfaces>
<!-- Extraído de apps/web/src/app/api/chat/unified/route.ts (Phase 12) -->
Estrutura do case unified_table atual (linhas ~526-558) — ESTE É O PONTO EXATO DE BIFURCAÇÃO:
  case "unified_table": {
    await findConversationExchanges(user.id, "unified_table");
    const payload = tableStubPayloadSchema.parse({
      kind: "table_stub", originalPrompt: promptResult.prompt, message: tableStubMessage(),
    });
    await confirmToolUse(quotaCheck.reservationKey); // ← bifurcar ANTES deste ponto
    await recordToolRequest({ userId: user.id, toolKind: "unified_table", mode: GENERATE_MODE, ... });
    await saveConversationExchange({ userId: user.id, toolKind: "unified_table", ... });
    return responseFromStream(createEventStream([intentEvent(classification), { type: "complete", payload }]));
  }

Padrão de releaseToolUse já em uso (needs_file, linhas ~314-316):
  await releaseToolUse(quotaCheck.reservationKey);
  return responseFromStream(needsFileStream(classification, classification.intent));

Padrão de asString para campos do body (linhas ~87-103):
  function asString(value: unknown) { return typeof value === "string" ? value : undefined; }
  overrideIntent: asString(input.overrideIntent),

Padrão de readObjectFields (para body JSON):
  function readObjectFields(input: Record<string, unknown>): UnifiedFields {
    return { ..., overrideIntent: asString(input.overrideIntent), ... }
  }

Tipo UnifiedFields (inferido do código existente):
  { prompt, overrideIntent?, platform, formulaLanguage, separator, sqlDialect, scriptType, lastIntent? }
  Phase 13: adicionar overrideGenerate?: string
  Phase 13: adicionar specOverride?: string  (spec editada serializada do ConfirmationCard — campo DEDICADO no body, separado de prompt; enviada pelo Plan 04 handleConfirmSpec)

Padrão de saveConversationExchange usado no case unified_table:
  await saveConversationExchange({ userId, toolKind: "unified_table", mode, userPrompt, assistantPayload });

Padrão de intentEvent:
  function intentEvent(classification): UnifiedStreamEvent { return { type: "intent_detected", ... }; }

<!-- Extraído de apps/web/tests/unified-route.test.ts (Phase 12) -->
Padrão de vi.hoisted mocks existente:
  const routeMocks = vi.hoisted(() => ({
    confirmToolUse: vi.fn(),
    releaseToolUse: vi.fn(),
    reserveToolUse: vi.fn().mockResolvedValue({ ...quotaFixture }),
    findConversationExchanges: vi.fn().mockResolvedValue([]),
    saveConversationExchange: vi.fn(),
    recordToolRequest: vi.fn(),
    // ... outros
  }));

Cenário existente de table_stub (Phase 12) — NÃO regredir:
  describe block "unified_table — table_stub" com test cujo nome contém "table_stub"
  (alvo do smoke de regressão da Task 1: `-t "table_stub"`)

Padrão de assertion CLAR-05 (crítico — assertion negativa):
  expect(routeMocks.confirmToolUse).not.toHaveBeenCalled();
  expect(routeMocks.releaseToolUse).toHaveBeenCalledOnce();

<!-- De quota-service.ts (padrão estabelecido Phase 2) -->
Assinaturas:
  releaseToolUse(reservationKey: string): Promise<void>
  confirmToolUse(reservationKey: string): Promise<void>
  reserveToolUse(userId, toolKind, mode): Promise<QuotaCheck & { reservationKey: string }>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bifurcar case unified_table — clarification path e generation path</name>
  <files>
    apps/web/src/app/api/chat/unified/route.ts
  </files>
  <read_first>
    apps/web/src/app/api/chat/unified/route.ts (arquivo completo — ler antes de qualquer modificação; especialmente linhas 80-120 de readObjectFields/readFormString, linhas 280-320 do needs_file path, e linhas 520-560 do case unified_table),
    apps/web/src/server/ai/table-clarifier.ts (criado no Plan 02 — importar askClarificationQuestion e buildTableSpec),
    apps/web/src/server/usage/quota-service.ts (confirmar assinatura de releaseToolUse e confirmToolUse),
    apps/web/src/server/tools/conversation-repository.ts (confirmar assinatura de findConversationExchanges e saveConversationExchange),
    packages/shared/src/unified-chat/schema.ts (confirmar tableSpecPayloadSchema — usado no safeParse de specOverride),
    .planning/phases/13-clarification-loop/13-RESEARCH.md (Padrão 1 — countClarTurns e mergeSpecFromHistory, Padrão 2 — quota release imediato, Armadinhas 1-5)
  </read_first>
  <behavior>
    clarification path (clarTurnCount < 2, sem overrideGenerate):
    - releaseToolUse chamado IMEDIATAMENTE (antes do LLM)
    - askClarificationQuestion chamado com (prompt, collectedSpec, clarTurnCount)
    - saveConversationExchange com assistantPayload.kind="table_clar_question", turnIndex=clarTurnCount, totalTurns=2, canSkip=true
    - resposta NDJSON: [intentEvent, { type: "complete", payload: { kind: "table_clar_question", question, turnIndex, totalTurns: 2, canSkip: true, spec: collectedSpec } }]
    - confirmToolUse NÃO é chamado

    generation path (clarTurnCount >= 2 OU overrideGenerate="true"):
    - confirmToolUse chamado (debita cota)
    - resolução da spec:
        - se specOverride presente no body: tableSpecPayloadSchema.safeParse(JSON.parse(specOverride)); se success → usar essa spec validada
        - se specOverride ausente OU safeParse falhar OU JSON.parse lançar → cair nos defaults: buildTableSpec(prompt, tableHistory) (reconstrói do histórico / defaults razoáveis)
    - saveConversationExchange com assistantPayload.kind="table_spec"
    - resposta NDJSON: [intentEvent, { type: "complete", payload: tableSpec }]

    overrideGenerate:
    - lido do body com asString(input.overrideGenerate)
    - validado no servidor: só ativa generation path se valor === "true"
    - NÃO confiar no valor numérico de clarTurnCount do body (derivar apenas do histórico)

    specOverride (campo dedicado, enviado pelo Plan 04 handleConfirmSpec):
    - lido do body com asString(input.specOverride) — NÃO derivado de prompt
    - mitigação de Tampering: re-validado server-side com tableSpecPayloadSchema.safeParse antes de qualquer uso
    - se ausente ou inválido → defaults (não confiar no client)

    fallback de histórico (armadilha 2):
    - se findConversationExchanges retornar [] E o prompt parecer resposta (heurística: não contém "tabela" ou similar), tratar conservadoramente como clarTurnCount = MAX_CLAR_TURNS (gerar com defaults)
    - implementar função local conservativeClarTurnCount(history, prompt, MAX): se history.length === 0 e !promptLooksLikeNewTableRequest(prompt), retornar MAX; senão retornar countClarTurns(history)
  </behavior>
  <action>
    Ler route.ts completo. Identificar e modificar:

    1. readObjectFields / readFormString: adicionar overrideGenerate: asString(input.overrideGenerate) E specOverride: asString(input.specOverride) ao objeto retornado e ao tipo UnifiedFields (ou type local de campos). specOverride é um campo PRÓPRIO do body (não sobrescreve prompt).

    2. Adicionar helpers locais no arquivo APÓS as funções existentes (antes do case unified_table):
       - countClarTurns(history: ConversationExchange[]): number — filter por assistantPayload.kind === "table_clar_question"
       - mergeSpecFromHistory(history: ConversationExchange[]): Record<string, unknown> — pega a spec mais recente de qualquer turn com kind "table_clar_question"
       - promptLooksLikeNewTableRequest(prompt: string): boolean — retorna true se prompt contém "tabela" (accent-insensitive); usado no fallback conservativo
       - conservativeClarTurnCount(history, prompt, MAX): number — como descrito no behavior
       - resolveOverrideSpec(specOverride: string | undefined): TableSpecPayload | null — JSON.parse em try/catch, tableSpecPayloadSchema.safeParse; retorna a spec validada ou null se ausente/inválida

    3. Adicionar imports no topo do arquivo:
       - import { askClarificationQuestion, buildTableSpec } from "../../../server/ai/table-clarifier" (ajustar path relativo conforme estrutura real)
       - import { tableSpecPayloadSchema } from "@tabelin/shared" se não já importado
       - import type { TableSpecPayload } from "@tabelin/shared" se não já importado

    4. Substituir o case "unified_table" inteiro pela lógica bifurcada:
       - Ler tableHistory via findConversationExchanges(user.id, "unified_table")
       - Calcular clarTurnCount com conservativeClarTurnCount
       - Derivar collectedSpec com mergeSpecFromHistory
       - Verificar overrideGenerate: fields.overrideGenerate === "true"
       - shouldGenerate = clarTurnCount >= MAX_CLAR_TURNS || overrideGenerate
       - if (!shouldGenerate) → CLARIFICATION PATH (releaseToolUse imediato, askClarificationQuestion, save, return)
       - else → GENERATION PATH (confirmToolUse; const overrideSpec = resolveOverrideSpec(fields.specOverride); const tableSpec = overrideSpec ?? buildTableSpec(prompt, tableHistory); save, return)

    MAX_CLAR_TURNS = 2 como constante no topo do bloco case.

    Decisão (per planner_open_questions #1): click em "Confirmar" no ConfirmationCard aciona um novo POST com overrideGenerate="true" + specOverride (spec editada no campo dedicado do body, NÃO em prompt) → o servidor detecta overrideGenerate, segue generation path, reserva + confirma cota (fluxo uniforme e testável). A spec do client (fields.specOverride) é re-validada com tableSpecPayloadSchema.safeParse antes de usar — se ausente/falhar, usar buildTableSpec (spec reconstruída do histórico).

    Decisão (per planner_open_questions #2): quando spec vazia (turno 1, "Gerar mesmo assim"), specOverride é undefined e buildTableSpec recebe spec vazia; os defaults razoáveis (10 linhas, 2 colunas genéricas) estão no system prompt de buildSpecSystemPrompt em table-clarifier.ts.

    Decisão (per planner_open_questions #3): respostas de clarificação usam `complete` direto (sem streaming delta) — simplidade de MVP; intent_detected pill é emitido antes da chamada LLM para feedback imediato.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck 2>&1 | tail -5</automated>
    <automated>pnpm --filter web test -- tests/unified-route.test.ts -t "table_stub" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "releaseToolUse" apps/web/src/app/api/chat/unified/route.ts retorna ao menos 2 matches (needs_file existente + novo clarification path)
    - grep "table_clar_question" apps/web/src/app/api/chat/unified/route.ts retorna ao menos 2 matches (kind no payload + kind no assistantPayload)
    - grep "table_spec" apps/web/src/app/api/chat/unified/route.ts retorna ao menos 2 matches (kind no payload + assistantPayload)
    - grep "MAX_CLAR_TURNS" apps/web/src/app/api/chat/unified/route.ts retorna ao menos 1 match
    - grep "overrideGenerate" apps/web/src/app/api/chat/unified/route.ts retorna ao menos 2 matches (leitura do campo + uso na lógica)
    - grep "specOverride" apps/web/src/app/api/chat/unified/route.ts retorna ao menos 2 matches (leitura do body via asString + uso no resolveOverrideSpec/safeParse)
    - grep "tableSpecPayloadSchema" apps/web/src/app/api/chat/unified/route.ts retorna ao menos 1 match (re-validação server-side da spec do client)
    - pnpm --filter web typecheck passa sem erros novos
    - pnpm --filter web test -- tests/unified-route.test.ts -t "table_stub" passa (smoke de regressão: a bifurcação não quebrou o cenário pré-existente)
  </acceptance_criteria>
  <done>route.ts bifurcado com os dois sub-caminhos determinísticos; quota correta em cada path; overrideGenerate validado server-side; specOverride lida do campo dedicado e re-validada com safeParse; fallback conservativo implementado; cenário table_stub pré-existente continua verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests de rota — CLAR-01..05 com assertions negativas de confirmToolUse</name>
  <files>
    apps/web/tests/unified-route.test.ts
  </files>
  <read_first>
    apps/web/tests/unified-route.test.ts (arquivo completo — ler antes de modificar; especialmente padrão de vi.hoisted, helper de execução de request, e cases existentes de table_stub),
    apps/web/src/app/api/chat/unified/route.ts (implementação criada na Task 1 — para confirmar assinaturas exatas dos mocks necessários e o nome do campo specOverride no body),
    .planning/phases/13-clarification-loop/13-VALIDATION.md (mapa completo de requisitos → testes, linhas 39-53)
  </read_first>
  <behavior>
    Cenário A (CLAR-01 + CLAR-05): clarTurnCount=0, sem overrideGenerate → complete.kind="table_clar_question", releaseToolUse.called, confirmToolUse.NOT.called
    Cenário B (CLAR-02 + CLAR-05): clarTurnCount=2 → complete.kind="table_spec", confirmToolUse.called, releaseToolUse.NOT.called (neste path)
    Cenário C (CLAR-03 + CLAR-05): clarTurnCount=0, overrideGenerate="true" → complete.kind="table_spec", confirmToolUse.called
    Cenário D (CLAR-02): clarTurnCount=1 → complete.kind="table_clar_question", turnIndex=1, totalTurns=2
    Cenário E (CLAR-05 regressão): todos os cenários A,D devem ter expect(confirmToolUse).not.toHaveBeenCalled()
    Cenário F (armadilha 2 — fallback conservativo): findConversationExchanges retorna [] com prompt que parece resposta (sem "tabela") → complete.kind="table_spec" (geração com defaults)
    Cenário G (CLAR-05 — overrideGenerate com specOverride editada): specOverride no body passa safeParse → generation path usa spec do client; specOverride inválida/ausente → generation path usa spec reconstruída do histórico (buildTableSpec)
  </behavior>
  <action>
    Ler unified-route.test.ts completo. Localizar o describe block referente ao case "table_stub" (existente). Adicionar um novo describe block "unified_table — clarification loop" APÓS o bloco existente de table_stub.

    No vi.hoisted existente, verificar se os mocks de releaseToolUse e findConversationExchanges já estão definidos. Se findConversationExchanges não existir nos mocks, adicioná-lo.

    Implementar os 7 cenários descritos no behavior:
    - Para cenário A: mockar findConversationExchanges retornando [] (0 turns), fazer POST com intent="tabela" simulado; assertar complete event tem kind="table_clar_question"; assertar releaseToolUse.toHaveBeenCalledOnce(); assertar confirmToolUse.not.toHaveBeenCalled()
    - Para cenário B: mockar findConversationExchanges retornando array com 2 exchanges de table_clar_question; assertar complete event tem kind="table_spec"; assertar confirmToolUse.toHaveBeenCalledOnce()
    - Para cenário G: enviar specOverride no body (campo dedicado). Caso válido: assertar que o complete payload reflete a spec do client (ex.: título editado). Caso inválido: assertar fallback para buildTableSpec.
    - Para cenários C, D, E, F: seguir o mesmo padrão de mock + request + assertion

    Helper para ler eventos NDJSON da resposta: reutilizar o padrão já existente no arquivo (linhas que leem body da response e fazem JSON.parse linha por linha).

    NÃO remover nenhum test existente. Todos os tests de table_stub existentes devem continuar passando — o antigo behavior do table_stub pode ter sido substituído pelo generation path; adaptar os mocks se necessário para que os tests existentes ainda sejam válidos (ou marcar como "updated behavior" com comentário).
  </action>
  <verify>
    <automated>pnpm --filter web test -- tests/unified-route.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter web test -- tests/unified-route.test.ts passa sem falhas
    - grep "not.toHaveBeenCalled.*confirm\|confirmToolUse.*not" apps/web/tests/unified-route.test.ts retorna ao menos 2 matches (assertions negativas de CLAR-05 em ao menos 2 cenários)
    - grep "table_clar_question" apps/web/tests/unified-route.test.ts retorna ao menos 3 matches (3 scenarios testados)
    - grep "table_spec" apps/web/tests/unified-route.test.ts retorna ao menos 2 matches (2 scenarios de generation path)
    - grep "specOverride" apps/web/tests/unified-route.test.ts retorna ao menos 1 match (cenário G — spec editada do client)
    - grep "releaseToolUse" apps/web/tests/unified-route.test.ts retorna ao menos 2 matches (assertions positivas)
    - pnpm --filter web test -- tests/unified-route.test.ts mostra zero failures nos tests existentes (sem regressão)
  </acceptance_criteria>
  <done>Tests cobrem todos os cenários CLAR-01..05 incluindo assertions negativas de confirmToolUse em cada path de clarificação e o cenário de specOverride; suite completa passa sem regressões.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /api/chat/unified | overrideGenerate e specOverride (spec editada) chegam do browser — validados server-side |
| route.ts→PostgreSQL | clarTurnCount derivado do banco — não do body do request |
| route.ts→quota-service | releaseToolUse em clarification path, confirmToolUse em generation path — dois caminhos mutuamente exclusivos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | npm installs | accept | Phase 13 não instala pacotes novos |
| T-13-07 | Elevation | overrideGenerate bypass | mitigate | overrideGenerate lido via asString() e comparado literalmente a "true"; não confiar em booleano do client; validado com z.literal("true").optional() ou comparação === "true" |
| T-13-08 | Elevation | clarTurnCount manipulado no body | mitigate | clarTurnCount derivado exclusivamente de findConversationExchanges(userId, "unified_table") — campo client-side não existe na lógica do servidor |
| T-13-09 | Spoofing/Abuse | quota em clarification turn | mitigate | releaseToolUse chamado IMEDIATAMENTE ao entrar no clarification path, antes de qualquer LLM call; testado com expect(confirmToolUse).not.toHaveBeenCalled() em todos os cenários de clarificação |
| T-13-10 | Tampering | spec editada (campo specOverride) no ConfirmationCard | mitigate | specOverride lida via asString(input.specOverride) do campo dedicado do body; JSON.parse em try/catch e re-validada com tableSpecPayloadSchema.safeParse antes de usar; se ausente/inválida → buildTableSpec reconstrói do histórico PostgreSQL |
| T-13-11 | Tampering | prompt injection via respostas do usuário | mitigate | spec coletada injetada no prompt via injectCollectedSpecIntoPrompt com delimitadores anti-injection (padrão context-messages.ts) — implementado em table-clarifier.ts (Plan 02) |

</threat_model>

<verification>
pnpm --filter web test -- tests/unified-route.test.ts
pnpm --filter web typecheck
</verification>

<success_criteria>
- route.ts tem dois sub-caminhos mutuamente exclusivos no case unified_table
- Clarification path: releaseToolUse chamado antes do LLM, confirmToolUse nunca chamado
- Generation path: confirmToolUse chamado, tableSpecPayload emitido
- clarTurnCount derivado exclusivamente de PostgreSQL
- overrideGenerate validado server-side
- specOverride lida do campo dedicado do body e re-validada com safeParse antes de usar
- Cenário table_stub pré-existente continua verde (regressão)
- Tests cobrem 7 cenários incluindo assertions negativas (CLAR-05)
- Suite completa passa sem regressões
</success_criteria>

<output>
Criar .planning/phases/13-clarification-loop/13-03-SUMMARY.md quando concluído
</output>
