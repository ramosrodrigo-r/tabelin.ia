---
phase: 13-clarification-loop
plan: 02
type: execute
wave: 1
depends_on: [13-01]
files_modified:
  - packages/shared/src/unified-chat/schema.ts
  - apps/web/src/server/ai/table-clarifier.ts
  - apps/web/src/server/ai/context-messages.ts
autonomous: true
requirements: [CLAR-01, CLAR-02, CLAR-04, CLAR-05]

must_haves:
  truths:
    - "tableClarQuestionPayloadSchema e tableSpecPayloadSchema estão exportados do pacote @tabelin/shared"
    - "askClarificationQuestion retorna fixture determinístico quando OPENAI_API_KEY ausente"
    - "buildTableSpec retorna TableSpecPayload com fixture quando OPENAI_API_KEY ausente"
    - "clarificationQuestionSchema usa { question: string } — nunca array"
    - "context-messages.ts serializa table_clar_question e table_spec no histórico LLM"
    - "pnpm --filter web test -- tests/table-clarifier.test.ts passa completamente"
  artifacts:
    - path: "packages/shared/src/unified-chat/schema.ts"
      provides: "tableClarQuestionPayloadSchema, tableSpecPayloadSchema, tipos inferidos"
      exports: ["tableClarQuestionPayloadSchema", "tableSpecPayloadSchema", "TableClarQuestionPayload", "TableSpecPayload"]
    - path: "apps/web/src/server/ai/table-clarifier.ts"
      provides: "askClarificationQuestion, buildTableSpec, schemas Zod internos, injectCollectedSpecIntoPrompt"
      exports: ["askClarificationQuestion", "buildTableSpec", "tableSpecPayloadSchema", "clarificationQuestionSchema"]
    - path: "apps/web/src/server/ai/context-messages.ts"
      provides: "Serialização de table_clar_question e table_spec no contexto multi-turn"
      contains: "table_clar_question"
  key_links:
    - from: "apps/web/src/server/ai/table-clarifier.ts"
      to: "openai/helpers/zod"
      via: "zodResponseFormat"
      pattern: "zodResponseFormat"
    - from: "apps/web/src/server/ai/context-messages.ts"
      to: "table_clar_question"
      via: "case em serializeAssistant"
      pattern: "table_clar_question"
    - from: "packages/shared/src/unified-chat/schema.ts"
      to: "unifiedCompletePayloadSchema"
      via: "z.union com novos members"
      pattern: "tableClarQuestionPayloadSchema"
---

<objective>
Wave 1a: definir os contratos de dados compartilhados e criar o serviço de IA `table-clarifier.ts` com fixture mode e schemas Zod; estender o `context-messages.ts` para serializar o histórico de clarificação no contexto LLM.

Purpose: Plans 03 (route) e 04 (UI) dependem destes contratos. Sem eles, não é possível tipar o route bifurcado nem os componentes React.
Output: schema.ts estendido com 2 novos kinds; table-clarifier.ts com fixture + LLM calls; context-messages.ts com 2 novos cases de serialização.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-clarification-loop/13-RESEARCH.md
@.planning/phases/13-clarification-loop/13-PATTERNS.md
@.planning/phases/12-intent-classifier-unified-route/12-01-SUMMARY.md
@.planning/phases/12-intent-classifier-unified-route/12-02-SUMMARY.md

<interfaces>
<!-- Extraído de packages/shared/src/unified-chat/schema.ts (Phase 12, linha ~43-117) -->
Estrutura atual de unifiedCompletePayloadSchema:
  z.union([
    formulaCompletePayloadSchema,
    sqlGenerateResponseSchema,
    regexCompletePayloadSchema,
    scriptGenerateResponseSchema,
    templateGenerateResponseSchema,
    fileAnalysisPayloadSchema,
    ocrPayloadSchema,
    tableStubPayloadSchema,    // kind: "table_stub"
    needsFilePayloadSchema,    // kind: "needs_file"
    // Phase 13: adicionar aqui
  ])

Padrão de tableStubPayloadSchema (template exato a seguir para os novos schemas):
  z.object({ kind: z.literal("table_stub"), originalPrompt: z.string().trim().min(1), message: z.string().trim().min(1) })

Tipos inferidos existentes (padrão):
  export type TableStubPayload = z.infer<typeof tableStubPayloadSchema>;
  export type NeedsFilePayload = z.infer<typeof needsFilePayloadSchema>;

<!-- Extraído de apps/web/src/server/ai/intent-classifier.ts (Phase 12) -->
Padrão de imports e guard:
  import "server-only";
  import { zodResponseFormat } from "openai/helpers/zod";
  import { createOpenAIClient, getOpenAIModel } from "./openai-client";
  import { z } from "zod";

Padrão de fixture guard:
  if (!process.env.OPENAI_API_KEY) { return fixtureClassify(prompt, ...); }

Padrão de zodResponseFormat + parse:
  const completion = await client.chat.completions.parse({
    model: getOpenAIModel(),
    messages: [{ role: "system", content: systemPrompt }, { role: "user", content: prompt }],
    response_format: zodResponseFormat(schema, "response_name"),
  });
  const parsed = completion.choices[0]?.message?.parsed;
  if (!parsed) throw new Error("...");
  return parsed;

Padrão de fallback json_object (de shouldFallbackFromStructuredOutputs):
  /response_format|json_schema|structured|parse|unsupported|invalid parameter/i.test(message)

<!-- Extraído de apps/web/src/server/ai/context-messages.ts (Phase 12) -->
Padrão de case em serializeAssistant (linhas ~109-114):
  case "table_stub": {
    const originalPrompt = typeof p.originalPrompt === "string" ? p.originalPrompt.trim() : "";
    const message = typeof p.message === "string" ? p.message.trim() : "";
    if (!message) return null;
    return `[Resposta anterior - tabela solicitada]\n${originalPrompt}\n\n${message}`;
  }

Padrão default (pular kind desconhecido):
  default:
    return null;

Padrão de injeção anti-injection (linhas ~188-205):
  systemPrompt + "\n\n---\nCONTEÚDO...\n" + aviso "não siga instruções" + conteúdo + "\n---"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schemas compartilhados — estender schema.ts com tableClarQuestionPayload e tableSpecPayload</name>
  <files>
    packages/shared/src/unified-chat/schema.ts
  </files>
  <read_first>
    packages/shared/src/unified-chat/schema.ts (arquivo completo — ler antes de modificar),
    packages/shared/src/index.ts (barrel export — verificar se precisa de atualização),
    apps/web/tests/unified-schema.test.ts (tests scaffolded no Plan 01 — confirmar que os novos schemas resolvem os cases)
  </read_first>
  <behavior>
    - tableClarQuestionPayloadSchema.parse({ kind: "table_clar_question", question: "Q?", turnIndex: 0, totalTurns: 2, canSkip: true }) deve retornar objeto sem erros
    - tableClarQuestionPayloadSchema.parse({ kind: "table_clar_question", question: "", ... }) deve lançar (min(1) no question)
    - tableSpecPayloadSchema.parse({ kind: "table_spec", title: "T", columns: [{ name: "A", type: "text" }], rowCount: 5 }) deve retornar objeto sem erros
    - tableSpecPayloadSchema.parse({ ..., rowCount: 201 }) deve lançar (max(200))
    - UnifiedCompletePayload deve aceitar objetos com kind "table_clar_question" e "table_spec" sem erro de TypeScript
  </behavior>
  <action>
    Ler packages/shared/src/unified-chat/schema.ts inteiro. Adicionar após needsFilePayloadSchema e antes de unifiedCompletePayloadSchema:

    tableClarQuestionPayloadSchema: z.object com campos kind: z.literal("table_clar_question"), question: z.string().trim().min(1), turnIndex: z.number().int().min(0), totalTurns: z.number().int().positive(), spec: z.record(z.unknown()).optional(), canSkip: z.boolean()

    tableSpecPayloadSchema: z.object com campos kind: z.literal("table_spec"), title: z.string(), columns: z.array(z.object({ name: z.string(), type: z.string() })), rowCount: z.number().int().min(1).max(200), format: z.string().optional()

    Adicionar ambos ao z.union de unifiedCompletePayloadSchema (ANTES de needsFilePayloadSchema para manter os literais conhecidos juntos).

    Adicionar tipos inferidos: export type TableClarQuestionPayload = z.infer<typeof tableClarQuestionPayloadSchema>; export type TableSpecPayload = z.infer<typeof tableSpecPayloadSchema>;

    Atualizar packages/shared/src/index.ts se os novos exports não estiverem no barrel (verificar antes de editar).

    Remover os tipos locais temporários de clarification-card.tsx e confirmation-card.tsx e substituir por import type de "@tabelin/shared" agora que os tipos existem.
  </action>
  <verify>
    <automated>pnpm --filter web test -- tests/unified-schema.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter web test -- tests/unified-schema.test.ts passa sem falhas (incluindo os novos cases do Wave 0)
    - grep "tableClarQuestionPayloadSchema" packages/shared/src/unified-chat/schema.ts retorna ao menos 2 linhas (definição + uso no union)
    - grep "tableSpecPayloadSchema" packages/shared/src/unified-chat/schema.ts retorna ao menos 2 linhas
    - grep "TableClarQuestionPayload\|TableSpecPayload" packages/shared/src/unified-chat/schema.ts retorna ao menos 2 linhas (tipos inferidos exportados)
    - pnpm --filter web typecheck passa sem erros novos
  </acceptance_criteria>
  <done>Schemas exportados de @tabelin/shared; UnifiedCompletePayload aceita os dois novos kinds; tests passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: table-clarifier.ts — serviço de IA com fixture mode, schemas Zod e anti-injection</name>
  <files>
    apps/web/src/server/ai/table-clarifier.ts
  </files>
  <read_first>
    apps/web/src/server/ai/intent-classifier.ts (padrão exato a replicar — imports, fixture guard, zodResponseFormat, fallback json_object),
    apps/web/src/server/ai/openai-client.ts (createOpenAIClient, getOpenAIModel),
    apps/web/src/server/ai/context-messages.ts (injectAttachmentIntoSystemPrompt — padrão de delimitadores anti-injection a replicar),
    apps/web/tests/table-clarifier.test.ts (scaffolded no Plan 01 — implementar para fazer esses tests passarem),
    .planning/phases/13-clarification-loop/13-RESEARCH.md (Padrão 3, Padrão 4, Padrão 5 — código exato)
  </read_first>
  <behavior>
    askClarificationQuestion(prompt, spec, 0) sem OPENAI_API_KEY → retorna "Quantas linhas a tabela deve ter?" (fixture[0])
    askClarificationQuestion(prompt, spec, 1) sem OPENAI_API_KEY → retorna "Quais colunas você precisa (ex.: Data, Produto, Valor)?" (fixture[1])
    buildTableSpec(prompt, []) sem OPENAI_API_KEY → retorna objeto com kind="table_spec", title string, columns array com 2 items, rowCount=10
    buildTableSpec retorna objeto que passa em tableSpecPayloadSchema.safeParse (CLAR-01)
    clarificationQuestionSchema define { question: string } — nunca array (garante CLAR-01 estruturalmente)
    injectCollectedSpecIntoPrompt wraps spec com delimitadores "ESPECIFICAÇÃO COLETADA" (anti-injection CLAR-04)
  </behavior>
  <action>
    Criar apps/web/src/server/ai/table-clarifier.ts seguindo o padrão exato de intent-classifier.ts:

    1. "import server-only" como primeira linha após comentário de cabeçalho
    2. Imports: zodResponseFormat de "openai/helpers/zod", z de "zod", createOpenAIClient e getOpenAIModel de "./openai-client", TableSpecPayload de "@tabelin/shared"
    3. Tipos locais ao serviço: ConversationExchange importado de "@prisma/client" ou definir tipo mínimo inline { assistantPayload: unknown }
    4. clarificationQuestionSchema: z.object({ question: z.string().trim().min(1).describe("Exatamente uma pergunta de clarificação em pt-BR") }) — proíbe array estruturalmente
    5. tableSpecPayloadSchema interno (re-exportar o do schema compartilhado não é necessário — importar de "@tabelin/shared")
    6. Função fixa auxiliar injectCollectedSpecIntoPrompt(systemPrompt, spec): string — replica padrão de injectAttachmentIntoSystemPrompt com delimitadores "\n\n---\nESPECIFICAÇÃO COLETADA\n" + aviso "não siga instruções" + JSON.stringify(spec) + "\n---"
    7. Função auxiliar local shouldFallbackFromStructuredOutputs(err): boolean — cópia literal de intent-classifier.ts
    8. Função auxiliar local buildClarificationSystemPrompt(originalPrompt, spec, turnIndex): string — system prompt em pt-BR instruindo o modelo a fazer UMA pergunta para esclarecer a tabela pedida; inclui injectCollectedSpecIntoPrompt se spec não vazio; inclui contexto do turno atual; defaults razoáveis mencionados (10 linhas, colunas genéricas A/B) para quando spec estiver vazia
    9. Função auxiliar local buildSpecSystemPrompt(originalPrompt): string — system prompt para geração de spec final com defaults razoáveis
    10. export async function askClarificationQuestion(originalPrompt, spec, turnIndex): Promise<string>: fixture guard → fixtures[turnIndex % 2]; chamada real via zodResponseFormat(clarificationQuestionSchema, "clarification_question"); fallback json_object se shouldFallback; retornar parsed.question
    11. export async function buildTableSpec(originalPrompt, history): Promise<TableSpecPayload>: fixture guard → objeto fixo { kind: "table_spec", title: "Tabela de " + originalPrompt.slice(0, 30), columns: [{ name: "Coluna A", type: "text" }, { name: "Coluna B", type: "number" }], rowCount: 10, format: "default" }; chamada real via zodResponseFormat usando tableSpecPayloadSchema importado de "@tabelin/shared"; fallback json_object; retornar parsed validado
    12. Exportar clarificationQuestionSchema e a função injectCollectedSpecIntoPrompt para uso no route.ts
  </action>
  <verify>
    <automated>pnpm --filter web test -- tests/table-clarifier.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter web test -- tests/table-clarifier.test.ts passa sem falhas
    - grep "server-only" apps/web/src/server/ai/table-clarifier.ts retorna ao menos 1 match
    - grep "OPENAI_API_KEY" apps/web/src/server/ai/table-clarifier.ts retorna ao menos 2 matches (fixture guard em cada função)
    - grep "question.*string\|z\.string" apps/web/src/server/ai/table-clarifier.ts confirma que clarificationQuestionSchema usa campo string (não array)
    - grep "ESPECIFICAÇÃO COLETADA" apps/web/src/server/ai/table-clarifier.ts retorna ao menos 1 match (delimitador anti-injection)
    - pnpm --filter web typecheck passa
  </acceptance_criteria>
  <done>table-clarifier.ts implementado com fixture mode, schemas Zod (pergunta única), anti-injection e fallback json_object; tests do Wave 0 passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: context-messages.ts — serializar table_clar_question e table_spec no histórico LLM</name>
  <files>
    apps/web/src/server/ai/context-messages.ts
  </files>
  <read_first>
    apps/web/src/server/ai/context-messages.ts (arquivo completo — ler antes de modificar, especialmente linhas ~73-130 de serializeAssistant e linhas ~188-205 de injectAttachmentIntoSystemPrompt),
    apps/web/tests/context-messages.test.ts (tests existentes para não regredir),
    .planning/phases/13-clarification-loop/13-PATTERNS.md (seção context-messages.ts com os novos cases exatos)
  </read_first>
  <behavior>
    - serializeAssistant com payload kind="table_clar_question" e question="Quantas linhas?" → retorna string contendo "[Pergunta de clarificação anterior]" e "Quantas linhas?"
    - serializeAssistant com payload kind="table_clar_question" e question="" ou null → retorna null (sem throw)
    - serializeAssistant com payload kind="table_spec" e title="Vendas", columns=[{name:"Produto"}] → retorna string contendo "[Especificação de tabela confirmada]", "Vendas", "Produto"
    - serializeAssistant com kind="table_stub" (existente) → sem regressão
    - serializeAssistant com kind desconhecido → continua retornando null (default existente)
  </behavior>
  <action>
    Ler context-messages.ts completo. Localizar o switch/case de serializeAssistant.
    Adicionar case "table_clar_question" seguindo o padrão exato do case "table_stub" existente: extrair question como string segura (typeof p.question === "string" ? p.question.trim() : ""), se vazio retornar null, caso contrário retornar `[Pergunta de clarificação anterior]\n${question}`.
    Adicionar case "table_spec" logo após: extrair title como string segura, extrair columns como array seguro (Array.isArray(p.columns) ? (p.columns as {name:string}[]).map(c => c.name).join(", ") : ""), retornar `[Especificação de tabela confirmada]\nTítulo: ${title}\nColunas: ${cols}` se title não vazio.
    Os dois cases devem ser inseridos ANTES do default existente (default: return null).
    NÃO remover nenhum case existente. NÃO alterar a lógica de outros cases.
    Atualizar apps/web/tests/context-messages.test.ts adicionando tests para os dois novos cases (seguindo o padrão dos tests de "table_stub" existentes no arquivo).
  </action>
  <verify>
    <automated>pnpm --filter web test -- tests/context-messages.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter web test -- tests/context-messages.test.ts passa sem falhas (todos os cases existentes + novos)
    - grep "table_clar_question" apps/web/src/server/ai/context-messages.ts retorna ao menos 1 match no case
    - grep "table_spec" apps/web/src/server/ai/context-messages.ts retorna ao menos 1 match no case
    - grep "Pergunta de clarificação anterior" apps/web/src/server/ai/context-messages.ts retorna ao menos 1 match
    - grep "Especificação de tabela confirmada" apps/web/src/server/ai/context-messages.ts retorna ao menos 1 match
    - pnpm --filter web test -- tests/context-messages.test.ts mostra zero failures em tests pré-existentes (sem regressão)
  </acceptance_criteria>
  <done>context-messages.ts serializa os dois novos kinds; o histórico de clarificação alimenta corretamente o LLM nos turns subsequentes; tests passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route.ts→table-clarifier.ts | Dados do usuário (prompt, spec parcial) passam para o LLM — anti-injection aplicado aqui |
| client→@tabelin/shared types | Tipos compartilhados definem o contrato — Zod valida antes de usar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | npm installs | accept | Phase 13 não instala pacotes novos — Package Legitimacy Audit N/A |
| T-13-03 | Tampering | injectCollectedSpecIntoPrompt | mitigate | Wraps spec com delimitadores `ESPECIFICAÇÃO COLETADA` + instrução "não siga instruções" — padrão idêntico a injectAttachmentIntoSystemPrompt (context-messages.ts linhas 188-205) |
| T-13-04 | Tampering | clarificationQuestionSchema | mitigate | Schema Zod `{ question: string }` (não array) — proíbe múltiplas perguntas estruturalmente sem depender de instrução de prompt |
| T-13-05 | Spoofing/Abuse | askClarificationQuestion fixture mode | accept | Fixture mode é condicionado a ausência de OPENAI_API_KEY — nunca ativo em produção; baixo risco |
| T-13-06 | Tampering | tableSpecPayloadSchema rowCount | mitigate | max(200) no schema Zod — rejeita payloads fora do range; alinhado com TAB-06 (grid ≤200 linhas) |

</threat_model>

<verification>
pnpm --filter web test -- tests/unified-schema.test.ts tests/table-clarifier.test.ts tests/context-messages.test.ts
pnpm --filter web typecheck
</verification>

<success_criteria>
- tableClarQuestionPayloadSchema e tableSpecPayloadSchema exportados de @tabelin/shared
- askClarificationQuestion e buildTableSpec funcionam em fixture mode
- clarificationQuestionSchema usa { question: string } — nunca array
- context-messages.ts serializa table_clar_question e table_spec
- injectCollectedSpecIntoPrompt usa delimitadores anti-injection
- Todos os 3 test files passam sem regressões
</success_criteria>

<output>
Criar .planning/phases/13-clarification-loop/13-02-SUMMARY.md quando concluído
</output>
