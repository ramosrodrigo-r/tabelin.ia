---
phase: 14-tabela-viva
plan: 02
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - packages/shared/src/unified-chat/schema.ts
  - packages/shared/src/table/formula-locale.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - LOC-01
  - LOC-02
  - TAB-02
  - TAB-06

must_haves:
  truths:
    - "tableSpecPayloadSchema aceita os campos novos (rows, formulaLanguage, separator, colunas com type:'formula') e continua aceitando specs sem rows (retrocompatibilidade Phase 13)"
    - "PT_BR_TO_EN exporta ~30 entradas incluindo PROCV, SOMASE, SE, CONT.SE (com alias com ponto)"
    - "translateFunctionName('PROCV') retorna 'VLOOKUP'"
    - "TableColumn type exportado com type z.enum incluindo 'formula'"
  artifacts:
    - path: "packages/shared/src/unified-chat/schema.ts"
      provides: "tableSpecPayloadSchema estendido retrocompat + tableColumnSchema + tipos"
      exports: ["tableColumnSchema", "tableSpecPayloadSchema", "TableColumn", "TableSpecPayload"]
    - path: "packages/shared/src/table/formula-locale.ts"
      provides: "Mapa PT_BR_TO_EN (~30 funções) + translateFunctionName helper"
      exports: ["PT_BR_TO_EN", "translateFunctionName", "FormulaBRFunctionName"]
    - path: "packages/shared/src/index.ts"
      provides: "Barrel export de ./table/formula-locale"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/table/formula-locale.ts"
      via: "export * from './table/formula-locale'"
      pattern: "formula-locale"
    - from: "packages/shared/src/unified-chat/schema.ts"
      to: "unifiedCompletePayloadSchema"
      via: "tableSpecPayloadSchema é membro da union — atualização automática"
      pattern: "tableSpecPayloadSchema"
---

<objective>
Wave 1 (parte A): Contratos compartilhados — schema estendido retrocompatível + mapa de
localização PT-BR→EN.

Purpose: Definir os tipos e contratos que todos os outros módulos (motor de fórmulas,
TableGridPanel, table-clarifier, render-dispatcher) dependem. Interface-first: esses
contratos são escritos antes das implementações.

Output: tableSpecPayloadSchema estendido em @tabelin/shared; formula-locale.ts com mapa
PT_BR_TO_EN completo; testes de schema do Wave 0 passam.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-tabela-viva/14-CONTEXT.md
@.planning/phases/14-tabela-viva/14-RESEARCH.md
@.planning/phases/14-tabela-viva/14-PATTERNS.md

<interfaces>
<!-- Schema atual que será SUBSTITUÍDO — packages/shared/src/unified-chat/schema.ts linhas 63-69 -->
export const tableSpecPayloadSchema = z.object({
  kind: z.literal("table_spec"),
  title: z.string(),
  columns: z.array(z.object({ name: z.string(), type: z.string() })),
  rowCount: z.number().int().min(1).max(200),
  format: z.string().optional(),
});
export type TableSpecPayload = z.infer<typeof tableSpecPayloadSchema>;

<!-- unifiedCompletePayloadSchema inclui tableSpecPayloadSchema na union (linha 98) — sem alteração estrutural -->

<!-- index.ts atual — adicionar entrada para ./table/formula-locale após linha 21 -->
export * from "./unified-chat/schema";  // já existe

<!-- Análogo de padrão em packages/shared/src/formula/platforms.ts -->
export const PT_BR_TO_EN: Record<string, string> = { ... };
export type FormulaBRFunctionName = keyof typeof PT_BR_TO_EN;
export function translateFunctionName(brName: string): string | null { ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender tableSpecPayloadSchema e criar tableColumnSchema</name>
  <files>packages/shared/src/unified-chat/schema.ts</files>
  <read_first>
    packages/shared/src/unified-chat/schema.ts (arquivo completo — ler uma vez, extrair tudo)
    .planning/phases/14-tabela-viva/14-PATTERNS.md (§schema.ts — extension pattern, linhas 62-93)
    .planning/phases/14-tabela-viva/14-RESEARCH.md (§Pattern 1 — forma concreta do schema estendido)
    apps/web/tests/unified-schema.test.ts (testes do Wave 0 que devem passar após esta task)
  </read_first>
  <behavior>
    - tableSpecPayloadSchema.safeParse({ kind:"table_spec", title:"T", columns:[{name:"C",type:"text"}], rowCount:5 }) → success:true (retrocompat sem rows)
    - tableSpecPayloadSchema.safeParse({ kind:"table_spec", title:"T", columns:[{name:"C",type:"formula",key:"c",formula:"=SOMA(A{row};B{row})"}], rowCount:5, rows:[{c:0}], formulaLanguage:"pt-BR", separator:";" }) → success:true
    - tableSpecPayloadSchema.safeParse({ kind:"table_spec", title:"T", columns:[{name:"C",type:"text"}], rowCount:5, rows:[{c:{nested:"obj"}}] }) → success:false (rows aceita apenas string|number nos valores)
    - TableColumn type inclui campo formula?: string e type "formula" no enum
    - tableColumnSchema.safeParse({ name:"Total", type:"formula", key:"total", formula:"=SOMA(B{row};C{row})" }) → success:true
  </behavior>
  <action>
    Substituir o bloco tableSpecPayloadSchema atual (linhas ~63-69) pelo schema estendido.

    1. Antes de tableSpecPayloadSchema, declarar tableColumnSchema com:
       - name: z.string()
       - type: z.enum(["text", "number", "date", "currency", "formula"])
       - key: z.string().optional()
       - formula: z.string().optional()
       - width: z.number().optional()

    2. Substituir tableSpecPayloadSchema para usar z.array(tableColumnSchema) em columns
       e adicionar campos opcionais (per D-01):
       - rows: z.array(z.record(z.string(), z.union([z.string(), z.number()]))).optional()
       - formulaLanguage: z.enum(["pt-BR", "en"]).optional()
       - separator: z.enum([";", ","]).optional()

    3. Adicionar export type TableColumn = z.infer<typeof tableColumnSchema> após os tipos
       existentes (manter todas as exportações existentes intactas).

    ATENÇÃO: unifiedCompletePayloadSchema usa tableSpecPayloadSchema como membro da union
    (linha 98) — sem alterar a estrutura da union, apenas o schema membro. Verificar que
    TypeScript compila sem erros após a mudança.

    Retrocompatibilidade: os campos novos são todos .optional() — specs existentes da Phase
    13 sem rows continuam passando no parse. Per D-01.
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm test --filter web -- run tests/unified-schema.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    unified-schema.test.ts passa; tableColumnSchema exportado; TableColumn type exportado;
    retrocompatibilidade confirmada (spec sem rows aceita);
    `tsc --noEmit` em packages/shared/ compila sem erros novos.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar formula-locale.ts e exportar pelo barrel</name>
  <files>
    packages/shared/src/table/formula-locale.ts
    packages/shared/src/index.ts
  </files>
  <read_first>
    .planning/phases/14-tabela-viva/14-RESEARCH.md (§Pattern 2 — mapa PT_BR_TO_EN completo, linhas 336-381)
    .planning/phases/14-tabela-viva/14-PATTERNS.md (§formula-locale.ts — padrão de platforms.ts)
    packages/shared/src/formula/platforms.ts (analog — estrutura de export puro sem framework)
    packages/shared/src/index.ts (barrel existente — onde adicionar a entrada)
    apps/web/tests/formula-engine.test.ts (testes do Wave 0 que dependem de PT_BR_TO_EN)
  </read_first>
  <behavior>
    - PT_BR_TO_EN["PROCV"] === "VLOOKUP"
    - PT_BR_TO_EN["SOMASE"] === "SUMIF"
    - PT_BR_TO_EN["SE"] === "IF"
    - PT_BR_TO_EN["CONT.SE"] === "COUNTIF" (chave com ponto)
    - PT_BR_TO_EN["CONT_SE"] === "COUNTIF" (alias sem ponto)
    - PT_BR_TO_EN["CONTSE"] === "COUNTIF" (alias sem ponto e underscore)
    - PT_BR_TO_EN["MÉDIA"] === "AVERAGE" (com acento)
    - translateFunctionName("PROCV") retorna "VLOOKUP"
    - translateFunctionName("procv") retorna "VLOOKUP" (case-insensitive)
    - translateFunctionName("DESCONHECIDA") retorna null
    - translateFunctionName("") retorna null
  </behavior>
  <action>
    1. Criar diretório packages/shared/src/table/ e arquivo formula-locale.ts.

    2. No arquivo, exportar:
       - PT_BR_TO_EN: Record&lt;string, string&gt; com todas as entradas do RESEARCH.md Pattern 2
         (~30 entradas cobrindo: Matemáticas: SOMA, SOMASE, SOMASES, MÉDIA, MÉDIASE, MÁXIMO,
         MÍNIMO, ABS, ARRED, MOD, RAIZ, POTÊNCIA; Lógica: SE, E, OU, NÃO; Contagem: CONT,
         CONTA, CONT.SE, CONT_SE, CONTSE; Busca: PROCV, PROCH, ÍNDICE, CORRESP; Texto:
         CONCATENAR, TEXTO, ESQUERDA, DIREITA, NÚM_CARACT; Data: HOJE, AGORA, ANO, MÊS, DIA)
       - FormulaBRFunctionName: type = keyof typeof PT_BR_TO_EN
       - translateFunctionName(brName: string): string | null — usa brName.toUpperCase(),
         retorna PT_BR_TO_EN[upperName] ?? null

    3. Sem dependências externas — arquivo puro TypeScript (safe para browser e Node).
       Sem imports de zod, react, ou qualquer outro módulo.

    4. Adicionar em packages/shared/src/index.ts após a linha "export * from './unified-chat/schema'":
       export * from "./table/formula-locale";

    Pitfall a evitar (RESEARCH.md Pitfall 6): incluir alias "CONT.SE" (com ponto) como chave
    explícita no mapa — o extrator do nome da função no motor usará regex /^=([A-Z][A-Z0-9._]*)\(/i
    para capturar o ponto.
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm test --filter web -- run tests/formula-engine.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    formula-locale.ts existe com >= 30 entradas no mapa; translateFunctionName case-insensitive;
    barrel re-exporta o módulo; testes de PT_BR_TO_EN map no formula-engine.test.ts passam
    (os testes de evaluateFormula continuam skip-graceful até Wave 1 parte B).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → tableSpecPayloadSchema | Dados gerados pelo LLM são validados pelo schema antes de chegar ao cliente |
| shared package → browser runtime | formula-locale.ts é importado no browser — sem acesso a Node built-ins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-INPUT | Tampering | tableSpecPayloadSchema rows field | mitigate | rows aceita apenas z.record(z.string(), z.union([z.string(), z.number()])) — objetos aninhados rejeitados pelo Zod parse; seed data não pode conter objetos arbitrários (per D-07, ASVS V5) |
| T-14-FORMULA-LOCALE | Tampering | PT_BR_TO_EN map | accept | Mapa é tabela estática de constantes — sem leitura de input externo; risco de injeção nulo |
| T-14-SC | Tampering | npm installs (concluídos no Wave 0) | transfer | Checkpoint human-verify no Plan 01 cobre instalação — este plano apenas usa as dependências já aprovadas |
</threat_model>

<verification>
Wave 1 (parte A) completo quando:
- [ ] pnpm test --filter web -- run tests/unified-schema.test.ts — verde (todos os casos Phase 14)
- [ ] pnpm test --filter web -- run tests/formula-engine.test.ts — verde para describe("PT_BR_TO_EN map") (skip-graceful para suites que dependem de evaluateFormula)
- [ ] tsc --noEmit no workspace compila sem erros novos em packages/shared/
</verification>

<success_criteria>
- tableColumnSchema e tableSpecPayloadSchema estendidos exportados por @tabelin/shared
- formula-locale.ts com PT_BR_TO_EN completo e translateFunctionName exportados
- Retrocompatibilidade: specs Phase 13 sem rows ainda passam no parse
- testes de schema e de mapa de localização passam
</success_criteria>

<output>
Criar `.planning/phases/14-tabela-viva/14-02-SUMMARY.md` ao concluir
</output>
