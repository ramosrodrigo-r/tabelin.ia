---
phase: 260618-l61-habilitar-edicao-direta-de-celulas-da-pl
plan: 01
subsystem: ui
tags: [react, datasheet-grid, direct-edit, table-grid-panel, keyboard]

requires:
  - phase: 260617-ukf-crie-as-funcoes-da-toolbar-da-planilha-q
    provides: Modelo de estilo por célula (cellStyles), activeCell, todos os 19 botões da toolbar funcionais
provides:
  - Edição direta de valor de célula via teclado, sem passar pelo chat da IA (ausente desde v1)
  - Captura de teclado global (handleCellEditKeyDown) com buffer de edição em estado React
  - Cache de renderers de célula estáveis por colKey (cellRendererCacheRef) — corrige desmonte/remonte em cascata de toda a grade a cada interação
affects: [unified-chat, table-grid-panel, workspace]

tech-stack:
  added: []
  patterns:
    - "Edição via captura de teclado global (window keydown) em vez de <input> focado — react-datasheet-grid rouba foco de DOM agressivamente"
    - "latestCellRenderDataRef + cellRendererCacheRef: função de célula com identidade estável lendo dados via ref (não closure), evitando remount a cada render"

key-files:
  created: []
  modified:
    - apps/web/src/features/unified-chat/components/table-grid-panel.tsx
    - apps/web/src/styles/globals.css

key-decisions:
  - "Abandonado <input> real focado por célula: react-datasheet-grid gerencia foco/seleção internos de forma agressiva (listener mousedown no document + ciclo de remontagem do React), fazendo o input perder foco em <300ms após montar, mesmo após estabilizar a identidade da função de célula. Confirmado via instrumentação de ref callback (MOUNT→UNMOUNT→MOUNT)."
  - "Edição implementada como estado React puro (editBuffer) + captura de teclado global — elimina toda a classe de bugs de foco/remontagem do react-datasheet-grid, ao custo de não ter um cursor de texto nativo de input (compensado com .cell-edit-caret CSS piscante)."
  - "cellRendererCacheRef/latestCellRenderDataRef: refactor necessário para parar de recriar a função component a cada recomputação de dsgColumns — sem isso, MESMO o indicador visual de célula ativa (fix anterior, 260617-ukf) ficaria sujeito a remounts a cada clique."
  - "Persistência: edição direta passa pelo mesmo dispatch/historyReducer (undo/redo) e WorkspaceStateContext/autosave (debounce 1.5s) já usados por Sigma/Mesclar — não é um novo mecanismo de armazenamento."

requirements-completed: [EDIT-01]

duration: ~90min (majoritariamente investigação ao vivo do bug de foco do react-datasheet-grid)
completed: 2026-06-18
status: complete
---

# Quick Task 260618-l61: Edição direta de células sem chat — Summary

**Usuário agora pode clicar numa célula e digitar diretamente para alterar seu valor — sem precisar passar pelo chat da IA antes. Funcionalidade ausente desde a v1 do projeto.**

## O que foi feito

- Clicar numa célula não-fórmula + digitar caractere imprimível → inicia edição em modo "replace" (substitui o valor)
- Clicar numa célula + Enter → inicia edição em modo "edit" com o valor atual pré-carregado
- Backspace remove caracteres do buffer de edição; Escape cancela sem alterar o valor original; Enter confirma (`commitCellEdit` → `dispatch` → mesmo pipeline de undo/redo do Sigma/Mesclar)
- Trocar de célula ativa com edição pendente commita automaticamente antes de trocar
- Colunas tipo fórmula (ex.: "Total") permanecem somente leitura — digitar não as altera
- Não intercepta teclas quando outro input/textarea da página tem foco (ex.: caixa de chat)
- Valor editado sobrevive ao autosave (debounce 1.5s) e a reload de página — verificado manualmente

## Jornada de depuração (root cause real, não óbvio)

A primeira tentativa (`<input>` real com `autoFocus` na célula ativa) **parecia** funcionar em testes superficiais, mas falhava silenciosamente: o `react-datasheet-grid` tem um listener `mousedown` global no `document` que gerencia seleção/edição internos e — combinado com o ciclo de re-render do React — fazia o `<input>` perder o foco em menos de 300ms após montar, mesmo sem nenhuma digitação. Instrumentação com `ref` callback confirmou o padrão exato: `MOUNT → UNMOUNT → MOUNT` (e às vezes um segundo ciclo) só de clicar numa célula, sem digitar nada.

Investigação revelou DOIS problemas empilhados:
1. **`dsgColumns` recriava a função `component` de cada célula a cada recomputação** (disparada por `cellStyles`/`activeCell`/`groupByKey` mudando) — o `react-datasheet-grid` usa essa função como o "tipo" do componente React (`createElement(Component, ...)`), então uma função nova = desmonte+remonte de TODAS as células visíveis a cada clique/toggle de formatação.
2. Mesmo após estabilizar essa identidade (via `cellRendererCacheRef` + `latestCellRenderDataRef`), o foco do `<input>` AINDA se perdia — o `react-datasheet-grid` rouba foco por conta própria, independente do ciclo de remount do React.

**Decisão final:** abandonar dependência de foco nativo de DOM inteiramente. A edição vive em estado React puro (`editBuffer`), alimentada por um listener de teclado GLOBAL (`window.addEventListener("keydown", ...)`), no mesmo padrão já usado pelo Ctrl+Z/Ctrl+Y existente. Isso elimina toda a classe de bugs de foco do `react-datasheet-grid`.

## Arquivos modificados

- `apps/web/src/features/unified-chat/components/table-grid-panel.tsx` — `editBuffer`, `handleCellEditKeyDown` (useEffect), `commitCellEdit`, `cellRendererCacheRef`, `latestCellRenderDataRef`, renderização do buffer de edição
- `apps/web/src/styles/globals.css` — `.cell-edit-active`, `.cell-edit-caret` (cursor piscante)

## Verificação

- `npx vitest run tests/table-grid-panel.test.tsx` — 65/65 passam, sem regressão
- `npx tsc --noEmit` — limpo
- Teste manual via script Playwright ad-hoc contra `next dev` real (jsdom não renderiza linhas de dados do `react-datasheet-grid` — limitação pré-existente já documentada em 260617-ukf, motivo pelo qual a verificação central foi manual/browser, não automatizada):
  - Digitação direta substitui valor ✓
  - Enter pré-carrega + edita ✓
  - Backspace remove caracteres ✓
  - Escape cancela sem alterar ✓
  - Coluna fórmula bloqueada (digitar "HACK" em célula de Total não altera) ✓
  - Não intercepta foco de outros inputs da página ✓
  - Persiste após autosave (2.5s) + reload de página ✓

## Issues Encontrados (não-bloqueantes, fora de escopo)

- `npx vitest run` (suíte completa do projeto) tem 1 teste falhando em `tests/unified-chat-tool.test.tsx:586` (`getByRole("button", { name: "Desfazer" })` encontra múltiplos elementos) — **confirmado PRÉ-EXISTENTE** via `git stash`/teste contra o código sem esta mudança; não é uma regressão desta task. Fora de escopo corrigir aqui.

## Threat Flags

Nenhuma superfície de ameaça nova além das registradas no `<threat_model>` do PLAN (T-260618-01, T-260618-02 — ambas `mitigate`): captura de teclado restrita por `activeCell`+ausência de foco em outro input/textarea+bloqueio de colunas fórmula; texto digitado segue o mesmo pipeline já hardenizado (`formatCellValue`, nunca `dangerouslySetInnerHTML`/`eval`) já auditado para Sigma/Mesclar em 260617-ukf.

## User Setup Required

Nenhum.

## Next Phase Readiness

Nenhuma fase planejada subsequente — quick task isolada. Limitação documentada: sem cursor de texto nativo (substituído por indicador CSS piscante); sem navegação por Tab/setas entre células durante edição (fora de escopo, não solicitado).

---
*Quick task: 260618-l61*
*Completed: 2026-06-18*
