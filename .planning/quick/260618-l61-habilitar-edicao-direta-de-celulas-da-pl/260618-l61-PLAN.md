---
phase: 260618-l61-habilitar-edicao-direta-de-celulas-da-pl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/features/unified-chat/components/table-grid-panel.tsx
  - apps/web/src/styles/globals.css
autonomous: true
requirements: [EDIT-01]

must_haves:
  truths:
    - "Clicar numa célula não-fórmula e digitar diretamente altera o valor da célula, sem passar pelo chat da IA"
    - "Enter sobre uma célula já ativa entra em edição com o valor atual pré-carregado (não substitui de imediato)"
    - "Escape durante a edição cancela sem alterar o valor original"
    - "Backspace remove caracteres do buffer de edição em andamento"
    - "Colunas do tipo fórmula permanecem somente leitura — digitar não altera seu valor"
    - "A edição não rouba teclas digitadas em outros campos da página (ex.: caixa de chat) — só atua quando nenhum input/textarea tem foco"
    - "O valor editado persiste após o autosave (debounce de 1.5s) e sobrevive a reload de página"
    - "Nenhuma regressão nas 5 tasks anteriores da toolbar (260617-ukf): formatação, número/zoom/fonte, sigma/mesclar/pintura, ordenar/agrupar/compartilhar"
  artifacts:
    - path: "apps/web/src/features/unified-chat/components/table-grid-panel.tsx"
      provides: "Captura de teclado global (handleCellEditKeyDown) + buffer de edição (editBuffer) + cache de renderers de célula estáveis por colKey (cellRendererCacheRef) + dados ao vivo via ref (latestCellRenderDataRef)"
  key_links:
    - from: "handleCellEditKeyDown (useEffect keydown global)"
      to: "commitCellEdit -> dispatch SET"
      via: "Enter confirma o buffer, escrevendo no valor real da célula"
      pattern: "commitCellEdit\\(activeCell"

threat_model:
  trust_boundaries:
    - boundary: "Usuário → valor de célula (digitação direta)"
      description: "Texto digitado pelo usuário entra no mesmo pipeline de dispatch/useFormulaEngine já hardenizado (renderização sempre via formatCellValue → string em span, nunca dangerouslySetInnerHTML); mesma superfície já aceita para Sigma/Mesclar (260617-ukf)"
  stride_register:
    - threat_id: "T-260618-01"
      category: "Tampering"
      component: "Captura de teclado global (handleCellEditKeyDown)"
      disposition: "mitigate"
      mitigation_plan: "O listener só atua quando activeCell está definido E document.activeElement não é INPUT/TEXTAREA — não intercepta digitação em outros campos da página (ex.: caixa de chat). Colunas type==='formula' são explicitamente bloqueadas (col?.type === 'formula' → return)."
    - threat_id: "T-260618-02"
      category: "Tampering"
      component: "Texto livre digitado na célula"
      disposition: "mitigate"
      mitigation_plan: "Valor digitado é tratado como string opaca via formatCellValue (nunca dangerouslySetInnerHTML, nunca eval) — mesma garantia já auditada para Sigma/Mesclar em 260617-ukf (SECURITY.md T-260617-03)."
---

<objective>
Habilitar edição direta de valores de célula na planilha, digitando diretamente, sem precisar passar pelo chat da IA antes — funcionalidade ausente desde a v1 do projeto.

**Contexto da implementação (retroativa — feita ad-hoc durante depuração ao vivo, documentada aqui para rastreabilidade):**

A primeira abordagem (um `<input>` real focado por célula, com `autoFocus`) falhou: o `react-datasheet-grid` gerencia seu próprio foco/edição internos de forma agressiva (um listener `mousedown` no `document` que silenciosamente faz `preventDefault()`/gerencia seleção, e o ciclo de remontagem do React em torno de cada clique fazia o `<input>` perder o foco quase imediatamente após montar — confirmado via instrumentação de `ref` callback mostrando MOUNT→UNMOUNT→MOUNT em menos de 300ms de um único clique, mesmo após estabilizar a identidade da função de célula via cache).

**Decisão de design:** abandonar dependência de foco nativo de DOM. A edição vive inteiramente em estado React (`editBuffer`) e é alimentada por um listener de teclado GLOBAL (`window.addEventListener("keydown", ...)`, mesmo padrão já usado para Ctrl+Z/Ctrl+Y). Isso elimina a classe de bugs de foco/remontagem do `react-datasheet-grid` por completo.

**Refactor de estabilidade adicional:** durante a investigação, identificou-se que `dsgColumns` (useMemo) recriava a função `component` de CADA célula a cada recomputação (disparada por `cellStyles`/`activeCell`/`groupByKey` mudando) — como o `react-datasheet-grid` usa essa função como o "tipo" do componente em `React.createElement(Component, ...)`, isso forçava desmonte+remonte de TODAS as células a cada interação. Corrigido com `cellRendererCacheRef` (uma função estável por `colKey`, reaproveitada entre recomputações) + `latestCellRenderDataRef` (valores que mudam, lidos em tempo de chamada via ref, não por closure). O array `dsgColumns.columns` continua sendo recriado a cada recomputação — isso já é suficiente para forçar o `react-datasheet-grid` (memoizado) a re-renderizar com dados atuais, sem precisar remontar.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Edição direta de célula via captura de teclado global</name>
  <files>apps/web/src/features/unified-chat/components/table-grid-panel.tsx, apps/web/src/styles/globals.css</files>
  <behavior>
    Verificado manualmente via Playwright contra o dev server real (não jsdom — a mesma limitação de virtualização que afeta os testes unitários da grade já documentada em 260617-ukf-SUMMARY.md impede testar isso via DOM virtualizado em jsdom):
    - Clicar numa célula com texto + digitar diretamente substitui o valor (modo "replace", como Excel/Sheets)
    - Clicar numa célula + Enter pré-carrega o valor atual no buffer (modo "edit", não substitui)
    - Backspace remove caracteres do buffer; Escape cancela sem commitar; Enter confirma
    - Trocar de célula ativa com edição pendente commita automaticamente antes de trocar
    - Coluna tipo fórmula (Total) bloqueada — digitar não altera seu valor
    - Foco em outro input/textarea da página (ex.: caixa de chat) não é interceptado
    - Valor editado sobrevive ao autosave (debounce 1.5s) + reload de página
  </behavior>
  <action>
    Adicionar estado `editBuffer: string | null` e um `useEffect` com `window.addEventListener("keydown", ...)` que: ignora quando outro input/textarea tem foco; ignora colunas `type === "formula"`; em modo edição (`editBuffer !== null`) trata Enter (commit), Escape (cancelar) e Backspace/caracteres imprimíveis; fora do modo edição, Enter pré-carrega o valor atual e qualquer caractere imprimível inicia edição em modo "replace".

    Adicionar `commitCellEdit(rowIndex, colKey, newValue)` que escreve o novo valor via `dispatch({ type: "SET", ... })` (mesmo pipeline de undo/redo já usado por Sigma/Mesclar).

    Refatorar a célula renderizada: quando `isActiveCell && !isFormula && editBuffer !== null`, mostrar um `<span className="cell-edit-active">` com o buffer + um cursor piscante (`.cell-edit-caret`), em vez do valor formatado.

    Refatorar `dsgColumns` (useMemo) para usar `cellRendererCacheRef` (Map por `colKey`) + `latestCellRenderDataRef` (ref com os valores que mudam: `cellStyles`, `activeCell`, `editBuffer`, `groupByKey`, etc.) — estabiliza a identidade da função `component` entre recomputações, evitando desmonte/remonte de toda a grade a cada interação.

    Em `globals.css`: `.cell-edit-active` (white-space: pre) e `.cell-edit-caret` (cursor piscante via `@keyframes`).
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run tests/table-grid-panel.test.tsx</automated>
    <manual>Testado via script Playwright ad-hoc contra `next dev` real: digitação direta, Enter-para-editar, Backspace, Escape, bloqueio de coluna fórmula, não-interceptação de outros inputs, persistência pós-reload — todos confirmados.</manual>
  </verify>
  <done>Usuário pode clicar numa célula e digitar diretamente para alterar seu valor, sem precisar passar pelo chat da IA; fórmulas permanecem protegidas; sem regressão nas funções da toolbar já existentes.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx vitest run tests/table-grid-panel.test.tsx` — 65/65 passam, sem regressão
- `cd apps/web && npx tsc --noEmit` — sem erros novos
- Teste manual via Playwright contra `next dev` real (jsdom não renderiza linhas de dados do react-datasheet-grid — limitação pré-existente documentada em 260617-ukf)
</verification>

<success_criteria>
- Digitação direta funciona em qualquer célula não-fórmula, sem depender de foco nativo de DOM (que o react-datasheet-grid rouba agressivamente)
- Fórmulas continuam protegidas (read-only)
- Edição não interfere com outros campos da página
- Sem regressão nas 65 testes unitários nem nas 5 tasks da toolbar (260617-ukf)
</success_criteria>

<output>
Create `.planning/quick/260618-l61-habilitar-edicao-direta-de-celulas-da-pl/260618-l61-SUMMARY.md` when done
</output>
