---
quick_id: 260525-s01
slug: corrigir-threats-seguranca-fase-2
status: complete
completed: 2026-05-25
commit: 107b908
verification: Verified
---

# Quick Task 260525-s01 Summary

Corrigidos os threats `T-02-02-04` e `T-02-03-03` apontados no `02-SECURITY.md` da fase 2.

## Changes

### Mercado Pago webhook signature

- `apps/web/src/app/api/billing/mercado-pago/webhook/route.ts` agora repassa `x-signature`, `x-request-id` e `request.url` para o servico.
- `apps/web/src/server/billing/webhook-service.ts` valida `x-signature` no formato `ts/v1`, monta o manifesto com `data.id`, `request-id` e `ts`, usa HMAC SHA-256 e comparacao em tempo constante.
- A validacao rejeita assinaturas vencidas por tolerancia de timestamp.
- Producao falha fechada quando `MERCADO_PAGO_WEBHOOK_SECRET` nao esta configurado.
- `apps/web/tests/mercado-pago-webhook.test.ts` cobre assinatura valida, assinatura invalida, assinatura vencida e fail-closed em producao.

### Pro support links

- Adicionado `apps/web/src/server/support/support-config.ts` para normalizar links de suporte antes do `Topbar`.
- Email Pro e transformado em `mailto:` somente apos validacao sintatica; valor invalido cai para `suporte@tabelin.ia`.
- WhatsApp so e renderizado quando a URL e HTTPS e usa host allowlistado do WhatsApp.
- `Topbar` agora recebe `supportLinks` ja normalizados do server component `/workspace`.
- `.env.example` documenta `NEXT_PUBLIC_PRO_SUPPORT_EMAIL` e `NEXT_PUBLIC_PRO_SUPPORT_WHATSAPP_URL`.
- `apps/web/tests/topbar.test.tsx` cobre Pro com links seguros, Free sem links Pro, e URL WhatsApp insegura omitida.

## Verification

- `pnpm --filter web test -- tests/mercado-pago-webhook.test.ts` passed.
- `pnpm --filter web test -- tests/topbar.test.tsx` passed.
- `pnpm --filter web test -- tests/formula-ui.test.tsx` passed.
- `pnpm --filter web test -- tests/mercado-pago-webhook.test.ts tests/topbar.test.tsx tests/formula-ui.test.tsx` passed.
- `pnpm --filter web typecheck` passed.
- `pnpm --filter web lint` passed.

Note: The Vitest commands executed the full current web test suite in this setup: 9 files, 53 tests.

## Follow-Up

Re-run `$gsd-secure-phase 2` to update `02-SECURITY.md` from `blocked` to verified if the audit confirms both mitigations are now closed.
