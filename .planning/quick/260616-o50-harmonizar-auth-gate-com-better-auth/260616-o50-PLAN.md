---
phase: quick-260616-o50
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/app/auth-gate-modal.tsx
  - apps/web/src/components/app/__tests__/auth-gate.test.tsx
autonomous: true
requirements: [QUICK-o50]
must_haves:
  truths:
    - "auth-gate-modal.tsx comment accurately reflects Better Auth origin check (not HMAC custom)"
    - "AuthGate component has unit tests covering authenticated branch (no overlay) and unauthenticated branch (overlay present)"
    - "All existing tests continue to pass after changes"
  artifacts:
    - path: "apps/web/src/components/app/__tests__/auth-gate.test.tsx"
      provides: "Unit tests for AuthGate component"
    - path: "apps/web/src/components/app/auth-gate-modal.tsx"
      provides: "Corrected comment referencing Better Auth"
  key_links:
    - from: "apps/web/src/components/app/auth-gate.tsx"
      to: "apps/web/src/components/app/auth-gate-modal.tsx"
      via: "renders AuthGateModal inside overlay when !isAuthenticated"
      pattern: "AuthGateModal"
    - from: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      to: "apps/web/src/server/auth/config.ts"
      via: "getCachedUser -> getCurrentUser -> auth.api.getSession"
      pattern: "getCachedUser"
---

<objective>
Harmonizar a documentação e cobertura de testes do auth gate com o Better Auth após o rebase.

Os dois ramos foram mesclados (260616-dw3 + 260615-ns8) e o sistema funciona corretamente em nível de código — os endpoints que o modal chama (/api/auth/sign-in/email, /api/auth/sign-up/email) são exatamente os paths que o Better Auth registra; a sessão é resolvida via auth.api.getSession; router.refresh() pós-login revalida os RSC que releem a sessão real. Nenhum conflito funcional foi encontrado.

O que está pendente:
1. Comentário de auth-gate-modal.tsx menciona "validateAuthPostOrigin" e "cookie HMAC httpOnly" — ambos removidos na ns8 (substituídos pelo originCheck nativo do Better Auth e pelo cookie better-auth.session_token). Comentário stale pode confundir leitura futura.
2. O componente AuthGate (auth-gate.tsx) não tem testes unitários próprios. Apenas auth-gate-modal.tsx tem teste. O layout.test.tsx cobre a renderização do overlay no contexto do layout, mas não testa o branch autenticado vs deslogado do AuthGate em isolamento.

Purpose: Fechar a higiene técnica: comentário preciso + cobertura mínima do componente gate.
Output: auth-gate.tsx com testes + comentário atualizado no modal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260616-dw3-auth-gate-workspace/260616-dw3-SUMMARY.md
@.planning/quick/260615-ns8-restaurar-verificacao-real-de-usuario-co/260615-ns8-SUMMARY.md
@apps/web/src/components/app/auth-gate.tsx
@apps/web/src/components/app/auth-gate-modal.tsx
@apps/web/src/components/app/__tests__/auth-gate-modal.test.tsx
@apps/web/src/app/(workspace)/workspace/layout.tsx
@apps/web/src/server/auth/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir comentário stale no auth-gate-modal.tsx</name>
  <files>apps/web/src/components/app/auth-gate-modal.tsx</files>
  <action>
    Atualizar o bloco de comentário JSDoc no topo de AuthGateModal para refletir a realidade pós-Better Auth.

    Substituir o texto atual do JSDoc (linhas 9-18 aproximadamente) por versão precisa:
    - Remover referência a "validateAuthPostOrigin" (função que não existe mais — removida na task 260615-ns8).
    - Remover referência a "cookie HMAC httpOnly" (cookie antigo `tabelin_session`). O cookie agora é `better-auth.session_token` gerenciado nativamente pelo Better Auth via toNextJsHandler.
    - Substituir pela descrição correta: as rotas /api/auth/sign-in/email e /api/auth/sign-up/email são gerenciadas pelo toNextJsHandler(auth.handler) do Better Auth; originCheck é feito nativamente pelo Better Auth via trustedOrigins configurado em auth.config.ts; o cookie de sessão é setado pelo Better Auth (better-auth.session_token httpOnly).
    - Manter as referências D-02/D-04/D-05 e T-dw3-03 (as decisões e ameaças continuam válidas, apenas a implementação subjacente mudou).
    - Manter o restante do componente INTOCADO (lógica, JSX, handlers).
  </action>
  <verify>
    <automated>grep -n "validateAuthPostOrigin\|HMAC httpOnly\|tabelin_session" apps/web/src/components/app/auth-gate-modal.tsx && echo "FAIL: stale references found" || echo "PASS: no stale references"</automated>
  </verify>
  <done>O arquivo não contém "validateAuthPostOrigin", "HMAC httpOnly" nem "tabelin_session". O JSDoc reflete que Better Auth (toNextJsHandler) gerencia origin check e cookie. Todos os testes existentes de auth-gate-modal.test.tsx continuam passando.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar testes unitários para AuthGate</name>
  <files>apps/web/src/components/app/__tests__/auth-gate.test.tsx</files>
  <behavior>
    - Test 1 (autenticado): quando isAuthenticated=true, AuthGate renderiza children sem overlay — container NÃO contém .auth-gate-overlay, children estão presentes.
    - Test 2 (deslogado): quando isAuthenticated=false, AuthGate renderiza children E o overlay — container contém .auth-gate-overlay, children estão presentes.
    - Test 3 (modal não-dispensável): quando isAuthenticated=false, o overlay contém um dialog (role="dialog") — não há botão com nome /fechar/i acessível no overlay.
    - Test 4 (data-testid do overlay): quando isAuthenticated=false, container contém elemento com data-testid="auth-gate-overlay".
  </behavior>
  <action>
    Criar apps/web/src/components/app/__tests__/auth-gate.test.tsx testando o componente AuthGate em isolamento.

    O componente AuthGate importa AuthGateModal. Mockar AuthGateModal para evitar dependência do router e do fetch:
      vi.mock("@/components/app/auth-gate-modal", () => ({
        AuthGateModal: () => &lt;div role="dialog" aria-label="mock-modal" /&gt;,
      }));

    Usar @testing-library/react render + screen + container.querySelector.

    Imports necessários: render, screen de @testing-library/react; describe, it, expect, vi de vitest; AuthGate de @/components/app/auth-gate.

    NÃO mockar next/navigation (AuthGate não usa roteamento).

    Seguir padrão de describe/it do auth-gate-modal.test.tsx existente.
  </action>
  <verify>
    <automated>cd /Users/RodrigoRR/projects/tabelin.ia/apps/web && npx vitest run src/components/app/__tests__/auth-gate.test.tsx --reporter=verbose 2>&1</automated>
  </verify>
  <done>auth-gate.test.tsx criado com 4 testes passando. Suite completa (vitest run) continua com todos os testes anteriores passando. Nenhuma regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/api/auth/* | Credenciais de login/cadastro cruzam aqui — gerenciado pelo Better Auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o50-01 | Tampering | auth-gate-modal.tsx comment | accept | Comentário stale não é superfície de ataque — corrigir por higiene |
| T-dw3-02 | Spoofing | AuthGate overlay | accept | Overlay é UX; proteção real é server-side no layout.tsx (getCachedUser → auth.api.getSession) |
| T-dw3-03 | Tampering | /api/auth/* | mitigate | Better Auth aplica originCheck nativo via trustedOrigins em auth.config.ts |
</threat_model>

<verification>
Run from apps/web:

```
npx vitest run --reporter=verbose
```

Expected: todos os 278+ testes passam (279 com os 4 novos de auth-gate.test.tsx). Zero regressões.

Check comentário corrigido:
```
grep -n "Better Auth\|toNextJsHandler\|better-auth.session_token" apps/web/src/components/app/auth-gate-modal.tsx
```
</verification>

<success_criteria>
- auth-gate-modal.tsx sem referências a "validateAuthPostOrigin", "HMAC httpOnly" ou "tabelin_session"
- auth-gate.test.tsx criado com 4 testes (autenticado sem overlay, deslogado com overlay, modal não-dispensável no overlay, data-testid presente)
- Suite completa: vitest run passa sem regressões
</success_criteria>

<output>
Create `.planning/quick/260616-o50-harmonizar-auth-gate-com-better-auth/260616-o50-SUMMARY.md` when done
</output>
