---
phase: quick-260616-o50
plan: "01"
subsystem: auth-gate
tags: [auth, tests, jsdoc, better-auth, hygiene]
dependency_graph:
  requires: [260616-dw3, 260615-ns8]
  provides: [auth-gate unit tests, corrected auth-gate-modal JSDoc]
  affects: [apps/web/src/components/app/auth-gate-modal.tsx, apps/web/src/components/app/__tests__/auth-gate.test.tsx]
tech_stack:
  added: []
  patterns: [vitest + @testing-library/react, vi.mock for component isolation]
key_files:
  created:
    - apps/web/src/components/app/__tests__/auth-gate.test.tsx
  modified:
    - apps/web/src/components/app/auth-gate-modal.tsx
decisions:
  - "Mock AuthGateModal (not AuthGate) in auth-gate.test.tsx to isolate gate logic from router/fetch dependencies"
  - "Verification run from main checkout using temp-copy approach due to worktree node_modules isolation"
metrics:
  duration: "~3 min"
  completed: "2026-06-16"
  tasks_completed: 2
  files_modified: 2
---

# Phase quick-260616-o50 Plan 01: Harmonizar auth gate com Better Auth — Summary

**One-liner:** Removidas referências obsoletas (validateAuthPostOrigin, HMAC httpOnly) do JSDoc de auth-gate-modal.tsx; adicionados 4 testes unitários para AuthGate cobrindo branches autenticado/deslogado em isolamento.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Corrigir comentário stale no auth-gate-modal.tsx | 8e9cd2a | apps/web/src/components/app/auth-gate-modal.tsx |
| 2 | Criar testes unitários para AuthGate | f37dc67 | apps/web/src/components/app/__tests__/auth-gate.test.tsx |

## What Was Done

### Task 1 — JSDoc atualizado em auth-gate-modal.tsx

O bloco JSDoc do componente `AuthGateModal` referenciava `validateAuthPostOrigin` e "cookie HMAC httpOnly" — ambos removidos quando o ns8 substituiu a implementação customizada de auth pelo Better Auth nativo.

O novo JSDoc descreve com precisão a realidade atual:
- Rotas `/api/auth/sign-in/email` e `/api/auth/sign-up/email` gerenciadas pelo `toNextJsHandler(auth.handler)` do Better Auth
- originCheck feito nativamente via `trustedOrigins` em `auth.config.ts`
- Cookie `better-auth.session_token` (httpOnly) gerenciado pelo Better Auth
- Referências D-02/D-04/D-05/T-dw3-03 mantidas (decisões e ameaças continuam válidas)
- Lógica e JSX intocados

Verificação: `grep -n "validateAuthPostOrigin\|HMAC httpOnly\|tabelin_session" auth-gate-modal.tsx` — PASS: no stale references.

### Task 2 — Testes unitários para AuthGate (TDD)

Criado `auth-gate.test.tsx` com 4 testes cobrindo o `AuthGate` em isolamento:

1. **Autenticado (isAuthenticated=true):** children renderizados, `.auth-gate-overlay` ausente
2. **Deslogado (isAuthenticated=false):** children E overlay presentes
3. **Modal não-dispensável:** overlay contém `role="dialog"`, sem botão com `/fechar/i`
4. **data-testid do overlay:** `data-testid="auth-gate-overlay"` presente quando deslogado

`AuthGateModal` é mockado via `vi.mock` para eliminar dependência de `useRouter` e `fetch`.

Resultado da suite completa (main checkout): **282 passed | 1 skipped** — zero regressões.

## Deviations from Plan

None — plan executed exactly as written.

Note: Verification run from main checkout (via temp-copy approach) because the worktree does not have `node_modules` installed and vitest binary lives in the main checkout's `apps/web/node_modules`. The worktree commit is the authoritative source; results are identical.

## Known Stubs

None.

## Threat Flags

No new security-relevant surface introduced. This task only modifies a JSDoc comment and adds test coverage.

## Self-Check: PASSED

- [x] `apps/web/src/components/app/auth-gate-modal.tsx` modified — stale references removed
- [x] `apps/web/src/components/app/__tests__/auth-gate.test.tsx` created
- [x] Commit 8e9cd2a exists (Task 1)
- [x] Commit f37dc67 exists (Task 2)
- [x] 4 new tests pass, 282 total pass, zero regressions
