---
phase: quick-260616-dw3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/page.tsx
  - apps/web/src/app/(workspace)/workspace/layout.tsx
  - apps/web/src/app/(workspace)/workspace/page.tsx
  - apps/web/src/components/app/auth-gate.tsx
  - apps/web/src/components/app/auth-gate-modal.tsx
  - apps/web/src/components/app/topbar.tsx
  - apps/web/src/styles/globals.css
  - apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx
  - apps/web/src/components/app/__tests__/auth-gate-modal.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Acessar / deslogado abre o workspace como preview travado (sem redirect para /sign-in)"
    - "Deslogado, a planilha renderiza com dados-demo (SAMPLE_SPEC), sem nenhuma query que dependa de user.id"
    - "Deslogado, qualquer clique no workspace mantém/revela o modal de login; o modal não fecha por overlay, ESC ou botão de fechar"
    - "O modal autentica chamando as MESMAS rotas /api/auth/sign-in/email e /api/auth/sign-up/email já existentes"
    - "Após login/cadastro com sucesso, a página revalida e o workspace passa a mostrar os dados reais do usuário, e o modal some"
    - "Logado, o workspace funciona exatamente como antes (histórico + spec ativo reais)"
  artifacts:
    - path: "apps/web/src/components/app/auth-gate.tsx"
      provides: "Overlay client que intercepta cliques e monta o modal não-dispensável quando deslogado"
    - path: "apps/web/src/components/app/auth-gate-modal.tsx"
      provides: "Modal com formulários de login/cadastro reusando /api/auth/*"
    - path: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      provides: "Layout que renderiza preview travado (SAMPLE_SPEC) quando !user, sem fetch de dados do usuário"
  key_links:
    - from: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      to: "apps/web/src/components/app/auth-gate.tsx"
      via: "passa isAuthenticated (server) ao AuthGate (client)"
      pattern: "AuthGate"
    - from: "apps/web/src/components/app/auth-gate-modal.tsx"
      to: "apps/web/src/app/api/auth/[...all]/route.ts"
      via: "fetch para /api/auth/sign-in/email e /api/auth/sign-up/email"
      pattern: "api/auth/sign-(in|up)/email"
---

<objective>
Remover a landing page e transformar o workspace em preview travado para usuários deslogados. A rota raiz deixa de redirecionar para /sign-in; o site abre direto no workspace (logado OU deslogado). Deslogado, a casca do workspace renderiza com dados-demo (SAMPLE_SPEC), qualquer clique abre/mantém um modal de login não-dispensável, e a autenticação reaproveita as rotas /api/auth existentes. Após autenticar, a página revalida e mostra os dados reais.

Purpose: Reduzir atrito de entrada — o valor do produto (planilha viva + chat) aparece imediatamente, e o login só é exigido ao interagir, sem nunca expor dados de outro usuário.
Output: AuthGate (overlay + modal), layout/page do workspace condicionais por sessão, rota raiz simplificada, CSS do modal, testes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@apps/web/src/app/page.tsx
@apps/web/src/app/(workspace)/workspace/layout.tsx
@apps/web/src/app/(workspace)/workspace/page.tsx
@apps/web/src/components/app/workspace-shell.tsx
@apps/web/src/components/app/topbar.tsx
@apps/web/src/app/api/auth/[...all]/route.ts
@apps/web/src/app/(auth)/sign-in/page.tsx
@apps/web/src/app/(auth)/sign-up/page.tsx
@apps/web/src/features/unified-chat/lib/sample-spec.ts
@apps/web/src/server/auth/session.ts
@apps/web/src/server/request-cache.ts
@apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rota raiz direto no workspace + layout/page condicionais por sessão (preview travado seguro)</name>
  <files>apps/web/src/app/page.tsx, apps/web/src/app/(workspace)/workspace/layout.tsx, apps/web/src/app/(workspace)/workspace/page.tsx, apps/web/src/components/app/topbar.tsx</files>
  <action>
Implementar D-03 e D-02 no servidor, preservando a segurança de dados (constraint: nenhum dado de usuário sem sessão).

1. apps/web/src/app/page.tsx: remover a leitura de getCurrentUser e o redirect condicional. A rota raiz passa a SEMPRE redirecionar para "/workspace" (logado ou deslogado). Manter o componente como server component assíncrono mínimo: `redirect("/workspace")`. Não importar mais getCurrentUser aqui.

2. apps/web/src/app/(workspace)/workspace/layout.tsx (D-02): remover o `redirect("/sign-in")` do ramo `!user`. Em vez disso, quando NÃO houver usuário, renderizar a MESMA casca do workspace porém em modo preview: usar SAMPLE_SPEC (de @/features/unified-chat/lib/sample-spec) como initialSpec e NÃO chamar getActiveSpreadsheetSpec (essa chamada só ocorre quando há user — proteção de dados). Quando houver usuário, manter o comportamento atual (initialSpec via getActiveSpreadsheetSpec(user.id)). Em ambos os ramos renderizar WorkspaceShell > workspace-page > Topbar + WorkspaceSplit(grid, children). Passar para o WorkspaceShell/Topbar a informação de autenticação: adicionar prop boolean `isAuthenticated` derivada de `Boolean(user)` e envolver todo o conteúdo do workspace-body com o componente AuthGate (criado na Task 2), passando `isAuthenticated`. O AuthGate, quando não autenticado, sobrepõe o modal e bloqueia cliques. A Topbar precisa de um `user`; quando deslogado, passar um placeholder seguro de preview SEM dados reais — definir e exportar de session.ts? NÃO: em vez disso tornar a prop `user` da Topbar opcional e, quando ausente, ocultar o e-mail/menu de conta e o botão "Nova conversa" (eles dependem de sessão). Ajustar Topbar conforme item 4.

3. apps/web/src/app/(workspace)/workspace/page.tsx (D-02): remover o `redirect("/sign-in")`. Quando NÃO houver usuário, renderizar `<UnifiedChatTool initialExchanges={[]} />` (sem histórico — não chamar findUnifiedConversationExchanges sem sessão, proteção de dados). Quando houver usuário, manter exatamente o comportamento atual (hidratação via findUnifiedConversationExchanges(user.id)).

4. apps/web/src/components/app/topbar.tsx: tornar `user` opcional (`user?: SessionUser`). Quando `user` for undefined (preview deslogado), não renderizar o bloco do menu de conta (e-mail/Sair) nem o botão "Nova conversa" (ambos exigem sessão). Manter a marca "Tabelin.IA" e o link "Privacidade". Os handlers signOut/handleDeleteHistory só são acionáveis quando `user` existe, então proteger a renderização desses controles por `user ? (...) : null`. Não alterar a lógica de signOut existente.

Garantir que NENHUM caminho deslogado invoque getActiveSpreadsheetSpec, findUnifiedConversationExchanges ou qualquer função que receba user.id.
  </action>
  <verify>
    <automated>cd apps/web && grep -n 'redirect("/sign-in")' src/app/\(workspace\)/workspace/layout.tsx src/app/\(workspace\)/workspace/page.tsx | grep -v '^#' ; test $? -ne 0 && echo "NO_SIGNIN_REDIRECT_OK"</automated>
  </verify>
  <done>page.tsx redireciona sempre para /workspace; layout e page do workspace não chamam mais redirect("/sign-in"); deslogado renderiza casca com SAMPLE_SPEC e UnifiedChatTool vazio, sem nenhuma chamada com user.id; logado mantém comportamento atual; Topbar oculta controles de conta quando sem user.</done>
</task>

<task type="auto">
  <name>Task 2: AuthGate (overlay bloqueante) + modal não-dispensável reusando /api/auth, com revalidação no sucesso</name>
  <files>apps/web/src/components/app/auth-gate.tsx, apps/web/src/components/app/auth-gate-modal.tsx, apps/web/src/styles/globals.css, apps/web/src/components/app/__tests__/auth-gate-modal.test.tsx</files>
  <action>
Implementar D-02, D-04 e D-05 no cliente.

1. apps/web/src/components/app/auth-gate.tsx ("use client"): componente que recebe `{ isAuthenticated: boolean; children: React.ReactNode }`. Quando `isAuthenticated` for true, renderizar apenas `{children}` (sem overlay). Quando false: renderizar `{children}` (a casca de preview com SAMPLE_SPEC) MAIS um overlay que cobre toda a área do workspace e intercepta os cliques — usar uma div overlay com posição fixa/absoluta cobrindo a viewport (pointer-events ativos), de modo que QUALQUER clique na área travada caia no overlay e (re)abra/mantenha o modal em vez de chegar ao conteúdo. O overlay sempre monta o AuthGateModal (D-02: o modal não é dispensável — ele permanece visível enquanto deslogado; não há estado de "fechado"). Passar ao modal um callback `onAuthenticated` (item 3).

2. apps/web/src/components/app/auth-gate-modal.tsx ("use client"): modal com dois modos alternáveis por um toggle interno (state "signin" | "signup"), reaproveitando a LÓGICA das páginas existentes (sign-in/page.tsx e sign-up/page.tsx) — formulários que dão fetch para as MESMAS rotas: POST /api/auth/sign-in/email ({email,password}) e POST /api/auth/sign-up/email ({name,email,password}), com headers content-type application/json (D-04). NÃO duplicar/reimplementar autenticação — apenas chamar as rotas existentes. Em caso de erro de resposta, exibir mensagem (reusar as cópias pt-BR das páginas atuais). NÃO renderizar botão de fechar, NÃO fechar por clique no overlay, NÃO fechar no ESC (D-02). O modal só "some" quando o usuário autentica e a página revalida (item 3).

3. Sucesso (D-05): após resposta ok do fetch de sign-in/sign-up, chamar `router.refresh()` (next/navigation) — isso revalida os server components (layout/page), que agora leem a sessão via cookie já setado pela rota de auth, passam `isAuthenticated=true` ao AuthGate e hidratam dados reais; o overlay/modal deixam de renderizar. Expor isso via `onAuthenticated` chamado pelo AuthGate, ou chamar router.refresh() diretamente no modal — preferir router.refresh() no modal após sucesso. Não usar router.push("/workspace") (já estamos no workspace); refresh basta para revalidar.

4. apps/web/src/styles/globals.css: adicionar estilos para o overlay (cobre a viewport, fundo semitransparente, z-index alto, pointer-events para capturar cliques) e o card do modal (centralizado). Reaproveitar variáveis/visual de .auth-page/.auth-panel já existentes onde fizer sentido; criar classes novas tipo .auth-gate-overlay e .auth-gate-modal sem quebrar .auth-page/.auth-panel atuais.

5. apps/web/src/components/app/__tests__/auth-gate-modal.test.tsx: criar teste (vitest + @testing-library/react, seguir o padrão de layout.test.tsx para mocks de next/navigation) cobrindo: (a) submeter o form de login dá fetch para "/api/auth/sign-in/email"; (b) ao alternar para cadastro e submeter, dá fetch para "/api/auth/sign-up/email"; (c) em resposta ok, router.refresh é chamado; (d) NÃO existe botão/elemento de fechar acessível (queryByLabelText("Fechar") é null). Mockar global.fetch resolvendo { ok: true }.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec vitest run src/components/app/__tests__/auth-gate-modal.test.tsx</automated>
  </verify>
  <done>AuthGate renderiza só children quando autenticado; quando não, sobrepõe overlay bloqueante + modal não-dispensável; o modal chama /api/auth/sign-in/email e /api/auth/sign-up/email e em sucesso chama router.refresh(); sem botão de fechar / sem fechar por overlay/ESC; testes do modal passam.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar teste do layout e validação cruzada (typecheck + suite)</name>
  <files>apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx</files>
  <action>
1. apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx: o teste "redireciona para /sign-in quando não há usuário autenticado" agora está OBSOLETO (D-03/D-02 removem o redirect). Substituí-lo por um teste que valida o novo comportamento: quando getCachedUser resolve null, WorkspaceLayout NÃO chama navigationMock.redirect e renderiza a casca de preview — esperar que a planilha-amostra ("Controle de Gastos", do SAMPLE_SPEC) esteja no documento e que o overlay do AuthGate (ex.: por className .auth-gate-overlay ou role/aria-label do modal) esteja presente. Manter os demais testes (render simultâneo grid+chat, ausência de Sidebar, painéis) — para o caso autenticado eles continuam usando user mockado. Ajustar quaisquer mocks novos necessários (ex.: o AuthGate é client e usa next/navigation já mockado; SAMPLE_SPEC é importado pelo layout). Não mockar fetch aqui salvo se o render exigir.

2. Garantir que a suite e o typecheck passam após as mudanças das Tasks 1-2.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec prisma generate >/dev/null 2>&1; pnpm exec tsc --noEmit && pnpm exec vitest run src/app/\(workspace\)/workspace/__tests__/layout.test.tsx src/components/app/__tests__/auth-gate-modal.test.tsx</automated>
  </verify>
  <done>Teste do layout atualizado para o preview travado (sem expectativa de redirect /sign-in); typecheck limpo; testes do layout e do modal passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser deslogado → RSC (layout/page) | Sessão ausente; nenhum dado de usuário pode ser carregado/serializado |
| modal (client) → /api/auth/* | Credenciais cruzam aqui; rotas existentes já validam origin + setam cookie HMAC httpOnly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-dw3-01 | Information Disclosure | layout.tsx / page.tsx ramo deslogado | mitigate | Ramo `!user` usa SAMPLE_SPEC e initialExchanges=[]; nunca invoca getActiveSpreadsheetSpec/findUnifiedConversationExchanges (sem user.id no caminho deslogado) |
| T-dw3-02 | Elevation of Privilege | AuthGate overlay (client) | accept | O gate é UX; a proteção real é server-side (RSC não envia dados sem sessão). Burlar o overlay no client só expõe a casca demo, sem dados |
| T-dw3-03 | Spoofing | auth-gate-modal → /api/auth | mitigate | Reusa rotas existentes que já fazem validateAuthPostOrigin e setam cookie HMAC httpOnly/sameSite=lax; sem nova superfície de auth |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` limpo em apps/web (rodar `pnpm exec prisma generate` antes — falso-positivo de worktree conhecido).
- `pnpm exec vitest run` para os testes tocados passa.
- grep confirma ausência de `redirect("/sign-in")` no layout e na page do workspace.
- grep confirma fetch para `/api/auth/sign-in/email` e `/api/auth/sign-up/email` no modal (sem nova lógica de auth).
- Manual (revisão): deslogado em "/" → cai em /workspace com planilha demo + modal travado; logar pelo modal → planilha real aparece e modal some.
</verification>

<success_criteria>
- Rota "/" não redireciona mais para /sign-in; abre o workspace (via redirect para /workspace).
- Deslogado: workspace renderiza preview com SAMPLE_SPEC, sem qualquer query por user.id; overlay intercepta cliques; modal não-dispensável (sem fechar por botão/overlay/ESC).
- Modal autentica pelas rotas /api/auth existentes; em sucesso, router.refresh() revalida e o workspace mostra dados reais, modal some.
- Logado: comportamento idêntico ao atual.
- typecheck + testes passam.
</success_criteria>

<output>
Create `.planning/quick/260616-dw3-auth-gate-workspace/260616-dw3-SUMMARY.md` when done
</output>
