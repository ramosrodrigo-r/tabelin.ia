---
phase: 05-ocr-charts-and-launch-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - apps/web/tests/e2e/smoke.spec.ts
  - apps/web/tests/fixtures/tabela-teste.png
autonomous: true
requirements:
  - OCR-01
  - OCR-02
  - OCR-03
  - CHRT-01
  - CHRT-02

must_haves:
  truths:
    - "npx playwright test smoke.spec.ts retorna 0 — todos os happy paths passam"
    - "Smoke test de auth: sign-up → workspace funciona end-to-end contra banco real"
    - "Smoke test de fórmula: prompt → streaming → output copiável (mock AI)"
    - "Smoke test de quota: 4o uso bloqueia usuário free (banco real)"
    - "Smoke test de checkout Pix: botão upgrade → checkout mockado"
    - "Smoke test de multi-tools: scripts, SQL, regex (mock AI)"
    - "Smoke test de upload + chat: CSV upload → chat com dados (mock AI)"
    - "Smoke test de OCR: upload imagem fixture → tabela reconstruída → copy TSV (mock API)"
    - "Smoke test de chart: upload CSV → Sugerir Gráfico → ChartMessage visível → alternar Bar/Line/Pie"
    - "Smoke test de privacy cleanup: upload deletado após logout/sessão encerrada"
  artifacts:
    - path: "apps/web/tests/e2e/smoke.spec.ts"
      provides: "Suite de smoke tests cobrindo todos os happy paths do MVP"
      contains: "smoke: OCR"
    - path: "apps/web/tests/fixtures/tabela-teste.png"
      provides: "Imagem PNG mínima válida para setInputFiles no teste de OCR"
  key_links:
    - from: "apps/web/tests/e2e/smoke.spec.ts"
      to: "/api/tools/ocr/process"
      via: "page.route('**/api/tools/ocr/process', ...) com mock JSON response"
      pattern: "api/tools/ocr/process"
    - from: "apps/web/tests/e2e/smoke.spec.ts"
      to: "/api/tools/file-analysis/chat"
      via: "page.route('**/api/tools/file-analysis/chat', ...) com NDJSON de chart mock"
      pattern: "api/tools/file-analysis/chat"
---

<objective>
Launch Hardening: criar smoke.spec.ts com Playwright cobrindo todos os happy paths do MVP — auth, formula, quota, checkout, scripts/SQL/regex, file upload + chat, OCR, charts, e privacy cleanup. Testes rodam contra banco Postgres real local com AI e webhooks mockados via page.route().

Purpose: Verificar que os deliverables de todos os planos 05-01 e 05-02 funcionam end-to-end, e que nenhuma regressão foi introduzida nos happy paths existentes (Fases 1-4).
Output: apps/web/tests/e2e/smoke.spec.ts + fixture de imagem PNG para teste de OCR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-ocr-charts-and-launch-hardening/05-CONTEXT.md
@.planning/phases/05-ocr-charts-and-launch-hardening/05-RESEARCH.md
@.planning/phases/05-ocr-charts-and-launch-hardening/05-PATTERNS.md

<interfaces>
<!-- Contratos e padrões existentes nos testes. Extraídos do codebase. -->

De apps/web/tests/e2e/formula.spec.ts (analog exato):
- import { expect, test } from "@playwright/test"
- Mock NDJSON: array de events JSON.stringify.join('\n')
- page.route("**/api/tools/...", async (route) => route.fulfill({ status: 200, contentType, body }))
- Sign-up: page.goto("/sign-up"), getByLabel("Nome"), getByLabel("Email"), getByLabel("Senha"), getByRole("button", { name: "Criar conta" }).click(), expect(page).toHaveURL(/workspace/)
- Clipboard: context.grantPermissions(["clipboard-read", "clipboard-write"])

De apps/web/tests/e2e/billing.spec.ts (analog para quota e checkout):
- Padrão de mock de webhook Mercado Pago sintético
- Quota block: fazer 4 tool uses e verificar banner de bloqueio

De apps/web/playwright.config.ts (verificar estado atual):
- baseURL, reuseExistingServer, webServer config

OCR mock response (JSON simples — não NDJSON):
{
  headers: ["Nome", "Valor", "Status"],
  rows: [["Alice", "100", "Ativo"], ["Bob", "200", "Inativo"]]
}

Chart mock NDJSON body (via complete event com JSON):
[
  { type: "delta", text: '{"chartType":"bar","title":"Valores por Nome","xKey":"Nome","yKey":"Valor","rows":[{"Nome":"Alice","Valor":100},{"Nome":"Bob","Valor":200}]}' },
  { type: "complete", content: '{"chartType":"bar","title":"Valores por Nome","xKey":"Nome","yKey":"Valor","rows":[{"Nome":"Alice","Valor":100},{"Nome":"Bob","Valor":200}]}' }
].map(e => JSON.stringify(e)).join("\n")

Fixture de imagem para OCR (tabela-teste.png):
- PNG mínimo válido de 1x1 pixel
- Suficiente para page.setInputFiles() — o mock da API não processa a imagem de verdade
- Criar via Buffer com bytes de PNG válido mínimo OU copiar de tests/fixtures/ se existir
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar fixture PNG e smoke.spec.ts com todos os happy paths do MVP</name>
  <files>
    apps/web/tests/fixtures/tabela-teste.png,
    apps/web/tests/e2e/smoke.spec.ts
  </files>
  <read_first>
    - apps/web/tests/e2e/formula.spec.ts — ler arquivo completo: padrão de import, estrutura de test(), sign-up flow, page.route mock, verificações com expect; estrutura do NDJSON mock body
    - apps/web/tests/e2e/billing.spec.ts — ler arquivo completo: padrão de quota block (4 uses), mock de webhook, verificação de estado Pro
    - apps/web/playwright.config.ts — ler arquivo: confirmar baseURL, reuseExistingServer, webServer config, diretório de testes e fixture
    - apps/web/tests/e2e/ — listar arquivos: ver quais helpers ou fixtures já existem (ex: setup files, test-utils)
    - apps/web/src/features/file-analysis/components/chat-panel.tsx — confirmar texto exato do botão "Sugerir Gráfico" conforme implementado no Plano 05-02 (getByText deve corresponder exatamente)
    - apps/web/src/features/ocr/components/image-upload-panel.tsx — confirmar texto do CTA "Enviar imagem" e do drop zone para os seletores Playwright
    - apps/web/src/features/ocr/components/ocr-result-panel.tsx — confirmar textos dos botões "Copiar TSV" e "Copiar CSV"
  </read_first>
  <action>
    Criar apps/web/tests/fixtures/tabela-teste.png como PNG válido mínimo de 1x1 pixel. Usar o seguinte método para gerar programaticamente via Node.js antes de criar o arquivo de teste: executar `node -e "const fs = require('fs'); const png1x1 = Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPhfDwAChwGA60e6kgAAAABJRU5ErkJggg==', 'base64'); fs.mkdirSync('tests/fixtures', { recursive: true }); fs.writeFileSync('tests/fixtures/tabela-teste.png', png1x1);"` no diretório apps/web. Este base64 é um PNG 1x1 válido (pixel transparente).

    Criar apps/web/tests/e2e/smoke.spec.ts com os seguintes blocos de test (usar email único com Date.now() em cada suite para isolamento):

    SUITE 1 — "smoke: auth flow" (sem mock): sign-up com email único → esperar URL /workspace → sign-out → sign-in → esperar URL /workspace. Testar com banco real (D-10). NÃO mockar rotas de auth.

    SUITE 2 — "smoke: formula generation" (mock AI): page.route para /api/tools/formula — NDJSON com delta + complete. Navegar para /workspace/formula, preencher prompt, clicar Gerar, esperar output copiável visível. Verificar getByText para o texto mock da fórmula.

    SUITE 3 — "smoke: quota block após 4 uses" (mock AI para tools): fazer 4 tool uses mockados e verificar que o 5o mostra banner de quota excedida. Testar com banco real — contagem de uso real é verificada. Mock apenas o AI, não a quota.

    SUITE 4 — "smoke: checkout Pix" (mock MP): page.route para /api/billing/checkout → retornar URL de checkout fake. Clicar botão upgrade Pro → verificar redirecionamento para URL mockada (ou abertura de nova aba).

    SUITE 5 — "smoke: multi-tools scripts/SQL/regex" (mock AI): page.route para /api/tools/scripts, /api/tools/sql, /api/tools/regex — cada um com NDJSON mock. Para cada ferramenta: navegar, preencher prompt, verificar output visível. Pode ser 3 sub-testes dentro da suite.

    SUITE 6 — "smoke: file upload + chat" (mock AI): page.route para /api/tools/file-analysis/chat com resposta NDJSON de texto pivot. Navegar para /workspace/file-analysis, fazer upload de CSV fixture (criar CSV mínimo inline via Blob se não existir arquivo fixture de CSV). Verificar SchemaPreview visível. Clicar "Resumo Pivô", verificar resposta no chat.

    SUITE 7 — "smoke: OCR imagem → tabela copiável" (mock OCR API): context.grantPermissions(["clipboard-read","clipboard-write"]). page.route para /api/tools/ocr/process com JSON mock { headers, rows }. Navegar para /workspace/ocr. Fazer upload de tests/fixtures/tabela-teste.png via page.setInputFiles. Clicar "Enviar imagem". Esperar tabela HTML visível (ex: expect(page.getByText("Alice")).toBeVisible()). Clicar "Copiar TSV". Verificar clipboard via page.evaluate(() => navigator.clipboard.readText()) retorna string com "Nome\tValor" (cabeçalho TSV).

    SUITE 8 — "smoke: chart sugestão e alternância" (mock AI): page.route para /api/tools/file-analysis/chat com NDJSON de chart mock. Navegar para /workspace/file-analysis, fazer upload de CSV fixture. Esperar estado chat. Clicar "Sugerir Gráfico". Esperar ChartMessage visível: expect(page.getByRole("img", { name: /Gráfico/ })).toBeVisible(). Clicar botão "Linhas". Verificar que botão "Linhas" ficou ativo (aria-pressed="true" OU verificar classe/estilo via getByRole("button", { name: "Linhas" }).getAttribute("aria-pressed")).

    SUITE 9 — "smoke: privacy cleanup" (banco real): após upload de arquivo e encerrar sessão (sign-out), verificar que chamada a /api/tools/file-analysis/chat com o uploadedFileId retorna 401 ou 404 (arquivo não mais acessível). ALTERNATIVA se difícil de verificar via Playwright: verificar que após sign-out o arquivo não aparece mais na UI ao fazer sign-in novamente.

    PADRÕES OBRIGATÓRIOS:
    - test.describe com nome descritivo por suite para organização
    - Email único: `test-${Date.now()}@tabelin-smoke.test` em cada suite (evita conflito entre runs)
    - page.route registrado ANTES da navegação relevante
    - Todos os expects com timeout implícito do Playwright (30s default)
    - Comentário no topo do arquivo: "// Smoke tests: rodam contra banco Postgres local real. AI mockado via page.route()."
    - Fixture tabela-teste.png: usar caminho absoluto relativo ao diretório de testes ou usar path.join(__dirname, "../fixtures/tabela-teste.png")

    D-11: OpenAI mockado via page.route — NÃO fazer chamadas reais à OpenAI. Mercado Pago mockado via page.route para /api/billing/. Banco de dados é REAL (D-10) — testar autenticação e quota contra Postgres local.

    CSV fixture para testes de file-analysis: criar inline no teste como Blob File: `const csvContent = 'Nome,Valor\nAlice,100\nBob,200'; const file = new File([csvContent], 'dados.csv', { type: 'text/csv' });` e usar via page.evaluateHandle + page.dispatchEvent, OU usar um arquivo CSV de fixture em tests/fixtures/dados.csv criado via fs.writeFileSync.
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia/apps/web && npx tsc --noEmit 2>&1 | grep "smoke.spec" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/tests/fixtures/tabela-teste.png existe e é arquivo binário PNG válido (file tests/fixtures/tabela-teste.png retorna "PNG image data")
    - apps/web/tests/e2e/smoke.spec.ts contém "smoke: OCR"
    - apps/web/tests/e2e/smoke.spec.ts contém "smoke: chart"
    - apps/web/tests/e2e/smoke.spec.ts contém "smoke: auth"
    - apps/web/tests/e2e/smoke.spec.ts contém "api/tools/ocr/process"
    - apps/web/tests/e2e/smoke.spec.ts contém "api/tools/file-analysis/chat"
    - apps/web/tests/e2e/smoke.spec.ts contém "setInputFiles"
    - apps/web/tests/e2e/smoke.spec.ts contém "grantPermissions"
    - apps/web/tests/e2e/smoke.spec.ts contém "clipboard"
    - npx tsc --project apps/web/tsconfig.json --noEmit retorna 0 (smoke.spec.ts sem erros de tipo)
  </acceptance_criteria>
  <done>smoke.spec.ts criado com 9 suites cobrindo todos os happy paths do MVP; fixture PNG criada para teste de OCR.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Executar smoke tests e verificar que todos os happy paths passam</name>
  <what-built>
    Suite completa de smoke tests E2E com Playwright cobrindo auth, formula, quota, checkout, scripts/SQL/regex, file upload + chat, OCR, charts e privacy cleanup.
  </what-built>
  <how-to-verify>
    1. Certifique-se de que o servidor de desenvolvimento está rodando: `cd /home/rodrigo/tabelin.ia && pnpm --filter web dev` (em outro terminal)
    2. Certifique-se de que o Postgres local está ativo: `pg_isready` deve retornar "accepting connections"
    3. Se necessário, instalar browsers Playwright: `cd apps/web && npx playwright install chromium`
    4. Rodar os smoke tests: `cd /home/rodrigo/tabelin.ia/apps/web && npx playwright test tests/e2e/smoke.spec.ts --reporter=list`
    5. Verificar que TODOS os testes passam (exit code 0)
    6. Se algum teste falhar:
       - Erros de seletor (element not found): verificar textos exatos nos componentes implementados e ajustar seletores
       - Erros de timeout: pode indicar problema na feature implementada nos Planos 05-01 ou 05-02
       - Erros de banco: verificar que Postgres local está rodando e schema está atualizado (npx prisma db push)
    7. Resultados esperados: 9 suites, todas passing (✓)
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se todos os smoke tests passarem (exit 0).
    Se algum teste falhar, descreva qual suite falhou e a mensagem de erro — o plano será ajustado.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → Next.js dev server | Testes enviam requests reais para auth e quota endpoints — banco Postgres local é alterado |
| page.route mock → testes | Mocks de AI e billing devem responder com formato exato esperado pelo código de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | Emails de teste no banco local | accept | Emails de teste são sintéticos (test-${Date.now()}@tabelin-smoke.test) — sem PII real; banco local não é produção |
| T-05-03-02 | Tampering | Mock page.route interceptando rotas de auth | mitigate | Rotas /api/auth/* NÃO são mockadas (D-10) — auth é testado contra banco real; apenas AI e billing são mockados |
| T-05-03-03 | DoS | Testes criando usuários no banco a cada run | accept | Emails únicos por Date.now() — sem conflito; ambiente local sem impacto em produção; sem cleanup obrigatório para MVP (pode ser adicionado em test.afterAll se necessário) |
| T-05-03-04 | Elevation of Privilege | smoke.spec.ts acessa APIs de admin ou privilegiadas | accept | smoke.spec.ts não acessa rotas administrativas; testa apenas fluxos de usuário final autenticado |
| T-05-03-SC | Tampering | npm/pnpm installs em configuração de CI | accept | Fase 5 roda localmente (D-09/D-11); CI/CD com Playwright é v2 (deferred em CONTEXT.md); sem risco de supply chain em ambiente local dev |
</threat_model>

<verification>
Verificações finais de launch hardening:

1. `cd /home/rodrigo/tabelin.ia/apps/web && npx playwright test tests/e2e/smoke.spec.ts --reporter=list` — exit code 0
2. `grep -c "test(" apps/web/tests/e2e/smoke.spec.ts` — retorna >= 9 (cobertura mínima de suites)
3. `grep "smoke: OCR" apps/web/tests/e2e/smoke.spec.ts` — encontra suite de OCR
4. `grep "smoke: chart" apps/web/tests/e2e/smoke.spec.ts` — encontra suite de chart
5. `file apps/web/tests/fixtures/tabela-teste.png` — retorna "PNG image data"
6. TypeScript final: `cd /home/rodrigo/tabelin.ia && npx tsc --project apps/web/tsconfig.json --noEmit` — retorna 0
</verification>

<success_criteria>
- npx playwright test tests/e2e/smoke.spec.ts retorna exit 0 com todos os testes passing
- Todos os 5 happy paths do MVP cobertos: auth, formula+quota+checkout, multi-tools, OCR, charts
- Privacy cleanup verificado (upload inacessível após sessão)
- Zero erros de tipo TypeScript no smoke.spec.ts
- Fixture tabela-teste.png é PNG válido (file command confirma)
</success_criteria>

<output>
Criar `.planning/phases/05-ocr-charts-and-launch-hardening/05-03-SUMMARY.md` quando concluído.
</output>
