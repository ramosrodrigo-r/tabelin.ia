---
phase: 09-extraction-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - apps/web/src/server/extraction/dispatcher.ts
  - apps/web/tests/extraction/dispatcher.test.ts
autonomous: true
requirements: [EXT-05, SEC-02]
must_haves:
  truths:
    - "O dispatcher único roteia cada tipo (CSV/XLSX/PNG/JPEG/PDF/TXT) ao extrator correto sem lógica duplicada (EXT-05)"
    - "Magic bytes e guard anti-ZIP-bomb são aplicados ANTES de qualquer parse (SEC-02)"
    - "Toda saída do dispatcher é ExtractionResult tipado (D-09)"
    - "Tipo não suportado / bytes inválidos retornam código acionável em pt-BR"
  artifacts:
    - path: "apps/web/src/server/extraction/dispatcher.ts"
      provides: "extractContent: roteamento por tipo detectado (EXT-05)"
      contains: "extractContent"
  key_links:
    - from: "apps/web/src/server/extraction/dispatcher.ts"
      to: "byte-validation + zip-guard + 4 extratores"
      via: "detectFileType → switch → extrator (zip-guard antes de xlsx)"
      pattern: "detectFileType|guardXlsxZip|extractPdf|extractImage|extractCsvXlsx|extractTxt"
---

<objective>
Construir o dispatcher único (`extractContent`, EXT-05) que orquestra todos os componentes das Plans 02/03: detecta o tipo por magic bytes (com fallback textual por extensão), aplica o guard anti-ZIP-bomb antes do parse XLSX, e roteia ao extrator correto — devolvendo sempre um `ExtractionResult` tipado. Um teste de integração end-to-end exercita todos os caminhos.

Purpose: É a costura final (Wave 3) que entrega EXT-05/D-08 (estrutura dispatcher + um arquivo por formato, sem lógica duplicada) e fecha SEC-02 (ordem correta: validação de bytes → guard → parse). Consumido pelas rotas dos 5 tools na Phase 10 (fora desta fase).
Output: `dispatcher.ts` + teste de integração cobrindo CSV/XLSX/PNG/JPEG/PDF/TXT/inválido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-extraction-infrastructure/09-CONTEXT.md
@.planning/phases/09-extraction-infrastructure/09-RESEARCH.md
@.planning/phases/09-extraction-infrastructure/09-PATTERNS.md
@apps/web/src/server/extraction/types.ts
@apps/web/src/server/extraction/byte-validation.ts
@apps/web/src/server/extraction/zip-guard.ts
@apps/web/src/server/extraction/pdf-extractor.ts
@apps/web/src/server/extraction/csv-xlsx-extractor.ts
@apps/web/src/server/extraction/image-extractor.ts
@apps/web/src/server/extraction/txt-extractor.ts

<interfaces>
<!-- Contratos das Plans 02/03 que o dispatcher consome. Sem exploração de codebase necessária. -->

types.ts: ExtractionResult = { ok: true; text: string } | { ok: false; code; message }

byte-validation.ts (Plan 03): detectFileType(bytes: Uint8Array) → discriminante { kind: "pdf"|"png"|"jpg"|"xlsx"|"text"|"unsupported" } (shape final é o entregue pela Plan 03; ler o arquivo)
zip-guard.ts (Plan 03):     guardXlsxZip(bytes: Uint8Array): { ok: true } | ExtractionError
pdf-extractor.ts (Plan 03): extractPdf(bytes: Uint8Array): Promise<ExtractionResult>

csv-xlsx-extractor.ts (Plan 02): extractCsvXlsx(buffer, mimeType: "csv"|"xlsx", fileName): ExtractionResult
image-extractor.ts (Plan 02):    extractImage(buffer, mimeType: "image/png"|"image/jpeg"): Promise<ExtractionResult>
txt-extractor.ts (Plan 02):      extractTxt(buffer): ExtractionResult

Esqueleto de roteamento de referência: RESEARCH 395-428.
Conversões de buffer (Pitfall 6): file-type/fflate querem Uint8Array; parseFile quer ArrayBuffer; processImageOcr quer base64. Centralizar AQUI.
Input canônico recomendado: Node Buffer + nome declarado (Open Question 2 / RESEARCH 480-483).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dispatcher de roteamento por tipo (extractContent, EXT-05)</name>
  <files>apps/web/src/server/extraction/dispatcher.ts</files>
  <read_first>
    - .planning/phases/09-extraction-infrastructure/09-RESEARCH.md (linhas 134-178 — diagrama de arquitetura; linhas 395-428 — esqueleto do dispatcher; linhas 386-389 — Pitfall 6 conversões)
    - .planning/phases/09-extraction-infrastructure/09-PATTERNS.md (linhas 54-68 — pattern assignment dispatcher; linhas 230-254 — Pattern 2 fallback textual)
    - apps/web/src/server/extraction/byte-validation.ts (shape real do retorno de detectFileType)
    - apps/web/src/server/extraction/zip-guard.ts, pdf-extractor.ts, csv-xlsx-extractor.ts, image-extractor.ts, txt-extractor.ts (assinaturas reais)
    - apps/web/src/server/extraction/types.ts (contrato)
  </read_first>
  <behavior>
    - Test: bytes binários detectados como PDF → roteia a extractPdf.
    - Test: bytes PNG/JPEG → roteia a extractImage com o mimeType mapeado (png→image/png, jpg→image/jpeg).
    - Test: bytes XLSX → chama guardXlsxZip ANTES de extractCsvXlsx; se o guard falhar (ZIP_BOMB), retorna sem parsear.
    - Test: bytes sem magic bytes ("text") + nome ".csv" → extractCsvXlsx("csv"); nome ".txt" → extractTxt.
    - Test: binário não suportado → `{ ok: false, code: "UNSUPPORTED_TYPE" }`; bytes que não casam nenhum caminho → UNSUPPORTED_TYPE/INVALID_BYTES (pt-BR).
  </behavior>
  <action>
    Criar `dispatcher.ts` com `import "server-only";` e função pública `extractContent(buffer: Buffer, declaredName: string): Promise<ExtractionResult>` (input canônico Node Buffer + nome — Open Question 2). Centralizar as conversões de buffer aqui (Pitfall 6): derivar `Uint8Array` para `detectFileType`/`guardXlsxZip`, `ArrayBuffer` para `extractCsvXlsx`, e o próprio Buffer para `extractImage` (que converte a base64 internamente).
    Fluxo: chamar `detectFileType(bytes)`. Para binários (`pdf`/`png`/`jpg`/`xlsx`) rotear pelo tipo DETECTADO, ignorando a extensão declarada (D-10). Caminho `xlsx`: chamar `guardXlsxZip(bytes)` PRIMEIRO; se `ok:false`, retornar esse erro ZIP_BOMB sem parsear (anti-pattern RESEARCH 325). Caminho `png`/`jpg`: mapear mimeType e chamar `extractImage`. Caminho `pdf`: `extractPdf`. Para `text` (sem magic bytes), decidir CSV vs TXT pela extensão de `declaredName` (`.csv`→extractCsvXlsx("csv"), `.txt`→extractTxt, outro→UNSUPPORTED_TYPE). `unsupported`/sem match → `{ ok: false, code: "UNSUPPORTED_TYPE", message }` (pt-BR). Sem lógica de parsing duplicada — só orquestração (EXT-05). Sem fenced code de implementação.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run tests/extraction/dispatcher.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export async function extractContent\|export function extractContent' apps/web/src/server/extraction/dispatcher.ts` retorna 1
    - `grep -cE 'detectFileType|guardXlsxZip|extractPdf|extractImage|extractCsvXlsx|extractTxt' apps/web/src/server/extraction/dispatcher.ts` retorna ≥6 (todos orquestrados)
    - Teste: no caminho xlsx, `guardXlsxZip` é chamado antes de `extractCsvXlsx` (ZIP_BOMB curto-circuita o parse)
    - Teste: png→image/png, jpg→image/jpeg; ".csv" sem magic bytes → CSV; ".txt" → TXT; não suportado → UNSUPPORTED_TYPE
    - `cd apps/web && npx tsc --noEmit` sem erro em `extraction/dispatcher.ts`
  </acceptance_criteria>
  <done>`extractContent` detecta tipo por magic bytes, aplica o guard antes do parse XLSX, roteia cada tipo ao extrator correto e retorna sempre ExtractionResult.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teste de integração end-to-end do dispatcher (EXT-05 + SEC-02)</name>
  <files>apps/web/tests/extraction/dispatcher.test.ts</files>
  <read_first>
    - apps/web/tests/file-parser.test.ts (linhas 1-30 — helpers de geração de buffer CSV/XLSX reais)
    - .planning/phases/09-extraction-infrastructure/09-RESEARCH.md (linhas 134-178 — todos os caminhos do diagrama)
    - apps/web/src/server/extraction/dispatcher.ts (a função sob teste)
  </read_first>
  <behavior>
    - Test (EXT-01): CSV real via extractContent → `{ ok: true }` com schema + amostra.
    - Test (EXT-01/D-05): XLSX real multi-aba → texto com `## Aba:` para cada aba.
    - Test (EXT-02): imagem (fixture-mode sem OPENAI_API_KEY) → `{ ok: true }` com tabela Markdown.
    - Test (EXT-04): TXT → `{ ok: true, text }` direto.
    - Test (SEC-02): XLSX malicioso (excede caps) → `{ ok: false, code: "ZIP_BOMB" }`, sem parsear.
    - Test (SEC-02): binário disfarçado (ex.: bytes PNG com nome ".txt") → roteado por magic bytes como imagem, NÃO como texto.
    - Test: formato não suportado → `{ ok: false, code: "UNSUPPORTED_TYPE" }`.
  </behavior>
  <action>
    Criar `dispatcher.test.ts` exercitando `extractContent` por TODOS os caminhos do diagrama (RESEARCH 134-178). Reusar os helpers de geração de XLSX/CSV reais do estilo de file-parser.test.ts (gerar buffers via lib `xlsx`, não fixtures opacos). Provar o caso de segurança chave do SEC-02: um arquivo com magic bytes de imagem mas nome `.txt` deve ser roteado pelo TIPO REAL (imagem), confirmando que a extensão declarada não bypassa a detecção (D-10). Provar que XLSX que excede caps retorna ZIP_BOMB antes de qualquer parse. Cobrir EXT-01/02/04 + UNSUPPORTED_TYPE. Sem fenced code de implementação; o executor escreve o teste no estilo vitest do projeto.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run tests/extraction/dispatcher.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run tests/extraction/dispatcher.test.ts` sai 0
    - O teste contém um caso de "magic bytes sobrepõem extensão" (binário com nome .txt → roteado pelo tipo real)
    - O teste contém um caso ZIP_BOMB que retorna sem parsear
    - Caminhos CSV, XLSX multi-aba, imagem(fixture), TXT e UNSUPPORTED_TYPE todos verdes
  </acceptance_criteria>
  <done>Teste de integração cobre todos os caminhos do dispatcher e prova o roteamento por magic bytes + guard anti-ZIP-bomb (EXT-05/SEC-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bytes do arquivo → dispatcher | Ponto único de entrada de bytes não confiáveis; a ordem das checagens (magic bytes → guard → parse) é o controle de segurança central. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Spoofing | dispatcher (extensão declarada) | mitigate | Binários roteados pelo tipo DETECTADO (magic bytes), nunca pela extensão; teste prova que nome `.txt` com bytes de imagem é tratado como imagem (D-10). |
| T-09-04-02 | Denial of Service | dispatcher caminho xlsx | mitigate | `guardXlsxZip` é invocado ANTES de `extractCsvXlsx`; ZIP_BOMB curto-circuita o parse (D-11). |
| T-09-04-03 | Information Disclosure | erros do dispatcher | mitigate | Saídas são códigos tipados + mensagens pt-BR; nenhum byte raw exposto/logado (PRIV-02). |
| T-09-04-04 | Tampering | input não roteável | mitigate | Default explícito → UNSUPPORTED_TYPE/INVALID_BYTES; nenhum caminho silencioso de fall-through. |
</threat_model>

<verification>
- Dispatcher roteia todos os tipos sem lógica de parsing duplicada (EXT-05).
- magic bytes → guard → parse na ordem correta; binário disfarçado de texto é detectado (SEC-02).
- Toda saída é ExtractionResult tipado com mensagens pt-BR (D-09).
</verification>

<success_criteria>
- `extractContent` entregue como ponto único de extração, consumível pelas rotas da Phase 10.
- Teste de integração cobre CSV/XLSX/PNG/JPEG/PDF/TXT/ZIP_BOMB/UNSUPPORTED.
- `cd apps/web && npx vitest run tests/extraction/` (toda a suíte da fase) sai 0.
</success_criteria>

<output>
Create `.planning/phases/09-extraction-infrastructure/09-04-SUMMARY.md` when done
</output>
