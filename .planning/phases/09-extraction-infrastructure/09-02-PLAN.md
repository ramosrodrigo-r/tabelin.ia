---
phase: 09-extraction-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/server/ai/file-chat-stream.ts
  - apps/web/src/server/extraction/csv-xlsx-extractor.ts
  - apps/web/src/server/extraction/image-extractor.ts
  - apps/web/src/server/extraction/txt-extractor.ts
  - apps/web/tests/extraction/reuse-extractors.test.ts
autonomous: true
requirements: [EXT-01, EXT-02, EXT-04]
must_haves:
  truths:
    - "Um CSV/XLSX retorna schema + ~10 linhas de amostra como texto plano (EXT-01)"
    - "Um XLSX multi-aba concatena todas as abas com rótulo '## Aba: <nome>' (D-05)"
    - "Uma imagem PNG/JPEG retorna tabela Markdown via OCR, com fixture-mode preservado (EXT-02)"
    - "Um TXT retorna seu conteúdo direto via TextDecoder (EXT-04)"
  artifacts:
    - path: "apps/web/src/server/extraction/csv-xlsx-extractor.ts"
      provides: "Wrapper de parseFile + serialização schema + ~10 linhas + cap ~200 linhas/aba (D-01/D-02/D-05/D-06)"
      contains: "## Aba:"
    - path: "apps/web/src/server/extraction/image-extractor.ts"
      provides: "Wrapper de processImageOcr + serialização Markdown (D-03)"
      contains: "processImageOcr"
    - path: "apps/web/src/server/extraction/txt-extractor.ts"
      provides: "Leitura TXT via TextDecoder (D-04)"
      contains: "TextDecoder"
  key_links:
    - from: "apps/web/src/server/extraction/csv-xlsx-extractor.ts"
      to: "apps/web/src/server/ai/file-chat-stream.ts"
      via: "import { formatSchemaForPrompt }"
      pattern: "formatSchemaForPrompt"
    - from: "apps/web/src/server/extraction/image-extractor.ts"
      to: "apps/web/src/server/ai/ocr-processor.ts"
      via: "import { processImageOcr }"
      pattern: "processImageOcr"
---

<objective>
Construir os 3 extratores que EMBRULHAM código já existente (sem novas deps): CSV/XLSX (reusa `parseFile` + `formatSchemaForPrompt`), imagem (reusa `processImageOcr`), e TXT (`TextDecoder`). Inclui o seam crítico de tornar `formatSchemaForPrompt` exportável (mudança aditiva, compatível com D-07).

Purpose: Estes extratores cobrem EXT-01/EXT-02/EXT-04 reusando os parsers de schema e OCR já validados (D-07: zero toque nas rotas/repos). São a metade "reuse" da Wave 2 — rodam em paralelo com a Plan 03 (security/new-lib) por não compartilharem arquivos.
Output: 3 extratores em `server/extraction/`, o export aditivo de `formatSchemaForPrompt`, e testes cobrindo multi-aba/amostra/cap-200/Markdown/TXT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-extraction-infrastructure/09-CONTEXT.md
@.planning/phases/09-extraction-infrastructure/09-RESEARCH.md
@.planning/phases/09-extraction-infrastructure/09-PATTERNS.md
@apps/web/src/server/extraction/types.ts
@apps/web/src/server/file-analysis/file-parser.ts
@apps/web/src/server/ai/file-chat-stream.ts
@apps/web/src/server/ai/ocr-processor.ts

<interfaces>
<!-- Contratos exatos extraídos do codebase. O executor usa estes diretamente — sem exploração. -->

types.ts (Plan 01):
  type ExtractionResult = { ok: true; text: string } | { ok: false; code: ExtractionErrorCode; message: string }

file-parser.ts (EXISTENTE — reusar, NÃO reescrever):
  export function parseFile(buffer: ArrayBuffer, mimeType: "csv" | "xlsx", sheetName?: string, fileName?: string): FileSchema
  // IMPORTANTE: parseFile NÃO expõe parâmetro de limite de linhas. Internamente usa MAX_ROWS=1000 (só no path XLSX, file-parser.ts:129)
  // e extractSchema fatia sampleRows a 10 (file-parser.ts:82). O FileSchema retornado SÓ traz `sampleRows` (≤10) + `rowCount` (total da aba).
  // ⇒ O extrator NÃO recebe de volta as ~1000 linhas; recebe 10 amostras + a contagem total. O cap de D-06 (~200) deve operar
  //    sobre algo que o extrator REALMENTE controla na camada de serialização (ver Task 1) — não sobre a leitura interna do parser.

FileSchema (@tabelin/shared):
  { columns: { name: string; type: "numero"|"data"|"booleano"|"texto"; sampleValues: unknown[] }[];
    sampleRows: Record<string, unknown>[]; rowCount: number; sheetName?: string; fileName: string }

file-chat-stream.ts (MOD aditiva — adicionar `export` à função local linha 17):
  formatSchemaForPrompt(schema: FileSchema): string  // já traz delimitadores anti-injection

ocr-processor.ts (EXISTENTE — reusar):
  export async function processImageOcr(imageBase64: string, mimeType: "image/png" | "image/jpeg"): Promise<{ headers: string[]; rows: string[][] }>
  // Fixture-mode automático quando !process.env.OPENAI_API_KEY (ocr-processor.ts:40-43) → herdado ao embrulhar.

Helpers de serialização (Claude's discretion no formato exato): sampleRowsToBlock (RESEARCH 295-304), ocrToMarkdown (RESEARCH 313-319).
Para multi-aba XLSX: ler workbook.SheetNames e chamar parseFile(buffer,"xlsx",sheetName) por aba.
Conversões de buffer (Pitfall 6): parseFile quer ArrayBuffer; processImageOcr quer base64.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exportar formatSchemaForPrompt + extrator CSV/XLSX (EXT-01)</name>
  <files>apps/web/src/server/ai/file-chat-stream.ts, apps/web/src/server/extraction/csv-xlsx-extractor.ts</files>
  <read_first>
    - apps/web/src/server/ai/file-chat-stream.ts (linhas 1-33 — função local formatSchemaForPrompt a exportar; Pitfall 5)
    - apps/web/src/server/file-analysis/file-parser.ts (linhas 72-146 — parseFile NÃO expõe limite de linhas; extractSchema fatia sampleRows a 10 e retorna rowCount total; multi-aba via XLSX.read)
    - .planning/phases/09-extraction-infrastructure/09-RESEARCH.md (linhas 289-307 — Pattern 4: D-01/D-02/D-05/D-06; linhas 475-478 — Open Question RESOLVED sobre ~10 vs ~200 linhas)
    - .planning/phases/09-extraction-infrastructure/09-PATTERNS.md (linhas 97-117 — pattern assignment csv-xlsx; linhas 165-167 — export aditivo)
    - apps/web/src/server/extraction/types.ts (contrato ExtractionResult)
  </read_first>
  <behavior>
    - Test: um CSV simples → `{ ok: true, text }` contendo "Arquivo:", "Colunas", e um bloco "Amostra de linhas:" com até 10 linhas (D-02).
    - Test: um XLSX com 2 abas → texto contém "## Aba: <nome1>" E "## Aba: <nome2>", cada bloco com seu schema (D-05).
    - Test (cap D-06 EXERCITADO): uma aba cujo `schema.rowCount` > MAX_ROWS_PER_SHEET (=200) → a contagem de linhas REPORTADA no texto daquela aba é clampada a 200 (ex.: aparece "200" / "≥200 linhas", NÃO o total bruto), provando que o cap recorta o resultado. O bloco de amostra serializado permanece ≤10 linhas (D-02). O teste DEVE importar/referenciar a constante `MAX_ROWS_PER_SHEET` e asseverar que o número reportado === `MAX_ROWS_PER_SHEET` (não um magic number solto).
    - Test: CSV/XLSX vazio (sem colunas) → `{ ok: false, code: "EMPTY_EXTRACTION" }`.
  </behavior>
  <action>
    Primeiro: adicionar `export` à função `formatSchemaForPrompt` em `file-chat-stream.ts` linha 17 (mudança aditiva — NÃO altera assinatura nem comportamento; compatível com D-07 / A4). Não tocar em mais nada nesse arquivo.

    Criar `csv-xlsx-extractor.ts` com `import "server-only";` e função `extractCsvXlsx(buffer, mimeType: "csv"|"xlsx", fileName)` retornando `ExtractionResult`. Declarar e EXPORTAR a constante `export const MAX_ROWS_PER_SHEET = 200;` (D-06) no topo do módulo, para que o teste a referencie.

    Para CSV: `parseFile(arrayBuffer, "csv", undefined, fileName)` → serializar via `formatSchemaForPrompt(schema)` + um bloco de ~10 linhas completas (`schema.sampleRows.slice(0,10)`, datas via `.toISOString()` — D-02).

    Para XLSX: ler `workbook.SheetNames` (via `XLSX.read` para enumerar abas), iterar chamando `parseFile(arrayBuffer,"xlsx",sheetName,fileName)` por aba, prefixar cada bloco com `## Aba: <nome>` (D-05) e concatenar.

    ENFORCEMENT CONCRETO DO CAP ~200 (D-06) — onde realmente recortar (NÃO é no MAX_ROWS=1000 do parser, que o extrator não controla): `parseFile` devolve apenas `sampleRows` (≤10) + `rowCount` (total da aba). O cap opera na CAMADA DE SERIALIZAÇÃO, sobre o que o extrator possui:
      (a) bloco de amostra continua ≤10 linhas (D-02 — `MAX_ROWS_PER_SHEET` NUNCA aumenta a amostra);
      (b) a CONTAGEM efetiva reportada por aba é `effectiveRowCount = Math.min(schema.rowCount, MAX_ROWS_PER_SHEET)` — é esse valor (limitado a 200) que entra no texto/serialização da aba, não o `rowCount` bruto. Assim uma aba com 5.000 linhas produz um resultado BOUNDED por 200 (anti-DoS / economia de tokens dado que D-05 extrai TODAS as abas), e o cap deixa de ser no-op porque altera observação do output.
    `formatSchemaForPrompt` consome `FileSchema`; para não duplicar a serialização, passe à formatação uma cópia do schema com `rowCount: effectiveRowCount` (clamp) — ou serialize a contagem clampada no rótulo da aba — mantendo `formatSchemaForPrompt` e o bloco de ~10 linhas intactos. Escolha o ponto que o teste consegue asseverar (a contagem reportada === `MAX_ROWS_PER_SHEET` quando o total excede 200).

    Schema sem colunas (rowCount 0 / columns vazio) → `EMPTY_EXTRACTION`. Converter input para `ArrayBuffer` antes de `parseFile` (Pitfall 6). Resolução da Open Question 1: `~10` = tamanho do bloco de amostra (D-02); `~200` = `MAX_ROWS_PER_SHEET`, teto da contagem efetiva serializada por aba (D-06). Sem fenced code de implementação; usar identificadores do bloco `<interfaces>`.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run tests/extraction/reuse-extractors.test.ts -t "csv|xlsx|aba|cap|200" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' apps/web/src/server/ai/file-chat-stream.ts | grep -c 'export function formatSchemaForPrompt\|export { formatSchemaForPrompt'` retorna ≥1
    - `csv-xlsx-extractor.ts` importa `formatSchemaForPrompt` e `parseFile` (grep por ambos)
    - `grep -v '^#' apps/web/src/server/extraction/csv-xlsx-extractor.ts | grep -c '## Aba:'` retorna ≥1
    - `grep -v '^//' apps/web/src/server/extraction/csv-xlsx-extractor.ts | grep -c 'MAX_ROWS_PER_SHEET'` retorna ≥1 (constante existe e é referenciada na lógica de cap, não só comentada)
    - O teste de cap importa `MAX_ROWS_PER_SHEET` do extrator e assevera: aba com `rowCount` > 200 → contagem reportada no texto === `MAX_ROWS_PER_SHEET` (cap EXERCITADO, não no-op)
    - Teste: XLSX 2-abas produz texto com ambos os rótulos `## Aba:`; CSV produz bloco "Amostra de linhas:" com ≤10 linhas
    - Vazio → `{ ok: false, code: "EMPTY_EXTRACTION" }`
  </acceptance_criteria>
  <done>`formatSchemaForPrompt` exportado; `extractCsvXlsx` reusa `parseFile`, serializa schema+~10 linhas, itera multi-aba com `## Aba:`, e capa a contagem efetiva por aba a `MAX_ROWS_PER_SHEET` (=200) na serialização — provado por teste que referencia a constante.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extrator de imagem OCR → Markdown (EXT-02)</name>
  <files>apps/web/src/server/extraction/image-extractor.ts</files>
  <read_first>
    - apps/web/src/server/ai/ocr-processor.ts (linhas 36-110 — processImageOcr, assinatura base64+mimeType, fixture-mode linhas 40-43)
    - .planning/phases/09-extraction-infrastructure/09-RESEARCH.md (linhas 309-321 — Pattern 5 ocrToMarkdown; linhas 374-378 — Pitfall 4 base64)
    - .planning/phases/09-extraction-infrastructure/09-PATTERNS.md (linhas 120-133 — pattern assignment image-extractor)
    - apps/web/src/server/extraction/types.ts (contrato ExtractionResult)
  </read_first>
  <behavior>
    - Test: imagem com OPENAI_API_KEY ausente → fixture-mode aciona (via wrap de processImageOcr) e retorna `{ ok: true, text }` com tabela Markdown (`| col | col |` + separador `---`).
    - Test: `{ headers: [], rows: [] }` (sem tabela) → `{ ok: false, code: "EMPTY_EXTRACTION" }`.
    - Test: mapeamento de mimeType — chamada com `image/jpeg` repassa "image/jpeg" a processImageOcr (não quebra a assinatura png|jpeg).
  </behavior>
  <action>
    Criar `image-extractor.ts` com `import "server-only";` e função `extractImage(buffer, mimeType: "image/png"|"image/jpeg")` retornando `Promise<ExtractionResult>`. Converter o buffer para base64 com `buffer.toString("base64")` (Pitfall 4 — processImageOcr espera base64, NÃO Buffer) e chamar `processImageOcr(base64, mimeType)`. Serializar `{ headers, rows }` como tabela Markdown via helper `ocrToMarkdown` (header row + linha separadora `| --- | --- |` + body — formato exato é Claude's discretion). `headers.length === 0` → `EMPTY_EXTRACTION`. Não logar bytes raw (PRIV-02); em erro inesperado, `try/catch` → `EMPTY_EXTRACTION` sem expor conteúdo.
    O fixture-mode é herdado automaticamente ao embrulhar `processImageOcr` (não reimplementar). Sem fenced code de implementação.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run tests/extraction/reuse-extractors.test.ts -t "image|ocr|markdown" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'processImageOcr' apps/web/src/server/extraction/image-extractor.ts` retorna ≥1
    - `grep -c "toString(\"base64\")\|toString('base64')" apps/web/src/server/extraction/image-extractor.ts` retorna ≥1
    - Teste (sem OPENAI_API_KEY): retorna `{ ok: true }` com texto contendo `|` e `---`
    - Teste headers vazio → `{ ok: false, code: "EMPTY_EXTRACTION" }`
  </acceptance_criteria>
  <done>`extractImage` converte buffer→base64, reusa `processImageOcr` (fixture-mode preservado) e serializa para tabela Markdown; sem tabela → EMPTY_EXTRACTION.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extrator TXT via TextDecoder (EXT-04)</name>
  <files>apps/web/src/server/extraction/txt-extractor.ts</files>
  <read_first>
    - apps/web/src/server/file-analysis/file-parser.ts (linha 135 — técnica TextDecoder("utf-8").decode)
    - .planning/phases/09-extraction-infrastructure/09-PATTERNS.md (linhas 153-161 — pattern assignment txt-extractor)
    - apps/web/src/server/extraction/types.ts (contrato ExtractionResult)
  </read_first>
  <behavior>
    - Test: um buffer UTF-8 com texto "olá mundo" → `{ ok: true, text: "olá mundo" }`.
    - Test: buffer vazio ou só whitespace → `{ ok: false, code: "EMPTY_EXTRACTION" }`.
  </behavior>
  <action>
    Criar `txt-extractor.ts` com `import "server-only";` e função `extractTxt(buffer)` retornando `ExtractionResult`. Decodificar com `new TextDecoder("utf-8").decode(...)` (mesma técnica do CSV path em file-parser.ts:135). Se `text.trim().length === 0` → `{ ok: false, code: "EMPTY_EXTRACTION", message }` (pt-BR). Caso contrário `{ ok: true, text }` (D-04 — texto direto, sem reformatação tabular). Sem fenced code de implementação.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run tests/extraction/reuse-extractors.test.ts -t "txt" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TextDecoder' apps/web/src/server/extraction/txt-extractor.ts` retorna ≥1
    - Teste: texto UTF-8 retorna `{ ok: true, text: <conteúdo> }` idêntico ao input decodificado
    - Teste: vazio/whitespace → `{ ok: false, code: "EMPTY_EXTRACTION" }`
  </acceptance_criteria>
  <done>`extractTxt` decodifica UTF-8 e devolve o texto direto; vazio → EMPTY_EXTRACTION.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bytes do arquivo → extrator | Conteúdo de arquivo não confiável é decodificado/parseado; risco de prompt injection no texto e de exaustão de memória. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | conteúdo do arquivo → prompt | mitigate | Serialização CSV/XLSX reusa `formatSchemaForPrompt`, que já emite delimitadores anti-injection (file-chat-stream.ts:12-33). Injeção no prompt final é SEC-01/Phase 10. |
| T-09-02-02 | Information Disclosure | buffer bruto em logs | mitigate | PRIV-02: nenhum `console.log` de conteúdo raw; `catch` retorna código tipado sem expor bytes (PATTERNS 192-200). |
| T-09-02-03 | Denial of Service | XLSX multi-aba grande | mitigate | Cap `MAX_ROWS_PER_SHEET=200` (D-06) clampa a contagem efetiva serializada por aba (Task 1), limitando tokens dado que D-05 extrai TODAS as abas; o guard anti-ZIP-bomb que precede o parse é entregue na Plan 03. |
</threat_model>

<verification>
- CSV/XLSX → texto plano com schema + ~10 linhas; multi-aba com `## Aba:` (EXT-01).
- Cap `MAX_ROWS_PER_SHEET=200` exercitado: aba > 200 linhas → contagem reportada clampada a 200 (D-06).
- Imagem → tabela Markdown via OCR, fixture-mode preservado (EXT-02).
- TXT → conteúdo direto (EXT-04).
- `formatSchemaForPrompt` exportado sem alterar comportamento (D-07/A4 honrado).
</verification>

<success_criteria>
- 3 extratores de reuso entregues, cada um retornando `ExtractionResult`.
- Nenhuma rota/repo de tool dedicado tocada (D-07): apenas export aditivo em file-chat-stream.ts.
- Cap D-06 (`MAX_ROWS_PER_SHEET=200`) não é no-op: teste referencia a constante e assevera o clamp da contagem por aba.
- `cd apps/web && npx vitest run tests/extraction/reuse-extractors.test.ts` sai 0.
</success_criteria>

<output>
Create `.planning/phases/09-extraction-infrastructure/09-02-SUMMARY.md` when done
</output>
