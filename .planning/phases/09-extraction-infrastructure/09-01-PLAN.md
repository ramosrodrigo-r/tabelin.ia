---
phase: 09-extraction-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/server/extraction/types.ts
  - apps/web/package.json
  - apps/web/tests/extraction/zip-guard-deps.test.ts
autonomous: false
requirements: [SEC-02]
must_haves:
  truths:
    - "O contrato de erro tipado ExtractionResult existe e é importável pelos extratores"
    - "As 3 dependências novas (unpdf, file-type, fflate) estão instaladas e importáveis"
  artifacts:
    - path: "apps/web/src/server/extraction/types.ts"
      provides: "Discriminated union ExtractionResult + ExtractionErrorCode (D-09)"
      contains: "export type ExtractionResult"
    - path: "apps/web/package.json"
      provides: "Dependências unpdf, file-type, fflate"
      contains: "unpdf"
  key_links:
    - from: "todos os extratores das Plans 02/03/04"
      to: "apps/web/src/server/extraction/types.ts"
      via: "import type { ExtractionResult }"
      pattern: "from \"./types\""
---

<objective>
Criar a fundação da infraestrutura de extração: o contrato de erro tipado (`types.ts`, D-09) que todos os extratores das fases seguintes importam, e instalar as 3 dependências novas (`unpdf`, `file-type`, `fflate`) — precedidas por um checkpoint humano de legitimidade de pacote (slopcheck não estava disponível na pesquisa).

Purpose: Sem o `ExtractionResult` definido e os pacotes instalados, nenhum extrator das Plans 02–04 compila. Esta é a Wave 1 que desbloqueia todo o paralelismo subsequente.
Output: `apps/web/src/server/extraction/types.ts`, 3 deps em `package.json`, e um teste-smoke que prova que os pacotes importam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-extraction-infrastructure/09-CONTEXT.md
@.planning/phases/09-extraction-infrastructure/09-RESEARCH.md
@.planning/phases/09-extraction-infrastructure/09-PATTERNS.md

<interfaces>
<!-- Convenção Result já existente no codebase. Extraída de apps/web/src/server/auth/reset-password.ts (linhas 9-16). -->
<!-- O novo ExtractionResult ESTENDE essa convenção com `code` + `message` pt-BR (D-09). -->

Padrão Result do projeto (reset-password.ts):
  type PasswordResetFailure = { ok: false; reason: "expired" | "invalid" };
  type PasswordResetSuccess = { ok: true };

Contrato-alvo desta plan (types.ts), conforme 09-RESEARCH.md linhas 203-221 e 09-PATTERNS.md linhas 42-48:
  ExtractionErrorCode = "SCANNED_PDF" | "INVALID_BYTES" | "ZIP_BOMB" | "EMPTY_EXTRACTION" | "UNSUPPORTED_TYPE"
  ExtractionError    = { ok: false; code: ExtractionErrorCode; message: string }  // message em pt-BR
  ExtractionSuccess  = { ok: true; text: string }
  ExtractionResult   = ExtractionSuccess | ExtractionError

Versões verificadas (09-RESEARCH.md linhas 84-117): unpdf@1.6.2, file-type@22.0.1, fflate@0.8.3.
Comando de install (workspace): npm install unpdf@1.6.2 file-type@22.0.1 fflate@0.8.3 --workspace apps/web
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir o contrato de erro tipado (types.ts)</name>
  <files>apps/web/src/server/extraction/types.ts</files>
  <read_first>
    - apps/web/src/server/auth/reset-password.ts (linhas 9-16 — convenção Result do codebase a estender)
    - .planning/phases/09-extraction-infrastructure/09-RESEARCH.md (linhas 196-228 — Pattern 1, códigos e mensagens pt-BR sugeridas)
    - .planning/phases/09-extraction-infrastructure/09-PATTERNS.md (linhas 25-50 — pattern assignment de types.ts)
  </read_first>
  <behavior>
    - Test: o módulo exporta o type `ExtractionResult` como union de `ExtractionSuccess | ExtractionError`.
    - Test: `ExtractionErrorCode` cobre exatamente os 5 códigos: SCANNED_PDF, INVALID_BYTES, ZIP_BOMB, EMPTY_EXTRACTION, UNSUPPORTED_TYPE.
    - Test: um valor `{ ok: false, code: "SCANNED_PDF", message: "..." }` é atribuível a `ExtractionResult`; `{ ok: true, text: "..." }` também.
  </behavior>
  <action>
    Criar `apps/web/src/server/extraction/types.ts` começando com `import "server-only";` (convenção do projeto — ver file-parser.ts:1).
    Exportar: `ExtractionErrorCode` (union literal dos 5 códigos D-09), `ExtractionError` (`{ ok: false; code: ExtractionErrorCode; message: string }`), `ExtractionSuccess` (`{ ok: true; text: string }`), e `ExtractionResult = ExtractionSuccess | ExtractionError`.
    O campo `message` é sempre pt-BR e acionável. NÃO definir as strings de mensagem aqui (elas vivem em cada extrator); este arquivo é só o contrato de tipos. Sem fenced code de implementação — usar exatamente os identificadores e o shape do bloco `<interfaces>`.
    Como `server-only` no ambiente de teste é aliased para `tests/server-only.ts` (ver vitest.config.ts), o import não quebra os testes.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -i "extraction/types" || echo "types.ts compila sem erro"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/server/extraction/types.ts` existe e começa com `import "server-only";`
    - `grep -c 'export type ExtractionResult' apps/web/src/server/extraction/types.ts` retorna 1
    - Os 5 códigos literais aparecem no arquivo (grep por cada um: SCANNED_PDF, INVALID_BYTES, ZIP_BOMB, EMPTY_EXTRACTION, UNSUPPORTED_TYPE)
    - `cd apps/web && npx tsc --noEmit` não reporta erro em `extraction/types.ts`
  </acceptance_criteria>
  <done>O contrato `ExtractionResult` está exportado, tipa os 5 códigos D-09, e compila sob `tsc --noEmit`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Checkpoint de legitimidade dos 3 pacotes (slopcheck indisponível)</name>
  <read_first>
    - .planning/phases/09-extraction-infrastructure/09-RESEARCH.md (linhas 119-132 — Package Legitimacy Audit; slopcheck indisponível na pesquisa)
  </read_first>
  <action>
    NÃO rodar `npm install` ainda. Apresentar ao humano os 3 pacotes `[ASSUMED]` (`unpdf@1.6.2`, `file-type@22.0.1`, `fflate@0.8.3`) para verificação de procedência em npmjs.com antes de autorizar o install (T-09-SC). Pausar até receber "aprovado". Este gate é `blocking-human` e ignora auto_advance — NUNCA auto-aprovar.
  </action>
  <what-built>
    Nada foi instalado ainda. Os 3 pacotes novos (`unpdf@1.6.2`, `file-type@22.0.1`, `fflate@0.8.3`) NÃO passaram por slopcheck (a ferramenta não estava disponível no ambiente de pesquisa). A pesquisa marcou todos como `[ASSUMED]` com procedência forte, mas a política de segurança exige verificação humana antes do install (T-09-SC).
  </what-built>
  <how-to-verify>
    Verifique a procedência de cada pacote na fonte autoritativa antes de aprovar o install:
    1. unpdf → https://www.npmjs.com/package/unpdf (repo github.com/unjs/unpdf — org unjs). Confirme versão 1.6.2 e downloads/manutenção saudáveis.
    2. file-type → https://www.npmjs.com/package/file-type (repo github.com/sindresorhus/file-type). Confirme versão 22.0.1, autor sindresorhus, ~50M+ downloads/semana.
    3. fflate → https://www.npmjs.com/package/fflate (repo github.com/101arrowz/fflate). Confirme versão 0.8.3, autor 101arrowz.
    Nenhum desses pode ser auto-aprovado — este gate ignora auto_advance.
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirmou explicitamente que os 3 pacotes/versões/repos conferem com as URLs npmjs.com acima.
    - Se algum pacote parecer suspeito/typosquat, o humano descreve o problema e o install NÃO prossegue.
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" para autorizar o `npm install` dos 3 pacotes, ou descreva o problema encontrado.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Instalar as 3 deps e provar import (smoke test)</name>
  <files>apps/web/package.json, apps/web/tests/extraction/zip-guard-deps.test.ts</files>
  <read_first>
    - .planning/phases/09-extraction-infrastructure/09-RESEARCH.md (linhas 108-117 — comando exato de install e versões; linhas 356-360 — Pitfall 1 ESM/dynamic import de file-type)
    - apps/web/tests/file-parser.test.ts (linhas 1-30 — estrutura de teste vitest do projeto)
    - apps/web/vitest.config.ts (aliases — server-only é stubado em testes)
  </read_first>
  <behavior>
    - Test: `await import("unpdf")` expõe `extractText` e `getDocumentProxy` (typeof function).
    - Test: `await import("file-type")` expõe `fileTypeFromBuffer` (typeof function).
    - Test: `await import("fflate")` expõe `unzipSync` (typeof function).
  </behavior>
  <action>
    Rodar exatamente: `npm install unpdf@1.6.2 file-type@22.0.1 fflate@0.8.3 --workspace apps/web` a partir da raiz do repo (registra as 3 deps em `apps/web/package.json` + lockfile). NÃO usar `npx --yes` nem import dinâmico não verificado (anti-pattern RESEARCH 327).
    Criar `apps/web/tests/extraction/zip-guard-deps.test.ts` com um único `describe` de smoke: usar `await import(...)` (dynamic import, alinhado ao precedente `await import("xlsx")` em upload/route.ts:59 e à Pitfall 1 sobre ESM-only do file-type@22) e asserir que `extractText`/`getDocumentProxy` (unpdf), `fileTypeFromBuffer` (file-type) e `unzipSync` (fflate) são `function`. Este teste DISCHARGE a disponibilidade real dos pacotes no runtime de teste antes das Plans 03/04 dependerem deles.
    Sem fenced code de implementação no plano; o executor escreve o teste seguindo o estilo de file-parser.test.ts.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run tests/extraction/zip-guard-deps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./apps/web/package.json'); ['unpdf','file-type','fflate'].forEach(d=>{if(!p.dependencies[d]) throw new Error('missing '+d)})"` sai 0
    - `cd apps/web && npx vitest run tests/extraction/zip-guard-deps.test.ts` sai 0
    - Os 3 imports dinâmicos resolvem sem erro "require() of ES Module" / "Cannot use import statement"
  </acceptance_criteria>
  <done>As 3 deps estão em package.json e o smoke test prova que `extractText`, `fileTypeFromBuffer` e `unzipSync` são importáveis no runtime de teste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → projeto | Pacotes de terceiros entram na árvore de dependências; risco de typosquat/supply-chain (slopcheck indisponível). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | npm install unpdf/file-type/fflate | mitigate | Checkpoint `blocking-human` (Task 2) verifica procedência em npmjs.com antes do install; versões pinadas exatas (sem ranges); pacotes `[ASSUMED]` nunca auto-aprovados. |
| T-09-01-01 | Tampering | types.ts (contrato compartilhado) | accept | Arquivo de tipos puro, sem I/O nem input externo; superfície de ataque nula. |
</threat_model>

<verification>
- `types.ts` exporta `ExtractionResult` com os 5 códigos D-09 e compila sob `tsc --noEmit`.
- As 3 deps estão pinadas em `apps/web/package.json` e importáveis (smoke test verde).
- O checkpoint de legitimidade foi aprovado por humano antes do install.
</verification>

<success_criteria>
- `apps/web/src/server/extraction/types.ts` define o contrato tipado completo (D-09).
- `unpdf@1.6.2`, `file-type@22.0.1`, `fflate@0.8.3` instalados e provados importáveis.
- Plans 02–04 podem importar `./types` e os 3 pacotes sem bloqueio.
</success_criteria>

<output>
Create `.planning/phases/09-extraction-infrastructure/09-01-SUMMARY.md` when done
</output>
