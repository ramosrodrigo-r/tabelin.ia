---
phase: 14-tabela-viva
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/tests/formula-engine.test.ts
  - apps/web/tests/table-grid-panel.test.tsx
  - apps/web/tests/unified-schema.test.ts
  - apps/web/tests/table-clarifier.test.ts
autonomous: false
requirements:
  - TAB-01
  - TAB-02
  - TAB-03
  - TAB-04
  - TAB-05
  - TAB-06
  - LOC-01
  - LOC-02
  - LOC-03
  - SEC-05

must_haves:
  truths:
    - "Dois novos arquivos de teste existem (formula-engine.test.ts, table-grid-panel.test.tsx) com suites skip-graceful que redtem quando módulos do Wave 1 não existem"
    - "unified-schema.test.ts contém casos para o schema estendido (rows, colunas com formula)"
    - "table-clarifier.test.ts contém caso para fixture estendida"
    - "pnpm test --filter web -- run tests/formula-engine.test.ts tests/unified-schema.test.ts é executado e passa (mesmo que skip)"
  artifacts:
    - path: "apps/web/tests/formula-engine.test.ts"
      provides: "Scaffold de teste para motor de fórmulas (LOC-01, LOC-02, TAB-02)"
    - path: "apps/web/tests/table-grid-panel.test.tsx"
      provides: "Scaffold de teste para o componente grid (TAB-01,03,04,05,06, LOC-03, SEC-05)"
    - path: "apps/web/tests/unified-schema.test.ts"
      provides: "Testes de schema estendido (retrocompat + novos campos)"
    - path: "apps/web/tests/table-clarifier.test.ts"
      provides: "Testes de fixture estendida com rows + fórmulas"
  key_links:
    - from: "apps/web/tests/formula-engine.test.ts"
      to: "apps/web/src/features/unified-chat/hooks/use-formula-engine.ts"
      via: "import com @ts-ignore — módulo Wave 1"
      pattern: "use-formula-engine"
    - from: "apps/web/tests/table-grid-panel.test.tsx"
      to: "apps/web/src/features/unified-chat/components/table-grid-panel.tsx"
      via: "import com @ts-ignore — módulo Wave 2"
      pattern: "table-grid-panel"

user_setup:
  - service: npm
    why: "Instalar react-datasheet-grid e @formulajs/formulajs antes dos scaffolds de teste"
    env_vars: []
    dashboard_config:
      - task: "Verificar identidade dos pacotes npm antes de instalar"
        location: "https://www.npmjs.com/package/react-datasheet-grid e https://www.npmjs.com/package/@formulajs/formulajs"
---

<objective>
Wave 0: checkpoint de vettagem de pacotes + scaffolds de teste.

Purpose: Instalar as duas dependências novas (após human-verify), criar os 4 arquivos de
teste que definem os contratos de cada requisito antes de qualquer implementação — seguindo
a Nyquist Rule. Resolve o concern carregado do STATE.md: o scaffold formula-engine.test.ts
força a validação empírica do mapa PT_BR_TO_EN com formulajs real no Wave 1.

Output: 4 arquivos de teste (2 novos, 2 modificados); dependências instaladas; suite verde
(skip-graceful enquanto módulos Wave 1+ não existem).
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-tabela-viva/14-CONTEXT.md
@.planning/phases/14-tabela-viva/14-RESEARCH.md
@.planning/phases/14-tabela-viva/14-VALIDATION.md
@.planning/phases/14-tabela-viva/14-PATTERNS.md
@.planning/STATE.md

<interfaces>
<!-- Estrutura atual — extraída do codebase -->

De packages/shared/src/unified-chat/schema.ts (linhas 63-69):
export const tableSpecPayloadSchema = z.object({
  kind: z.literal("table_spec"),
  title: z.string(),
  columns: z.array(z.object({ name: z.string(), type: z.string() })),
  rowCount: z.number().int().min(1).max(200),
  format: z.string().optional(),
});
export type TableSpecPayload = z.infer<typeof tableSpecPayloadSchema>;

De apps/web/tests/table-clarifier.test.ts (padrão existente):
const REAL_OPENAI_API_KEY = process.env.OPENAI_API_KEY;
beforeEach(() => { delete process.env.OPENAI_API_KEY; });
afterAll(() => { if (REAL_OPENAI_API_KEY) process.env.OPENAI_API_KEY = REAL_OPENAI_API_KEY; });

De apps/web/vitest.config.ts — alias já configurado:
"@tabelin/shared" → packages/shared/src/index.ts
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Vettagem humana dos pacotes npm antes da instalação</name>
  <read_first>
    .planning/phases/14-tabela-viva/14-RESEARCH.md (§Package Legitimacy Audit — slopcheck indisponível, pacotes [ASSUMED])
    .planning/phases/14-tabela-viva/14-UI-SPEC.md (§Registry Safety)
  </read_first>
  <what-built>
    Os pacotes react-datasheet-grid@4.11.6 e @formulajs/formulajs@4.6.0 foram pesquisados no
    RESEARCH.md com marcação [ASSUMED] porque slopcheck estava indisponível. Antes de instalar,
    um humano deve confirmar a identidade de cada pacote no registro npm.
  </what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/react-datasheet-grid
       Verificar: autor "nick-keller", repositório github.com/nick-keller/react-datasheet-grid,
       licença MIT, versão 4.11.6 presente, sem scripts postinstall suspeitos.

    2. Abrir https://www.npmjs.com/package/@formulajs/formulajs
       Verificar: escopo @formulajs, repositório github.com/formulajs/formulajs,
       licença MIT, versão 4.6.0 presente, sem scripts postinstall suspeitos.

    3. Confirmar que nenhum dos dois pacotes solicita acesso a rede em postinstall, não tem
       dependências com nomes suspeitos (typosquatting), e os mantenedores estão ativos
       (último publish em 2026 para DSG; 2026 para formulajs).
  </how-to-verify>
  <acceptance_criteria>
    - react-datasheet-grid@4.11.6: autor correto, MIT, sem postinstall malicioso
    - @formulajs/formulajs@4.6.0: escopo @formulajs, MIT, sem postinstall malicioso
    - Ambos aprovados pelo humano antes de executar o pnpm add
  </acceptance_criteria>
  <resume-signal>Digitar "aprovado" após verificar os dois pacotes nas URLs acima</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Instalar dependências e criar scaffolds de teste Wave 0</name>
  <read_first>
    .planning/phases/14-tabela-viva/14-VALIDATION.md (§Wave 0 Requirements — lista completa)
    .planning/phases/14-tabela-viva/14-PATTERNS.md (§formula-engine.test.ts e §table-grid-panel.test.tsx — padrão de estrutura)
    apps/web/tests/table-clarifier.test.ts (padrão beforeEach/afterAll, describe/it/expect)
    apps/web/tests/unified-schema.test.ts (padrão existente a estender)
    apps/web/vitest.config.ts (confirmar environment: "jsdom", alias @tabelin/shared)
    apps/web/tests/setup.ts (confirmar import @testing-library/jest-dom/vitest)
  </read_first>
  <action>
    Passo 1 — Instalar dependências (executar na raiz do monorepo, usar pnpm):
      pnpm add react-datasheet-grid --filter web
      pnpm add @formulajs/formulajs --filter web

    Passo 2 — Criar apps/web/tests/formula-engine.test.ts:
      Importar: describe, it, expect de "vitest"
      Importar com @ts-ignore: PT_BR_TO_EN de "@tabelin/shared" (módulo criado no Wave 1)
      Importar com @ts-ignore: { evaluateFormula, parseA1, parseRange } de
        "../src/features/unified-chat/hooks/use-formula-engine" (Wave 1)
      Usar padrão de skip-graceful: if (!evaluateFormula) { expect(true).toBe(true); return; }

      Suites requeridas (per 14-VALIDATION.md):
        describe("PT_BR_TO_EN map") — LOC-01:
          it("PROCV mapeia para VLOOKUP")
          it("SOMASE mapeia para SUMIF")
          it("SE mapeia para IF")
          it("CONT.SE mapeia para COUNTIF") — testar alias com ponto
          it("SOMA mapeia para SUM")
          it("MÉDIA mapeia para AVERAGE")

        describe("parseA1") — motor interno:
          it("B3 → { row: 2, col: 1 }")
          it("A1 → { row: 0, col: 0 }")
          it("Z10 → { row: 9, col: 25 }")
          it("retorna null para ref inválida como '3B'")

        describe("evaluateFormula — PROCV / LOC-01 empírico") — CRÍTICO, resolve concern STATE.md:
          it("=PROCV(lookup;tabela2D;2;0) retorna valor correto via formulajs real")
          it("=PROCV com valor ausente retorna #N/A")
          Usar dados: rows = [["produto_a",100],["produto_b",200]], lookup = "produto_b" → 200

        describe("evaluateFormula — SOMASE / LOC-01 empírico"):
          it("=SOMASE(range;critério;soma_range) retorna soma filtrada")
          Usar dados: categoryRange=["Moradia","Alimentação","Alimentação"], values=[2000,800,300], critério="Alimentação" → 1100

        describe("evaluateFormula — SE / LOC-01 empírico"):
          it("=SE(verdadeiro;sim;nao) retorna sim")
          it("=SE(falso;sim;nao) retorna nao")

        describe("separadores BR / LOC-02"):
          it("ponto-e-vírgula como separador de arg, vírgula como decimal")
          it("literal string com ponto-e-vírgula não é quebrado como separador")

        describe("detecção de ciclo"):
          it("fórmula circular retorna #CIRC!")

    Passo 3 — Criar apps/web/tests/table-grid-panel.test.tsx:
      Importar: describe, it, expect de "vitest"
      Importar: render, screen, fireEvent de "@testing-library/react"
      Importar com @ts-ignore: { TableGridPanel } de
        "../src/features/unified-chat/components/table-grid-panel" (Wave 2)
      Importar React de "react"

      Const SPEC_FIXTURE = { kind: "table_spec", title: "Controle de Gastos",
        columns: [
          { name: "Descrição", type: "text", key: "descricao" },
          { name: "Valor", type: "currency", key: "valor" },
          { name: "Total", type: "formula", key: "total", formula: "=SOMA(B{row};0)" }
        ],
        rowCount: 3,
        rows: [{ descricao: "Aluguel", valor: 2000 }, { descricao: "Internet", valor: 150 }, { descricao: "Academia", valor: 120 }],
        formulaLanguage: "pt-BR", separator: ";" }

      Suites requeridas:
        describe("TableGridPanel — render / TAB-01"):
          it("renderiza título da spec no output-header")
          it("renderiza sem crash com spec mínima")

        describe("TableGridPanel — SEC-05 XSS"):
          it("conteúdo de célula com <script> não executa: window.__xss permanece undefined")
          Antes do render: (window as any).__xss = undefined
          spec com rows: [{ valor: '<script>window.__xss = true;</script>' }]
          Após render: expect((window as any).__xss).toBeUndefined()

        describe("TableGridPanel — LOC-03 formatação BR"):
          it("valor currency 2000 exibe 'R$' no DOM")
          Verificar: screen.getByText(/R\$/)

        describe("TableGridPanel — TAB-06 virtualização"):
          it("renderiza sem crash com 200 linhas")
          rows = Array.from({ length: 200 }, (_, i) => ({ valor: i }))

        describe("TableGridPanel — TAB-03 add/remove"):
          it("estado inicial: rowCount linhas presentes")

        describe("TableGridPanel — TAB-04 undo/redo"):
          it("Ctrl+Z não lança erro quando sem histórico")

        describe("TableGridPanel — TAB-05 sort"):
          it("sort não muta o array original de rows")

      Todos os testes com skip-graceful: if (!TableGridPanel) { expect(true).toBe(true); return; }

    Passo 4 — Estender apps/web/tests/unified-schema.test.ts:
      Adicionar describe("tableSpecPayloadSchema — campos Phase 14") com:
        it("aceita spec mínima sem rows (retrocompat Phase 13)")
        it("aceita spec com rows e colunas formula (Phase 14)")
        it("rejeita rows com objeto aninhado (apenas string|number)")
        it("aceita formulaLanguage pt-BR")
        it("aceita separator ';'")
        Usar tableSpecPayloadSchema.safeParse({...}) — assertivas em .success

    Passo 5 — Estender apps/web/tests/table-clarifier.test.ts:
      Adicionar describe("buildTableSpec — fixture estendida Phase 14") com:
        it("fixture mode retorna rows com 5 entradas")
        it("fixture mode retorna coluna com type: 'formula' e campo formula definido")
        it("fixture mode retorna formulaLanguage: 'pt-BR'")
        it("fixture mode retorna separator: ';'")
        Usar padrão beforeEach/afterAll existente (delete process.env.OPENAI_API_KEY)

    NUNCA usar fenced code blocks na action. Todos os nomes de identifiers aqui são
    diretivas de implementação, não código literal.
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm test --filter web -- run tests/formula-engine.test.ts tests/unified-schema.test.ts tests/table-clarifier.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - react-datasheet-grid e @formulajs/formulajs aparecem em apps/web/package.json
    - apps/web/tests/formula-engine.test.ts existe com >= 12 it() blocks
    - apps/web/tests/table-grid-panel.test.tsx existe com >= 7 it() blocks
    - unified-schema.test.ts modificado com >= 5 novos it() para campos Phase 14
    - table-clarifier.test.ts modificado com >= 3 novos it() para fixture estendida
    - pnpm test --filter web -- run tests/formula-engine.test.ts tests/unified-schema.test.ts retorna 0 falhas (testes skip-graceful passam; unified-schema pode falhar com schema ainda mínimo — aceitável se apenas schema tests falham, formula-engine deve passar no skip-graceful)
  </acceptance_criteria>
  <done>
    Dependências instaladas. Quatro arquivos de teste presentes. Suite roda sem erros de
    sintaxe/import. Wave 0 completo — Wave 1 pode iniciar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local filesystem | Pacotes instalados via pnpm add sem slopcheck disponível |
| test environment → browser globals | Testes de SEC-05 verificam que window.__xss não é modificado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | npm install (react-datasheet-grid, @formulajs/formulajs) | mitigate | Checkpoint human-verify Task 1 bloqueia install até humano confirmar identidade no npmjs.com; ambos marcados [ASSUMED] pelo researcher |
| T-14-XSS-SCAFFOLD | Tampering | table-grid-panel.test.tsx (SEC-05 test) | mitigate | Teste de XSS cria fixture com `<script>` e asserta que window.__xss permanece undefined — gate que bloqueia merge de Wave 2 se textContent não for usado |
| T-14-INPUT-SCAFFOLD | Tampering | unified-schema.test.ts | mitigate | Teste rejeita rows com objetos aninhados (apenas string|number) — gate que bloqueia schema inválido |
</threat_model>

<verification>
Wave 0 completo quando:
- [ ] pnpm add concluiu sem erros; react-datasheet-grid e @formulajs/formulajs em apps/web/package.json
- [ ] 4 arquivos de teste presentes/modificados
- [ ] pnpm test --filter web -- run tests/formula-engine.test.ts tests/unified-schema.test.ts retorna sem falhas de sintaxe
- [ ] Testes skip-graceful não travam a suite
</verification>

<success_criteria>
- Checkpoint humano de vettagem concluído antes de qualquer pnpm add
- Dois novos arquivos de teste com scaffolds completos e skip-graceful
- Dois arquivos de teste existentes estendidos com casos Phase 14
- Suite roda sem erro (testes pulam graciosamente quando módulos Wave 1 não existem)
- Bloqueio de Wave 1: formula-engine.test.ts e unified-schema.test.ts DEVEM passar antes de avançar
</success_criteria>

<output>
Criar `.planning/phases/14-tabela-viva/14-01-SUMMARY.md` ao concluir
</output>
