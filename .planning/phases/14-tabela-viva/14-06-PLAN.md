---
phase: 14-tabela-viva
plan: 06
type: execute
wave: 3
depends_on:
  - 14-05
files_modified:
  - apps/web/src/features/unified-chat/components/render-dispatcher.tsx
  - apps/web/src/features/unified-chat/components/confirmation-card.tsx
autonomous: false
requirements:
  - TAB-01
  - TAB-02
  - TAB-03
  - TAB-04
  - TAB-05
  - TAB-06
  - LOC-01
  - LOC-02
  - LOC-03
  - SEC-05

must_haves:
  truths:
    - "render-dispatcher: case 'table_spec' com rows.length > 0 renderiza TableGridPanel"
    - "render-dispatcher: case 'table_spec' sem rows renderiza ConfirmationCard (Phase 13 intacta)"
    - "confirmation-card.tsx: onConfirm passa rows/formulaLanguage/separator intactos para overrideGenerate"
    - "Suite completa pnpm test --filter web -- run é verde (sem regressão)"
    - "Usuário pode editar célula, digitar fórmula pt-BR, ver recálculo — verificado visualmente"
    - "=PROCV(A1;B1:C10;2;0) avalia sem #NAME? — critério de sucesso #3 do ROADMAP"
  artifacts:
    - path: "apps/web/src/features/unified-chat/components/render-dispatcher.tsx"
      provides: "Wiring final: case table_spec roteia para TableGridPanel ou ConfirmationCard"
    - path: "apps/web/src/features/unified-chat/components/confirmation-card.tsx"
      provides: "Passa campos estendidos intactos no onConfirm"
  key_links:
    - from: "apps/web/src/features/unified-chat/components/render-dispatcher.tsx"
      to: "apps/web/src/features/unified-chat/components/table-grid-panel.tsx"
      via: "import { TableGridPanel }"
      pattern: "TableGridPanel"
    - from: "apps/web/src/features/unified-chat/components/render-dispatcher.tsx"
      to: "payload.rows"
      via: "Array.isArray(payload.rows) && payload.rows.length > 0"
      pattern: "rows"
---

<objective>
Wave 3 (parte B): Wiring final + verificação humana E2E.

Purpose: Conectar o TableGridPanel ao render-dispatcher e garantir que ConfirmationCard
passa os campos estendidos intactos. Após o wiring automatizado, um checkpoint humano
verifica o fluxo E2E completo: gerar tabela, editar célula, ver fórmula recalcular,
testar PROCV, verificar que nenhum script executa.

Output: Tabela Viva funcionando no thread de conversa; todos os requisitos TAB, LOC e
SEC-05 verificados humano + automatizado; fase 14 completa.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-tabela-viva/14-CONTEXT.md
@.planning/phases/14-tabela-viva/14-RESEARCH.md
@.planning/phases/14-tabela-viva/14-PATTERNS.md

<interfaces>
<!-- render-dispatcher.tsx — case table_spec atual (linhas 242-249) -->
case "table_spec":
  return (
    <ConfirmationCard
      payload={payload as TableSpecPayload}
      onConfirm={onConfirm ?? (() => {})}
    />
  );

<!-- TableGridPanel (criado em 14-05) -->
export function TableGridPanel({ spec }: { spec: TableSpecPayload }): JSX.Element

<!-- confirmation-card.tsx — handleConfirm atual -->
function handleConfirm() {
  onConfirm(editedSpec);  // editedSpec é { ...payload } com modificações do usuário
}
// editedSpec via setEditedSpec com spread — novos campos opcionais são preservados automaticamente

<!-- TypeScript: payload as TableSpecPayload já inclui rows?, formulaLanguage?, separator? após 14-02 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wiring render-dispatcher e verificação de retrocompat confirmation-card</name>
  <files>
    apps/web/src/features/unified-chat/components/render-dispatcher.tsx
    apps/web/src/features/unified-chat/components/confirmation-card.tsx
  </files>
  <read_first>
    apps/web/src/features/unified-chat/components/render-dispatcher.tsx (arquivo completo)
    apps/web/src/features/unified-chat/components/confirmation-card.tsx (arquivo completo)
    .planning/phases/14-tabela-viva/14-PATTERNS.md (§render-dispatcher.tsx — modificação linhas 300-338)
    .planning/phases/14-tabela-viva/14-RESEARCH.md (§Pitfall 7 — ConfirmationCard renderiza quando spec tem rows)
  </read_first>
  <behavior>
    - render-dispatcher com payload {kind:"table_spec", rows:[{a:1}]} → renderiza TableGridPanel
    - render-dispatcher com payload {kind:"table_spec"} (sem rows) → renderiza ConfirmationCard
    - render-dispatcher com payload {kind:"table_spec", rows:[]} (array vazio) → renderiza ConfirmationCard
    - confirmation-card com payload {rows:[...], formulaLanguage:"pt-BR"} → onConfirm chamado com esses campos intactos
    - pnpm test --filter web -- run → suite completa sem falhas (sem regressão Phase 13)
  </behavior>
  <action>
    Modificação 1 — render-dispatcher.tsx:

    a. Adicionar import: import { TableGridPanel } from "./table-grid-panel";
       (adicionar após os imports existentes de ConfirmationCard e TableIntentStub)

    b. Substituir o case "table_spec" (linhas ~242-249) por:
       case "table_spec": {
         const hasRows = Array.isArray((payload as TableSpecPayload).rows) &&
           ((payload as TableSpecPayload).rows?.length ?? 0) > 0;
         if (hasRows) {
           return <TableGridPanel spec={payload as TableSpecPayload} />;
         }
         return (
           <ConfirmationCard
             payload={payload as TableSpecPayload}
             onConfirm={onConfirm ?? (() => {})}
           />
         );
       }
       Usar exatamente o padrão do 14-PATTERNS.md linhas 316-330.

    Modificação 2 — confirmation-card.tsx:

    a. Verificar (não modificar estrutura) que handleConfirm chama onConfirm(editedSpec) e
       que editedSpec é inicializado via useState(payload) — o spread pattern existente
       preserva campos opcionais automaticamente.

    b. Se o estado inicial for inicializado com uma cópia parcial de payload que descarta
       campos desconhecidos, corrigir para: useState&lt;TableSpecPayload&gt;(payload) sem desestruturação
       destrutiva — garantindo que rows/formulaLanguage/separator passem intactos.

    c. Não alterar o JSX do ConfirmationCard — apenas garantir a passagem dos campos.

    Verificação final de retrocompat: verificar que tests/unified-schema.test.ts (casos
    Phase 13 sem rows) ainda passam após as modificações.
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm test --filter web -- run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test --filter web -- run retorna 0 falhas (suite completa verde)
    - grep "TableGridPanel" apps/web/src/features/unified-chat/components/render-dispatcher.tsx retorna match
    - grep "hasRows" apps/web/src/features/unified-chat/components/render-dispatcher.tsx retorna match
    - Nenhum arquivo de Phase 13 modificado além de confirmation-card.tsx (verificar com git diff)
  </acceptance_criteria>
  <done>
    render-dispatcher roteando corretamente; confirmation-card passando campos estendidos;
    suite completa verde sem regressão.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação humana E2E da Tabela Viva</name>
  <what-built>
    TableGridPanel completo conectado ao thread de conversa unificado:
    - Grid editável com DynamicDataSheetGrid (react-datasheet-grid v4.11.6)
    - Motor de fórmulas pt-BR sobre @formulajs/formulajs (parseA1, ranges, PT_BR_TO_EN)
    - Undo/redo (Ctrl+Z/Y), sort por coluna, add/remove linhas e colunas
    - Formatação BRL e datas BR; células de fórmula read-only
    - Render seguro: apenas textContent, nunca dangerouslySetInnerHTML
  </what-built>
  <how-to-verify>
    1. Iniciar o servidor de desenvolvimento:
       pnpm dev --filter web
       Abrir http://localhost:3000/workspace no browser.

    2. Verificar critério de sucesso #1 (TAB-01 — edição):
       - Pedir uma tabela (ex.: "cria uma tabela de controle de gastos mensais")
       - Confirmar spec no ConfirmationCard
       - Clicar em uma célula não-fórmula do grid resultante
       - Digitar um valor e pressionar Tab → foco avança para próxima célula
       - Pressionar Enter → foco avança para célula abaixo
       Esperado: comportamento natural de planilha, sem reload

    3. Verificar critério de sucesso #2 (TAB-02 — recálculo):
       - Localizar uma coluna de fórmula (ex.: "Total" com =SOMA(B{row};C{row}))
       - Editar o valor em B2 (ex.: mudar de 800 para 999)
       - Observar célula "Total" da linha 2
       Esperado: Total atualiza imediatamente sem reload

    4. Verificar critério de sucesso #3 (LOC-01, LOC-02, LOC-03):
       - Solicitar tabela com PROCV: "tabela de produtos com lookup de preço por nome"
       - Confirmar e verificar que =PROCV(A1;B1:C10;2;0) avalia sem #NAME?
       - Verificar colunas currency exibem "R$ 1.500,00" (não "1500")
       - Verificar colunas date exibem "31/12/2025" (não "2025-12-31")
       Esperado: nenhum #NAME?, formatação brasileira correta

    5. Verificar critério de sucesso #4 (TAB-03, TAB-04, TAB-05):
       - Clicar "+ Linha" → nova linha aparece no final
       - Clicar "+ Coluna" → nova coluna "Nova Coluna" aparece
       - Clicar X na linha 1 (stickyRightColumn) → linha removida
       - Pressionar Ctrl+Z → linha restaurada
       - Pressionar Ctrl+Y → undo desfeito
       - Clicar cabeçalho de uma coluna → ícone de seta aparece (sort asc)
       - Clicar novamente → sort desc; terceiro clique → sort desativado
       Esperado: todas as operações funcionam conforme descrito

    6. Verificar critério de sucesso #5 (TAB-06, SEC-05):
       - Solicitar "tabela com 200 linhas de dados de exemplo"
       - Rolar o grid rapidamente (scroll)
       Esperado: rolagem suave sem jank perceptível
       - Em uma célula, colar o texto: <script>alert('xss')</script>
       - Pressionar Enter
       Esperado: o texto aparece como texto literal na célula, nenhum alert aparece,
                 nenhum script executa
  </how-to-verify>
  <acceptance_criteria>
    - Critério 1: Tab/Enter navegam naturalmente
    - Critério 2: Fórmula recalcula sem reload
    - Critério 3: PROCV avalia sem #NAME?; R$ e DD/MM/AAAA corretos
    - Critério 4: add/remove, undo/redo e sort todos funcionam
    - Critério 5: 200 linhas rolam suavemente; script não executa
  </acceptance_criteria>
  <resume-signal>
    Digitar "aprovado" se todos os 5 critérios passaram, ou descrever o que falhou
    (ex.: "PROCV retorna #NAME?" ou "undo não funciona") para que Claude corrija antes de fechar a fase
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render-dispatcher → TableGridPanel | payload da stream NDJSON chega ao componente após Zod parse |
| ConfirmationCard.onConfirm → overrideGenerate | editedSpec enviado de volta ao servidor com campos da Phase 14 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-DISPATCH | Tampering | render-dispatcher — Pitfall 7 (ConfirmationCard com rows presentes) | mitigate | Condição hasRows = Array.isArray(rows) && rows.length > 0; senão ConfirmationCard. Testado em Task 1: spec com rows vazio → ConfirmationCard; spec com rows → TableGridPanel. |
| T-14-XSS | Tampering | render-dispatcher → TableGridPanel (SEC-05) | mitigate | TableGridPanel usa apenas textContent (implementado em 14-05); checkpoint humano task 2 verifica que <script> colado não executa no grid real. |
| T-14-REGRESS | Tampering | confirmation-card.tsx — campos Phase 13 perdidos no onConfirm | mitigate | Verificar que editedSpec preserva spread do payload original — campos opcionais novos passam intactos; teste de regressão: pnpm test -- run tests/unified-schema.test.ts confirma retrocompat. |
</threat_model>

<verification>
Phase 14 completa quando:
- [ ] pnpm test --filter web -- run — suite completa verde (zero falhas)
- [ ] Critérios de sucesso do ROADMAP Phase 14 verificados humanamente (todos os 5)
- [ ] grep -r "dangerouslySetInnerHTML" apps/web/src/features/unified-chat/components/ — sem matches novos
- [ ] Concern STATE.md resolvido: formula-engine.test.ts verde com PROCV, SOMASE, SE
- [ ] VALIDATION.md nyquist_compliant: todos os requisitos têm teste automatizado
</verification>

<success_criteria>
- render-dispatcher roteia TableGridPanel vs ConfirmationCard por rows.length > 0
- Fluxo E2E completo: gerar tabela → editar → recalcular fórmula pt-BR → undo/redo/sort → sem XSS
- Checkpoint humano aprovado para todos os 5 critérios de sucesso do ROADMAP Phase 14
- Suite completa verde — sem regressão em Phase 12 ou Phase 13
</success_criteria>

<output>
Criar `.planning/phases/14-tabela-viva/14-06-SUMMARY.md` ao concluir
</output>
