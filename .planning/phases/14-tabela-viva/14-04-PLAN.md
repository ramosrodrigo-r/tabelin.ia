---
phase: 14-tabela-viva
plan: 04
type: execute
wave: 2
depends_on:
  - 14-02
files_modified:
  - apps/web/src/server/ai/table-clarifier.ts
autonomous: true
requirements:
  - TAB-01
  - TAB-02
  - LOC-01
  - LOC-02
  - LOC-03

must_haves:
  truths:
    - "buildTableSpec sem OPENAI_API_KEY retorna fixture com rows (5 entradas), coluna formula, formulaLanguage:'pt-BR', separator:';'"
    - "buildSpecSystemPrompt instrui o LLM a gerar rows, keys camelCase, formula templates com {row}, separador ';', decimal ',', nomes de função em português"
    - "Schema usado no zodResponseFormat é o tableSpecPayloadSchema estendido (Phase 14)"
    - "Fallback json_object path também usa tableSpecPayloadSchema.parse() estendido"
    - "ConfirmationCard.onConfirm passa campos rows/formulaLanguage/separator intactos (backward-compat)"
  artifacts:
    - path: "apps/web/src/server/ai/table-clarifier.ts"
      provides: "buildTableSpec estendido com seed data, fórmulas e fixture mode Phase 14"
  key_links:
    - from: "apps/web/src/server/ai/table-clarifier.ts"
      to: "tableSpecPayloadSchema"
      via: "zodResponseFormat(tableSpecPayloadSchema, 'table_spec')"
      pattern: "zodResponseFormat"
---

<objective>
Wave 2: Extensão do gerador backend — buildTableSpec com seed data e fórmulas.

Purpose: Fazer o servidor gerar TableSpecPayload completo com rows (seed data) e colunas
de fórmula, de modo que o render-dispatcher possa detectar rows.length > 0 e renderizar
TableGridPanel em vez de ConfirmationCard. A fixture mode também é estendida para
desenvolvimento sem OPENAI_API_KEY.

Output: table-clarifier.ts com buildSpecSystemPrompt estendido e fixture Phase 14; testes
de table-clarifier.test.ts verdes.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-tabela-viva/14-RESEARCH.md
@.planning/phases/14-tabela-viva/14-PATTERNS.md
@.planning/phases/14-tabela-viva/14-CONTEXT.md

<interfaces>
<!-- buildTableSpec atual — apps/web/src/server/ai/table-clarifier.ts -->
export type BuildTableSpecInput = {
  prompt: string;
  collectedSpec?: CollectedSpec;
};
export async function buildTableSpec(input: BuildTableSpecInput): Promise<TableSpecPayload>

<!-- fixture mode atual (linhas ~229-240) -->
if (!process.env.OPENAI_API_KEY) {
  return { kind: "table_spec", title: "Tabela de " + prompt.slice(0,30),
    columns: [{ name: "Coluna A", type: "text" }, { name: "Coluna B", type: "number" }],
    rowCount: 10, format: "default" };
}

<!-- buildSpecSystemPrompt atual (linhas ~119-130) -->
function buildSpecSystemPrompt(originalPrompt: string): string {
  return `Você é um assistente especialista em planilhas brasileiro. Gere especificação com:
  - title, columns (name + type), rowCount, format. ...`;
}

<!-- Schema estendido (criado em 14-02) — importar de @tabelin/shared -->
tableSpecPayloadSchema: z.object({ kind, title, columns (com key, formula, type:enum), rowCount,
  rows: optional, formulaLanguage: optional, separator: optional })

<!-- Padrão Structured Outputs + fallback (linhas 249-284 table-clarifier.ts) — manter intacto -->
try {
  const completion = await client.chat.completions.parse({
    model: getOpenAIModel(), messages: [...],
    response_format: zodResponseFormat(tableSpecPayloadSchema, "table_spec"),
  });
  const parsed = completion.choices[0]?.message?.parsed;
  if (!parsed) throw new Error("...");
  return parsed;
} catch (err) {
  if (!shouldFallbackFromStructuredOutputs(err)) throw err;
  // json_object fallback → tableSpecPayloadSchema.parse(raw)
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender buildSpecSystemPrompt e fixture mode no table-clarifier.ts</name>
  <files>apps/web/src/server/ai/table-clarifier.ts</files>
  <read_first>
    apps/web/src/server/ai/table-clarifier.ts (arquivo completo — ler uma vez, extrair tudo)
    .planning/phases/14-tabela-viva/14-RESEARCH.md (§Pattern 8 — buildSpecSystemPrompt estendido e fixture estendida)
    .planning/phases/14-tabela-viva/14-PATTERNS.md (§table-clarifier.ts — padrão de fixture mode e prompt, linhas 141-196)
    apps/web/tests/table-clarifier.test.ts (casos Phase 14 adicionados no Wave 0 que devem passar)
  </read_first>
  <behavior>
    - buildTableSpec com OPENAI_API_KEY ausente retorna objeto com rows.length === 5
    - Fixture retorna ao menos uma coluna com type === "formula" e campo formula definido (não undefined)
    - Fixture retorna formulaLanguage === "pt-BR"
    - Fixture retorna separator === ";"
    - tableSpecPayloadSchema.parse(fixture_result) → success (sem throw)
    - buildSpecSystemPrompt retorna string contendo as palavras "rows", "formulaLanguage", "separator", "{row}"
  </behavior>
  <action>
    Modificações em apps/web/src/server/ai/table-clarifier.ts:

    1. Substituir o bloco de fixture mode (if (!process.env.OPENAI_API_KEY)) pela fixture
       estendida do RESEARCH.md Pattern 8:
       - title: "Controle de Gastos"
       - columns: 5 colunas — descricao (text), categoria (text), valor (currency),
         desconto (currency), total (formula, key:"total", formula:"=SOMA(C{row};-D{row})")
       - rowCount: 5
       - rows: 5 objetos com descricao/categoria/valor (number)/desconto (number)
         (Aluguel/Moradia/2000/100, Supermercado/Alimentação/800/50,
         Internet/Serviços/150/0, Academia/Saúde/120/20, Netflix/Lazer/55/5)
       - formulaLanguage: "pt-BR"
       - separator: ";"
       O retorno deve passar em tableSpecPayloadSchema.parse() — verificar tipos (valor e
       desconto como number, não string).

    2. Substituir buildSpecSystemPrompt para instruir o LLM (RESEARCH.md Pattern 8):
       - Pedir title, columns (com name, type, key camelCase, formula se type=formula)
       - formula: template usando {row} como placeholder, ex: "=SOMA(B{row};C{row})"
         SEMPRE ponto-e-vírgula como separador, vírgula como decimal, funções em PORTUGUÊS
       - rowCount: número de linhas (padrão 10)
       - rows: array de rowCount objetos com dados de exemplo realistas (valores numéricos
         como number, datas como string "YYYY-MM-DD")
       - formulaLanguage: "pt-BR"
       - separator: ";"

    3. Não alterar a estrutura try/catch do Structured Outputs + fallback — apenas o
       schema referenciado já foi estendido (tableSpecPayloadSchema agora inclui os novos
       campos opcionais via o import de @tabelin/shared).

    4. Verificar que o import de tableSpecPayloadSchema em table-clarifier.ts vem de
       "@tabelin/shared" (não do arquivo local) — se necessário, ajustar o import para
       usar o schema estendido da Phase 14.

    ATENÇÃO (D-01): campos novos são opcionais no schema — table-clarifier.ts de Phase 13
    que emitia specs sem rows continua funcionando. A fixture estendida é ativada apenas
    quando !process.env.OPENAI_API_KEY (modo de desenvolvimento).
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm test --filter web -- run tests/table-clarifier.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    table-clarifier.test.ts passa (incluindo os casos Phase 14 adicionados no Wave 0).
    Fixture mode retorna rows com 5 entradas, coluna formula, formulaLanguage e separator.
    buildSpecSystemPrompt estendido contém instruções para rows e formulaLanguage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM (OpenAI) → buildTableSpec → tableSpecPayloadSchema | Output do LLM é validado por Zod parse antes de ser retornado |
| tableSpecPayloadSchema → ConversationExchange.assistantPayload | Payload persistido no DB passa pelo schema Zod que rejeita objetos aninhados em rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-LLM-INJECT | Tampering | buildTableSpec — LLM payload injection em seed data (per RESEARCH.md §Security Domain) | mitigate | tableSpecPayloadSchema.parse() valida rows como Record&lt;string, string|number&gt; — objetos aninhados lançam ZodError antes de chegar ao grid. Mitigação verificada pelo teste "rejeita rows com objeto aninhado" no Wave 0. |
| T-14-DOS | DoS | buildTableSpec — payload oversized | accept | Limite de 32KB existente em conversation-repository.ts (guardPayloadSize) cobre seed data de 200 linhas × 26 colunas. Seed padrão de 5-10 linhas fica muito abaixo do limite. |
| T-14-PROMPT | Tampering | buildSpecSystemPrompt — prompt injection via originalPrompt | accept | Prompt de sistema instrui o LLM a gerar estrutura, não a executar conteúdo do usuário. Structured Outputs com zodResponseFormat restringe o output ao schema Zod — campos fora do schema são descartados. |
</threat_model>

<verification>
Wave 2 completo quando:
- [ ] pnpm test --filter web -- run tests/table-clarifier.test.ts — VERDE (incluindo casos Phase 14)
- [ ] Fixture estendida valida com tableSpecPayloadSchema.parse() sem throw
- [ ] pnpm test --filter web -- run — suite completa verde (sem regressão de Phase 13)
</verification>

<success_criteria>
- buildTableSpec fixture mode retorna payload completo com rows, formulaLanguage, separator
- buildSpecSystemPrompt instrui LLM a gerar seed data e fórmulas em pt-BR
- Schema Zod de referência é o estendido da Phase 14
- Testes verdes; sem regressão nos testes de table-clarifier existentes
</success_criteria>

<output>
Criar `.planning/phases/14-tabela-viva/14-04-SUMMARY.md` ao concluir
</output>
