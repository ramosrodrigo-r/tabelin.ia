---
phase: 10-persistence-llm-context
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - apps/web/src/app/api/tools/sql/generate/route.ts
  - apps/web/src/app/api/tools/regex/generate/route.ts
  - apps/web/src/app/api/tools/scripts/generate/route.ts
  - apps/web/src/app/api/tools/template/generate/route.ts
  - apps/web/src/server/ai/sql-stream.ts
  - apps/web/src/server/ai/regex-stream.ts
  - apps/web/src/server/ai/scripts-stream.ts
  - apps/web/src/server/ai/template-stream.ts
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - CTX-03
  - CTX-05
  - PRO-02
  - PRO-03

must_haves:
  truths:
    - "sql, regex, scripts routes aplicam Pro-gate condicional quando há arquivo (feature:'attachment')"
    - "template route mantém Pro-gate incondicional existente (não é removido/alterado)"
    - "os 4 routes (sql, regex, scripts, template) aceitam multipart/form-data mantendo backward-compat JSON"
    - "os 4 stream modules aceitam attachmentContext?: string e propagam para buildToolContextMessages"
    - "attachmentContext é persistido em saveConversationExchange nos 4 routes"
    - "falha de extração chama releaseToolUse antes de retornar 422 nos 4 routes"
  artifacts:
    - path: "apps/web/src/app/api/tools/sql/generate/route.ts"
      provides: "sql route com multipart, Pro-gate condicional, extração, persistência"
    - path: "apps/web/src/app/api/tools/regex/generate/route.ts"
      provides: "regex route com multipart, Pro-gate condicional, extração, persistência"
    - path: "apps/web/src/app/api/tools/scripts/generate/route.ts"
      provides: "scripts route com multipart, Pro-gate condicional, extração, persistência"
    - path: "apps/web/src/app/api/tools/template/generate/route.ts"
      provides: "template route com multipart, extração, persistência (Pro-gate existente mantido)"
    - path: "apps/web/src/server/ai/sql-stream.ts"
      provides: "resolveSqlPayload aceita attachmentContext"
    - path: "apps/web/src/server/ai/regex-stream.ts"
      provides: "resolveRegexPayload aceita attachmentContext"
    - path: "apps/web/src/server/ai/scripts-stream.ts"
      provides: "resolveScriptPayload aceita attachmentContext"
    - path: "apps/web/src/server/ai/template-stream.ts"
      provides: "resolveTemplatePayload aceita attachmentContext"
  key_links:
    - from: "sql/generate/route.ts"
      to: "apps/web/src/server/extraction/dispatcher.ts"
      via: "extractContent(buffer, file.name) dentro do try-block"
      pattern: "extractContent"
    - from: "apps/web/src/server/ai/sql-stream.ts"
      to: "apps/web/src/server/ai/context-messages.ts"
      via: "buildToolContextMessages('sql', history, systemPrompt, prompt, attachmentContext)"
      pattern: "buildToolContextMessages.*attachmentContext"
    - from: "template/generate/route.ts"
      to: "getUserEntitlement"
      via: "Pro-gate incondicional existente — não modificado"
      pattern: "isPro.*entitlement"
---

<objective>
Estender os 4 routes (sql, regex, scripts, template) e seus 4 stream modules para aceitar multipart/form-data, aplicar Pro-gate condicional de attachment (exceto template que já tem gate global), extrair conteúdo via dispatcher Phase 9 e propagar attachmentContext para buildToolContextMessages e saveConversationExchange.

Purpose: sql, regex e scripts já têm multi-turn (findConversationExchanges + buildToolContextMessages) — apenas o wiring de attachment precisa ser adicionado. Template tem gate Pro global — cuidado para não remover. Este plano é paralelo ao 10-02 (formula) pois modifica arquivos distintos.

Output: 4 routes e 4 stream modules com pipeline de attachment completo, sem regressão nos testes de multi-turn existentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-persistence-llm-context/10-RESEARCH.md
@.planning/phases/10-persistence-llm-context/10-PATTERNS.md

<interfaces>
<!-- Contratos que o executor DEVE usar — extraídos do codebase (Phase 9 + 10-01) -->

De apps/web/src/server/extraction/dispatcher.ts (Phase 9):
  export async function extractContent(buffer: Buffer, declaredName: string): Promise<ExtractionResult>
  // ExtractionResult = { ok: true, text: string } | { ok: false, code: ExtractionErrorCode, message: string }

De apps/web/src/server/billing/entitlements.ts:
  export async function getUserEntitlement(userId: string): Promise<UserEntitlement>
  // isPro = entitlement.plan === "pro" && entitlement.status === "active"

De apps/web/src/server/ai/context-messages.ts (após 10-01):
  export function buildToolContextMessages(
    toolKind: string,
    history: ConversationExchange[],
    systemPrompt: string,
    userPrompt: string,
    attachmentContext?: string   // 5º parâmetro opcional — callers existentes sem este param continuam compilando
  ): ChatCompletionMessageParam[]

De apps/web/src/server/tools/conversation-repository.ts (após 10-01):
  export async function saveConversationExchange(input: { ...; attachmentContext?: string }): Promise<ConversationExchange | null>

Padrão Pro-gate incondicional JÁ EXISTENTE em template/generate/route.ts (NÃO ALTERAR):
  const entitlement = await getUserEntitlement(user.id);
  const isPro = entitlement.plan === "pro" && entitlement.status === "active";
  if (!isPro) {
    return NextResponse.json({ code: "pro_required", cta: "pro_checkout" }, { status: 403 });
  }
  // NÃO adicionar feature:"attachment" aqui — afeta todos os requests, não só os com arquivo

Padrão Pro-gate condicional para sql/regex/scripts (NOVO):
  const hasFile = contentType.includes("multipart/form-data") && file !== null;
  if (hasFile) {
    const entitlement = await getUserEntitlement(user.id);
    const isPro = entitlement.plan === "pro" && entitlement.status === "active";
    if (!isPro) {
      return NextResponse.json(
        { code: "pro_required", feature: "attachment", cta: "pro_checkout" },
        { status: 403 }
      );
    }
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Stream modules — adicionar attachmentContext aos 4 resolve functions</name>
  <files>
    apps/web/src/server/ai/sql-stream.ts,
    apps/web/src/server/ai/regex-stream.ts,
    apps/web/src/server/ai/scripts-stream.ts,
    apps/web/src/server/ai/template-stream.ts
  </files>

  <read_first>
    - apps/web/src/server/ai/sql-stream.ts — ler inteiro: tipo de input de resolveSqlPayload, onde chama buildToolContextMessages (linhas 17-47), o 4º argumento atual (request.prompt)
    - apps/web/src/server/ai/regex-stream.ts — ler inteiro: tipo RegexModeInput, branch generate, chamada buildToolContextMessages
    - apps/web/src/server/ai/scripts-stream.ts — ler inteiro: tipo de input, chamada buildToolContextMessages
    - apps/web/src/server/ai/template-stream.ts — ler inteiro: tipo de input, chamada buildToolContextMessages
    - .planning/phases/10-persistence-llm-context/10-PATTERNS.md — seções de cada stream module com a mudança exata (adicionar attachmentContext? ao input type e como 5º argumento de buildToolContextMessages)
  </read_first>

  <action>
    Para cada um dos 4 stream modules, aplicar a mesma mudança mínima:

    **sql-stream.ts:** No tipo de input de `resolveSqlPayload`, adicionar `attachmentContext?: string` após `history?: ConversationExchange[]`. Na chamada a `buildToolContextMessages`, adicionar `, input.attachmentContext` como 5º argumento (após `request.prompt`).

    **regex-stream.ts:** No union type `RegexModeInput`, no branch `mode: "generate"`, adicionar `attachmentContext?: string`. Na chamada a `buildToolContextMessages` dentro do branch generate, adicionar `, input.attachmentContext` como 5º argumento.

    **scripts-stream.ts:** Mesmo padrão — ler o arquivo para confirmar o nome exato do tipo de input, adicionar `attachmentContext?: string` e passar como 5º argumento.

    **template-stream.ts:** No tipo de input de `resolveTemplatePayload`, adicionar `attachmentContext?: string` após `history?: ConversationExchange[]`. Na chamada a `buildToolContextMessages`, adicionar `, input.attachmentContext` como 5º argumento.

    Em todos os casos: NÃO alterar o fixture mode (bloco `if (!process.env.OPENAI_API_KEY) { return ... }`). O fixture retorna antes de montar as messages — não é afetado pelo novo parâmetro.

    NÃO alterar nenhuma outra lógica dos stream modules (schema parse, OpenAI call, response schema).
  </action>

  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>

  <acceptance_criteria>
    - resolveSqlPayload input type tem `attachmentContext?: string`
    - resolveRegexPayload (ou RegexModeInput branch generate) tem `attachmentContext?: string`
    - resolveScriptPayload input type tem `attachmentContext?: string`
    - resolveTemplatePayload input type tem `attachmentContext?: string`
    - Cada chamada a buildToolContextMessages nesses módulos passa `input.attachmentContext` como 5º argumento
    - `pnpm --filter web typecheck` sai 0
    - Callers existentes dos 4 resolve functions (route handlers) continuam compilando sem erro (parâmetro é opcional)
  </acceptance_criteria>

  <done>Os 4 stream modules aceitam e propagam attachmentContext para buildToolContextMessages, mantendo fixture mode intacto e backward-compat com callers existentes.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Routes sql, regex, scripts — multipart + Pro-gate condicional + extração</name>
  <files>
    apps/web/src/app/api/tools/sql/generate/route.ts,
    apps/web/src/app/api/tools/regex/generate/route.ts,
    apps/web/src/app/api/tools/scripts/generate/route.ts
  </files>

  <read_first>
    - apps/web/src/app/api/tools/sql/generate/route.ts — ler inteiro: imports atuais, onde está o request.json(), onde chama findConversationExchanges, onde chama resolveSqlPayload, onde chama saveConversationExchange, estrutura do try/catch
    - apps/web/src/app/api/tools/regex/generate/route.ts — ler inteiro: mesmo propósito
    - apps/web/src/app/api/tools/scripts/generate/route.ts — ler inteiro: confirmar toolKind usado ("script" singular — regressão conhecida do MULTI-03)
    - .planning/phases/10-persistence-llm-context/10-PATTERNS.md — seções sql, regex, scripts com as mudanças exatas; seção "Shared Patterns" com os blocos de código reutilizáveis
  </read_first>

  <action>
    Para cada um dos 3 routes (sql, regex, scripts), aplicar o mesmo conjunto de mudanças:

    1. Adicionar imports que faltam: `getUserEntitlement` from `"@/server/billing/entitlements"` e `extractContent` from `"@/server/extraction/dispatcher"` (verificar se já existem antes de adicionar).

    2. Substituir `body = await request.json()` pela detecção de Content-Type com backward-compat:
       ```
       const contentType = request.headers.get("content-type") ?? "";
       let body: unknown;
       let file: File | null = null;
       if (contentType.includes("multipart/form-data")) {
         const formData = await request.formData();
         body = {
           prompt: formData.get("prompt"),
           // demais campos do schema do tool — ler cada schema (sqlGenerateRequestSchema, regexGenerateRequestSchema, scriptsGenerateRequestSchema) para listar os campos
         };
         file = formData.get("file") as File | null;
       } else {
         body = await request.json().catch(() => null);
       }
       ```
       Para sql: campos esperados pelo `sqlGenerateRequestSchema` (prompt, dialect, etc.)
       Para regex: campos esperados pelo `regexGenerateRequestSchema` (verificar se é só prompt)
       Para scripts: campos esperados pelo `scriptsGenerateRequestSchema` (verificar)

    3. Pro-gate condicional após auth guard, ANTES de reserveToolUse:
       ```
       const hasFile = contentType.includes("multipart/form-data") && file !== null;
       if (hasFile) {
         const entitlement = await getUserEntitlement(user.id);
         const isPro = entitlement.plan === "pro" && entitlement.status === "active";
         if (!isPro) {
           return NextResponse.json(
             { code: "pro_required", feature: "attachment", cta: "pro_checkout" },
             { status: 403 }
           );
         }
       }
       ```

    4. Dentro do try-block, ANTES de `findConversationExchanges`, adicionar bloco de extração:
       ```
       let attachmentContext: string | undefined;
       if (file) {
         if (file.size > 5 * 1024 * 1024) {
           await releaseToolUse(quotaCheck.reservationKey);
           return NextResponse.json({ code: "FILE_TOO_LARGE", message: "Arquivo excede 5 MB." }, { status: 413 });
         }
         const buffer = Buffer.from(await file.arrayBuffer());
         const result = await extractContent(buffer, file.name);
         if (!result.ok) {
           await releaseToolUse(quotaCheck.reservationKey);
           return NextResponse.json({ code: result.code, message: result.message }, { status: 422 });
         }
         attachmentContext = result.text;
       }
       ```

    5. Passar `attachmentContext` para a chamada do resolve function (ex: `resolveSqlPayload({ ..., attachmentContext })`).

    6. Adicionar `attachmentContext` ao `saveConversationExchange({ ..., attachmentContext })`.

    Scripts: confirmar que o toolKind passado para `findConversationExchanges` e `saveConversationExchange` é `"script"` (singular) — não alterar esse detalhe.
  </action>

  <verify>
    <automated>pnpm --filter web test -- tests/multi-turn-context.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - POST sql/generate com JSON sem arquivo → 200 (backward-compat)
    - POST sql/generate com multipart e usuário free → 403 `{"code":"pro_required","feature":"attachment","cta":"pro_checkout"}`
    - POST regex/generate com multipart e usuário free → 403 com mesmo body
    - POST scripts/generate com multipart e usuário free → 403 com mesmo body
    - Nos 3 routes: `extractContent` é chamado apenas quando há arquivo; releaseToolUse é chamado antes de return 422 se a extração falha
    - `attachmentContext` passado para o resolve function e para saveConversationExchange nos 3 routes
    - `pnpm --filter web test -- tests/multi-turn-context.test.ts` sai 0 (nenhuma regressão nos testes de multi-turn de sql/regex/scripts/template)
    - `pnpm --filter web typecheck` sai 0
    - toolKind de scripts é "script" (singular) — não foi alterado
  </acceptance_criteria>

  <done>sql, regex, scripts routes com pipeline completo de attachment: multipart backward-compat, Pro-gate condicional, extração inline, propagação de attachmentContext para stream module e persistência.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Route template — multipart + extração (preservar Pro-gate incondicional)</name>
  <files>apps/web/src/app/api/tools/template/generate/route.ts</files>

  <read_first>
    - apps/web/src/app/api/tools/template/generate/route.ts — ler o arquivo INTEIRO antes de qualquer modificação: localizar exatamente o Pro-gate incondicional (linhas 18-23 conforme PATTERNS.md), onde está request.json(), onde chama findConversationExchanges, resolveTemplatePayload, saveConversationExchange
    - .planning/phases/10-persistence-llm-context/10-PATTERNS.md — seção "template/generate/route.ts" e LANDMINE-02 (Pro-gate incondicional NÃO deve ser removido)
  </read_first>

  <action>
    O template route já tem Pro-gate incondicional que bloqueia TODO request de usuário free (com ou sem arquivo). NUNCA remover esse bloco. A Phase 10 apenas adiciona wiring de multipart e extração.

    1. Adicionar import: `extractContent` from `"@/server/extraction/dispatcher"` (verificar se já existe).

    2. Substituir `body = await request.json()` pela detecção de Content-Type com backward-compat. Para o template, os campos de FormData devem corresponder ao `templateGenerateRequestSchema` — ler o arquivo do schema para confirmar os campos antes de montar o objeto body.

    3. NÃO adicionar Pro-gate condicional (`if (hasFile)`). O gate incondicional existente já bloqueia free users para qualquer request. Para template, não há diferença entre "gate de attachment" e "gate do tool" — o gate existente cobre ambos.

    4. Dentro do try-block, após reserveToolUse, adicionar bloco de extração (idêntico ao padrão dos outros routes):
       ```
       let attachmentContext: string | undefined;
       if (file) {
         if (file.size > 5 * 1024 * 1024) {
           await releaseToolUse(quotaCheck.reservationKey);
           return NextResponse.json({ code: "FILE_TOO_LARGE", message: "Arquivo excede 5 MB." }, { status: 413 });
         }
         const buffer = Buffer.from(await file.arrayBuffer());
         const result = await extractContent(buffer, file.name);
         if (!result.ok) {
           await releaseToolUse(quotaCheck.reservationKey);
           return NextResponse.json({ code: result.code, message: result.message }, { status: 422 });
         }
         attachmentContext = result.text;
       }
       ```

    5. Passar `attachmentContext` para `resolveTemplatePayload({ ..., attachmentContext })`.

    6. Adicionar `attachmentContext` ao `saveConversationExchange({ ..., attachmentContext })`.

    Verificação de regressão crítica: após a edição, o teste de que usuário free sem arquivo no template recebe 403 DEVE continuar passando — isso confirma que o gate incondicional não foi alterado.
  </action>

  <verify>
    <automated>pnpm --filter web test -- tests/multi-turn-context.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - O Pro-gate incondicional de template (linhas antes de reserveToolUse que bloqueiam TODO request free) está intacto — `grep -c "pro_required" apps/web/src/app/api/tools/template/generate/route.ts` → ≥ 1 ANTES da verificação de hasFile
    - POST template/generate com JSON sem arquivo e usuário free → ainda retorna 403 (gate incondicional funcionando)
    - POST template/generate com multipart e usuário Pro → fluxo de extração executado
    - `attachmentContext` passado para resolveTemplatePayload e saveConversationExchange
    - `pnpm --filter web test -- tests/multi-turn-context.test.ts` sai 0
    - `pnpm --filter web typecheck` sai 0
  </acceptance_criteria>

  <done>template route aceita multipart e propaga attachmentContext, preservando intacto o Pro-gate incondicional existente que bloqueia qualquer request de usuário free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| multipart/form-data → route handlers (4 rotas) | Bytes do arquivo chegam sem sanitização; dispatcher Phase 9 valida magic bytes |
| template route → Pro-gate incondicional | Gate cobre TODOS os requests; alteração acidental quebraria controle de acesso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Elevation of Privilege | sql/regex/scripts Pro-gate condicional | mitigate | getUserEntitlement antes de reserveToolUse quando hasFile; 403 com feature:"attachment" |
| T-10-03-02 | Elevation of Privilege | template Pro-gate incondicional | mitigate | Gate existente preservado intacto — não envolvido em if(hasFile); bloqueia free users em qualquer request |
| T-10-03-03 | Denial of Service | file.size > 5MB | mitigate | Verificação de 5 MB antes de alocar buffer em todos os 4 routes; releaseToolUse chamado antes do return 413 |
| T-10-03-04 | Tampering | attachmentContext via extractContent | mitigate | Delimitadores anti-injection em context-messages.ts (10-01); dispatcher Phase 9 já validou magic bytes |
| T-10-03-05 | Denial of Service | releaseToolUse em falha de extração | mitigate | extractContent dentro do try-block; releaseToolUse explícito antes de return 422 inline |
| T-10-03-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — dispatcher e extratores já instalados na Phase 9 |
</threat_model>

<verification>
1. `pnpm --filter web typecheck` → sai 0
2. `pnpm --filter web test -- tests/multi-turn-context.test.ts tests/formula-api.test.ts --reporter=verbose` → passam
3. `grep -c 'pro_required' apps/web/src/app/api/tools/sql/generate/route.ts` → ≥ 1
4. `grep -c 'pro_required' apps/web/src/app/api/tools/regex/generate/route.ts` → ≥ 1
5. `grep -c 'pro_required' apps/web/src/app/api/tools/scripts/generate/route.ts` → ≥ 1
6. `grep -c 'extractContent' apps/web/src/app/api/tools/template/generate/route.ts` → ≥ 1
7. `grep -c 'attachmentContext' apps/web/src/app/api/tools/template/generate/route.ts` → ≥ 2
8. Verificar gate incondicional template preservado: `grep -n 'pro_required' apps/web/src/app/api/tools/template/generate/route.ts` — deve aparecer antes de qualquer verificação de hasFile
</verification>

<success_criteria>
- 4 stream modules aceitam attachmentContext e propagam para buildToolContextMessages
- 3 routes (sql, regex, scripts) com multipart backward-compat, Pro-gate condicional, extração, attachmentContext em save
- template route com multipart e extração, Pro-gate incondicional preservado
- multi-turn-context.test.ts sem regressão
</success_criteria>

<output>
Criar `.planning/phases/10-persistence-llm-context/10-03-SUMMARY.md` ao concluir.
</output>
