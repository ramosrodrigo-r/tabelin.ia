---
phase: 10-persistence-llm-context
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified:
  - apps/web/tests/attachment-context.test.ts
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - CTX-03
  - CTX-04
  - PRO-02
  - PRO-03

must_haves:
  truths:
    - "teste cobre: geração com arquivo → attachmentContext injetado no system prompt com delimitadores"
    - "teste cobre: follow-up sem arquivo → latestWithAttachment injetado automaticamente (CTX-03)"
    - "teste cobre: usuário free com arquivo → 403 pro_required nos 5 routes (PRO-02)"
    - "teste cobre: conteúdo acima de 8000 chars é truncado (CTX-04)"
    - "teste cobre: falha de extração chama releaseToolUse e retorna 422 (PRO-03)"
    - "pnpm --filter web test sai 0 com não mais de 1 falha pré-existente (formula-ui.test.tsx)"
  artifacts:
    - path: "apps/web/tests/attachment-context.test.ts"
      provides: "Suite de integração cobrindo CTX-01..04, PRO-02, PRO-03"
  key_links:
    - from: "apps/web/tests/attachment-context.test.ts"
      to: "apps/web/src/server/ai/context-messages.ts"
      via: "MAX_EXTRACTED_CHARS importado e verificado"
      pattern: "MAX_EXTRACTED_CHARS"
    - from: "apps/web/tests/attachment-context.test.ts"
      to: "apps/web/src/app/api/tools/*/generate/route.ts"
      via: "POST com multipart/form-data simulado via vi.mock do dispatcher"
      pattern: "extractContent"
---

<objective>
Criar a suite de testes de integração que verifica o pipeline completo de attachment: injeção no system prompt, follow-up (CTX-03), truncagem (CTX-04), Pro-gate (PRO-02) e cota (PRO-03) — para todos os 5 tools.

Purpose: Os testes existentes (context-messages.test.ts, multi-turn-context.test.ts, formula-api.test.ts) não cobrem o pipeline de attachment. Este plano fecha esse gap de cobertura e serve como verificação final da Phase 10.

Output: apps/web/tests/attachment-context.test.ts com cobertura dos 7 critérios de sucesso da fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-persistence-llm-context/10-RESEARCH.md

<interfaces>
<!-- Padrões de test existentes para reuso — extraídos do codebase -->

De apps/web/tests/formula-api.test.ts (padrão de authedRequest + readEvents):
  import { createSessionToken, createSessionUser } from "@/server/auth/session";
  // authedRequest: cria Request com cookie de sessão autenticada (JSON)
  // authedFormDataRequest: adaptar para criar Request com FormData + cookie

De apps/web/tests/multi-turn-context.test.ts (padrão de mocks hoisted):
  const quotaMocks = vi.hoisted(() => ({
    reserveToolUse: vi.fn(),
    confirmToolUse: vi.fn(),
    releaseToolUse: vi.fn()
  }));
  vi.mock("@/server/usage/quota-service", () => quotaMocks);

  const repoMocks = vi.hoisted(() => ({
    findConversationExchanges: vi.fn(),
    saveConversationExchange: vi.fn(),
    deleteConversationExchanges: vi.fn()
  }));
  vi.mock("@/server/tools/conversation-repository", () => repoMocks);

  const entitlementMocks = vi.hoisted(() => ({
    getUserEntitlement: vi.fn()
  }));
  vi.mock("@/server/billing/entitlements", () => entitlementMocks);

De apps/web/src/server/ai/context-messages.ts (após 10-01):
  export const MAX_EXTRACTED_CHARS: number  // = 8_000
  export function buildToolContextMessages(toolKind, history, systemPrompt, userPrompt, attachmentContext?): ...

Dispatcher mock necessário (novo):
  const dispatcherMocks = vi.hoisted(() => ({
    extractContent: vi.fn()
  }));
  vi.mock("@/server/extraction/dispatcher", () => dispatcherMocks);
  // Comportamento feliz: dispatcherMocks.extractContent.mockResolvedValue({ ok: true, text: "conteúdo extraído" })
  // Comportamento de falha: dispatcherMocks.extractContent.mockResolvedValue({ ok: false, code: "SCANNED_PDF", message: "PDF sem texto" })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar apps/web/tests/attachment-context.test.ts</name>
  <files>apps/web/tests/attachment-context.test.ts</files>

  <read_first>
    - apps/web/tests/multi-turn-context.test.ts — ler inteiro: padrão de mocks hoisted, helper authedRequest, beforeEach, estrutura de describe/it; este é o template base a seguir
    - apps/web/tests/formula-api.test.ts — ler linhas 1-80: padrão de authedRequest + readEvents NDJSON, imports do route handler
    - apps/web/src/server/ai/context-messages.ts — ler as primeiras 30 linhas: confirmar o nome exato dos exports (MAX_EXTRACTED_CHARS, buildToolContextMessages)
    - apps/web/tests/context-messages.test.ts — ler linhas 1-30: padrão do helper makeExchange para criar ConversationExchange fake com campos necessários (incluindo attachmentContext)
  </read_first>

  <action>
    Criar o arquivo apps/web/tests/attachment-context.test.ts com as seguintes seções:

    **Imports e mocks hoisted (topo do arquivo):**
    - Importar os 5 POST handlers (formula, sql, regex, scripts, template generate routes)
    - Mocks hoisted para: quotaMocks (reserveToolUse, confirmToolUse, releaseToolUse), repoMocks (findConversationExchanges, saveConversationExchange), entitlementMocks (getUserEntitlement), dispatcherMocks (extractContent)
    - Helper `authedRequest(path, body)` — JSON request autenticado (replicar de formula-api.test.ts)
    - Helper `authedFormDataRequest(path, formDataObj)` — multipart request autenticado: criar FormData, popular com campos de formDataObj (incluindo `file: new File([content], filename, { type: mimeType })`), criar Request com headers `cookie` e sem `content-type` explícito (FormData define automaticamente com boundary)
    - Helper `makeExchange(overrides)` — criar ConversationExchange fake (replicar de context-messages.test.ts, adicionar campo `attachmentContext: overrides.attachmentContext ?? null`)

    **beforeEach padrão:**
    - vi.clearAllMocks()
    - quotaMocks.reserveToolUse.mockResolvedValue({ allowed: true, reservationKey: "res_test" })
    - quotaMocks.confirmToolUse.mockResolvedValue({ confirmed: true })
    - quotaMocks.releaseToolUse.mockResolvedValue({ released: true })
    - repoMocks.findConversationExchanges.mockResolvedValue([])
    - repoMocks.saveConversationExchange.mockResolvedValue({ id: "exc_test" })
    - entitlementMocks.getUserEntitlement.mockResolvedValue({ plan: "pro", status: "active" })
    - dispatcherMocks.extractContent.mockResolvedValue({ ok: true, text: "conteúdo extraído do documento" })

    **Suite 1 — Pro-gate (PRO-02): testar todos os 5 routes:**
    Para sql, formula, regex, scripts: POST multipart com usuário free (getUserEntitlement retorna `{ plan: "free", status: "active" }`) → esperar status 403 com body `{ code: "pro_required", feature: "attachment" }`. Importante: reserveToolUse NÃO deve ser chamado (verificar `expect(quotaMocks.reserveToolUse).not.toHaveBeenCalled()`).
    Para template: POST JSON sem arquivo com usuário free → deve retornar 403 (gate incondicional; não precisa de arquivo).

    **Suite 2 — Extração e injeção (CTX-01):**
    Testar o sql route com usuário Pro e arquivo: verificar que `dispatcherMocks.extractContent` foi chamado. Verificar que `repoMocks.saveConversationExchange` foi chamado com `expect.objectContaining({ attachmentContext: "conteúdo extraído do documento" })` (CTX-02).

    **Suite 3 — Follow-up / latestWithAttachment (CTX-03):**
    Testar o sql route com usuário Pro, sem arquivo no request atual, mas com histórico contendo exchange com `attachmentContext: "contexto do turno anterior"` (mockado via `repoMocks.findConversationExchanges.mockResolvedValue([makeExchange({ attachmentContext: "contexto do turno anterior" })])`). Verificar que saveConversationExchange foi chamado com `attachmentContext: undefined` (turno atual sem arquivo). Verificar que o route retorna 200 — o contexto do turno anterior é injetado via buildToolContextMessages (verificação indireta via cobertura da função em context-messages.test.ts).

    **Suite 4 — Truncagem (CTX-04):**
    Testar context-messages diretamente (importar buildToolContextMessages e MAX_EXTRACTED_CHARS): passar attachmentContext com string de 10 000 chars → verificar que o system prompt resultante contém no máximo MAX_EXTRACTED_CHARS chars do conteúdo (buscar a posição de "---" final e medir o conteúdo entre os delimitadores).

    **Suite 5 — Cota / falha de extração (PRO-03):**
    Configurar dispatcherMocks.extractContent para retornar `{ ok: false, code: "SCANNED_PDF", message: "PDF sem texto" }`. Enviar POST sql com multipart e usuário Pro. Esperar status 422. Verificar que `quotaMocks.releaseToolUse` foi chamado com "res_test". Verificar que `quotaMocks.confirmToolUse` NÃO foi chamado.

    **Notas de implementação:**
    - NÃO usar `vi.mock` de context-messages — testar buildToolContextMessages importado diretamente na Suite 4
    - Para criar o File fake em Node.js (ambiente de teste Vitest/jsdom): `new File(["conteúdo"], "doc.csv", { type: "text/csv" })` — isso funciona em Vitest com `testEnvironment: 'node'` ou 'jsdom'
    - Fixtures de response: como os resolveXxxPayload retornam fixtures quando sem OPENAI_API_KEY, os routes devem retornar 200 com NDJSON válido nos testes de sucesso
  </action>

  <verify>
    <automated>pnpm --filter web test -- tests/attachment-context.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter web test -- tests/attachment-context.test.ts` sai 0 com todos os testes passando
    - Suite 1 (Pro-gate): 5 testes passam — 4 routes com gate condicional retornam 403 para free com arquivo; template retorna 403 para free sem arquivo
    - Suite 2 (CTX-01/CTX-02): extractContent chamado; saveConversationExchange chamado com attachmentContext correto
    - Suite 3 (CTX-03): route retorna 200 com histórico contendo attachmentContext — sem regressão
    - Suite 4 (CTX-04): conteúdo de 10000 chars truncado a MAX_EXTRACTED_CHARS no system prompt
    - Suite 5 (PRO-03): falha de extração → 422, releaseToolUse chamado, confirmToolUse NÃO chamado
    - `pnpm --filter web test` (suite completa) sai com no máximo 1 falha (a pré-existente formula-ui.test.tsx)
  </acceptance_criteria>

  <done>Suite de integração cobre os 7 critérios de sucesso da Phase 10. A suite completa de testes passa com no máximo 1 falha pré-existente não relacionada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes → route handlers | Requests de teste simulam boundary multipart/JSON; mocks controlam comportamento das dependências |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Elevation of Privilege | testes de Pro-gate | mitigate | Suites verificam que free users não passam do 403; testes confirmam que reserveToolUse não é chamado antes do gate |
| T-10-04-02 | Denial of Service | teste de cota (PRO-03) | mitigate | Suite verifica que releaseToolUse é chamado quando extração falha — sem debit indevido |
| T-10-04-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — Vitest já instalado |
</threat_model>

<verification>
1. `pnpm --filter web test -- tests/attachment-context.test.ts --reporter=verbose` → sai 0, todos os testes passam
2. `pnpm --filter web test 2>&1 | grep -E "Tests|failed|passed"` → mostra no máximo 1 falha (formula-ui.test.tsx pré-existente)
3. `pnpm --filter web typecheck` → sai 0
</verification>

<success_criteria>
- attachment-context.test.ts cria cobertura de integração para CTX-01, CTX-02, CTX-03, CTX-04, PRO-02, PRO-03
- Suite completa de testes sem novas regressões além da falha pré-existente em formula-ui.test.tsx
- Phase 10 verificável automaticamente por qualquer desenvolvedor com `pnpm --filter web test`
</success_criteria>

<output>
Criar `.planning/phases/10-persistence-llm-context/10-04-SUMMARY.md` ao concluir.
</output>
