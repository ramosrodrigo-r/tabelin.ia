---
phase: 21-export-persistencia-da-planilha-conversa
plan: 03
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [PERS-03, PERS-04]
files_modified:
  - packages/shared/src/unified-chat/schema.ts
  - apps/web/src/server/tools/conversation-repository.ts
  - apps/web/src/app/api/workspace/state/route.ts
  - apps/web/src/components/app/workspace-state-context.tsx
  - apps/web/src/features/unified-chat/unified-chat-tool.tsx
  - apps/web/tests/workspace-state-route.test.ts
  - apps/web/tests/workspace-state-context.test.tsx
  - apps/web/tests/unified-chat-tool.test.tsx
files_created:
  - apps/web/tests/conversation-repository-active-spec.test.ts

must_haves:
  truths:
    - "Uma planilha com nomes de coluna colidentes (ex.: duas colunas 'Total') faz round-trip de persistência sem perder os dados de nenhuma coluna no reload"
    - "Uma planilha grande no limite do schema (200 linhas × 26 colunas com conteúdo pt-BR) persiste e recarrega sem cair no SAMPLE_SPEC"
    - "Quando a gravação do spec ativo falha no banco, a rota POST /api/workspace/state responde 500 e o cliente NÃO marca o estado como salvo (lastSavedRef não avança)"
    - "Após 'Nova conversa', o banco fica com a linha unified_table apagada (não re-semeada com SAMPLE_SPEC); nenhum auto-save POST é disparado pelo reset"
  artifacts:
    - path: "packages/shared/src/unified-chat/schema.ts"
      provides: "tableColumnSchema/tableSpecPayloadSchema com unicidade de key garantida ou teto de bytes coerente"
      contains: "superRefine"
    - path: "apps/web/src/server/tools/conversation-repository.ts"
      provides: "saveActiveSpreadsheetSpec que propaga falha; guardActiveSpecSize que rejeita oversize do spec ativo em vez de placeholder"
      exports: ["saveActiveSpreadsheetSpec", "getActiveSpreadsheetSpec"]
    - path: "apps/web/src/components/app/workspace-state-context.tsx"
      provides: "seedToGridState com keys deduplicadas; resetToSeed que suprime o auto-save"
      contains: "lastSavedRef"
    - path: "apps/web/tests/conversation-repository-active-spec.test.ts"
      provides: "Regressões do helper real (oversize rejeitado, propagação de erro) — não mockado"
      min_lines: 30
  key_links:
    - from: "apps/web/src/app/api/workspace/state/route.ts"
      to: "apps/web/src/server/tools/conversation-repository.ts"
      via: "saveActiveSpreadsheetSpec lança em falha → rota mapeia 500"
      pattern: "saveActiveSpreadsheetSpec"
    - from: "apps/web/src/components/app/workspace-state-context.tsx"
      to: "apps/web/src/app/api/workspace/state/route.ts"
      via: "auto-save NÃO dispara POST quando resetToSeed pré-marca lastSavedRef"
      pattern: "lastSavedRef.current"
---

<objective>
Fechar os quatro defeitos de perda de dados encontrados na verificação da Phase 21 (gaps_found, 2/5 must-haves). O export (SC1/SC2) e a hidratação da conversa (SC4) já passam; esta entrega corrige a **garantia de round-trip da planilha (SC3)** e o **reset coerente de "Nova conversa" (SC5)**, que estão quebrados para casos realistas.

Os gaps a fechar (rastreáveis ao 21-REVIEW.md):
- **CR-02:** colisão de key derivada em `seedToGridState` sobrescreve coluna no reload.
- **WR-04:** spec ativo >32 KB vira placeholder `{truncated:true}` e cai no SAMPLE_SPEC no reload — planilha grande perdida.
- **WR-03:** `saveActiveSpreadsheetSpec` engole o erro; rota sempre 200; cliente marca como salvo um save perdido.
- **CR-01:** `resetToSeed()` dispara auto-save que recria a linha `unified_table` (SAMPLE_SPEC) após o DELETE do "Nova conversa".

Build SOBRE o código já entregue em 21-01/21-02 (não refazer). Cada correção adiciona um teste de regressão que exercita o modo de falha real — a verificação apontou que os 27 testes atuais passam mas NÃO cobrem essas falhas (o teste de 500 da rota mocka o helper para rejeitar, comportamento que o helper real nunca produz).

Purpose: tornar a persistência da planilha uma garantia confiável de round-trip e o reset determinístico no banco.
Output: schema com unicidade/teto coerente, helper de persistência que falha-em-voz-alta, derivação de grid sem colisão, e coordenação reset↔auto-save — todos cobertos por regressões reais.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-export-persistencia-da-planilha-conversa/21-VERIFICATION.md
@.planning/phases/21-export-persistencia-da-planilha-conversa/21-REVIEW.md
@.planning/phases/21-export-persistencia-da-planilha-conversa/21-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persistência do spec ativo falha-em-voz-alta e rejeita oversize; schema garante key única</name>
  <files>packages/shared/src/unified-chat/schema.ts, apps/web/src/server/tools/conversation-repository.ts, apps/web/src/app/api/workspace/state/route.ts, apps/web/tests/workspace-state-route.test.ts, apps/web/tests/conversation-repository-active-spec.test.ts</files>
  <read_first>
    - packages/shared/src/unified-chat/schema.ts (tableColumnSchema l.22-28, tableSpecPayloadSchema l.30-42 — alvo da edição)
    - apps/web/src/server/tools/conversation-repository.ts (guardPayloadSize l.39-57, saveActiveSpreadsheetSpec l.160-188, getActiveSpreadsheetSpec l.139-153, MAX_PAYLOAD_BYTES l.26 — alvo da edição)
    - apps/web/src/app/api/workspace/state/route.ts (POST l.32-38 — alvo da edição)
    - apps/web/tests/workspace-state-route.test.ts (padrão de mock do repositório e de Request autenticada — alvo da edição)
  </read_first>
  <behavior>
    - Schema: um tableSpecPayload com duas colunas que derivam a mesma key (ex.: duas colunas name "Total" sem key explícita, ou keys explícitas iguais) é REJEITADO pelo schema com erro de unicidade. Um spec com keys distintas passa.
    - Helper guardActiveSpecSize: um spec ativo serializado acima de MAX_PAYLOAD_BYTES LANÇA um Error explícito (não retorna placeholder). Um spec no limite de 200×26 com conteúdo pt-BR cabe sob um teto elevado e NÃO lança.
    - saveActiveSpreadsheetSpec: quando a transação Prisma rejeita, o helper PROPAGA o erro (não engole, não retorna void silencioso). Em sucesso, completa sem lançar.
    - Rota POST: com o helper real lançando (oversize ou falha de banco), a rota responde 500 e NÃO retorna { ok: true }.
  </behavior>
  <action>
    Corrige WR-04, WR-03 e CR-02 (parte de schema) na camada server/contrato.

    Em packages/shared/src/unified-chat/schema.ts: adicionar validação de unicidade de key efetiva a tableSpecPayloadSchema via superRefine. Para cada coluna, a key efetiva é `column.key` quando presente, senão a key derivada do mesmo modo que seedToGridState (lowercase + espaços→underscore). Se duas colunas produzem a mesma key efetiva, emitir issue no caminho da segunda coluna com mensagem em pt-BR (ex.: "Colunas com chave duplicada não são permitidas."). Exportar uma função utilitária pura `deriveColumnKey(name: string): string` reutilizável para que provider e schema compartilhem exatamente a mesma normalização (evita drift entre escrita e validação, conforme nota do CR-02 sobre fragilidade da normalização). Não relaxar os limites existentes min(1).max(26)/max(200).

    Em apps/web/src/server/tools/conversation-repository.ts: elevar o teto do SPEC ATIVO para acomodar o máximo legítimo do schema (200 linhas × 26 colunas com conteúdo pt-BR). Definir uma constante dedicada `MAX_ACTIVE_SPEC_BYTES` (dimensionar com folga sobre o máximo do schema; o histórico de chat continua usando o teto de 32 KB existente via guardPayloadSize — não mudar esse caminho). Substituir o uso de guardPayloadSize dentro de saveActiveSpreadsheetSpec por um guard dedicado `guardActiveSpecSize(spec)` que, quando o JSON serializado excede MAX_ACTIVE_SPEC_BYTES, LANÇA um Error explícito (mensagem em pt-BR) em vez de retornar placeholder `{truncated:true}`. Remover o try/catch que engole o erro de saveActiveSpreadsheetSpec: deixar a transação e o guard propagarem para o caller — assinatura permanece Promise<void> mas agora rejeita em falha. Não tocar em saveConversationExchange nem em guardPayloadSize (histórico de chat tolera truncamento).

    Em apps/web/src/app/api/workspace/state/route.ts: o try/catch existente (l.32-38) já mapeia exceção para 500 — agora ele passa a disparar de fato porque o helper propaga. Garantir que o ramo de catch NÃO retorne { ok: true } e que o log não vaze detalhe interno na resposta. Nenhuma mudança estrutural além de confirmar o mapeamento.

    Em apps/web/tests/workspace-state-route.test.ts: substituir o teste de 500 que mockava o helper para rejeitar artificialmente por uma asserção alinhada ao comportamento real (helper real propaga). Manter o mock onde necessário para isolar a rota, mas o mock deve refletir que o helper REJEITA em falha (não que sempre resolve). Não inserir o literal exato de nenhum critério de aceite negativo no corpo de comentários.
  </action>
  <acceptance_criteria>
    - tableSpecPayloadSchema.safeParse de um spec com duas colunas de mesma key efetiva retorna success=false; com keys distintas retorna success=true.
    - `deriveColumnKey` exportado de @tabelin/shared e usado tanto no schema quanto importável pelo provider.
    - MAX_ACTIVE_SPEC_BYTES existe e é maior que o pior caso de 200×26 pt-BR; guardActiveSpecSize lança Error quando excedido e retorna o spec intacto quando dentro do teto.
    - saveActiveSpreadsheetSpec não contém mais catch que apenas console.warn + retorna void para o caminho do spec ativo; a falha propaga.
    - POST /api/workspace/state responde 500 quando saveActiveSpreadsheetSpec rejeita; responde 200 apenas em sucesso real.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm exec prisma generate --schema apps/web/prisma/schema.prisma >/dev/null 2>&1; pnpm --filter @tabelin/shared test 2>&1 | tail -5; pnpm --filter web exec vitest run tests/workspace-state-route.test.ts tests/conversation-repository-active-spec.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Schema rejeita keys colidentes; spec ativo grande persiste sem placeholder; save falho propaga e a rota retorna 500. Testes novos cobrem oversize-rejeitado e propagação-de-erro contra o helper real (sem mock que mascare).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derivação de grid sem colisão de key + supressão do auto-save no reset</name>
  <files>apps/web/src/components/app/workspace-state-context.tsx, apps/web/tests/workspace-state-context.test.tsx</files>
  <read_first>
    - apps/web/src/components/app/workspace-state-context.tsx (seedToGridState l.31-51, RESET_TO_SEED l.96-103, resetToSeed l.148, lastSavedRef + auto-save useEffect l.160-188 — alvo da edição)
    - packages/shared/src/unified-chat/schema.ts (deriveColumnKey exportado na Task 1 — importar aqui)
    - apps/web/tests/workspace-state-context.test.tsx (padrão de fake timers + act + stubGlobal fetch — alvo da edição)
  </read_first>
  <behavior>
    - seedToGridState com duas colunas que colidem na key derivada produz duas keys DISTINTAS (sufixo de índice na colisão); ambos os conjuntos de dados de coluna sobrevivem (nenhum newRow[key] sobrescreve o outro).
    - Round-trip: um spec com nomes de coluna colidentes passado como initialSpec rende dois valores de célula distintos no grid (não um sobrescrevendo o outro).
    - resetToSeed: ao resetar para SAMPLE_SPEC, o auto-save NÃO dispara POST (lastSavedRef já reflete o estado de reset antes do dispatch); avançar os timers além do debounce não chama fetch.
  </behavior>
  <action>
    Corrige CR-02 (escrita) e CR-01 (supressão do auto-save) no provider.

    Em apps/web/src/components/app/workspace-state-context.tsx, seedToGridState: importar `deriveColumnKey` de @tabelin/shared e substituir a derivação inline `c.name.toLowerCase().replace(/\s+/g, "_")`. Ao montar `columns`, garantir keys únicas: manter um Set de keys já vistas e, em colisão, sufixar o índice da coluna (ex.: `${key}_${index}`) até obter uma key inédita — exatamente como descrito no fix do CR-02. Usar a key resolvida (única) como índice de escrita em `newRow[resolvedKey]`, de modo que duas colunas colidentes nunca sobrescrevam a mesma chave. O lookup de leitura (`r[c.key] ?? r[c.name] ?? r[resolvedKey]`) permanece tolerante.

    Para CR-01: o auto-save (useEffect l.168-188) dispara POST sempre que `specJson !== lastSavedRef.current`. Tornar `resetToSeed` (e qualquer reset que deva ser tratado como "limpar persistência") suprimir esse POST: antes de despachar RESET_TO_SEED, computar o specJson resultante do estado de reset e atribuí-lo a `lastSavedRef.current`, de forma que o efeito veja `specJson === lastSavedRef.current` e retorne cedo (sem POST). Como `resetToSeed` está num useCallback e o spec resultante depende de seedToGridState(SAMPLE_SPEC), extrair a derivação do specJson de reset para uma forma reutilizável (helper puro a partir de um GridState/seed) e usá-la tanto na atribuição de lastSavedRef quanto, se conveniente, na inicialização. Não introduzir POST de delete aqui — a coordenação é por supressão (o DELETE do "Nova conversa" no Topbar já apaga a linha; suprimir o auto-save impede a ressurreição). Documentar em comentário curto que a supressão evita a corrida CR-01.

    Em apps/web/tests/workspace-state-context.test.tsx: adicionar (a) um teste de round-trip de colisão — initialSpec com duas colunas que derivam a mesma key, asserindo que ambos os valores de célula da primeira linha sobrevivem distintos; (b) um teste de reset — após resetToSeed, avançar os timers além de AUTO_SAVE_DEBOUNCE_MS e asserir que fetch NÃO foi chamado (auto-save suprimido). Reusar o padrão de fake timers/act/stubGlobal já presente no arquivo.
  </action>
  <acceptance_criteria>
    - seedToGridState com colunas colidentes gera keys únicas e preserva os dados de ambas; teste de round-trip de colisão passa.
    - Após resetToSeed, nenhum POST para /api/workspace/state é disparado (fetch não chamado após avançar o debounce); teste de supressão passa.
    - seedToGridState importa e usa deriveColumnKey de @tabelin/shared (mesma normalização do schema).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm exec prisma generate --schema apps/web/prisma/schema.prisma >/dev/null 2>&1; pnpm --filter web exec vitest run tests/workspace-state-context.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>Colisão de key não perde dados no reload e resetToSeed não ressuscita o spec via auto-save. Ambos cobertos por regressão no provider.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: "Nova conversa" deixa o banco limpo (reset não dispara save) — cobertura end-to-end do componente</name>
  <files>apps/web/src/features/unified-chat/unified-chat-tool.tsx, apps/web/tests/unified-chat-tool.test.tsx</files>
  <read_first>
    - apps/web/src/features/unified-chat/unified-chat-tool.tsx (handleNewConversation l.103-116 — chama workspaceState.resetToSeed())
    - apps/web/src/components/app/topbar.tsx (handleDeleteHistory l.43-48 — await DELETE então onNewConversation())
    - apps/web/src/components/app/workspace-state-context.tsx (resetToSeed com supressão da Task 2)
    - apps/web/tests/unified-chat-tool.test.tsx (padrão renderUnifiedChatTool, WorkspaceProbe, "new conversation resets the live grid" l.371-404 — alvo da edição)
  </read_first>
  <behavior>
    - Após mutar a planilha viva via chat→grade e então invocar "Nova conversa", a grade volta à semente E nenhum auto-save POST para /api/workspace/state é disparado pela sequência de reset (a linha unified_table apagada pelo DELETE não é re-semeada).
    - O teste distingue o POST de auto-save (/api/workspace/state) das chamadas de stream (/api/chat/unified) e do DELETE — asserindo zero POSTs de estado após o reset, mesmo avançando o tempo além do debounce.
  </behavior>
  <action>
    Corrige a verificação observável do CR-01 no nível do componente (a supressão em si vem da Task 2; aqui garantimos o comportamento end-to-end do fluxo "Nova conversa").

    handleNewConversation já chama workspaceState.resetToSeed() (l.115) — com a supressão da Task 2 isso passa a NÃO disparar o auto-save. Não é necessário mudar a lógica de unified-chat-tool.tsx além de confirmar que o reset usa o resetToSeed coordenado; se a Task 2 expôs uma variante explícita de "reset que limpa persistência", usá-la aqui. Caso contrário, nenhuma mudança de código de produção neste arquivo.

    Em apps/web/tests/unified-chat-tool.test.tsx: estender (ou adicionar ao lado de) o teste "new conversation resets the live grid to the seed (D-04)" para também asserir a ausência de auto-save após o reset. Como o auto-save é debancado (1.5s) e o teste atual usa timers reais via userEvent, usar fake timers de forma controlada OU inspecionar o fetch mock filtrando por URL: após "Limpar", avançar/aguardar além do debounce e asserir que nenhuma chamada de fetch com URL "/api/workspace/state" e method POST ocorreu na sequência de reset. Garantir que a asserção não conte os fetches de "/api/chat/unified" do stream. Manter o teste existente de reset-da-grade verde.
  </action>
  <acceptance_criteria>
    - Existe um teste que invoca "Nova conversa" após uma mutação da planilha e asserta: (a) a grade volta à semente; (b) nenhum POST para "/api/workspace/state" é disparado pela sequência de reset, mesmo após o debounce.
    - Os testes pré-existentes de unified-chat-tool.test.tsx permanecem verdes.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm exec prisma generate --schema apps/web/prisma/schema.prisma >/dev/null 2>&1; pnpm --filter web exec vitest run tests/unified-chat-tool.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>O fluxo "Nova conversa" deixa o banco coerentemente limpo (sem re-semear SAMPLE_SPEC via auto-save), comprovado por regressão de componente que distingue o POST de estado das chamadas de stream.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Novos símbolos/contratos introduzidos por esta entrega de gap-closure:
- `deriveColumnKey(name: string): string` — exportado de `@tabelin/shared` (packages/shared/src/unified-chat/schema.ts); normalização canônica de key compartilhada entre schema e provider.
- `tableSpecPayloadSchema` — agora com `superRefine` de unicidade de key efetiva (rejeita colunas colidentes).
- `MAX_ACTIVE_SPEC_BYTES` + `guardActiveSpecSize(spec)` — em conversation-repository.ts; teto dedicado do spec ativo que LANÇA em oversize (não placeholder).
- `saveActiveSpreadsheetSpec` — agora propaga falha (rejeita) em vez de engolir o erro.
- `resetToSeed` (workspace-state-context.tsx) — agora pré-marca `lastSavedRef` para suprimir o auto-save POST do reset.
- `apps/web/tests/conversation-repository-active-spec.test.ts` — nova suíte que exercita o helper real (oversize rejeitado, propagação de erro), sem mock que mascare o comportamento.
</artifacts_this_phase_produces>

<verification>
Checks de fechamento dos gaps:
- CR-02: `tableSpecPayloadSchema` rejeita keys colidentes E `seedToGridState` desambigua keys na escrita (round-trip de colisão preserva dados).
- WR-04: spec ativo no máximo do schema (200×26 pt-BR) persiste e recarrega sem placeholder/SAMPLE_SPEC; oversize legítimo é rejeitado com erro explícito (nunca placeholder descartável).
- WR-03: `saveActiveSpreadsheetSpec` propaga falha → rota 500 → cliente não avança lastSavedRef.
- CR-01: `resetToSeed` suprime o auto-save → "Nova conversa" não ressuscita a linha unified_table.
- Cada gap tem regressão que exercita o modo de falha real (não mock que mascare).
- Suíte completa verde: `pnpm --filter web test` e `pnpm --filter @tabelin/shared test`.
- `pnpm -r typecheck` limpo (rodar `pnpm exec prisma generate` antes em worktree — falso-positivo `.prisma/client/default`).
</verification>

<success_criteria>
- SC3 (round-trip de persistência) e SC5 (reset coerente) do ROADMAP passam para casos realistas: upload, sheets grandes, nomes de coluna duplicados, falha de gravação, fluxo de "Nova conversa".
- Os quatro gaps (CR-01, CR-02, WR-03, WR-04) fechados com regressão dedicada cada.
- Nenhum mock mascarando o comportamento real do helper de persistência.
- requirements: PERS-03 (round-trip) e PERS-04 (reset coerente) satisfeitos; export PERS-01/PERS-02 não re-reivindicados (já satisfeitos por código da Phase 15).
</success_criteria>

<output>
Create `.planning/phases/21-export-persistencia-da-planilha-conversa/21-03-SUMMARY.md` when done
</output>
