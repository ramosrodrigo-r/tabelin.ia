---
phase: "04"
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - apps/web/src/server/file-analysis/cleanup-job.ts
  - apps/web/src/instrumentation.ts
  - apps/web/src/components/app/sidebar.tsx
  - apps/web/public/privacidade.html
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02
  - PRIV-03
  - PRIV-04

must_haves:
  truths:
    - "D-03: cron job executa a cada 15 min e apaga UploadedFile com lastChatAt ou createdAt > 1 hora de inatividade"
    - "Cron job apaga UploadedFile com inatividade > 1 hora automaticamente a cada 15 min"
    - "Cron é registrado uma única vez via instrumentation.ts com guard globalThis contra hot reload"
    - "Sidebar File Analysis link está ativo e navega para /workspace/file-analysis"
    - "Página de privacidade documenta política de não-uso de dados para retreinamento pela OpenAI API"
    - "PRIV-01, PRIV-02, PRIV-03, PRIV-04 verificáveis por código e documento"
  artifacts:
    - path: "apps/web/src/server/file-analysis/cleanup-job.ts"
      provides: "D-03: startCleanupJob com cron de 15min, cutoff de 1h, e globalThis guard"
      contains: "_cleanupJobStarted"
    - path: "apps/web/src/instrumentation.ts"
      provides: "register() hook que chama startCleanupJob no runtime nodejs"
      contains: "NEXT_RUNTIME"
    - path: "apps/web/src/components/app/sidebar.tsx"
      provides: "File Analysis com href ativo /workspace/file-analysis"
      contains: "workspace/file-analysis"
    - path: "apps/web/public/privacidade.html"
      provides: "Documentação de política de privacidade com referência à OpenAI API commercial terms"
  key_links:
    - from: "apps/web/src/instrumentation.ts"
      to: "apps/web/src/server/file-analysis/cleanup-job.ts"
      via: "dynamic import em register()"
      pattern: "import.*cleanup-job"
    - from: "apps/web/src/server/file-analysis/cleanup-job.ts"
      to: "apps/web/src/server/db/client.ts"
      via: "prisma.uploadedFile.deleteMany"
      pattern: "uploadedFile.deleteMany"
---

<objective>
Ciclo de vida de privacidade completo: cron de limpeza automática (PRIV-01), ativação do sidebar (navegação), e documentação de privacidade (PRIV-03). Também verifica que PRIV-02 e PRIV-04 estão satisfeitos pelos artefatos dos planos anteriores.

Purpose: PRIV-01 exige deleção após 1 hora de inatividade independente de ação do usuário. Sem o cron, registros ficam no Postgres indefinidamente. PRIV-03 exige documentação da política — sem ela o requisito está aberto. O sidebar ativo fecha o loop de navegação para o usuário.

Output: cleanup-job.ts com guard, instrumentation.ts registrando o cron, sidebar ativado, página de privacidade criada.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-spreadsheet-file-analysis/04-CONTEXT.md
@.planning/phases/04-spreadsheet-file-analysis/04-RESEARCH.md
@.planning/phases/04-spreadsheet-file-analysis/04-02-SUMMARY.md

<interfaces>
<!-- Contratos necessários para este plano. -->

De apps/web/src/server/db/client.ts (padrão de import já estabelecido):
```typescript
import { prisma } from "@/server/db/client";
```

De apps/web/src/components/app/sidebar.tsx (estado atual — linha a modificar):
```typescript
{ label: "File Analysis", icon: FileText, disabled: true },
```
Deve tornar-se:
```typescript
{ label: "File Analysis", icon: FileText, href: "/workspace/file-analysis" },
```

Padrão cleanup-job de RESEARCH.md (Pattern 4 + Pitfall 4):
```typescript
import "server-only";
import cron from "node-cron";
import { prisma } from "@/server/db/client";

export function startCleanupJob() {
  const g = globalThis as typeof globalThis & { _cleanupJobStarted?: boolean };
  if (g._cleanupJobStarted) return;
  g._cleanupJobStarted = true;
  cron.schedule("*/15 * * * *", async () => {
    const cutoff = new Date(Date.now() - 60 * 60 * 1000);
    await prisma.uploadedFile.deleteMany({
      where: { OR: [
        { lastChatAt: { lt: cutoff } },
        { lastChatAt: null, createdAt: { lt: cutoff } }
      ]}
    });
  });
}
```

Padrão instrumentation.ts de RESEARCH.md (Pattern 4):
```typescript
export async function register() {
  if (process.env.NEXT_RUNTIME === "nodejs") {
    const { startCleanupJob } = await import("./server/file-analysis/cleanup-job");
    startCleanupJob();
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cron de limpeza e instrumentation.ts</name>
  <files>
    apps/web/src/server/file-analysis/cleanup-job.ts
    apps/web/src/instrumentation.ts
  </files>
  <read_first>
    - .planning/phases/04-spreadsheet-file-analysis/04-RESEARCH.md — Pattern 4 (cron + instrumentation.ts), Pitfall 4 (guard globalThis contra hot reload)
    - .planning/phases/04-spreadsheet-file-analysis/04-PATTERNS.md — seção "No Analog Found" com código exato do cleanup-job.ts
    - apps/web/src/server/db/client.ts — confirmar path de import do prisma singleton
    - .planning/phases/04-spreadsheet-file-analysis/04-CONTEXT.md — D-03 (cron 15 min, deleção por lastChatAt OU createdAt > 1h)
  </read_first>
  <action>
    Criar apps/web/src/server/file-analysis/cleanup-job.ts:
    - Primeira linha: "import 'server-only'"
    - Importar cron de "node-cron" e prisma de "@/server/db/client"
    - Função exportada startCleanupJob():
      - Guard globalThis: const g = globalThis as typeof globalThis & { _cleanupJobStarted?: boolean }; if (g._cleanupJobStarted) return; g._cleanupJobStarted = true;
      - cron.schedule("*/15 * * * *", async () => { ... })
      - Dentro do callback: cutoff = new Date(Date.now() - 60 * 60 * 1000) (1 hora exata)
      - prisma.uploadedFile.deleteMany com where: OR de [lastChatAt < cutoff, { lastChatAt: null, createdAt: < cutoff }]
      - onDelete: Cascade no schema garante que ChatMessage é apagado junto — não precisa de deleteMany separado
      - Nenhum console.log de IDs ou conteúdo de schema; apenas console.info de contagem (ex: "cleanup: N registros removidos") se deletedCount > 0
    - Nenhuma exportação default — somente startCleanupJob como named export

    Verificar se apps/web/src/instrumentation.ts já existe:
    - Se existe: ler o arquivo e adicionar a chamada startCleanupJob dentro do bloco nodejs existente sem substituir código existente
    - Se não existe: criar com o conteúdo exato do RESEARCH.md Pattern 4:
      export async function register() {
        if (process.env.NEXT_RUNTIME === "nodejs") {
          const { startCleanupJob } = await import("./server/file-analysis/cleanup-job");
          startCleanupJob();
        }
      }
    - Usar dynamic import para evitar carregar o módulo no edge runtime
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && grep -c "_cleanupJobStarted" apps/web/src/server/file-analysis/cleanup-job.ts</automated>
    <automated>cd /home/rodrigo/tabelin.ia && grep -c "NEXT_RUNTIME" apps/web/src/instrumentation.ts</automated>
    <automated>cd /home/rodrigo/tabelin.ia && grep -c "cleanup-job" apps/web/src/instrumentation.ts</automated>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm --filter web tsc --noEmit 2>&1 | grep -E "cleanup-job|instrumentation" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - cleanup-job.ts contém "import 'server-only'"
    - cleanup-job.ts contém "_cleanupJobStarted" (guard contra hot reload)
    - cleanup-job.ts contém "uploadedFile.deleteMany"
    - cleanup-job.ts contém query com "lastChatAt: null" como segundo caso do OR (cobre arquivos nunca chatados)
    - cleanup-job.ts não contém "console.log" (apenas console.info de contagem opcionalmente)
    - instrumentation.ts contém "NEXT_RUNTIME" e "cleanup-job"
    - instrumentation.ts usa dynamic import (await import) — não import estático
    - pnpm --filter web tsc --noEmit não retorna erros nos dois arquivos
  </acceptance_criteria>
  <done>Cron de limpeza implementado com guard, registrado via instrumentation.ts, type-check passa.</done>
</task>

<task type="auto">
  <name>Task 2: Ativar sidebar + criar página de privacidade (PRIV-03)</name>
  <files>
    apps/web/src/components/app/sidebar.tsx
    apps/web/public/privacidade.html
  </files>
  <read_first>
    - apps/web/src/components/app/sidebar.tsx — ver linha atual com File Analysis disabled: true (linha 26 aprox.)
    - .planning/phases/04-spreadsheet-file-analysis/04-PATTERNS.md — seção "sidebar.tsx" com a mudança exata a fazer
    - .planning/REQUIREMENTS.md — PRIV-03: "The product documents that customer data is not used for public model retraining by selected commercial AI providers unless explicitly opted in."
    - .planning/phases/04-spreadsheet-file-analysis/04-RESEARCH.md — seção PRIV-03 e PRIV-04 (arquitetura de parse local satisfaz PRIV-04; PRIV-03 precisa de documentação)
  </read_first>
  <action>
    Em apps/web/src/components/app/sidebar.tsx:
    - Localizar a linha: { label: "File Analysis", icon: FileText, disabled: true }
    - Substituir por: { label: "File Analysis", icon: FileText, href: "/workspace/file-analysis" }
    - Nenhuma outra mudança no arquivo — apenas essa linha

    Criar apps/web/public/privacidade.html com conteúdo HTML simples em português:
    - Título da página: "Privacidade de Dados — Tabelin.IA"
    - Heading h1: "Política de Privacidade de Dados"
    - Seção sobre dados de planilhas enviados: explicar que arquivos CSV e XLSX são processados localmente no servidor, o conteúdo raw nunca é armazenado em disco ou banco de dados, e apenas metadados estruturados (nomes de colunas, tipos inferidos, amostras de dados) são usados para análise.
    - Seção sobre IA: informar que os dados processados são enviados à API comercial da OpenAI. Incluir declaração explícita: "De acordo com os Termos de Uso da API Comercial da OpenAI, dados enviados via API não são utilizados para treinar os modelos públicos da OpenAI, a menos que o cliente opte explicitamente por compartilhá-los. Tabelin.IA não opta por compartilhamento de dados para treinamento." Incluir link de referência: https://openai.com/policies/usage-policies
    - Seção sobre retenção: informar que registros de análise de arquivo são automaticamente apagados após 1 hora de inatividade.
    - Seção sobre logs: informar que conteúdo de células não é registrado em logs de aplicação.
    - Footer com data de última atualização e contato (email do responsável ou placeholder).
    - HTML semântico simples (h1, h2, p, ul), sem frameworks externos, charset UTF-8.
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && grep -c "workspace/file-analysis" apps/web/src/components/app/sidebar.tsx</automated>
    <automated>cd /home/rodrigo/tabelin.ia && grep -c "disabled: true" apps/web/src/components/app/sidebar.tsx</automated>
    <automated>cd /home/rodrigo/tabelin.ia && grep -c "OpenAI" apps/web/public/privacidade.html</automated>
    <automated>cd /home/rodrigo/tabelin.ia && grep -c "treinamento" apps/web/public/privacidade.html 2>/dev/null || grep -c "retreinamento\|trein" apps/web/public/privacidade.html</automated>
  </verify>
  <acceptance_criteria>
    - sidebar.tsx contém "href: \"/workspace/file-analysis\"" para o item File Analysis
    - sidebar.tsx não contém mais "disabled: true" para File Analysis (a linha foi removida/substituída)
    - public/privacidade.html existe e contém referência à política de não-treinamento da OpenAI API comercial
    - public/privacidade.html contém menção explícita a "dados não são utilizados para treinar" os modelos da OpenAI
    - public/privacidade.html acessível em /privacidade no servidor Next.js (arquivo estático em public/)
    - pnpm --filter web tsc --noEmit não retorna erros em sidebar.tsx
  </acceptance_criteria>
  <done>Sidebar ativo com link para /workspace/file-analysis, página de privacidade criada satisfazendo PRIV-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Cron job → Postgres | Delete em massa por inatividade — operação privilegiada interna |
| Página pública /privacidade.html | Conteúdo estático — sem dados sensíveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Denial of Service | cleanup-job.ts — cron job deleteMany | accept | Guard globalThis previne múltiplas instâncias; deleteMany é operação atômica no Postgres; 5MB + 1h inatividade limita volume |
| T-04-03-02 | Information Disclosure | cleanup-job.ts — logs de limpeza | mitigate | Apenas console.info com contagem (N registros removidos) se deletedCount > 0; nunca logar userId, fileName ou schema |
| T-04-SC | Tampering | node-cron@3 — job scheduler | mitigate | Versão fixada no package.json; sem callbacks externos; executa somente deleteMany interno |
</threat_model>

<verification>
Fase completa quando:
- Cron job inicia via instrumentation.ts no processo Next.js
- guard globalThis impede registros duplicados em hot reload
- prisma.uploadedFile.deleteMany remove registros com lastChatAt ou createdAt > 1 hora
- /workspace/file-analysis está acessível via sidebar (link ativo, não disabled)
- /privacidade está servindo a página HTML com declaração de política de não-treinamento
- pnpm build passa sem erros (build completo com todos os planos)
- pnpm --filter web tsc --noEmit passa sem erros em todos os arquivos novos da Fase 4
</verification>

<success_criteria>
- startCleanupJob() registra job cron sem lançar exceção
- Dois UploadedFile com lastChatAt null e createdAt há 2 horas são apagados pelo job (cascata apaga ChatMessage junto)
- Sidebar mostra File Analysis como link ativo para /workspace/file-analysis
- /privacidade.html documenta explicitamente que OpenAI API comercial não usa dados para retreinamento público
- Fase 4 completa: todos os 9 requisitos (FILE-01 a FILE-05, PRIV-01 a PRIV-04) cobertos nos três planos
</success_criteria>

<output>
Criar `.planning/phases/04-spreadsheet-file-analysis/04-03-SUMMARY.md` ao finalizar.
</output>
