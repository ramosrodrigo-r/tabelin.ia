---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - packages/shared/src/file-analysis/schema.ts
  - packages/shared/src/file-analysis/fixtures.ts
  - packages/shared/src/index.ts
  - apps/web/src/server/file-analysis/file-parser.ts
  - apps/web/src/server/file-analysis/file-repository.ts
  - apps/web/package.json
autonomous: true
requirements:
  - FILE-01
  - FILE-02
  - PRIV-01
  - PRIV-02

must_haves:
  truths:
    - "Modelos UploadedFile e ChatMessage existem no Postgres com os índices corretos"
    - "csv-parse, xlsx e node-cron estão instalados em apps/web"
    - "Contratos Zod para upload, chat e stream events estão exportados de @tabelin/shared"
    - "file-parser.ts infere tipos de coluna e extrai schema sem salvar buffer raw"
    - "file-repository.ts consulta UploadedFile sempre com userId (guard IDOR)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Modelos UploadedFile e ChatMessage com cascades e índices"
      contains: "model UploadedFile"
    - path: "packages/shared/src/file-analysis/schema.ts"
      provides: "Zod schemas: fileSchemaSchema, uploadResponseSchema, chatRequestSchema, chatStreamEventSchema"
      exports:
        - fileSchemaSchema
        - uploadResponseSchema
        - chatRequestSchema
        - chatStreamEventSchema
    - path: "apps/web/src/server/file-analysis/file-parser.ts"
      provides: "parseFile(buffer, mimeType, sheetName?) → FileSchema"
      min_lines: 60
    - path: "apps/web/src/server/file-analysis/file-repository.ts"
      provides: "CRUD UploadedFile e ChatMessage com userId scoping"
      exports:
        - createUploadedFile
        - findUploadedFileByIdAndUser
        - updateLastChatAt
        - getRecentMessages
        - appendChatMessages
  key_links:
    - from: "apps/web/src/server/file-analysis/file-parser.ts"
      to: "packages/shared/src/file-analysis/schema.ts"
      via: "FileSchema type"
      pattern: "import.*fileSchemaSchema"
    - from: "apps/web/src/server/file-analysis/file-repository.ts"
      to: "apps/web/src/server/db/client.ts"
      via: "prisma singleton"
      pattern: "import.*prisma.*db/client"
---

<objective>
Fundação de dados e contratos da feature File Analysis: instalar dependências de parse, adicionar modelos Prisma, executar db push, definir Zod contracts compartilhados, implementar o parser de arquivo (csv-parse + xlsx com inferência de tipos) e o repositório de dados (UploadedFile + ChatMessage com IDOR guard).

Purpose: Os planos 04-02 e 04-03 dependem de todos esses artefatos. Sem o schema no Postgres e os contratos Zod, o route handler de upload não pode persistir e o route handler de chat não pode validar requests.

Output: Libs instaladas, schema Prisma atualizado, db push executado, contratos Zod em @tabelin/shared, file-parser.ts, file-repository.ts.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-spreadsheet-file-analysis/04-CONTEXT.md
@.planning/phases/04-spreadsheet-file-analysis/04-RESEARCH.md
@.planning/phases/04-spreadsheet-file-analysis/04-PATTERNS.md

<interfaces>
<!-- Contratos e padrões que o executor precisa conhecer. -->

De prisma/schema.prisma (modelo existente para referência):
```prisma
model ToolRequest {
  id        String   @id @default(cuid())
  userId    String
  ...
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  @@index([userId, createdAt])
}
```

De apps/web/src/server/db/client.ts (singleton importado por repositórios):
```typescript
import { prisma } from "@/server/db/client";
```

De packages/shared/src/scripts/schema.ts (padrão Zod a replicar):
```typescript
export const chatStreamEventSchema = z.discriminatedUnion("type", [...]);
export type ChatStreamEvent = z.infer<typeof chatStreamEventSchema>;
```

De packages/shared/src/index.ts (ponto de re-export a estender):
```typescript
export * from "./scripts/schema";
export * from "./scripts/fixtures";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependências de parse e adicionar modelos Prisma</name>
  <files>
    apps/web/package.json
    prisma/schema.prisma
  </files>
  <read_first>
    - prisma/schema.prisma — ver estrutura atual, posição correta para adicionar modelos após BillingCheckout
    - apps/web/package.json — confirmar versão do Prisma e dependências já instaladas
    - .planning/phases/04-spreadsheet-file-analysis/04-RESEARCH.md — seção "Standard Stack" para versões exatas
  </read_first>
  <action>
    Instalar as três dependências novas em apps/web:

    ```
    pnpm --filter web add csv-parse@6.2.1 xlsx@0.18.5 node-cron@3
    pnpm --filter web add -D @types/node-cron
    ```

    Adicionar ao prisma/schema.prisma, após o modelo BillingCheckout:

    Modelo UploadedFile com campos: id (cuid), userId (String), fileName (String), fileSize (Int), mimeType (String), schema (Json), rowCount (Int), lastChatAt (DateTime?), createdAt (DateTime @default(now())), updatedAt (DateTime @updatedAt). Relação: user User @relation(fields: [userId], references: [id], onDelete: Cascade) e messages ChatMessage[]. Índices: @@index([userId, createdAt]) e @@index([lastChatAt]).

    Modelo ChatMessage com campos: id (cuid), uploadedFileId (String), role (String), content (String @db.Text), createdAt (DateTime @default(now())). Relação: uploadedFile UploadedFile @relation(fields: [uploadedFileId], references: [id], onDelete: Cascade). Índice: @@index([uploadedFileId, createdAt]).

    No modelo User (topo do schema): adicionar campo uploadedFiles UploadedFile[] na lista de relações (após billingCheckouts BillingCheckouts[]).

    Em seguida executar o db push (BLOQUEANTE — Plano 04-02 não pode prosseguir sem isso):
    ```
    npx prisma db push
    ```
    Se aparecer prompt interativo sobre dados destrutivos: usar npx prisma db push --accept-data-loss.
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && grep -c "model UploadedFile" prisma/schema.prisma && grep -c "model ChatMessage" prisma/schema.prisma && grep -c "uploadedFiles" prisma/schema.prisma</automated>
    <automated>cd /home/rodrigo/tabelin.ia && node -e "const { PrismaClient } = require('./apps/web/node_modules/@prisma/client'); const p = new PrismaClient(); p.uploadedFile.count().then(n => { console.log('UploadedFile count:', n); p.\$disconnect(); }).catch(e => { console.error(e); p.\$disconnect(); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contém "model UploadedFile" e "model ChatMessage"
    - prisma/schema.prisma contém "uploadedFiles UploadedFile[]" no model User
    - npx prisma db push completa sem erro (exit 0)
    - A tabela UploadedFile existe no Postgres (count() retorna sem erro)
    - apps/web/node_modules/csv-parse existe
    - apps/web/node_modules/xlsx existe
    - apps/web/node_modules/node-cron existe
  </acceptance_criteria>
  <done>Dependências instaladas, modelos Prisma no schema, db push executado, tabelas acessíveis no Postgres.</done>
</task>

<task type="auto">
  <name>Task 2: Criar contratos Zod compartilhados e exportar de @tabelin/shared</name>
  <files>
    packages/shared/src/file-analysis/schema.ts
    packages/shared/src/file-analysis/fixtures.ts
    packages/shared/src/index.ts
  </files>
  <read_first>
    - packages/shared/src/scripts/schema.ts — padrão exato de Zod discriminatedUnion + type exports
    - packages/shared/src/scripts/fixtures.ts — padrão de fixture array tipado
    - packages/shared/src/index.ts — onde adicionar os novos re-exports
    - .planning/phases/04-spreadsheet-file-analysis/04-RESEARCH.md — seção "Code Examples" com os schemas Zod definidos
    - .planning/phases/04-spreadsheet-file-analysis/04-PATTERNS.md — seção "packages/shared/src/file-analysis/schema.ts"
  </read_first>
  <action>
    Criar packages/shared/src/file-analysis/schema.ts com os seguintes exports:

    fileSchemaColumnSchema: z.object com name (z.string()), type (z.enum(["numero", "data", "booleano", "texto"])), sampleValues (z.array(z.unknown())).

    fileSchemaSchema: z.object com columns (array de fileSchemaColumnSchema), sampleRows (z.array(z.record(z.unknown()))), rowCount (z.number()), sheetName (z.string().optional()), fileName (z.string()).

    uploadResponseSchema: z.discriminatedUnion("type", [
      z.object({ type: z.literal("sheet_selection"), sheetNames: z.array(z.string()) }),
      z.object({ type: z.literal("upload_complete"), uploadedFileId: z.string(), schema: fileSchemaSchema })
    ]).

    chatRequestSchema: z.object com uploadedFileId (z.string().cuid()), message (z.string().trim().min(1)).

    chatStreamEventSchema: z.discriminatedUnion("type", [
      { type: "delta", text: string },
      { type: "quota_warning", lastFreeUse: boolean },
      { type: "complete", content: string },
      { type: "error", message: string }
    ]).

    Exportar todos os tipos inferidos: FileSchemaColumn, FileSchema, UploadResponse, ChatRequest, ChatStreamEvent.

    Criar packages/shared/src/file-analysis/fixtures.ts exportando FILE_ANALYSIS_FIXTURES como UploadResponse[] com pelo menos um item type="upload_complete" com schema de exemplo (3 colunas: "Produto" texto, "Quantidade" numero, "Data" data; rowCount: 5; fileName: "vendas.csv").

    Em packages/shared/src/index.ts adicionar ao final:
    ```
    export * from "./file-analysis/schema";
    export * from "./file-analysis/fixtures";
    ```
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && node -e "const s = require('./packages/shared/src/file-analysis/schema'); console.log(Object.keys(s).join(', '))"</automated>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm --filter shared build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/file-analysis/schema.ts exporta fileSchemaSchema, uploadResponseSchema, chatRequestSchema, chatStreamEventSchema
    - packages/shared/src/file-analysis/fixtures.ts exporta FILE_ANALYSIS_FIXTURES com pelo menos 1 item
    - packages/shared/src/index.ts contém "export * from './file-analysis/schema'" e "export * from './file-analysis/fixtures'"
    - pnpm --filter shared build completa sem erro TypeScript
    - chatStreamEventSchema.parse({ type: "delta", text: "x" }) não lança exceção (validável via node -e)
  </acceptance_criteria>
  <done>Contratos Zod exportados de @tabelin/shared, build do pacote shared passa sem erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar file-parser.ts e file-repository.ts</name>
  <files>
    apps/web/src/server/file-analysis/file-parser.ts
    apps/web/src/server/file-analysis/file-repository.ts
  </files>
  <read_first>
    - .planning/phases/04-spreadsheet-file-analysis/04-RESEARCH.md — Pattern 2 (inferência de tipos), Pattern 5 (Pitfall 1: datas Excel como números, usar {cellDates:true}; Pitfall 2: separador `;` no CSV br)
    - .planning/phases/04-spreadsheet-file-analysis/04-PATTERNS.md — seção "file-repository.ts" com funções obrigatórias e IDOR guard pattern
    - apps/web/src/server/tools/tool-repository.ts — padrão de try/catch + console.warn, import prisma
    - packages/shared/src/file-analysis/schema.ts — tipo FileSchema que parseFile deve retornar
    - .planning/phases/04-spreadsheet-file-analysis/04-CONTEXT.md — D-04, D-05 (parse local, nunca enviar raw para AI)
  </read_first>
  <behavior>
    file-parser.ts:
    - parseFile(buffer: ArrayBuffer, mimeType: "csv" | "xlsx", sheetName?: string): FileSchema
    - CSV: detectar separador contando ocorrências de "," vs ";" na primeira linha; usar o mais frequente como delimiter em csv-parse/sync com {columns: true, skip_empty_lines: true, trim: true}
    - XLSX: usar XLSX.read(buffer, {type:"array", cellDates:true}), XLSX.utils.sheet_to_json com {defval: null}
    - inferType(samples): "numero" | "data" | "booleano" | "texto" — heurísticas: booleano (true/false/sim/nao/s/n/1/0), numero (parseFloat sem NaN), data (length>5 && !isNaN(Date.parse)), texto (fallback)
    - extractSchema(rows, fileName, sheetName?): tomar rows.slice(0,10) como sampleRows; inferType usando rows.slice(0,50) para cada coluna; retornar FileSchema válido
    - Limitar sheet_to_json a 1000 linhas (anti-DoS T-04-02-01)
    - Buffer raw descartado após parse — nenhum campo armazena o buffer (PRIV-02)

    file-repository.ts:
    - Todas as queries em UploadedFile incluem userId no where (IDOR guard T-04-01-01)
    - findUploadedFileByIdAndUser(id, userId): prisma.uploadedFile.findFirst({ where: { id, userId } })
    - createUploadedFile({ userId, fileName, fileSize, mimeType, schema, rowCount }): prisma.uploadedFile.create
    - updateLastChatAt(id, userId): prisma.uploadedFile.updateMany({ where: { id, userId }, data: { lastChatAt: new Date() } })
    - getRecentMessages(uploadedFileId, limit=10): prisma.chatMessage.findMany({ where: { uploadedFileId }, orderBy: { createdAt: "desc" }, take: limit }) — retorna em ordem cronológica para o AI (reverter array)
    - appendChatMessages(uploadedFileId, messages: {role:string; content:string}[]): prisma.chatMessage.createMany
  </behavior>
  <action>
    Criar apps/web/src/server/file-analysis/file-parser.ts com "import 'server-only'" na primeira linha. Importar parse de "csv-parse/sync" e XLSX de "xlsx". Implementar funções detectDelimiter, inferType, extractSchema, e parseFile conforme os comportamentos acima. Nenhum console.log de buffer ou conteúdo de célula (PRIV-02) — apenas console.warn de metadados se necessário.

    Criar apps/web/src/server/file-analysis/file-repository.ts com "import 'server-only'" na primeira linha. Importar prisma de "@/server/db/client". Implementar as cinco funções com IDOR guard em todas as queries. Usar try/catch no padrão do tool-repository.ts existente (console.warn + return null em erros de persistence não-críticos).

    Ambos os arquivos devem exportar suas funções como named exports (sem default export).
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && pnpm --filter web tsc --noEmit 2>&1 | grep -i "file-parser\|file-repository" | head -20</automated>
    <automated>cd /home/rodrigo/tabelin.ia && node -e "
      const { parse } = require('csv-parse/sync');
      const XLSX = require('xlsx');
      // Smoke test CSV parse
      const rows = parse('nome,valor\nAlice,100\nBob,200', { columns: true });
      console.log('CSV rows:', rows.length);
      // Smoke test XLSX
      const wb = XLSX.utils.book_new();
      const ws = XLSX.utils.aoa_to_sheet([['A','B'],[1,2]]);
      XLSX.utils.book_append_sheet(wb, ws, 'Sheet1');
      const buf = XLSX.write(wb, { type: 'array', bookType: 'xlsx' });
      const wb2 = XLSX.read(buf, { type: 'array', cellDates: true });
      console.log('XLSX sheets:', wb2.SheetNames);
    " 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/server/file-analysis/file-parser.ts contém "import 'server-only'"
    - apps/web/src/server/file-analysis/file-repository.ts contém "import 'server-only'"
    - file-repository.ts não contém "findFirst({ where: { id }" sem "userId" na mesma query (IDOR guard)
    - file-parser.ts contém "cellDates: true" (fix de datas Excel)
    - file-parser.ts não contém "console.log" (PRIV-02: sem log de conteúdo)
    - pnpm --filter web tsc --noEmit retorna sem erros nos dois novos arquivos
    - Smoke test CSV e XLSX no node passa sem exceção
  </acceptance_criteria>
  <done>file-parser.ts e file-repository.ts implementados, type-check passa, smoke tests de parse funcionam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Prisma schema → Postgres | Novos modelos com cascade delete; sem migração de dados existentes |
| Zod contracts → shared package | Contratos exportados para cliente e servidor; sem dados sensíveis nos tipos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Elevation of Privilege | file-repository.ts — queries UploadedFile | mitigate | Toda query inclui userId no where: prisma.uploadedFile.findFirst({ where: { id, userId } }); nunca buscar só por id |
| T-04-01-02 | Information Disclosure | file-parser.ts — buffer raw do arquivo | mitigate | Nenhum console.log de buffer, texto de célula, ou schema raw; apenas metadata (fileName, fileSize, rowCount) permitido em logs |
| T-04-01-03 | Tampering | file-parser.ts — XLSX com macros ou fórmulas injetadas | accept | xlsx@0.18.5 no modo read não executa macros; validação de extensão + MIME type feita no route handler (plano 04-02) antes de chamar parseFile |
| T-04-SC | Tampering | pnpm install — csv-parse, xlsx, node-cron | mitigate | Versões fixadas: csv-parse@6.2.1, xlsx@0.18.5, node-cron@3; verificar checksums do lock file |
</threat_model>

<verification>
Fase completa quando:
- prisma/schema.prisma contém UploadedFile e ChatMessage com @@index corretos
- npx prisma db push executou sem erro e tabelas existem no Postgres
- packages/shared exporta fileSchemaSchema, uploadResponseSchema, chatRequestSchema, chatStreamEventSchema
- file-parser.ts e file-repository.ts passam no type-check
- Nenhum console.log de conteúdo de arquivo em file-parser.ts
</verification>

<success_criteria>
- Três libs instaladas (csv-parse, xlsx, node-cron) + @types/node-cron em apps/web
- Modelos UploadedFile e ChatMessage no Postgres acessíveis via Prisma client
- @tabelin/shared exporta todos os Zod contracts da feature file-analysis
- parseFile(buffer, "csv") retorna FileSchema válido sem persistir o buffer
- Todas as queries de file-repository.ts incluem userId no where clause
</success_criteria>

<output>
Criar `.planning/phases/04-spreadsheet-file-analysis/04-01-SUMMARY.md` ao finalizar.
</output>
