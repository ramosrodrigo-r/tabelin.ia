---
phase: 16-tela-nica-fim-da-navega-o-multi-ferramenta
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/web/src/app/(workspace)/workspace/layout.tsx
  - apps/web/src/styles/globals.css
  - apps/web/src/components/app/workspace-split.tsx
  - apps/web/src/components/app/sidebar.tsx
  - apps/web/src/components/app/tool-nav.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, CLEAN-05]

must_haves:
  truths:
    - "Usuário autenticado em /workspace vê a TableGridPanel (planilha-amostra) no espaço principal e o UnifiedChatTool ao lado, sem navegar"
    - "Em telas largas (>=901px), a planilha ocupa ~70% e o chat ~30%, lado a lado"
    - "Em telas estreitas (<900px), um toggle Planilha/Chat alterna qual painel é visível, sem desmontar nenhum dos dois (preserva estado de edição/undo)"
    - "Sidebar e ToolNav não são renderizados em nenhuma tela; nenhum componente de navegação multi-ferramenta é importado pelo layout"
  artifacts:
    - path: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      provides: "WorkspaceLayout reescrito: Topbar enxuta + split TableGridPanel/UnifiedChatTool, sem Sidebar"
    - path: "apps/web/src/components/app/workspace-split.tsx"
      provides: "Client component com toggle Planilha/Chat (estado local, display:none, sem desmontar)"
    - path: "apps/web/src/styles/globals.css"
      provides: ".workspace-grid-panel/.workspace-chat-panel/.workspace-mobile-toggle; remoção de .workspace-content/.workspace-center/.sidebar*"
  key_links:
    - from: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      to: "apps/web/src/features/unified-chat/lib/sample-spec.ts"
      via: "import SAMPLE_SPEC, prop spec da TableGridPanel"
      pattern: "spec={SAMPLE_SPEC}"
    - from: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      to: "apps/web/src/components/app/workspace-split.tsx"
      via: "import WorkspaceSplit, composição grid+chat"
      pattern: "<WorkspaceSplit"
    - from: "apps/web/src/components/app/workspace-split.tsx"
      to: "apps/web/src/styles/globals.css"
      via: "classNames .workspace-grid-panel/.workspace-chat-panel/.workspace-mobile-toggle"
      pattern: "workspace-grid-panel"
---

<objective>
Reescrever o `WorkspaceLayout` (`/workspace`) para a tela única: remover a `Sidebar` (CLEAN-05) e montar um split lado-a-lado com a `TableGridPanel` persistente (planilha-amostra `SAMPLE_SPEC`, ~70%) no espaço principal e o `UnifiedChatTool` (`children` da page, ~30%) ao lado (D-01/D-02/D-04). Em telas estreitas (<900px), um toggle "Planilha"/"Chat" alterna a visibilidade dos dois painéis via `display:none` sem desmontá-los (D-03). Atualizar `globals.css` com as novas classes de split/toggle e remover o CSS órfão de `.sidebar*`/`.workspace-content`/`.workspace-center` e (se removido) `ToolNav`. Remover os componentes `Sidebar` e `ToolNav` (já órfão) do código.

Purpose: Cumprir SHELL-01 (planilha viva no espaço principal + chat ao lado, sem navegação) e CLEAN-05 (navegação multi-ferramenta removida da UI), completando a parte de layout/CSS desta fase. Depende da Plan 01 apenas pelo `SAMPLE_SPEC` (import) e pela Topbar já enxuta (sem `useWorkspaceToolKind`).
Output: `layout.tsx` reescrito com split + Topbar enxuta; novo `workspace-split.tsx` (client component, toggle mobile); `globals.css` atualizado; `sidebar.tsx`/`tool-nav.tsx` removidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-CONTEXT.md
@.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-RESEARCH.md
@.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-PATTERNS.md
@.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-UI-SPEC.md
@.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-01-SUMMARY.md

<interfaces>
<!-- Estado atual de layout.tsx (a reescrever in place) -->
import { redirect } from "next/navigation";
import { Sidebar } from "@/components/app/sidebar";
import { Topbar } from "@/components/app/topbar";
import { WorkspaceShell } from "@/components/app/workspace-shell";
import { getCachedEntitlement, getCachedUser } from "@/server/request-cache";
import { getSupportLinks } from "@/server/support/support-config";

export default async function WorkspaceLayout({ children }: { children: React.ReactNode }) {
  const user = await getCachedUser();
  if (!user) redirect("/sign-in");
  const entitlement = await getCachedEntitlement(user.id);
  const supportLinks = getSupportLinks();
  return (
    <WorkspaceShell>
      <div className="workspace-page">
        <Topbar user={user} entitlement={entitlement} supportLinks={supportLinks} />
        <div className="workspace-body">
          <Sidebar />
          <main className="workspace-content">
            <div className="workspace-center">{children}</div>
          </main>
        </div>
      </div>
    </WorkspaceShell>
  );
}

<!-- TableGridPanel signature (apps/web/src/features/unified-chat/components/table-grid-panel.tsx linha 117) -->
export function TableGridPanel({ spec }: { spec: TableSpecPayload }) { ... }
<!-- "use client" component — pode ser renderizado dentro de server component (layout.tsx) sem problema -->

<!-- SAMPLE_SPEC (criado pela Plan 01) -->
import { SAMPLE_SPEC } from "@/features/unified-chat/lib/sample-spec";
<!-- export const SAMPLE_SPEC: TableSpecPayload = { kind: "table_spec", title: "Controle de Gastos", ... } -->

<!-- page.tsx atual — NÃO EDITAR (continua retornando UnifiedChatTool, vira children) -->
import { UnifiedChatTool } from "@/features/unified-chat/unified-chat-tool";
import { getCachedEntitlement, getCachedUser } from "@/server/request-cache";

export default async function WorkspacePage() {
  const user = await getCachedUser();
  const entitlement = await getCachedEntitlement(user!.id);
  return <UnifiedChatTool entitlement={entitlement} />;
}

<!-- WorkspaceShell (inalterado, envolve toda a árvore) -->
export function WorkspaceShell({ children }: { children: React.ReactNode }) {
  return <WorkspaceConversationProvider>{children}</WorkspaceConversationProvider>;
}

<!-- CSS atual relevante (apps/web/src/styles/globals.css) -->
<!-- linhas 251-274: .workspace-page (manter), .workspace-body (manter), .workspace-content (remover), .workspace-center (remover) -->
<!-- linhas 278-338: .sidebar, .sidebar-brand, .sidebar-brand strong, .sidebar-brand span, .sidebar-nav, .sidebar-nav .nav-item(:hover/[data-active]/[data-disabled]) (remover bloco inteiro) -->
<!-- linhas 502-537: .tool-nav, .tool-pill (e variantes :hover/[data-active]) — TOP-LEVEL, usado por intent-pill.tsx (.tsx linha 108: className="tool-pill intent-pill-trigger") — NÃO REMOVER -->
<!-- linhas 712-724: .chat-input-bottom-nav, .chat-input-bottom-nav .tool-nav, .chat-input-bottom-nav .tool-pill — CSS morto (ToolNav nunca passado a bottomNav) — remover SE ToolNav for removido nesta plan -->
<!-- linhas 945-966: @media (max-width: 600px) { .workspace-content {...}; .sidebar { display: none; }; .topbar {...}; .tool-nav {...}; .tool-pill {...} } -->
<!--   -> remover apenas a entrada .workspace-content e .sidebar deste bloco; manter .topbar/.tool-nav/.tool-pill (top-level, ainda em uso) -->

<!-- ToolNav atual (apps/web/src/components/app/tool-nav.tsx) — já confirmado ZERO imports em qualquer .tsx/.ts -->
<!-- chat-input.tsx tem prop bottomNav (linhas 18,33,81) que nunca recebe valor — NÃO EDITAR chat-input.tsx nesta fase (Phase 18/22) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar WorkspaceSplit (toggle responsivo grade/chat, client component)</name>
  <files>apps/web/src/components/app/workspace-split.tsx</files>
  <read_first>
    apps/web/src/components/app/sidebar.tsx
    apps/web/src/styles/globals.css (linhas 313-332, padrão data-active de .sidebar-nav .nav-item e .tool-pill[data-active])
    .planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-RESEARCH.md (Pattern 2 — split panel com toggle responsivo)
    .planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-UI-SPEC.md (Mobile / telas estreitas, Copywriting Contract)
  </read_first>
  <action>
    Criar `apps/web/src/components/app/workspace-split.tsx` como client component (`"use client"`) exportando `WorkspaceSplit({ grid, chat }: { grid: React.ReactNode; chat: React.ReactNode })`. Estado local `useState<"grid" | "chat">("grid")` para `activeTab`. Renderiza:
    - `<div className="workspace-mobile-toggle">` com dois `<button type="button">`: "Planilha" (`onClick` seta `activeTab` para `"grid"`, `data-active={activeTab === "grid"}`) e "Chat" (`onClick` seta `"chat"`, `data-active={activeTab === "chat"}`) — copy exata conforme UI-SPEC Copywriting Contract ("Planilha" / "Chat").
    - `<div className="workspace-grid-panel" data-hidden={activeTab === "chat"}>{grid}</div>`
    - `<div className="workspace-chat-panel" data-hidden={activeTab === "grid"}>{chat}</div>`
    Usar `data-hidden` (não `style={{display:...}}` inline) — consistente com o padrão `data-active` já usado em `.sidebar-nav .nav-item[data-active="true"]`/`.tool-pill[data-active="true"]` (Pattern do PATTERNS.md "data-active para estado de UI"). O CSS da Task 3 define `[data-hidden="true"] { display: none; }` dentro do media query mobile — em desktop `data-hidden` é ignorado (ambos os painéis visíveis). Não desmontar `grid`/`chat` condicionalmente — ambos sempre renderizados (preserva estado do grid/chat ao alternar, conforme RESEARCH.md anti-pattern).
  </action>
  <acceptance_criteria>
    - Arquivo `apps/web/src/components/app/workspace-split.tsx` existe, primeira linha `"use client";`
    - `grep -c "export function WorkspaceSplit" apps/web/src/components/app/workspace-split.tsx` retorna 1
    - `grep -c "useState" apps/web/src/components/app/workspace-split.tsx` retorna 1
    - `grep -c "workspace-mobile-toggle" apps/web/src/components/app/workspace-split.tsx` retorna 1
    - `grep -c "workspace-grid-panel" apps/web/src/components/app/workspace-split.tsx` retorna 1
    - `grep -c "workspace-chat-panel" apps/web/src/components/app/workspace-split.tsx` retorna 1
    - `grep -c "data-hidden" apps/web/src/components/app/workspace-split.tsx` retorna 2
    - `grep -c "Planilha" apps/web/src/components/app/workspace-split.tsx` retorna 1 e `grep -c "Chat" apps/web/src/components/app/workspace-split.tsx` retorna >= 1
    - `pnpm exec tsc --noEmit` (em apps/web) sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>WorkspaceSplit renderiza toggle Planilha/Chat + dois painéis sempre montados com data-hidden controlado por estado local; typecheck verde</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescrever WorkspaceLayout (split TableGridPanel + UnifiedChatTool, sem Sidebar)</name>
  <files>
    apps/web/src/app/(workspace)/workspace/layout.tsx
    apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx
  </files>
  <read_first>
    apps/web/src/app/(workspace)/workspace/layout.tsx
    apps/web/src/app/(workspace)/workspace/page.tsx
    apps/web/src/components/app/workspace-shell.tsx
    apps/web/src/features/unified-chat/components/table-grid-panel.tsx
    .planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-01-SUMMARY.md
  </read_first>
  <behavior>
    - Teste 1: `WorkspaceLayout` (renderizado com `children` mock, ex.: `<div data-testid="chat-child">chat</div>`, e mocks de `getCachedUser`/`getCachedEntitlement`/`getSupportLinks`) renderiza simultaneamente um elemento contendo o título "Controle de Gastos" (da `TableGridPanel` com `SAMPLE_SPEC`) E o `data-testid="chat-child"` no mesmo DOM — confirma SHELL-01 (planilha + chat presentes sem navegação).
    - Teste 2: o DOM renderizado NÃO contém `aria-label="Ferramentas"` (atributo do `Sidebar` removido) — confirma CLEAN-05.
    - Teste 3: o DOM contém um elemento com `className` incluindo `workspace-grid-panel` e outro com `workspace-chat-panel`.
    - Se `getCachedUser` mockado retornar `null`, `redirect("/sign-in")` é chamado (preserva guard de auth existente).
  </behavior>
  <action>
    Criar teste em `apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx` (mockar `next/navigation` `redirect`, `@/server/request-cache` `getCachedUser`/`getCachedEntitlement`, `@/server/support/support-config` `getSupportLinks`, seguindo padrão de mocks já usado em testes de outros server components do projeto — verificar um teste existente de layout/page server component para o padrão de mock de `next/navigation`/`@/server/request-cache` antes de escrever). Escrever os 4 casos do bloco `<behavior>`, rodar e confirmar RED (falha porque `layout.tsx` ainda monta `Sidebar`/`workspace-content`).

    Em seguida (GREEN), reescrever `apps/web/src/app/(workspace)/workspace/layout.tsx`:
    1. Remover `import { Sidebar } from "@/components/app/sidebar";` e `<Sidebar />`.
    2. Adicionar imports: `TableGridPanel` de `@/features/unified-chat/components/table-grid-panel`, `SAMPLE_SPEC` de `@/features/unified-chat/lib/sample-spec`, `WorkspaceSplit` de `@/components/app/workspace-split`.
    3. Manter inalterados: `redirect("/sign-in")` guard, `getCachedUser`/`getCachedEntitlement`/`getSupportLinks`, `<WorkspaceShell>`, `<div className="workspace-page">`, `<Topbar user={user} entitlement={entitlement} supportLinks={supportLinks}/>` como primeiro filho.
    4. Substituir o bloco `<div className="workspace-body"><Sidebar /><main className="workspace-content"><div className="workspace-center">{children}</div></main></div>` por `<WorkspaceSplit grid={<TableGridPanel spec={SAMPLE_SPEC} />} chat={children} />` dentro de `<div className="workspace-body">` (ou `WorkspaceSplit` já renderiza seu próprio `.workspace-body` internamente — escolher uma abordagem e manter consistência com o CSS da Task 3; recomendado: `WorkspaceSplit` recebe `grid`/`chat` como props e renderiza `.workspace-mobile-toggle` + `.workspace-grid-panel` + `.workspace-chat-panel` dentro do `.workspace-body` já existente no `layout.tsx`).

    Rodar o teste novamente e confirmar GREEN.
  </action>
  <acceptance_criteria>
    - `apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx` existe com pelo menos 4 casos de teste (RED antes, GREEN depois)
    - `grep -c "Sidebar" apps/web/src/app/(workspace)/workspace/layout.tsx` retorna 0
    - `grep -c "workspace-content\|workspace-center" apps/web/src/app/(workspace)/workspace/layout.tsx` retorna 0
    - `grep -c "TableGridPanel" apps/web/src/app/(workspace)/workspace/layout.tsx` retorna 1
    - `grep -c "SAMPLE_SPEC" apps/web/src/app/(workspace)/workspace/layout.tsx` retorna 1
    - `grep -c "WorkspaceSplit" apps/web/src/app/(workspace)/workspace/layout.tsx` retorna 1
    - `grep -c 'redirect("/sign-in")' apps/web/src/app/(workspace)/workspace/layout.tsx` retorna 1 (guard preservado)
    - `pnpm --filter web test -- layout.test.tsx` passa (todos os casos GREEN)
    - `pnpm exec tsc --noEmit` (em apps/web) sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm exec vitest run src/app/\(workspace\)/workspace/__tests__/layout.test.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>layout.tsx monta TableGridPanel(SAMPLE_SPEC) + children(UnifiedChatTool) via WorkspaceSplit, sem Sidebar; teste novo cobre presença simultânea de ambos no DOM e ausência de aria-label="Ferramentas"; guard de auth preservado; typecheck e teste verdes</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar CSS do shell (split panels, toggle mobile, remover sidebar/workspace-content/center) e remover Sidebar/ToolNav órfãos</name>
  <files>
    apps/web/src/styles/globals.css
    apps/web/src/components/app/sidebar.tsx
    apps/web/src/components/app/tool-nav.tsx
  </files>
  <read_first>
    apps/web/src/styles/globals.css
    apps/web/src/components/app/sidebar.tsx
    apps/web/src/components/app/tool-nav.tsx
    apps/web/src/features/unified-chat/components/intent-pill.tsx
    .planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-RESEARCH.md (Pattern 2 — CSS completo do split)
    .planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-UI-SPEC.md (Layout Contract, Spacing Scale, Typography)
  </read_first>
  <action>
    Em `apps/web/src/styles/globals.css`:
    1. Na seção `/* ── Workspace layout ── */` (linhas ~249-274): manter `.workspace-page` e `.workspace-body` como estão (`display: flex`). Remover `.workspace-content` e `.workspace-center` por completo (confirmados sem uso fora de `layout.tsx`, que está sendo reescrito na Task 2).
    2. Adicionar logo após `.workspace-body`: `.workspace-grid-panel { flex: 7; min-width: 0; overflow: auto; padding: 16px; }`, `.workspace-chat-panel { flex: 3; border-left: 1px solid var(--border); background: var(--surface); overflow: auto; padding: 16px; }`, e `.workspace-mobile-toggle { display: none; }`.
    3. No bloco `@media (max-width: 900px)` (NOVO — criar após o bloco `@media (max-width: 600px)` existente, ou adjacente): `.workspace-body { flex-direction: column; }`; `.workspace-mobile-toggle { display: flex; border-bottom: 1px solid var(--border); background: var(--surface); }`; `.workspace-mobile-toggle button { flex: 1; min-height: 44px; border: none; background: none; color: var(--muted); font-weight: 400; }`; `.workspace-mobile-toggle button[data-active="true"] { color: var(--primary); font-weight: 700; border-bottom: 2px solid var(--primary); }`; `.workspace-grid-panel[data-hidden="true"], .workspace-chat-panel[data-hidden="true"] { display: none; }`; `.workspace-grid-panel, .workspace-chat-panel { flex: none; width: 100%; border-left: none; }`. Usar pesos 400/700 apenas (UI-SPEC Typography — consolidar, não usar 600).
    4. Remover a seção `/* ── Sidebar ── */` inteira (linhas ~276-338: `.sidebar`, `.sidebar-brand`, `.sidebar-brand strong`, `.sidebar-brand span`, `.sidebar-nav`, `.sidebar-nav .nav-item` e variantes `:hover`/`[data-active]`/`[data-disabled]`).
    5. No bloco `@media (max-width: 600px)` existente (linhas ~945-966): remover a entrada `.sidebar { display: none; }` e a entrada `.workspace-content { padding: ... }`. MANTER `.topbar { padding: 0 16px; }`, `.tool-nav { gap: 4px; }` e `.tool-pill { font-size: 11px; padding: 4px 10px; }` — estes últimos são top-level (linhas ~504-537) e usados por `intent-pill.tsx` (`className="tool-pill intent-pill-trigger"`), portanto NÃO remover `.tool-nav`/`.tool-pill` top-level nem suas referências no media query 600px.
    6. Remover o bloco `.chat-input-bottom-nav .tool-nav { gap: 4px; }` e `.chat-input-bottom-nav .tool-pill { font-size: 11px; padding: 4px 8px; }` (linhas ~717-724) — CSS morto associado ao `ToolNav` removido nesta task (mantendo `.chat-input-bottom-nav { padding: ...; border-top: ...; }` em si, pois `bottomNav` prop de `chat-input.tsx` permanece — Phase 18/22).

    Em seguida, remover os componentes de navegação órfãos:
    - Deletar `apps/web/src/components/app/sidebar.tsx` (após confirmar zero imports restantes — `layout.tsx` já não importa `Sidebar` após a Task 2).
    - Deletar `apps/web/src/components/app/tool-nav.tsx` (já confirmado zero imports em qualquer `.tsx`/`.ts` pelo RESEARCH.md).
  </action>
  <acceptance_criteria>
    - `grep -c "\.workspace-content\|\.workspace-center" apps/web/src/styles/globals.css` retorna 0
    - `grep -c "^\.sidebar\b\|\.sidebar-brand\|\.sidebar-nav" apps/web/src/styles/globals.css` retorna 0
    - `grep -c "workspace-grid-panel" apps/web/src/styles/globals.css` >= 1 (regra base) e está presente também dentro de `@media (max-width: 900px)`
    - `grep -c "workspace-chat-panel" apps/web/src/styles/globals.css` >= 1
    - `grep -c "workspace-mobile-toggle" apps/web/src/styles/globals.css` >= 1
    - `grep -c "@media (max-width: 900px)" apps/web/src/styles/globals.css` retorna 1
    - `grep -c "\.tool-pill\b" apps/web/src/styles/globals.css` ainda >= 2 (top-level + media query 600px preservados)
    - `grep -c "chat-input-bottom-nav .tool-nav\|chat-input-bottom-nav .tool-pill" apps/web/src/styles/globals.css` retorna 0
    - `test -f apps/web/src/components/app/sidebar.tsx` retorna falso (arquivo removido)
    - `test -f apps/web/src/components/app/tool-nav.tsx` retorna falso (arquivo removido)
    - `grep -rn "from \"@/components/app/sidebar\"\|from \"./sidebar\"\|from \"@/components/app/tool-nav\"" apps/web/src` retorna vazio
    - `pnpm exec tsc --noEmit` (em apps/web) sai com código 0
    - `pnpm -r test` permanece verde
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit && cd /home/rodrigo/tabelin.ia && pnpm -r test</automated>
  </verify>
  <done>CSS do shell atualizado com split panels + toggle mobile responsivo (900px breakpoint), .sidebar*/.workspace-content/.workspace-center removidos sem regressão visual de .topbar-brand/.tool-pill; sidebar.tsx e tool-nav.tsx deletados sem imports pendentes; typecheck e suíte completa verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /workspace (layout) | Usuário autenticado; layout monta TableGridPanel (client, edição local) + UnifiedChatTool (client, chat) |
| Browser (toggle mobile) → DOM | Estado de UI puro (`activeTab`), sem payload de rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | `WorkspaceSplit` toggle (`activeTab` state) | accept | Estado client-side puro (`useState`), não persiste, não trafega para o servidor — sem superfície de ataque nova |
| T-16-05 | Information Disclosure | `TableGridPanel` com `SAMPLE_SPEC` montada sempre, mesmo se usuário só usar o chat | accept | `SAMPLE_SPEC` é dado estático/público (planilha-amostra "Controle de Gastos"), sem dados de usuário — sem persistência (D-06) |
| T-16-06 | Elevation of Privilege | Guard `redirect("/sign-in")` em `layout.tsx` | mitigate | Preservado exatamente como estava (Task 2 não remove nem reordena o guard); coberto pelo teste novo de `layout.test.tsx` (caso "getCachedUser null -> redirect") |
| T-16-07 | Tampering (CSS regression) | Remoção de `.sidebar*`/`.workspace-content`/`.workspace-center` | mitigate | Verificação por grep confirma zero referências residuais antes da remoção; `.topbar-brand` (marca) já tem valores próprios — não depende de `.sidebar-brand*` (Pitfall 4 do RESEARCH.md já mapeado: nenhuma cópia necessária para a marca) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova instalada neste plan |
</threat_model>

<verification>
- `cd apps/web && pnpm exec vitest run src/app/\(workspace\)/workspace/__tests__/layout.test.tsx` passa (Task 2)
- `cd apps/web && pnpm exec tsc --noEmit` sai com código 0 após cada task
- `pnpm -r test` (raiz do monorepo) verde ao final do plano (Task 3 verify)
- Verificação manual (cobre toda a fase, não bloqueante para este plano):
  1. `pnpm --filter web dev`, visitar `/workspace` autenticado: confirmar `TableGridPanel` ("Controle de Gastos") visível à esquerda (~70%) e `UnifiedChatTool` à direita (~30%), sem Sidebar
  2. Reduzir a largura da janela abaixo de 900px: confirmar toggle "Planilha"/"Chat" aparece, alterna painéis sem perder estado (editar uma célula, trocar de aba, voltar — edição permanece)
  3. Reiniciar `next dev` (mudança em `next.config.ts` da Plan 01) e rodar `curl -I http://localhost:3000/workspace/sql` — esperar `HTTP/1.1 308` e `location: /workspace`
</verification>

<success_criteria>
- `/workspace` renderiza `TableGridPanel(SAMPLE_SPEC)` (espaço principal, ~70%) e `UnifiedChatTool` (lateral, ~30%) simultaneamente, sem Sidebar/ToolNav (SHELL-01, CLEAN-05)
- Em <900px, toggle "Planilha"/"Chat" alterna painéis via `data-hidden`/CSS, preservando estado de ambos (D-03)
- `Sidebar`/`ToolNav` removidos do código e do CSS sem imports pendentes (CLEAN-05, SHELL-02 — zero referência de UI)
- `pnpm exec tsc --noEmit` (apps/web) e `pnpm -r test` permanecem verdes
- Nota de sequenciamento (CONTEXT.md): endpoints `/api/tools/*` permanecem órfãos intencionalmente — não é gap desta fase, é Phase 18 (CLEAN-01/02/03/06)
</success_criteria>

<output>
Create `.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-02-SUMMARY.md` when done
</output>
