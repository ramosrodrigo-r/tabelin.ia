---
phase: 16-tela-nica-fim-da-navega-o-multi-ferramenta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/next.config.ts
  - apps/web/src/components/app/topbar.tsx
  - apps/web/src/features/unified-chat/lib/sample-spec.ts
autonomous: true
requirements: [SHELL-02, SHELL-03]

must_haves:
  truths:
    - "Visitar /workspace/sql, /workspace/regex, /workspace/scripts, /workspace/templates, /workspace/file-analysis ou /workspace/ocr redireciona (308) para /workspace"
    - "A Topbar exibe sessão do usuário, ação Sair e um link para /privacidade, sem detectar toolKind pela URL"
    - "Existe um spec estático (SAMPLE_SPEC) pronto para alimentar a TableGridPanel persistente da Plan 02"
  artifacts:
    - path: "apps/web/next.config.ts"
      provides: "redirects() com 6 entradas /workspace/{sql,regex,scripts,templates,file-analysis,ocr} -> /workspace, permanent: true"
    - path: "apps/web/src/components/app/topbar.tsx"
      provides: "Topbar enxuta sem useWorkspaceToolKind/usePathname, com link /privacidade"
    - path: "apps/web/src/features/unified-chat/lib/sample-spec.ts"
      provides: "SAMPLE_SPEC: TableSpecPayload (planilha-amostra Controle de Gastos, pt-BR)"
      exports: ["SAMPLE_SPEC"]
  key_links:
    - from: "apps/web/src/components/app/topbar.tsx"
      to: "/privacidade"
      via: "anchor href"
      pattern: "href=\"/privacidade\""
    - from: "apps/web/next.config.ts"
      to: "/workspace"
      via: "redirects() destination"
      pattern: "destination: \"/workspace\""
---

<objective>
Preparar a base não-visual da tela única: (1) configurar os redirects 308 das 6 rotas antigas de página de tool para `/workspace` (D-07/D-08, sem editar os `page.tsx`), (2) enxugar a `Topbar` removendo a detecção de `toolKind` por rota (código morto após o corte de navegação) e adicionando o link `/privacidade` exigido por SHELL-03/D-11, e (3) extrair a planilha-amostra estática (`SAMPLE_SPEC`) que a Plan 02 vai injetar na `TableGridPanel` persistente do painel principal (D-04).

Purpose: Esta fase é shell/layout + corte de navegação (SHELL-01/02/03, CLEAN-05). Este plan cobre a parte de roteamento/topbar/dados que não depende da reescrita do layout — pode ser executado em paralelo com a Plan 02 ser planejada, e a Plan 02 consome `SAMPLE_SPEC` na Wave 2.
Output: `next.config.ts` com `redirects()`; `topbar.tsx` enxuta com link de privacidade; novo arquivo `sample-spec.ts` exportando `SAMPLE_SPEC`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-CONTEXT.md
@.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-RESEARCH.md
@.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-PATTERNS.md
@.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-UI-SPEC.md

<interfaces>
<!-- TableSpecPayload shape (packages/shared/src/unified-chat/schema.ts) — usado pelo sample-spec.ts -->
<!-- Confirmado em apps/web/src/server/ai/table-clarifier.ts (fixture buildTableSpec, linhas ~266-291): -->

```typescript
export const SAMPLE_SPEC: TableSpecPayload = {
  kind: "table_spec",
  title: "Controle de Gastos",
  columns: [
    { name: "Descrição", type: "text", key: "descricao" },
    { name: "Categoria", type: "text", key: "categoria" },
    { name: "Valor (R$)", type: "currency", key: "valor" },
    { name: "Desconto", type: "currency", key: "desconto" },
    { name: "Total", type: "formula", key: "total", formula: "=SOMA(C{row};-D{row})" },
  ],
  rowCount: 5,
  rows: [
    { descricao: "Aluguel", categoria: "Moradia", valor: 2000, desconto: 100 },
    { descricao: "Supermercado", categoria: "Alimentação", valor: 800, desconto: 50 },
    { descricao: "Internet", categoria: "Serviços", valor: 150, desconto: 0 },
    { descricao: "Academia", categoria: "Saúde", valor: 120, desconto: 20 },
    { descricao: "Netflix", categoria: "Lazer", valor: 55, desconto: 5 },
  ],
  formulaLanguage: "pt-BR",
  separator: ";",
};
```

Import pattern (mesmo de table-clarifier.ts linha 6): `import type { TableSpecPayload } from "@tabelin/shared";`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configurar redirects 308 das rotas antigas de tool</name>
  <files>apps/web/next.config.ts</files>
  <read_first>
    apps/web/next.config.ts
    .planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-RESEARCH.md (Pattern 1 — redirects centralizado)
  </read_first>
  <action>
    Editar `apps/web/next.config.ts` adicionando um método assíncrono `redirects()` ao objeto `nextConfig` exportado, retornando um array de 6 entradas `{ source, destination: "/workspace", permanent: true }` para `source` igual a `/workspace/sql`, `/workspace/regex`, `/workspace/scripts`, `/workspace/templates`, `/workspace/file-analysis` e `/workspace/ocr` — match exato, sem `:path*`/wildcard (D-07). Manter `reactStrictMode: true` existente. Não editar nenhum arquivo `page.tsx` das 6 rotas (D-08) — `redirects()` roda antes do filesystem router e intercepta sem precisar de código nas páginas.
  </action>
  <acceptance_criteria>
    - `apps/web/next.config.ts` exporta `nextConfig` com `async redirects()` retornando array com 6 objetos
    - Cada objeto tem `source` começando em `/workspace/` (sql, regex, scripts, templates, file-analysis, ocr), `destination: "/workspace"`, `permanent: true`
    - `grep -c "permanent: true" apps/web/next.config.ts` retorna 6
    - Nenhum arquivo em `apps/web/src/app/(workspace)/workspace/{sql,regex,scripts,templates,file-analysis,ocr}/page.tsx` foi modificado (`git status` não lista esses arquivos)
    - `pnpm exec tsc --noEmit` (em apps/web) sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>next.config.ts contém redirects() com as 6 entradas corretas; typecheck verde; nenhum page.tsx de tool tocado</done>
</task>

<task type="auto">
  <name>Task 2: Enxugar Topbar (remover detecção de rota, adicionar link /privacidade)</name>
  <files>apps/web/src/components/app/topbar.tsx</files>
  <read_first>
    apps/web/src/components/app/topbar.tsx
    .planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-PATTERNS.md (seção topbar.tsx)
    .planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-UI-SPEC.md (Topbar enxuta D-10/D-11/D-12)
  </read_first>
  <action>
    Editar `apps/web/src/components/app/topbar.tsx`:
    1. Remover a função `useWorkspaceToolKind()` (linhas 12-25) por completo — código morto após o corte de rotas de tool (só `/workspace` é alcançável).
    2. Substituir as linhas que derivam `toolKind` (`const toolKindFromPath = useWorkspaceToolKind(); const toolKind = toolKindProp ?? toolKindFromPath;`) por `const toolKind = toolKindProp ?? "unified";` (Pattern 4, opção A do RESEARCH.md) — preserva o fluxo "Nova conversa"/"Apagar histórico" que usa `toolKind` em `handleDeleteHistory` (`DELETE /api/conversations/${toolKind}`) e na copy `deleteCopy`.
    3. Atualizar o import de `"next/navigation"`: remover `usePathname` (não usado mais), manter `useRouter` (usado em `signOut`).
    4. Adicionar um link `<a href="/privacidade" className="ghost-button">Privacidade</a>` dentro de `<div className="topbar-actions">`, posicionado antes do `account-menu-container` final (antes do menu de conta do usuário, linha ~150) — reutiliza a classe `.ghost-button` já existente, sem CSS novo.
    5. Manter inalterado: `<strong className="topbar-brand">Tabelin.IA</strong>` (D-12 já cumprido — marca já está na Topbar), `isPro`/`pro-badge`, `signOut`, `handleDeleteHistory`, popover "Nova conversa", menu de conta/suporte Pro, `useInvokeNewConversation`.
  </action>
  <acceptance_criteria>
    - `grep -c "useWorkspaceToolKind" apps/web/src/components/app/topbar.tsx` retorna 0
    - `grep -c "usePathname" apps/web/src/components/app/topbar.tsx` retorna 0
    - `grep -n "useRouter" apps/web/src/components/app/topbar.tsx` ainda presente (import e uso em signOut)
    - `grep -c 'const toolKind = toolKindProp ?? "unified"' apps/web/src/components/app/topbar.tsx` retorna 1
    - `grep -c 'href="/privacidade"' apps/web/src/components/app/topbar.tsx` retorna 1
    - `grep -c 'topbar-brand' apps/web/src/components/app/topbar.tsx` retorna 1 (marca preservada)
    - `pnpm exec tsc --noEmit` (em apps/web) sai com código 0
    - `pnpm -r test -- topbar` não falha (se não houver teste de topbar, nenhum teste quebra)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit && pnpm exec vitest run --reporter=basic -t topbar 2>/dev/null; pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Topbar não deriva mais toolKind de usePathname/regex; toolKind fixo "unified" preserva fluxo de Nova conversa/Apagar histórico; link /privacidade visível em topbar-actions; typecheck verde</done>
</task>

<task type="auto">
  <name>Task 3: Extrair SAMPLE_SPEC (planilha-amostra estática) para o painel principal</name>
  <files>apps/web/src/features/unified-chat/lib/sample-spec.ts</files>
  <read_first>
    apps/web/src/server/ai/table-clarifier.ts
    apps/web/src/features/unified-chat/components/table-grid-panel.tsx
    .planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-PATTERNS.md (seção sample-spec.ts)
  </read_first>
  <action>
    Criar `apps/web/src/features/unified-chat/lib/sample-spec.ts` exportando uma constante `SAMPLE_SPEC: TableSpecPayload` (import type de `@tabelin/shared`, mesmo padrão de `apps/web/src/server/ai/table-clarifier.ts` linha 6). O conteúdo é o objeto literal "Controle de Gastos" já usado na fixture `buildTableSpec` de `table-clarifier.ts` (linhas ~266-291): `kind: "table_spec"`, `title: "Controle de Gastos"`, 5 colunas (Descrição/Categoria/Valor (R$)/Desconto/Total — a última é `type: "formula"` com `formula: "=SOMA(C{row};-D{row})"`), `rowCount: 5`, 5 linhas de dados (Aluguel/Supermercado/Internet/Academia/Netflix), `formulaLanguage: "pt-BR"`, `separator: ";"`. Adicionar um comentário JSDoc curto explicando que é a planilha-amostra estática do painel principal da tela única (D-04), sem persistência (D-06), reaproveitando a fixture pt-BR de `table-clarifier.ts`. Este arquivo será consumido pela Plan 02 via `<TableGridPanel spec={SAMPLE_SPEC} />` — `TableGridPanel` recebe `spec: TableSpecPayload` (não existe prop `initialColumns` direta, conforme `table-grid-panel.tsx` linha 117).
  </action>
  <acceptance_criteria>
    - Arquivo `apps/web/src/features/unified-chat/lib/sample-spec.ts` existe e exporta `SAMPLE_SPEC`
    - `grep -c "export const SAMPLE_SPEC" apps/web/src/features/unified-chat/lib/sample-spec.ts` retorna 1
    - `grep -c '"table_spec"' apps/web/src/features/unified-chat/lib/sample-spec.ts` retorna 1
    - `grep -c "formulaLanguage" apps/web/src/features/unified-chat/lib/sample-spec.ts` retorna 1
    - `grep -c "import type { TableSpecPayload }" apps/web/src/features/unified-chat/lib/sample-spec.ts` retorna 1
    - `pnpm exec tsc --noEmit` (em apps/web) sai com código 0 — confirma que `SAMPLE_SPEC` satisfaz o schema `TableSpecPayload` sem erros de tipo
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>SAMPLE_SPEC exportado, tipado como TableSpecPayload, conteúdo pt-BR "Controle de Gastos" com 5 colunas (incl. coluna formula) e 5 linhas; typecheck verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js router | Requisições para rotas antigas de tool (`/workspace/{sql,...}`) — usuário autenticado ou não |
| Browser → /privacidade | Link estático na Topbar, sem dados sensíveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering (open redirect) | `next.config.ts` redirects() | accept | `destination` é literal estático `"/workspace"` em todas as 6 entradas, não interpolado de input do usuário — sem possibilidade de redirecionar para domínio externo controlável |
| T-16-02 | Information Disclosure | Cadeia `/workspace/sql` → 308 → `/workspace` → (layout, sem sessão) → `/sign-in` | accept | Comportamento já existente para `/workspace` direto; layout continua sendo o gate de auth (não alterado neste plan) — adicionar 6 redirects para o mesmo destino não cria nova superfície |
| T-16-03 | Tampering | Topbar simplificada (`toolKind` fixo "unified") | accept | `toolKind` fixo não introduz novo input — `handleDeleteHistory` continua chamando `DELETE /api/conversations/unified`, endpoint já existente e autenticado via sessão (não alterado) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova instalada neste plan (RESEARCH.md confirma Package Legitimacy Audit "não aplicável") |
</threat_model>

<verification>
- `cd apps/web && pnpm exec tsc --noEmit` deve sair com código 0 após cada task
- `pnpm -r test` (raiz do monorepo) deve permanecer verde ao final do plano
- Verificação manual (não bloqueante para este plano, cobrirá toda a fase no gate final): após Plan 02, `next dev` reiniciado + `curl -I http://localhost:3000/workspace/sql` deve retornar `HTTP/1.1 308` e header `location: /workspace`
</verification>

<success_criteria>
- `next.config.ts` redireciona as 6 rotas antigas de página de tool para `/workspace` com 308, sem editar nenhum `page.tsx` de tool (D-07/D-08)
- `Topbar` não deriva mais nada de `usePathname()`; expõe sessão, "Sair" e link `/privacidade` (SHELL-03/D-11); marca "Tabelin.IA" preservada (D-12)
- `SAMPLE_SPEC` existe, tipado e pronto para a Plan 02 montar `<TableGridPanel spec={SAMPLE_SPEC}/>` no painel principal (D-04)
- `pnpm exec tsc --noEmit` (apps/web) e `pnpm -r test` permanecem verdes
</success_criteria>

<output>
Create `.planning/phases/16-tela-nica-fim-da-navega-o-multi-ferramenta/16-01-SUMMARY.md` when done
</output>
