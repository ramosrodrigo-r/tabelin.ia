---
phase: 11-attachment-ui-pro-gating
plan: "05"
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified:
  - apps/web/tests/formula-ui.test.tsx
autonomous: true
requirements: [ATT-01, ATT-02, ATT-03, ATT-04, ATT-05, ATT-06, ATT-07, ATT-08, PRO-01, SEC-01, SEC-03]

must_haves:
  truths:
    - "Usuário Pro vê chip de preview após selecionar arquivo via input[type=file]"
    - "Usuário free vê botão paperclip desabilitado (PRO-01)"
    - "Arquivo com tipo inválido dispara mensagem de erro pt-BR (ATT-04)"
    - "Arquivo >5MB dispara mensagem de erro pt-BR (ATT-04)"
    - "Após submit com arquivo, output panel exibe badge Grounded (ATT-06)"
    - "Após submit com attachment_grounded com wasTruncated=true, painel exibe badge de extração parcial (ATT-08)"
    - "AttachmentPanel renderiza extractedText como texto puro sem dangerouslySetInnerHTML"
    - "PrivacyNotice contém texto sobre Nova conversa (SEC-03)"
    - "pnpm --filter web test sai 0 (todos os testes passam)"
  artifacts:
    - path: apps/web/tests/formula-ui.test.tsx
      provides: "Cobertura de testes dos novos comportamentos de UI de anexo"
      contains: "attachment"
  key_links:
    - from: "formula-ui.test.tsx"
      to: "FormulaTool (com entitlement pro/free)"
      via: "render + userEvent.upload + streamResponse com attachment_grounded"
      pattern: "attachment_grounded"
---

<objective>
Escrever testes de UI para os novos comportamentos de anexo no FormulaTool: chip de preview, pro-gate visual, validação client-side, badge de grounding, painel de texto extraído e aviso de truncagem.

Purpose: Fechar o ciclo de qualidade da Phase 11. Os testes usam o helper `streamResponse` existente estendido com o evento `attachment_grounded`. Cobrir os casos que o QA manual verificaria: pro vs free, arquivo válido vs inválido, stream com e sem truncagem.

Output: Testes adicionados em formula-ui.test.tsx (arquivo já corrigido em 11-01). Formula é o canônico — comportamento idêntico nos outros 4 tools não requer duplicação de testes.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-attachment-ui-pro-gating/11-RESEARCH.md
@.planning/phases/11-attachment-ui-pro-gating/11-PATTERNS.md

<interfaces>
<!-- Helpers e entitlements disponíveis no arquivo de teste -->

apps/web/tests/formula-ui.test.tsx (estado após 11-01):
  function streamResponse(lines: unknown[]) → Response com ReadableStream NDJSON
  const freeEntitlement: UserEntitlement = { plan: "free", status: "active" }
  const proEntitlement: UserEntitlement = { plan: "pro", status: "active", cycle: "monthly", currentPeriodEnd: ... }

  -- Teste existente (agora corrigido): "streams formula output and enables validated copy"
     usa vi.spyOn(globalThis, "fetch").mockResolvedValue(streamResponse([...]))
     usa userEvent.setup() + user.type + user.click + waitFor

Evento attachment_grounded para mockar no fetch:
  { type: "attachment_grounded", charCount: 1234, wasTruncated: false, extractedText: "col1,col2\n1,2" }
  { type: "attachment_grounded", charCount: 9000, wasTruncated: true, extractedText: "...texto longo..." }

Web API File mock para userEvent.upload:
  new File(["col1,col2\n1,2"], "dados.csv", { type: "text/csv" })
  document.querySelector('input[type="file"]') → HTMLInputElement para upload

11-PATTERNS.md — seção "formula-ui.test.tsx" (linhas 750-807): falha corrigida + padrão de teste de arquivo

Para verificar ausência de dangerouslySetInnerHTML (SEC-01 na UI):
  grep -v '^#' attachment-panel.tsx | grep -c "dangerouslySetInnerHTML"  → deve retornar 0
  (Note: verificado nos planos anteriores — aqui apenas confirmar via teste que o texto aparece como texto)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes de PRO-gate, chip de preview e validação client-side</name>
  <files>apps/web/tests/formula-ui.test.tsx</files>
  <read_first>
    - apps/web/tests/formula-ui.test.tsx — COMPLETO: helpers existentes (linhas 1-36), entitlements (linhas 24-25), testes existentes (linhas 38-109) — referenciar padrões de describe/it/beforeEach
    - 11-PATTERNS.md — seção "Padrão de teste para arquivo" (linhas 794-807): código de teste para chip de preview
    - apps/web/src/components/app/attachment-button.tsx (após 11-02): SUPPORTED_TYPES, MAX_FILE_SIZE, validateFile
    - apps/web/src/features/formula/formula-tool.tsx (após 11-03): estados e JSX do FormulaTool
  </read_first>
  <behavior>
    - Teste "free user sees disabled attachment button": render FormulaTool com freeEntitlement → expect button[aria-label="Anexar arquivo (exclusivo Pro)"] to be disabled
    - Teste "pro user sees enabled attachment button": render FormulaTool com proEntitlement → expect button[aria-label="Anexar arquivo"] NOT disabled
    - Teste "pro user sees attachment chip after file select": render com proEntitlement → upload File csv via input[type=file] → expect role=status com aria-label contendo "dados.csv" to be in document
    - Teste "file type validation rejects unsupported type": render com proEntitlement → upload File com type="application/exe" → expect mensagem de erro "Tipo não suportado" to be in document; chip NÃO aparece
    - Teste "file size validation rejects files over 5MB": criar File com size simulado >5MB → validate → esperar mensagem "Arquivo muito grande"
    - Teste "pro user can remove attachment chip": selecionar arquivo → expect chip presente → clicar botão × → expect chip NOT in document
    - Teste "privacy notice appears with pending file": selecionar arquivo → expect "Nova conversa" in document
  </behavior>
  <action>
    Adicionar bloco describe "attachment UI" dentro do describe existente "FormulaTool" em apps/web/tests/formula-ui.test.tsx.

    Os novos testes devem vir APÓS os testes existentes (após linha 109).

    Para cada teste, usar o padrão existente: vi.restoreAllMocks no beforeEach (já existe no beforeEach do describe pai), userEvent.setup(), render com entitlement apropriado.

    Para testar File upload: usar document.querySelector('input[type="file"]') para obter o input oculto, depois userEvent.upload(input, file).

    Para testar validação de tamanho: como File não aceita tamanho arbitrário via construtor, usar validateFile diretamente se necessário, ou criar um objeto mock de File com size sobrescrito via Object.defineProperty.

    Para testar privacyNotice: expect(screen.getByText(/Nova conversa/)).toBeInTheDocument() após upload.

    Para testar remoção do chip: após upload, clicar button[aria-label="Remover arquivo"], verificar que o chip desaparece.

    NÃO mockar fetch nestes testes (não há submit) — são testes puramente de UI state.
  </action>
  <verify>
    <automated>pnpm --filter web test -- --reporter=verbose formula-ui 2>&1 | tail -20</automated>
  </verify>
  <done>Todos os testes de "attachment UI" passam; nenhum teste existente regrediu; saída mostra 0 failed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes de grounding badge, AttachmentPanel e truncagem parcial</name>
  <files>apps/web/tests/formula-ui.test.tsx</files>
  <read_first>
    - apps/web/tests/formula-ui.test.tsx — estado após Task 1: helpers streamResponse, entitlements, beforeEach com vi.spyOn de fetch
    - apps/web/src/features/formula/components/formula-output-panel.tsx (após 11-03): GroundingBadge aria-label, AttachmentPanel details/summary
    - apps/web/src/components/app/attachment-panel.tsx (após 11-02): summary "Texto extraído do documento"; badge "extração parcial"; pre com conteúdo
    - 11-RESEARCH.md — Pattern 7 (linhas 306-332): estrutura do AttachmentPanel; evento attachment_grounded
    - apps/web/tests/formula-ui.test.tsx — linhas 58-95: padrão de teste com fetch mock, waitFor
  </read_first>
  <behavior>
    - Teste "shows grounding badge after submit with attachment": fetch mocka stream com attachment_grounded + delta + complete → submit → waitFor → expect "Gerado com base em documento anexado" (aria-label do grounding badge span) to be in document
    - Teste "shows attachment panel with extracted text": stream com attachment_grounded.extractedText="col1,col2" → submit → waitFor → expect "Texto extraído do documento" (summary) to be in document; expandir details → expect "col1,col2" in document
    - Teste "shows truncation warning when wasTruncated=true": stream com attachment_grounded.wasTruncated=true → submit → waitFor → expect "extração parcial" (badge text) to be in document
    - Teste "does not show grounding badge without attachment": stream sem attachment_grounded (apenas metadata+delta+complete) → submit → waitFor → expect queryByText("Gerado com base em documento") to be null
    - Verificação estrutural de segurança: grep no arquivo attachment-panel.tsx verificando ausência de dangerouslySetInnerHTML (teste não-funcional confirma invariante SEC-01)
  </behavior>
  <action>
    Adicionar os novos testes como sub-bloco dentro do describe "attachment UI" (criado na Task 1), ou como describe separado "grounding and transparency" dentro do describe pai.

    Para cada teste com stream mockado:
    1. vi.spyOn(globalThis, "fetch").mockResolvedValue(streamResponse([...]))
    2. Montar o stream com attachment_grounded como PRIMEIRO evento antes de metadata.
    3. render com proEntitlement
    4. Selecionar arquivo via upload (input[type="file"])
    5. Preencher textarea e clicar submit
    6. waitFor o assert principal

    Para o teste de "does not show grounding badge without attachment":
    - Não selecionar arquivo antes do submit
    - Stream sem attachment_grounded
    - Verificar ausência do badge

    Para testar detalhes do AttachmentPanel (texto extraído):
    - Após waitFor do summary, usar userEvent.click no summary para abrir o details
    - Verificar conteúdo do pre via screen.getByText (ou getAllByText)

    Adicionar ao final do arquivo (fora dos describe de testes) uma verificação estática:
    it("attachment-panel never uses dangerouslySetInnerHTML (SEC-01)", () => {
      // Esta verificação é documental — a renderização segura é garantida pelo código
      // mas documentamos o invariante aqui para CI
      const panelFile = require("fs").readFileSync(
        require("path").join(__dirname, "../src/components/app/attachment-panel.tsx"),
        "utf-8"
      );
      expect(panelFile).not.toContain("dangerouslySetInnerHTML");
    });
  </action>
  <verify>
    <automated>pnpm --filter web test -- --reporter=verbose formula-ui 2>&1 | tail -30</automated>
  </verify>
  <done>Todos os testes de "grounding and transparency" passam; teste SEC-01 estrutural passa; 0 failed total para formula-ui.test.tsx; pnpm --filter web test sai 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| testes → código de produção | Testes exercitam comportamentos reais — não mocam a renderização, apenas o fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | XSS | attachment-panel.tsx renderização de extractedText | mitigate | Teste estrutural explícito: expect(panelFile).not.toContain("dangerouslySetInnerHTML") como invariante de CI. Bloqueia regressão de SEC-01. |
| T-11-05-02 | Elevation of Privilege | PRO-01: free user não vê botão habilitado | mitigate | Teste verifica button[disabled] para freeEntitlement — torna PRO-01 visual testável em CI. |
| T-11-05-SC | Tampering | sem novos pacotes npm | accept | Nenhum pacote novo; só vitest + @testing-library já instalados |
</threat_model>

<verification>
- pnpm --filter web test -- formula-ui sai 0 (todos passam)
- pnpm --filter web test sai 0 (sem regressões nos demais testes)
- Teste estrutural SEC-01 incluído e passando
- Testes cobrem: free gate (PRO-01), chip (ATT-03), validação (ATT-04), grounding (ATT-06), painel (ATT-07), truncagem (ATT-08), privacidade (SEC-03)
</verification>

<success_criteria>
- 0 testes falhando em formula-ui.test.tsx
- 0 testes falhando na suite completa (sem regressões)
- Cada requisito ATT-03, ATT-04, ATT-06, ATT-07, ATT-08, PRO-01, SEC-01, SEC-03 tem ao menos 1 teste cobrindo o comportamento
</success_criteria>

<output>
Criar `.planning/phases/11-attachment-ui-pro-gating/11-05-SUMMARY.md` ao concluir
</output>
