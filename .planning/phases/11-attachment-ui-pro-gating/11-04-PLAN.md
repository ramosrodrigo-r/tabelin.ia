---
phase: 11-attachment-ui-pro-gating
plan: "04"
type: execute
wave: 2
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - apps/web/src/features/sql/sql-tool.tsx
  - apps/web/src/features/sql/hooks/use-sql-stream.ts
  - apps/web/src/features/sql/components/sql-input-panel.tsx
  - apps/web/src/features/sql/components/sql-output-panel.tsx
  - apps/web/src/features/regex/regex-tool.tsx
  - apps/web/src/features/regex/hooks/use-regex-stream.ts
  - apps/web/src/features/regex/components/regex-input-panel.tsx
  - apps/web/src/features/regex/components/regex-output-panel.tsx
  - apps/web/src/features/scripts/scripts-tool.tsx
  - apps/web/src/features/scripts/hooks/use-scripts-stream.ts
  - apps/web/src/features/scripts/components/scripts-input-panel.tsx
  - apps/web/src/features/scripts/components/scripts-output-panel.tsx
  - apps/web/src/features/template/template-tool.tsx
  - apps/web/src/features/template/hooks/use-template-stream.ts
  - apps/web/src/features/template/components/template-input-panel.tsx
  - apps/web/src/features/template/components/template-output-panel.tsx
autonomous: true
requirements: [ATT-01, ATT-02, ATT-03, ATT-04, ATT-05, ATT-06, ATT-07, ATT-08, PRO-01, SEC-03]

must_haves:
  truths:
    - "SQL/Regex/Scripts/Template replicam identicamente o padrão Formula de upload"
    - "Hooks de SQL/Regex/Scripts/Template montam FormData com campos corretos para cada schema (dialect para SQL, sem campos extras para Regex/Scripts/Template)"
    - "Template tool preserva Pro-gate incondicional existente (não adicionar gate condicional)"
    - "Todos os 4 tools têm drag-and-drop, chip de preview e aviso LGPD"
    - "Todos os 4 output panels exibem grounding badge e AttachmentPanel"
  artifacts:
    - path: apps/web/src/features/sql/hooks/use-sql-stream.ts
      provides: "Hook SQL com file? em SubmitSqlInput, FormData com dialect"
      contains: "FormData"
    - path: apps/web/src/features/template/components/template-input-panel.tsx
      provides: "InputPanel preservando showProGate existente; AttachmentButton só renderiza quando !showProGate"
      contains: "AttachmentButton"
  key_links:
    - from: "apps/web/src/features/template/template-tool.tsx"
      to: "apps/web/src/features/template/components/template-input-panel.tsx"
      via: "isPro prop passa para AttachmentButton via InputPanel"
      pattern: "isPro"
    - from: "apps/web/src/features/sql/hooks/use-sql-stream.ts"
      to: "/api/tools/sql/generate"
      via: "FormData com dialect + file; sem Content-Type manual"
      pattern: "FormData"
---

<objective>
Replicar o padrão de anexo do tool canônico Formula nos outros 4 tools: SQL, Regex, Scripts e Template. Cada tool segue a mesma estrutura com variações nos campos do FormData. Template tem diferença crítica: Pro-gate incondicional existente deve ser preservado intacto.

Purpose: Completar a cobertura de ATT-01/02 ("nos 5 tools"). A execução é mecânica a partir do analog Formula (11-03), mas os campos do FormData variam por tool. Usar 11-03-SUMMARY.md como referência canônica ao executar.

Output: 16 arquivos modificados (4 tools × 4 arquivos cada).
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-attachment-ui-pro-gating/11-RESEARCH.md
@.planning/phases/11-attachment-ui-pro-gating/11-PATTERNS.md
@.planning/phases/10-persistence-llm-context/10-03-SUMMARY.md

<interfaces>
<!-- Variações por tool extraídas de 11-PATTERNS.md "Variações por Tool" -->

SQL (use-sql-stream.ts):
  SubmitSqlInput tem: { dialect: string; text: string }  — sem mode/platform/formulaLanguage
  FormData: fd.append("prompt", input.text); fd.append("dialect", input.dialect); fd.append("file", input.file)
  Endpoint: /api/tools/sql/generate (único)
  createSqlEventStream assinatura (após 11-01): (payload, lastFreeUse?, attachmentMeta?)

Regex (use-regex-stream.ts):
  SubmitRegexInput: verificar campos reais no arquivo — apenas { text: string } + outros campos específicos
  FormData: fd.append("prompt", input.text); + campos de regex; fd.append("file", input.file)
  Endpoint: /api/tools/regex/generate

Scripts (use-scripts-stream.ts):
  SubmitScriptsInput: { scriptType: string; text: string }
  FormData: fd.append("prompt", input.text); fd.append("scriptType", input.scriptType); fd.append("file", input.file)
  Endpoint: /api/tools/scripts/generate

Template (use-template-stream.ts):
  SubmitTemplateInput: { text: string }
  FormData: fd.append("prompt", input.text); fd.append("file", input.file)
  Endpoint: /api/tools/template/generate
  DIFERENÇA CRÍTICA: use-template-stream.ts JÁ trata proBlocked (linhas 25, 36, 45-51)
  template-input-panel.tsx JÁ tem showProGate = !isPro || proBlocked (linhas 29-81)
  AttachmentButton em template-input-panel deve ser renderizado apenas quando !showProGate

Após 11-01: todos os 4 schemas têm evento attachment_grounded disponível.
Após 11-02: AttachmentButton, AttachmentChip, AttachmentPanel, PrivacyNotice disponíveis em @/components/app/
Após 11-03: Formula é o padrão canônico — usar 11-03-SUMMARY.md como referência.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL e Regex — hooks, tools e painéis com padrão Formula</name>
  <files>
    apps/web/src/features/sql/sql-tool.tsx,
    apps/web/src/features/sql/hooks/use-sql-stream.ts,
    apps/web/src/features/sql/components/sql-input-panel.tsx,
    apps/web/src/features/sql/components/sql-output-panel.tsx,
    apps/web/src/features/regex/regex-tool.tsx,
    apps/web/src/features/regex/hooks/use-regex-stream.ts,
    apps/web/src/features/regex/components/regex-input-panel.tsx,
    apps/web/src/features/regex/components/regex-output-panel.tsx
  </files>
  <read_first>
    - apps/web/src/features/sql/hooks/use-sql-stream.ts — COMPLETO: SubmitSqlInput (campos reais), bloco de fetch, loop NDJSON, return do hook
    - apps/web/src/features/sql/sql-tool.tsx — COMPLETO: estrutura state, submit(), JSX .tool-chat
    - apps/web/src/features/sql/components/sql-input-panel.tsx — props interface atual, uso de ChatInput
    - apps/web/src/features/sql/components/sql-output-panel.tsx — props interface atual
    - apps/web/src/features/regex/hooks/use-regex-stream.ts — COMPLETO: SubmitRegexInput (campos reais)
    - apps/web/src/features/regex/regex-tool.tsx — estrutura geral
    - apps/web/src/features/regex/components/regex-input-panel.tsx — props
    - apps/web/src/features/regex/components/regex-output-panel.tsx — props
    - 11-PATTERNS.md — seção "Variações por tool SQL" (linhas 815-820) e "Regex" (linhas 822-827)
    - apps/web/src/features/formula/formula-tool.tsx + hooks/use-formula-stream.ts (após 11-03) — referência canônica
  </read_first>
  <action>
    Para SQL e Regex, aplicar exatamente as mesmas modificações do padrão Formula (11-03), com as seguintes variações:

    HOOKS (use-sql-stream.ts e use-regex-stream.ts):
    - Adicionar file?: File ao SubmitXxxInput
    - Adicionar attachmentStatus e attachmentMeta ao state
    - Reset no início de submit
    - Bloco FormData condicional: NÃO setar Content-Type; montar fd com os campos CORRETOS para cada schema:
      SQL: fd.append("prompt", input.text), fd.append("dialect", input.dialect), fd.append("file", input.file)
      Regex: fd.append("prompt", input.text), demais campos do schema regex + fd.append("file", input.file)
      (verificar campos reais de SubmitRegexInput nos arquivos antes de mapear)
    - Tratamento 403 pro_required com feature:attachment
    - Transição uploading → extracting → null
    - Captura de attachment_grounded no loop NDJSON
    - Adicionar attachmentStatus e attachmentMeta ao return

    TOOLS (sql-tool.tsx, regex-tool.tsx):
    - Importar validateFile
    - Adicionar estados: pendingFile, fileError, dragOver
    - Adicionar handleFileSelect com validateFile
    - Modificar submit(): fileSnapshot + setPendingFile(null) + passar file para hook
    - Adicionar DnD handlers no div.tool-chat com guard !isPro
    - Passar novos props para InputPanel (pendingFile, fileError, onFileSelect, onFileRemove)
    - Passar attachmentMeta para OutputPanel (corrente + arquivados)
    - Indicadores de dois estágios (uploading/extracting)
    - Estender tipo XxxExchange com attachmentMeta?: {...} | null

    INPUT PANELS (sql-input-panel.tsx, regex-input-panel.tsx):
    - Importar AttachmentButton, AttachmentChip, PrivacyNotice
    - Adicionar pendingFile, fileError, onFileSelect, onFileRemove à interface de props
    - leftAction={&lt;AttachmentButton isPro={isPro} disabled={pending || quotaBlocked} onFileSelect={onFileSelect} /&gt;} no ChatInput
    - Chip + PrivacyNotice após ChatInput quando pendingFile

    OUTPUT PANELS (sql-output-panel.tsx, regex-output-panel.tsx):
    - Importar AttachmentPanel
    - Adicionar attachmentMeta? à interface de props
    - GroundingBadge span + AttachmentPanel após conteúdo principal
  </action>
  <verify>
    <automated>grep -c "attachmentMeta\|pendingFile\|AttachmentButton" apps/web/src/features/sql/sql-tool.tsx apps/web/src/features/regex/regex-tool.tsx && grep -c "FormData" apps/web/src/features/sql/hooks/use-sql-stream.ts apps/web/src/features/regex/hooks/use-regex-stream.ts && pnpm --filter web typecheck</automated>
  </verify>
  <done>sql-tool e regex-tool contêm ≥3 termos; hooks contêm FormData; typecheck sai 0; grep "content-type.*multipart" nesses hooks retorna 0</done>
</task>

<task type="auto">
  <name>Task 2: Scripts e Template — hooks, tools e painéis (com preservação do Pro-gate incondicional do Template)</name>
  <files>
    apps/web/src/features/scripts/scripts-tool.tsx,
    apps/web/src/features/scripts/hooks/use-scripts-stream.ts,
    apps/web/src/features/scripts/components/scripts-input-panel.tsx,
    apps/web/src/features/scripts/components/scripts-output-panel.tsx,
    apps/web/src/features/template/template-tool.tsx,
    apps/web/src/features/template/hooks/use-template-stream.ts,
    apps/web/src/features/template/components/template-input-panel.tsx,
    apps/web/src/features/template/components/template-output-panel.tsx
  </files>
  <read_first>
    - apps/web/src/features/scripts/hooks/use-scripts-stream.ts — COMPLETO: SubmitScriptsInput (campos reais)
    - apps/web/src/features/scripts/scripts-tool.tsx — estrutura geral
    - apps/web/src/features/scripts/components/scripts-input-panel.tsx — props interface
    - apps/web/src/features/scripts/components/scripts-output-panel.tsx — props
    - apps/web/src/features/template/hooks/use-template-stream.ts — COMPLETO: proBlocked handling (linhas 25, 36, 45-51), SubmitTemplateInput
    - apps/web/src/features/template/template-tool.tsx — estrutura; isPro derivado
    - apps/web/src/features/template/components/template-input-panel.tsx — COMPLETO: showProGate bloco (linhas 29-81) DEVE ser preservado; onde encaixar AttachmentButton
    - apps/web/src/features/template/components/template-output-panel.tsx — props
    - 11-PATTERNS.md — seção "Scripts" (linhas 828-832) e "Template" (linhas 833-836): diferença crítica do template
    - 10-03-SUMMARY.md — decisão: "Template Pro-gate incondicional preservado intacto em linha 23"
  </read_first>
  <action>
    SCRIPTS — aplicar padrão Formula idêntico ao SQL/Regex:
    - use-scripts-stream.ts: file? em SubmitScriptsInput; FormData com fd.append("prompt", input.text), fd.append("scriptType", input.scriptType), fd.append("file", input.file); demais alterações como SQL
    - scripts-tool.tsx, scripts-input-panel.tsx, scripts-output-panel.tsx: idênticos ao padrão SQL

    TEMPLATE — ATENÇÃO: diferenças críticas:
    1. use-template-stream.ts: adicionar file? ao SubmitTemplateInput; FormData com fd.append("prompt", input.text), fd.append("file", input.file). O tratamento de proBlocked EXISTENTE não deve ser alterado. Adicionar attachmentStatus/attachmentMeta ao estado. O hook JÁ retorna proBlocked — verificar e preservar. O 403 pro_required para attachment (feature:"attachment") é DIFERENTE do proBlocked normal do template (que não tem feature:attachment na resposta) — tratar separadamente.

    2. template-tool.tsx: adicionar pendingFile, fileError, dragOver, handleFileSelect. No handler onDrop, verificar !isPro antes de processar (comportamento consistente com outros tools). Passar file para hook no submit. Arquivar attachmentMeta nos exchanges.

    3. template-input-panel.tsx: AttachmentButton deve ser renderizado APENAS quando !showProGate (ou seja, quando usuário é Pro e não está em proBlocked):
       {!showProGate ? (
         leftAction no ChatInput: &lt;AttachmentButton isPro={true} disabled={pending} onFileSelect={onFileSelect} /&gt;
       ) : null_or_leftAction_absent}
       E o chip/aviso:
       {!showProGate && pendingFile ? (&lt;&gt;&lt;AttachmentChip ... /&gt;&lt;PrivacyNotice /&gt;&lt;/&gt;) : null}
       O bloco showProGate (linhas 29-81) deve permanecer intacto — é o Pro-gate do template inteiro, não só do attachment.

    4. template-output-panel.tsx: mesmo padrão dos outros — GroundingBadge + AttachmentPanel quando attachmentMeta.

    IMPORTANTE: não usar attachment condicional onde não há precedente — o template é Pro-only portanto o AttachmentButton nunca precisa da branch isPro=false (o gate já bloqueia tudo).
  </action>
  <verify>
    <automated>grep -c "attachmentMeta\|pendingFile\|AttachmentButton" apps/web/src/features/scripts/scripts-tool.tsx apps/web/src/features/template/template-tool.tsx && grep -c "showProGate" apps/web/src/features/template/components/template-input-panel.tsx && pnpm --filter web typecheck</automated>
  </verify>
  <done>scripts-tool e template-tool contêm ≥3 termos; template-input-panel tem showProGate (Pro-gate incondicional preservado); typecheck sai 0; grep "pro_required.*line 23" em template/generate/route.ts intacto (verificar via 10-03-SUMMARY)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| user file input → handleFileSelect (4 tools) | Arquivo validado por validateFile antes de setar state |
| use-template-stream → /api/tools/template | Pro-gate incondicional no backend (linha 23 do route) cobre toda geração, não só attachment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | XSS | extractedText em 4 output panels | mitigate | AttachmentPanel (11-02) usa JSX text node — defesa centralizada. Verificar grep dangerouslySetInnerHTML = 0 nos 8 arquivos de panel desta wave. |
| T-11-04-02 | Elevation of Privilege | Template: AttachmentButton visível apenas quando !showProGate | mitigate | showProGate = !isPro || proBlocked cobre 100% dos usuários não-Pro. O backend (10-03) tem Pro-gate incondicional na linha 23. A UI é UX, defesa real é server-side. |
| T-11-04-03 | Tampering | Content-Type manual em FormData nos 4 hooks | mitigate | Verificar que nenhum dos 4 hooks adiciona "content-type" manual ao FormData. Grep "content-type.*multipart" nos 4 hooks retorna 0. |
| T-11-04-04 | Denial of Service | DnD sem guard !isPro nos 4 tools | mitigate | Handler onDrop em cada tool verifica !isPro antes de chamar handleFileSelect — consistente com Formula (11-03). |
| T-11-04-SC | Tampering | sem novos pacotes npm | accept | Nenhum pacote novo; só imports de componentes internos |
</threat_model>

<verification>
- grep -c "pendingFile" em todos os 4 tools ≥ 2 cada
- grep -c "FormData" nos 4 hooks ≥ 1 cada
- grep "content-type.*multipart" nos 4 hooks retorna 0
- grep "showProGate" em template-input-panel.tsx retorna ≥ 2 (preservado)
- grep "dangerouslySetInnerHTML" nos 8 output/input panels desta wave retorna 0
- pnpm --filter web typecheck sai 0
</verification>

<success_criteria>
- SQL, Regex, Scripts, Template têm fluxo completo de anexo (ATT-01/02/03/04/05/06/07/08)
- Template preserva Pro-gate incondicional existente intacto
- Nenhum dos 4 hooks seta Content-Type manualmente para FormData
- Todos os 4 output panels exibem GroundingBadge e AttachmentPanel quando attachmentMeta
- AttachmentButton em template-input-panel renderizado apenas quando !showProGate
- pnpm --filter web typecheck sai 0
</success_criteria>

<output>
Criar `.planning/phases/11-attachment-ui-pro-gating/11-04-SUMMARY.md` ao concluir
</output>
