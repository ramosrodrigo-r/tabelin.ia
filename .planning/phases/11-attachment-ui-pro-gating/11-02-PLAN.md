---
phase: 11-attachment-ui-pro-gating
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/app/attachment-button.tsx
  - apps/web/src/components/app/attachment-chip.tsx
  - apps/web/src/components/app/attachment-panel.tsx
  - apps/web/src/components/app/privacy-notice.tsx
  - apps/web/src/styles/globals.css
autonomous: true
requirements: [ATT-01, ATT-03, ATT-04, ATT-06, ATT-07, ATT-08, PRO-01, SEC-01, SEC-03]

must_haves:
  truths:
    - "AttachmentButton renderiza botão disabled para usuário free (isPro=false)"
    - "AttachmentButton renderiza input[type=file] oculto e botão clicável para usuário Pro"
    - "AttachmentChip exibe nome, tamanho e botão × com role=status"
    - "AttachmentPanel renderiza texto extraído como conteúdo de texto puro (sem dangerouslySetInnerHTML)"
    - "PrivacyNotice exibe copy LGPD em pt-BR com referência a Nova conversa"
    - "validateFile rejeita arquivo >5MB e tipos não suportados com mensagem em pt-BR"
  artifacts:
    - path: apps/web/src/components/app/attachment-button.tsx
      provides: "AttachmentButton component + validateFile export"
      exports: [AttachmentButton, validateFile]
    - path: apps/web/src/components/app/attachment-chip.tsx
      provides: "AttachmentChip component"
      exports: [AttachmentChip]
    - path: apps/web/src/components/app/attachment-panel.tsx
      provides: "AttachmentPanel collapsible com extractedText como texto puro"
      exports: [AttachmentPanel]
    - path: apps/web/src/components/app/privacy-notice.tsx
      provides: "PrivacyNotice com copy LGPD pt-BR"
      exports: [PrivacyNotice]
  key_links:
    - from: "apps/web/src/components/app/attachment-panel.tsx"
      to: "Nenhum innerHTML/dangerouslySetInnerHTML"
      via: "extractedText renderizado como children de <pre>"
      pattern: "dangerouslySetInnerHTML"
---

<objective>
Criar os 4 componentes compartilhados de UI de anexo e adicionar as classes CSS necessárias em globals.css. Estes componentes são consumidos por todos os 5 tools nas waves seguintes.

Purpose: Centralizar os componentes de anexo evita duplicação nos 5 tools e garante consistência visual. Crítico de segurança: AttachmentPanel DEVE renderizar extractedText como nó de texto React (nunca dangerouslySetInnerHTML) — o conteúdo vem do documento do usuário e pode conter HTML.

Output: 4 arquivos novos de componente + classes CSS em globals.css.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-attachment-ui-pro-gating/11-RESEARCH.md
@.planning/phases/11-attachment-ui-pro-gating/11-PATTERNS.md

<interfaces>
<!-- Tokens CSS de referência em globals.css — extraídos para executor -->

apps/web/src/styles/globals.css:
  .metadata-row { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 12px; }
  .metadata-row span { border: 1px solid var(--border); border-radius: 999px;
    background: #f8fafc; color: var(--muted); font-size: 12px; font-weight: 650; padding: 4px 8px; }
  .pro-badge { ... }  — token de referência visual para pill
  .note-block { ... } — referência para AttachmentPanel
  .revoked-notice { ... } — referência para PrivacyNotice

apps/web/src/features/formula/components/copy-button.tsx — analog de AttachmentButton:
  "use client"; import { Check, Copy } from "lucide-react"; import { useEffect, useState } from "react"

apps/web/src/components/app/chat-input.tsx — slot:
  interface ChatInputProps { leftAction?: React.ReactNode; ... }

apps/web/src/features/template/components/template-input-panel.tsx — padrão proBlocked:
  const showProGate = !isPro || proBlocked;
  fetch("/api/billing/checkout", { method: "POST", body: JSON.stringify({ cycle: "monthly" }) })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AttachmentButton + validateFile (PRO-01, ATT-01, ATT-04)</name>
  <files>apps/web/src/components/app/attachment-button.tsx</files>
  <read_first>
    - apps/web/src/features/formula/components/copy-button.tsx — padrão "use client" + lucide-react + useRef + disabled prop + aria-label
    - 11-PATTERNS.md — seção "attachment-button.tsx NOVO COMPONENTE" (linhas 341-435): código completo com SUPPORTED_TYPES, MAX_FILE_SIZE, validateFile, AttachmentButton com branches isPro
    - 11-RESEARCH.md — Pattern 2 (linhas 161-213): padrão pro-gate visual; Pattern 3 (linhas 215-241): validateFile
    - apps/web/src/features/template/components/template-input-panel.tsx — padrão proBlocked + fetch checkout (linhas 29-81)
  </read_first>
  <behavior>
    - validateFile(file: File): string | null — retorna null para arquivo válido, string de erro pt-BR para tipo inválido ou >5MB
    - validateFile rejeita arquivo.type não em SUPPORTED_TYPES sem fallback de extensão .csv/.xlsx → retorna "Tipo não suportado. Use CSV, XLSX, PNG, JPEG, PDF ou TXT."
    - validateFile rejeita arquivo.size > 5MB → retorna "Arquivo muito grande. O limite é 5 MB."
    - AttachmentButton com isPro=false renderiza button[disabled] com aria-label "Anexar arquivo (exclusivo Pro)"
    - AttachmentButton com isPro=true renderiza input[type=file] oculto + button com aria-label "Anexar arquivo"
    - Input file tem accept=".csv,.xlsx,.png,.jpeg,.jpg,.pdf,.txt"
    - onChange do input: e.target.value = "" após capturar arquivo (reset para re-selecionar mesmo arquivo — Pitfall 5)
  </behavior>
  <action>
    Criar apps/web/src/components/app/attachment-button.tsx com "use client" directive.

    Exportar SUPPORTED_TYPES como constante (array de MIME types: text/csv, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet, image/png, image/jpeg, application/pdf, text/plain) e MAX_FILE_SIZE = 5 * 1024 * 1024.

    Exportar validateFile(file: File): string | null — guarda contra tipo não suportado (verificar MIME + fallback por extensão .csv/.xlsx para A4 de RESEARCH.md) e tamanho >5MB. Mensagens de erro em pt-BR.

    Exportar AttachmentButton({ isPro, onFileSelect, disabled }: { isPro: boolean; onFileSelect: (f: File) => void; disabled?: boolean }):
    - Branch isPro=false: retorna button[type=button, className="attachment-btn", disabled=true, aria-label="Anexar arquivo (exclusivo Pro)", title="Recurso exclusivo Pro"] com Paperclip icon (size=16, aria-hidden).
    - Branch isPro=true: retorna Fragment com input[type=file] oculto (ref, accept, onChange que captura arquivo e reseta target.value) + button[type=button, className="attachment-btn", disabled={disabled}, aria-label="Anexar arquivo"] com Paperclip icon.

    Imports: Paperclip de "lucide-react"; useRef de "react".
  </action>
  <verify>
    <automated>pnpm --filter web typecheck && grep -v "dangerouslySetInnerHTML" apps/web/src/components/app/attachment-button.tsx | grep -c "validateFile"</automated>
  </verify>
  <done>Arquivo existe; validateFile exportada; AttachmentButton exportado; typecheck sai 0; botão disabled quando isPro=false verificável por render test</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AttachmentChip, AttachmentPanel, PrivacyNotice + CSS (ATT-03, ATT-07, ATT-08, SEC-01, SEC-03)</name>
  <files>
    apps/web/src/components/app/attachment-chip.tsx,
    apps/web/src/components/app/attachment-panel.tsx,
    apps/web/src/components/app/privacy-notice.tsx,
    apps/web/src/styles/globals.css
  </files>
  <read_first>
    - 11-PATTERNS.md — seção "attachment-chip.tsx" (linhas 439-503): componente + classes CSS
    - 11-PATTERNS.md — seção "attachment-panel.tsx" (linhas 507-570): componente details/summary + classes CSS
    - 11-PATTERNS.md — seção "privacy-notice.tsx" (linhas 574-605): copy LGPD pt-BR
    - apps/web/src/features/formula/components/formula-output-panel.tsx — .note-block analog para AttachmentPanel
    - apps/web/src/styles/globals.css — tokens .metadata-row span, .pro-badge, .revoked-notice (para referenciar variáveis CSS existentes)
    - 11-RESEARCH.md — SEC-01 verificação (linhas 596-611): extractedText deve ser renderizado como texto puro; SEC-03 copy (linhas 584-591)
  </read_first>
  <behavior>
    - AttachmentChip renderiza div[role=status] com aria-label contendo nome e tamanho do arquivo
    - AttachmentChip renderiza botão × com aria-label "Remover arquivo" que dispara onRemove
    - AttachmentPanel renderiza extractedText APENAS como children de elemento pre (nunca dangerouslySetInnerHTML)
    - AttachmentPanel com wasTruncated=true renderiza span com texto "extração parcial"
    - AttachmentPanel com wasTruncated=false não renderiza badge de truncagem
    - PrivacyNotice renderiza texto com "Nova conversa" em negrito (strong)
  </behavior>
  <action>
    ATTACHMENT-CHIP: Criar apps/web/src/components/app/attachment-chip.tsx com "use client". Exportar AttachmentChip({ file: File, onRemove: () => void }). Calcular sizeLabel: >1MB → "X.X MB", senão "Y KB". JSX: div[className="attachment-chip", role="status", aria-label="Arquivo anexado: {file.name}, {sizeLabel}"] com FileText icon (size=14), span.attachment-chip-name (file.name), span.attachment-chip-size (sizeLabel), button[aria-label="Remover arquivo", className="attachment-chip-remove"] com X icon (size=12). Imports: FileText, X de "lucide-react".

    ATTACHMENT-PANEL: Criar apps/web/src/components/app/attachment-panel.tsx com "use client". Exportar AttachmentPanel({ extractedText: string, wasTruncated: boolean }). State: open (false). JSX: details[className="attachment-panel note-block", open, onToggle] com summary (FileText icon + "Texto extraído do documento" + {wasTruncated ? span.attachment-truncated-badge : null}) e pre[className="attachment-panel-content", style={{ maxHeight: "200px", overflowY: "auto" }}] com {extractedText} como children (JSX text node — NUNCA dangerouslySetInnerHTML). Imports: FileText de "lucide-react"; useState de "react".

    PRIVACY-NOTICE: Criar apps/web/src/components/app/privacy-notice.tsx com "use client". Exportar PrivacyNotice(). JSX: p[className="privacy-notice", aria-live="polite"] com texto "O conteúdo do documento ficará salvo no histórico desta conversa. Para removê-lo, use " seguido de strong{"Nova conversa"} e " no topo da página." Sem props; sem state.

    CSS: Adicionar ao final de apps/web/src/styles/globals.css as seguintes classes (conforme 11-PATTERNS.md seções CSS dos 3 componentes):
    - .attachment-chip (inline-flex, gap, border, border-radius:999px, background:#f8fafc, color:var(--muted), font-size:12px, padding:4px 8px, margin-top:8px)
    - .attachment-chip-name (font-weight:650, max-width:200px, overflow:hidden, text-overflow:ellipsis, white-space:nowrap)
    - .attachment-chip-size (color:var(--muted))
    - .attachment-chip-remove (background:none, border:none, cursor:pointer, color:var(--muted), padding:0, display:flex, align-items:center)
    - .attachment-btn (background:none, border:none, cursor:pointer, color:var(--muted), padding:4px, display:flex, align-items:center, border-radius:6px)
    - .attachment-btn:disabled (opacity:0.4, cursor:not-allowed)
    - .attachment-panel summary (cursor:pointer, display:flex, align-items:center, gap:6px, font-size:13px, color:var(--muted))
    - .attachment-panel-content (margin-top:8px, font-size:12px, white-space:pre-wrap, word-break:break-word)
    - .attachment-truncated-badge (border:1px solid var(--warning), border-radius:999px, background:rgb(181 71 8/8%), color:var(--warning), font-size:11px, padding:2px 6px, margin-left:8px)
    - .privacy-notice (margin:4px 0 0, font-size:12px, color:var(--muted), line-height:1.5)
  </action>
  <verify>
    <automated>grep -c "dangerouslySetInnerHTML" apps/web/src/components/app/attachment-panel.tsx; pnpm --filter web typecheck; grep -c "attachment-chip\|attachment-btn\|privacy-notice\|attachment-panel" apps/web/src/styles/globals.css</automated>
  </verify>
  <done>grep dangerouslySetInnerHTML em attachment-panel.tsx retorna 0; typecheck sai 0; globals.css contém todas as novas classes (≥8 matches); PrivacyNotice contém "Nova conversa"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| props → AttachmentPanel | extractedText recebido do event stream do próprio usuário — pode conter HTML/JS se o documento contiver |
| input[type=file] → onFileSelect callback | Arquivo selecionado pelo usuário — validado por validateFile antes de processar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | XSS | AttachmentPanel — renderização de extractedText | mitigate | **CRÍTICO**: renderizar extractedText APENAS como JSX text node (`{extractedText}`) dentro de `<pre>`. NUNCA usar dangerouslySetInnerHTML ou innerHTML. React escapa automaticamente HTML em JSX text nodes. Verificar com grep retornando 0. |
| T-11-02-02 | Elevation of Privilege | PRO-01 gate visual — AttachmentButton disabled | accept | Gate visual é UX, não controle de segurança. O backend (Phase 10) bloqueia com 403 antes de qualquer extração. Documentado explicitamente na ação do plano. |
| T-11-02-03 | Spoofing | validateFile — tipo declarado pelo browser | accept | Backend (Phase 9/SEC-02) valida magic bytes — defesa real. validateFile no client é UX para feedback imediato. Fallback por extensão .csv/.xlsx cobre A4 de RESEARCH.md (browsers antigos). |
| T-11-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta wave (lucide-react já instalado; File/FormData são Web APIs nativas) |
</threat_model>

<verification>
- 4 arquivos de componente existem em apps/web/src/components/app/
- grep dangerouslySetInnerHTML apps/web/src/components/app/attachment-panel.tsx retorna 0 (BLOQUEANTE)
- globals.css tem as 10 novas classes CSS
- pnpm --filter web typecheck sai 0
- AttachmentButton exporta validateFile
- PrivacyNotice contém "Nova conversa" em strong
</verification>

<success_criteria>
- Componentes criados e tipados sem erros
- AttachmentPanel não usa dangerouslySetInnerHTML (SEC-01 preservado na UI)
- AttachmentButton desabilita botão quando isPro=false (PRO-01 visual)
- PrivacyNotice exibe aviso LGPD com "Nova conversa" (SEC-03)
- validateFile rejeita tipos e tamanhos inválidos com mensagens pt-BR (ATT-04)
</success_criteria>

<output>
Criar `.planning/phases/11-attachment-ui-pro-gating/11-02-SUMMARY.md` ao concluir
</output>
