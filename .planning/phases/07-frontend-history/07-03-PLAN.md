---
phase: 07-frontend-history
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - apps/web/src/app/(workspace)/workspace/page.tsx
  - apps/web/src/app/(workspace)/workspace/sql/page.tsx
  - apps/web/src/app/(workspace)/workspace/regex/page.tsx
  - apps/web/src/app/(workspace)/workspace/scripts/page.tsx
  - apps/web/src/app/(workspace)/workspace/templates/page.tsx
autonomous: true
requirements:
  - HIST-03

must_haves:
  truths:
    - "Ao abrir /workspace (Formula), a page.tsx (server component) faz prefetch dos exchanges do usuário para toolKind 'formula' antes de renderizar o tool component — histórico no primeiro paint, sem spinner (D-01)"
    - "Ao abrir /workspace/sql, a page.tsx busca exchanges para toolKind 'sql'"
    - "Ao abrir /workspace/regex, a page.tsx busca exchanges para toolKind 'regex'"
    - "Ao abrir /workspace/scripts, a page.tsx busca exchanges para toolKind 'scripts'"
    - "Ao abrir /workspace/templates, a page.tsx busca exchanges para toolKind 'templates'"
    - "Se findConversationExchanges lançar exceção (retorna []), a page ainda renderiza o tool component vazio — nenhuma exceção propagada ao usuário (D-10)"
    - "Os exchanges buscados são passados como prop initialExchanges ao tool component correspondente"
  artifacts:
    - path: "apps/web/src/app/(workspace)/workspace/page.tsx"
      provides: "Prefetch de exchanges para formula + prop initialExchanges ao FormulaTool"
    - path: "apps/web/src/app/(workspace)/workspace/sql/page.tsx"
      provides: "Prefetch de exchanges para sql + prop initialExchanges ao SqlTool"
    - path: "apps/web/src/app/(workspace)/workspace/regex/page.tsx"
      provides: "Prefetch de exchanges para regex + prop initialExchanges ao RegexTool"
    - path: "apps/web/src/app/(workspace)/workspace/scripts/page.tsx"
      provides: "Prefetch de exchanges para scripts + prop initialExchanges ao ScriptsTool"
    - path: "apps/web/src/app/(workspace)/workspace/templates/page.tsx"
      provides: "Prefetch de exchanges para templates + prop initialExchanges ao TemplateTool"
  key_links:
    - from: "apps/web/src/app/(workspace)/workspace/page.tsx"
      to: "apps/web/src/server/tools/conversation-repository.ts"
      via: "import findConversationExchanges"
      pattern: "findConversationExchanges.*formula"
    - from: "apps/web/src/features/formula/formula-tool.tsx"
      to: "apps/web/src/app/(workspace)/workspace/page.tsx"
      via: "prop initialExchanges"
      pattern: "initialExchanges"
---

<objective>
Modificar os 5 server components de page.tsx dos tools de texto para buscar os exchanges históricos do usuário no mount (server-side prefetch) e passá-los como prop `initialExchanges` ao tool component.

Purpose: Implementa HIST-03 — o histórico aparece automaticamente ao abrir o workspace sem nenhuma ação do usuário, sem spinner, sem flash de tela vazia (server prefetch garante first paint com dados). Este plano corre em paralelo com o Plano 02 (route handler + topbar) pois não compartilha arquivos.
Output: 5 server components modificados, cada um chamando findConversationExchanges com o toolKind correto e passando initialExchanges ao seu tool component.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-frontend-history/07-CONTEXT.md
@.planning/phases/07-frontend-history/07-PATTERNS.md
@.planning/phases/07-frontend-history/07-01-SUMMARY.md

<interfaces>
<!-- Contratos dos arquivos que serão modificados e do repository. -->

Estado atual de cada page.tsx (padrão idêntico nos 5):
  import { [ToolName]Tool } from "@/features/[tool]/[tool]-tool";
  import { getCachedEntitlement, getCachedUser } from "@/server/request-cache";

  export default async function [Name]Page() {
    const user = await getCachedUser();
    const entitlement = await getCachedEntitlement(user!.id);
    return <[ToolName]Tool entitlement={entitlement} />;
  }

De conversation-repository.ts (criado no Plano 01):
  export async function findConversationExchanges(
    userId: string,
    toolKind: string
  ): Promise<ConversationExchange[]>
  // Retorna [] em caso de erro — nunca lança exceção

Mapeamento toolKind string → arquivo page.tsx:
  "formula"   → apps/web/src/app/(workspace)/workspace/page.tsx
  "sql"       → apps/web/src/app/(workspace)/workspace/sql/page.tsx
  "regex"     → apps/web/src/app/(workspace)/workspace/regex/page.tsx
  "scripts"   → apps/web/src/app/(workspace)/workspace/scripts/page.tsx
  "templates" → apps/web/src/app/(workspace)/workspace/templates/page.tsx

Prop nova que cada tool component receberá:
  initialExchanges?: ConversationExchange[]
  // Os tool components ainda não aceitam essa prop — será adicionada no Plano 04
  // Neste plano, passar a prop (TypeScript aceitará como prop extra ou emitirá aviso — ok)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar prefetch de exchanges nos 5 server components de page.tsx</name>
  <files>
    apps/web/src/app/(workspace)/workspace/page.tsx,
    apps/web/src/app/(workspace)/workspace/sql/page.tsx,
    apps/web/src/app/(workspace)/workspace/regex/page.tsx,
    apps/web/src/app/(workspace)/workspace/scripts/page.tsx,
    apps/web/src/app/(workspace)/workspace/templates/page.tsx
  </files>

  <read_first>
    - apps/web/src/app/(workspace)/workspace/page.tsx — leia antes de editar; verificar imports existentes e estrutura exata do return
    - apps/web/src/app/(workspace)/workspace/sql/page.tsx — leia antes de editar
    - apps/web/src/app/(workspace)/workspace/regex/page.tsx — leia antes de editar
    - apps/web/src/app/(workspace)/workspace/scripts/page.tsx — leia antes de editar
    - apps/web/src/app/(workspace)/workspace/templates/page.tsx — leia antes de editar
    - apps/web/src/server/tools/conversation-repository.ts — confirmar nome exato e assinatura de findConversationExchanges após Plano 01
  </read_first>

  <action>
    Aplicar o mesmo delta nos 5 arquivos de page.tsx. Para cada arquivo:

    1. Adicionar import: import { findConversationExchanges } from "@/server/tools/conversation-repository";

    2. Adicionar chamada após getCachedEntitlement:
       const initialExchanges = await findConversationExchanges(user!.id, "[toolKind]");
       Onde [toolKind] é o valor string literal correto para cada page:
       - workspace/page.tsx → "formula"
       - workspace/sql/page.tsx → "sql"
       - workspace/regex/page.tsx → "regex"
       - workspace/scripts/page.tsx → "scripts"
       - workspace/templates/page.tsx → "templates"

    3. Passar a prop ao tool component:
       return <[ToolName]Tool entitlement={entitlement} initialExchanges={initialExchanges} />;

    Não modificar getCachedUser, getCachedEntitlement, entitlement prop, nem a estrutura geral do componente.
    Não adicionar try/catch na page.tsx — findConversationExchanges já silencia erros internamente e retorna [].
    Não criar nenhum arquivo novo neste plano — apenas modificar os 5 existentes.

    Nota: Os tool components ainda não aceitam `initialExchanges` formalmente (será feito no Plano 04).
    O TypeScript pode emitir erro de prop não reconhecida — é esperado e será resolvido no Plano 04.
    Planos 02 e 03 correm em paralelo, então o Plano 04 deve executar após ambos.
  </action>

  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && grep -rn "findConversationExchanges" apps/web/src/app/ | grep page.tsx</automated>
  </verify>

  <acceptance_criteria>
    - grep -rn "findConversationExchanges" apps/web/src/app/ | grep page.tsx retorna exatamente 5 linhas (uma por page.tsx)
    - grep "findConversationExchanges.*formula" em workspace/page.tsx retorna match
    - grep "findConversationExchanges.*sql" em workspace/sql/page.tsx retorna match
    - grep "findConversationExchanges.*regex" em workspace/regex/page.tsx retorna match
    - grep "findConversationExchanges.*scripts" em workspace/scripts/page.tsx retorna match
    - grep "findConversationExchanges.*templates" em workspace/templates/page.tsx retorna match
    - grep "initialExchanges" em cada page.tsx retorna match (prop sendo passada ao tool component)
    - Nenhuma das pages importa getCachedUser ou getCachedEntitlement foi removida — essas linhas permanecem
    - File Analysis (file-analysis/page.tsx) NÃO é modificado — grep "findConversationExchanges" nesse arquivo retorna nada
    - OCR (ocr/page.tsx) NÃO é modificado
  </acceptance_criteria>

  <done>5 server components (formula, sql, regex, scripts, templates) buscam exchanges com toolKind correto e passam initialExchanges ao tool component. File Analysis e OCR não são tocados. TypeScript poderá ter erro de prop temporário até o Plano 04 — é esperado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component → repository | userId vem de getCachedUser() — já autenticado pelo middleware; toolKind é string literal no código, não vem da requisição |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information Disclosure | page.tsx → findConversationExchanges | mitigate | userId da sessão autenticada (getCachedUser); toolKind é literal hardcoded, não entrada do usuário; IDOR guard no repository já garante isolamento |
| T-07-09 | Denial of Service | findConversationExchanges (server-side no load) | accept | Cap de 50 já garantido pela Phase 6; página server-rendered já faz outras chamadas async (getCachedUser, getCachedEntitlement) — custo marginal baixo |
</threat_model>

<verification>
1. grep -rn "findConversationExchanges" apps/web/src/app/ | grep page.tsx retorna 5 matches
2. Cada match usa o toolKind correto para seu route (formula, sql, regex, scripts, templates)
3. File Analysis page.tsx não modificado
4. pnpm --filter web tsc --noEmit — pode ter erros de prop temporários (resolvidos no Plano 04); erros de import de findConversationExchanges não devem existir
</verification>

<success_criteria>
Os 5 server components de tool de texto buscam exchanges no mount via findConversationExchanges e passam initialExchanges ao tool component. File Analysis permanece efêmero (D-07).
</success_criteria>

<output>
Criar `.planning/phases/07-frontend-history/07-03-SUMMARY.md` ao concluir.
</output>
