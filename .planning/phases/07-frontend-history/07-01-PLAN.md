---
phase: 07-frontend-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/server/tools/conversation-repository.ts
autonomous: true
requirements:
  - HIST-03
  - HIST-05

must_haves:
  truths:
    - "findConversationExchanges(userId, toolKind) retorna array de ConversationExchange em ordem cronológica (asc) e retorna [] em caso de erro"
    - "deleteConversationExchanges(userId, toolKind) remove todos os exchanges do usuário para aquele tool e retorna null em caso de erro sem throw"
    - "Ambas as funções aplicam IDOR guard: filtram por userId E toolKind — nunca por toolKind sozinho"
  artifacts:
    - path: "apps/web/src/server/tools/conversation-repository.ts"
      provides: "findConversationExchanges e deleteConversationExchanges exportadas"
      exports: ["findConversationExchanges", "deleteConversationExchanges"]
  key_links:
    - from: "apps/web/src/app/api/conversations/[tool]/route.ts"
      to: "apps/web/src/server/tools/conversation-repository.ts"
      via: "import deleteConversationExchanges"
      pattern: "deleteConversationExchanges"
    - from: "apps/web/src/app/(workspace)/workspace/page.tsx"
      to: "apps/web/src/server/tools/conversation-repository.ts"
      via: "import findConversationExchanges"
      pattern: "findConversationExchanges"
---

<objective>
Adicionar duas funções ao `conversation-repository.ts` existente: leitura cronológica e deleção em massa de exchanges por usuário + tool.

Purpose: Estas funções são a base de toda a Phase 7 — o prefetch server-side (HIST-03) e o hard delete "Nova conversa" (HIST-05) dependem delas. Sem elas, nenhum outro plano pode ser executado.
Output: `conversation-repository.ts` com `findConversationExchanges` e `deleteConversationExchanges` exportadas, seguindo exatamente o padrão try/catch silencioso + IDOR guard já estabelecido no arquivo.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-frontend-history/07-CONTEXT.md
@.planning/phases/07-frontend-history/07-PATTERNS.md

<interfaces>
<!-- Estado atual de conversation-repository.ts — executor deve partir daqui. -->

Exportações existentes em apps/web/src/server/tools/conversation-repository.ts:
  export async function saveConversationExchange(input: {
    userId: string;
    toolKind: string;
    mode: string;
    platform?: string;
    dialect?: string;
    userPrompt: string;
    assistantPayload: unknown;
  }): Promise<ConversationExchange | null>

Schema do model alvo (prisma/schema.prisma):
  model ConversationExchange {
    id               String   @id @default(cuid())
    userId           String
    toolKind         String
    mode             String
    platform         String?
    dialect          String?
    userPrompt       String   @db.Text
    assistantPayload Json     @db.Json
    createdAt        DateTime @default(now())
    @@index([userId, toolKind, createdAt])
  }

Padrão IDOR guard do file-repository.ts (análogo exato):
  export async function findUploadedFileByIdAndUser(id: string, userId: string) {
    try {
      return await prisma.uploadedFile.findFirst({
        where: { id, userId }
      });
    } catch {
      console.warn("UploadedFile lookup failed.");
      return null;
    }
  }

Padrão de leitura com ordenação (file-repository.ts):
  const messages = await prisma.chatMessage.findMany({
    where: { uploadedFileId },
    orderBy: { createdAt: "desc" },
    take: limit
  });
  return messages.reverse();

Padrão de deleção em massa já presente no arquivo (dentro do saveConversationExchange):
  await tx.conversationExchange.deleteMany({
    where: { id: { in: toDelete.map((r) => r.id) } },
  });
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Adicionar findConversationExchanges e deleteConversationExchanges ao repository</name>
  <files>apps/web/src/server/tools/conversation-repository.ts</files>

  <read_first>
    - apps/web/src/server/tools/conversation-repository.ts — leia o arquivo inteiro antes de qualquer edição; as novas funções são ADICIONADAS após saveConversationExchange, não substituem nada
    - apps/web/src/server/file-analysis/file-repository.ts — padrão IDOR guard e try/catch silencioso a replicar
    - prisma/schema.prisma — confirmar colunas e índices de ConversationExchange antes de escrever as queries
  </read_first>

  <behavior>
    - findConversationExchanges("user-1", "formula") retorna array com todos os exchanges em ordem createdAt asc
    - findConversationExchanges("user-1", "formula") quando DB lança exceção retorna [] sem re-throw
    - findConversationExchanges("user-1", "formula") não retorna exchanges de "user-2" com toolKind "formula" (IDOR)
    - findConversationExchanges("user-1", "formula") não retorna exchanges de "user-1" com toolKind "sql" (IDOR)
    - deleteConversationExchanges("user-1", "formula") remove apenas os exchanges de user-1+formula
    - deleteConversationExchanges("user-1", "formula") quando DB lança exceção retorna null sem re-throw
  </behavior>

  <action>
    Adicionar ao final de apps/web/src/server/tools/conversation-repository.ts, após a função saveConversationExchange, as seguintes duas funções exportadas:

    1. findConversationExchanges(userId: string, toolKind: string): usa prisma.conversationExchange.findMany com where { userId, toolKind } (IDOR: ambos obrigatórios), orderBy { createdAt: "asc" }. Wrap em try/catch; em caso de erro: console.warn("ConversationExchange read skipped.", err) e return []. Não adicionar take/limit — retornar todos (D-03: já limitado ao cap de 50 pela Phase 6).

    2. deleteConversationExchanges(userId: string, toolKind: string): usa prisma.conversationExchange.deleteMany com where { userId, toolKind } (IDOR: ambos obrigatórios). Wrap em try/catch; em caso de erro: console.warn("ConversationExchange delete skipped.", err) e return null. Retornar o resultado do deleteMany em caso de sucesso.

    Não adicionar "server-only" — o arquivo já não tem essa diretiva (é importado por server components que já aplicam o guard no nível da page). Não modificar saveConversationExchange nem nenhum import existente.
  </action>

  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && grep -n "export async function findConversationExchanges" apps/web/src/server/tools/conversation-repository.ts && grep -n "export async function deleteConversationExchanges" apps/web/src/server/tools/conversation-repository.ts && grep -c "userId, toolKind" apps/web/src/server/tools/conversation-repository.ts</automated>
  </verify>

  <acceptance_criteria>
    - grep "export async function findConversationExchanges" retorna match no arquivo
    - grep "export async function deleteConversationExchanges" retorna match no arquivo
    - grep -c "userId, toolKind" retorna >= 2 (uma ocorrência por função — IDOR guard presente em ambas)
    - grep "orderBy.*createdAt.*asc" ou "orderBy: { createdAt: \"asc\" }" retorna match em findConversationExchanges
    - Nenhuma das duas funções contém re-throw (sem "throw" após o catch)
    - O arquivo compila sem erros TypeScript: pnpm --filter web tsc --noEmit retorna exit 0
  </acceptance_criteria>

  <done>findConversationExchanges e deleteConversationExchanges exportadas, IDOR guard em ambas (where: { userId, toolKind }), try/catch silencioso com console.warn, sem re-throw, TypeScript compila limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component → repository | Dados vêm de getCachedUser() — userId já autenticado; toolKind vem de string literal no código da page |
| route handler → repository | DELETE recebe toolKind via params de URL (não confiável); userId vem da sessão autenticada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | findConversationExchanges | mitigate | IDOR guard: where { userId, toolKind } — nunca buscar por toolKind sozinho; userId sempre vem da sessão, não da requisição |
| T-07-02 | Tampering | deleteConversationExchanges | mitigate | IDOR guard: where { userId, toolKind } — delete não pode afetar exchanges de outro usuário; toolKind validado no route handler antes de chegar aqui |
| T-07-03 | Denial of Service | findConversationExchanges (sem limit) | accept | Cap de 50 registros já garantido pelo saveConversationExchange (Phase 6); findMany sem take é seguro neste domínio |
</threat_model>

<verification>
1. pnpm --filter web tsc --noEmit passa sem erros
2. grep confirma ambas as funções exportadas com IDOR guard (where: { userId, toolKind })
3. Sem "throw" dentro dos blocos catch de nenhuma das duas funções
</verification>

<success_criteria>
conversation-repository.ts exporta findConversationExchanges e deleteConversationExchanges, ambas com IDOR guard, try/catch silencioso com console.warn, e TypeScript compila limpo.
</success_criteria>

<output>
Criar `.planning/phases/07-frontend-history/07-01-SUMMARY.md` ao concluir.
</output>
