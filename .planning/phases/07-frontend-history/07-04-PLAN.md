---
phase: 07-frontend-history
plan: 04
type: execute
wave: 3
depends_on:
  - "07-02"
  - "07-03"
files_modified:
  - apps/web/src/features/formula/formula-tool.tsx
  - apps/web/src/features/sql/sql-tool.tsx
  - apps/web/src/features/regex/regex-tool.tsx
  - apps/web/src/features/scripts/scripts-tool.tsx
  - apps/web/src/features/template/template-tool.tsx
autonomous: true
requirements:
  - HIST-03
  - HIST-05

must_haves:
  truths:
    - "Ao abrir o workspace de Formula com histórico salvo, os exchanges aparecem renderizados no .chat-thread sem nenhuma ação do usuário"
    - "Os seletores de plataforma, dialeto e modo restauram os valores do exchange mais recente (D-08)"
    - "Ao clicar 'Apagar histórico' no Topbar e confirmar, o chat-thread limpa completamente e o input fica pronto para nova conversa"
    - "O mesmo comportamento (seed + restore + clear) funciona nos 5 tools: Formula, SQL, Regex, Scripts, Template"
    - "O primeiro uso (sem histórico) abre o workspace exatamente como antes — vazio com o input pronto (D-09)"
    - "Cada exchange renderizado no histórico exibe o userText do prompt original e o output do assistantPayload salvo"
  artifacts:
    - path: "apps/web/src/features/formula/formula-tool.tsx"
      provides: "FormulaTool aceita initialExchanges, faz seed de exchanges state e seletores, expõe onNewConversation para Topbar"
    - path: "apps/web/src/features/sql/sql-tool.tsx"
      provides: "SqlTool com seed de exchanges e dialect, onNewConversation"
    - path: "apps/web/src/features/regex/regex-tool.tsx"
      provides: "RegexTool com seed de exchanges e mode, onNewConversation"
    - path: "apps/web/src/features/scripts/scripts-tool.tsx"
      provides: "ScriptsTool com seed de exchanges e scriptType, onNewConversation"
    - path: "apps/web/src/features/template/template-tool.tsx"
      provides: "TemplateTool com seed de exchanges, onNewConversation"
  key_links:
    - from: "apps/web/src/features/formula/formula-tool.tsx"
      to: "apps/web/src/components/app/topbar.tsx"
      via: "prop onNewConversation passada ao Topbar"
      pattern: "onNewConversation"
    - from: "apps/web/src/features/formula/formula-tool.tsx"
      to: "apps/web/src/app/(workspace)/workspace/page.tsx"
      via: "prop initialExchanges recebida da page"
      pattern: "initialExchanges"
---

<objective>
Modificar os 5 tool components para aceitar `initialExchanges` do server prefetch, fazer seed do estado de exchanges e dos seletores ativos, e expor `onNewConversation` callback para o Topbar limpar o estado local após hard delete.

Purpose: Fecha o ciclo completo de HIST-03 (histórico visível no mount) e HIST-05 (Nova conversa limpa o thread). Após este plano, a Phase 7 está completa: prefetch funciona, exchanges restauram, seletores restauram, "Nova conversa" limpa.
Output: 5 tool components modificados com seed de exchanges, restauração de seletores a partir do exchange mais recente, e callback onNewConversation wired ao Topbar.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-frontend-history/07-CONTEXT.md
@.planning/phases/07-frontend-history/07-PATTERNS.md
@.planning/phases/07-frontend-history/07-01-SUMMARY.md
@.planning/phases/07-frontend-history/07-02-SUMMARY.md
@.planning/phases/07-frontend-history/07-03-SUMMARY.md

<interfaces>
<!-- Contratos necessários para implementar o seed em cada tool component. -->

Tipo ConversationExchange retornado por findConversationExchanges (de prisma/schema.prisma + repository):
  {
    id: string;
    userId: string;
    toolKind: string;
    mode: string;
    platform: string | null;
    dialect: string | null;
    userPrompt: string;
    assistantPayload: unknown;  // Prisma.JsonValue — deserializado como object
    createdAt: Date;
  }

FormulaTool — estado atual relevante (formula-tool.tsx):
  const [mode, setMode] = useState<FormulaMode>("generate");
  const [platform, setPlatform] = useState<FormulaPlatform>("excel");
  const [formulaLanguage, setFormulaLanguage] = useState<FormulaLanguage>("pt-BR");
  const [exchanges, setExchanges] = useState<FormulaExchange[]>([]);
  type FormulaExchange = {
    id: string; userText: string; status: "complete" | "error";
    result: FormulaCompletePayload | null; metadata: FormulaMetadata | null;
    warnings: string[]; error: string;
  }
  Props atuais: { entitlement: UserEntitlement }
  O Topbar é renderizado no layout (workspace layout) — não dentro do FormulaTool;
  ver nota abaixo sobre como wirear onNewConversation.

SqlTool — estado atual relevante (sql-tool.tsx):
  const [dialect, setDialect] = useState<SqlDialect>("postgresql");
  const [exchanges, setExchanges] = useState<SqlExchange[]>([]);
  type SqlExchange = { id; userText; status; result: SqlGenerateResponse|null; metadata: SqlMetadata|null; warnings; error }

RegexTool — estado atual relevante (regex-tool.tsx):
  const [mode, setMode] = useState<RegexMode>("generate");
  const [exchanges, setExchanges] = useState<RegexExchange[]>([]);
  type RegexExchange = { id; userText; status; result: RegexCompletePayload|null; metadata: RegexMetadata|null; warnings; error }

ScriptsTool — estado atual relevante (scripts-tool.tsx):
  const [scriptType, setScriptType] = useState<ScriptType>("vba");
  const [exchanges, setExchanges] = useState<ScriptExchange[]>([]);
  type ScriptExchange = { id; userText; status; result: ScriptGenerateResponse|null; metadata: ScriptMetadata|null; warnings; error }

TemplateTool — estado atual relevante (template-tool.tsx):
  // Sem seletores de domínio — apenas text e exchanges
  const [exchanges, setExchanges] = useState<TemplateExchange[]>([]);
  type TemplateExchange = { id; userText; status; result: TemplateGenerateResponse|null; metadata: TemplateMetadata|null; warnings; error }

NOTA IMPORTANTE — onde mora o Topbar:
  O Topbar vive no workspace layout, não dentro de cada tool component.
  A prop onNewConversation precisa ser passada do tool component para cima ao Topbar.
  Opção aprovada (D-11 Claude's Discretion): cada page.tsx passa onNewConversation como prop ao tool component;
  o tool component implementa handleNewConversation e passa ao Topbar via prop.
  MAS o Topbar está no layout, não na page. Solução prática: verificar como o Topbar recebe suas props
  (via layout.tsx ou via page.tsx) antes de implementar — ler o layout do workspace.
  Se o Topbar não recebe props do tool component, a alternativa é Context API ou passar
  toolKind direto ao Topbar via page.tsx e deixar o Topbar chamar DELETE, com o tool
  component limpando via prop onNewConversation recebida da page.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 0: Investigar como Topbar e tool components são wired no layout do workspace</name>
  <files>apps/web/src/app/(workspace)/layout.tsx</files>

  <read_first>
    - apps/web/src/app/(workspace)/layout.tsx — LEIA PRIMEIRO; entender como Topbar recebe props e como o tool component é renderizado junto ao Topbar
    - apps/web/src/app/(workspace)/workspace/page.tsx — confirmar a relação entre page e layout
  </read_first>

  <action>
    Esta tarefa é de investigação e configuração — não escreve código de lógica.

    Ler apps/web/src/app/(workspace)/layout.tsx para determinar:
    1. O Topbar é renderizado no layout com props estáticas ou recebe props dinâmicas?
    2. Como user, entitlement e supportLinks chegam ao Topbar (via layout server component, via contexto, etc.)?
    3. O layout tem acesso ao toolKind do workspace atual?

    Com base na resposta, determinar a estratégia de wiring para onNewConversation:

    **Estratégia A (preferida se layout é server component e Topbar é filho direto):**
    Cada page.tsx passa toolKind como prop ao tool component. O tool component implementa handleNewConversation (limpar estado local). O Topbar recebe toolKind + onNewConversation como props via layout — mas como layout é server, precisaria de um Client wrapper.

    **Estratégia B (mais simples se layout já faz tudo server-side):**
    Cada page.tsx passa toolKind ao tool component como prop. O tool component implementa handleNewConversation e expõe via um mecanismo de elevação (callback prop ou context). O Topbar fica no layout e recebe toolKind do layout detectando a rota atual (usePathname).

    **Estratégia C (mais direta e sem refactor do layout):**
    O Topbar recebe toolKind via prop toolKind que o layout detecta da rota atual (usando usePathname em um client component wrapper). onNewConversation é implementado no tool component e comunicado via um evento customizado ou Context.

    Após ler o layout, escolher a estratégia mais simples que não introduza refactor grande. Documentar a escolha no SUMMARY. Se a estratégia C for necessária, criar o client wrapper mínimo necessário.

    Nota: se o layout já é um client component com acesso a usePathname, a solução é simples. Se for server component puro, escolher a estratégia de menor impacto.
  </action>

  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && cat apps/web/src/app/\(workspace\)/layout.tsx | head -60</automated>
  </verify>

  <acceptance_criteria>
    - O arquivo layout.tsx foi lido e a estratégia de wiring foi determinada
    - A tarefa documenta qual estratégia foi escolhida e por quê, no SUMMARY
    - Se um novo arquivo de wrapper for necessário, ele é criado nesta tarefa antes das demais
  </acceptance_criteria>

  <done>Estratégia de wiring entre Topbar e tool components determinada; qualquer arquivo de infra mínimo criado se necessário.</done>
</task>

<task type="auto">
  <name>Tarefa 1: Seed de exchanges e seletores nos 5 tool components + wiring onNewConversation</name>
  <files>
    apps/web/src/features/formula/formula-tool.tsx,
    apps/web/src/features/sql/sql-tool.tsx,
    apps/web/src/features/regex/regex-tool.tsx,
    apps/web/src/features/scripts/scripts-tool.tsx,
    apps/web/src/features/template/template-tool.tsx
  </files>

  <read_first>
    - apps/web/src/features/formula/formula-tool.tsx — leia inteiro antes de editar; padrão canônico para todos os outros
    - apps/web/src/features/sql/sql-tool.tsx — leia inteiro antes de editar
    - apps/web/src/features/regex/regex-tool.tsx — leia inteiro antes de editar
    - apps/web/src/features/scripts/scripts-tool.tsx — leia inteiro antes de editar
    - apps/web/src/features/template/template-tool.tsx — leia inteiro antes de editar
    - apps/web/src/server/tools/conversation-repository.ts — confirmar shape do ConversationExchange retornado (especialmente assistantPayload)
    - .planning/phases/07-frontend-history/07-PATTERNS.md — padrão de seed de exchanges e restauração de seletores (seção "Tool components")
    - Resultado da Tarefa 0 deste plano — estratégia de wiring onNewConversation determinada
  </read_first>

  <action>
    Aplicar o seguinte delta em cada um dos 5 tool components. FormulaTool é o padrão canônico — os outros seguem a mesma estrutura adaptando os tipos e seletores específicos de cada tool.

    **MODIFICAÇÕES EM CADA TOOL COMPONENT:**

    1. Definir tipo local PersistedExchange (ou importar ConversationExchange se disponível no @tabelin/shared):
       type PersistedExchange = {
         id: string;
         userPrompt: string;
         assistantPayload: unknown;
         mode: string;
         platform: string | null;
         dialect: string | null;
         createdAt: Date;
       }

    2. Adicionar prop initialExchanges à assinatura do componente:
       { entitlement, initialExchanges = [] }: { entitlement: UserEntitlement; initialExchanges?: PersistedExchange[] }

    3. Substituir useState de exchanges por inicialização lazy com map dos dados persistidos:
       const [exchanges, setExchanges] = useState<[Tool]Exchange[]>(() =>
         initialExchanges.map((ex) => ({
           id: ex.id,
           userText: ex.userPrompt,
           status: "complete" as const,
           result: ex.assistantPayload as [ToolCompletePayload] ?? null,
           metadata: null,     // metadata não é persistido — usar null
           warnings: [],       // warnings não persistidos — usar []
           error: "",
         }))
       );

    4. Restaurar seletores a partir do exchange mais recente (D-08):
       const lastEx = initialExchanges.length > 0
         ? initialExchanges[initialExchanges.length - 1]
         : undefined;

       Adaptar por tool:
       - FormulaTool: useState<FormulaPlatform>((lastEx?.platform as FormulaPlatform) ?? "excel"), useState<FormulaLanguage>((lastEx?.dialect as FormulaLanguage) ?? "pt-BR"), useState<FormulaMode>((lastEx?.mode as FormulaMode) ?? "generate")
       - SqlTool: useState<SqlDialect>((lastEx?.dialect as SqlDialect) ?? "postgresql") — sem platform ou mode seletor
       - RegexTool: useState<RegexMode>((lastEx?.mode as RegexMode) ?? "generate") — sem platform ou dialect
       - ScriptsTool: useState<ScriptType>((lastEx?.platform as ScriptType) ?? "vba") — scriptType é salvo em `platform` ou em `mode`? Verificar conversation-repository.ts para confirmar qual campo o scripts route handler usa — ler apps/web/src/app/api/tools/scripts/generate/route.ts para confirmar antes de implementar
       - TemplateTool: sem seletores de domínio — apenas manter mode default "generate" implícito

    5. Implementar handleNewConversation:
       function handleNewConversation() {
         setExchanges([]);
         setSubmittedText("");
         // Se stream tem reset, chamar — verificar o hook do tool antes de implementar
       }

    6. Wirear onNewConversation ao Topbar usando a estratégia determinada na Tarefa 0:
       - Se o Topbar aceitar onNewConversation como prop via page → passar de volta pela page
       - Se a estratégia for Context: usar o provider criado na Tarefa 0
       - Se a estratégia for usePathname no Topbar com toolKind e o Topbar faz o fetch: o tool component apenas expõe handleNewConversation via prop recebida da page (onNewConversation prop)
       Em todos os casos, adicionar prop onNewConversation?: () => void ao tool component e chamar no handleNewConversation.

    **IMPORTANTE — rendering fiel do assistantPayload:**
    O assistantPayload salvo pela Phase 6 tem a estrutura completa do output do LLM (ex: FormulaCompletePayload com formula/explanation). Fazer cast direto: result: ex.assistantPayload as FormulaCompletePayload ?? null. Não re-chamar o LLM. O output panel existente renderiza corretamente — exchanges históricos entram com status "complete" e o painel renderiza o result normalmente.

    **IMPORTANTE — submittedText inicial:**
    Não pré-popular submittedText — ele representa o exchange em progresso no streaming atual. Deixar como useState("") como hoje.

    **NÃO MODIFICAR:**
    - A função submit() — deve permanecer exatamente igual
    - A lógica de streaming e o hook do tool
    - A renderização do exchange em progresso (submittedText + stream.status)
    - A lógica de quota/entitlement
  </action>

  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && grep -rn "initialExchanges" apps/web/src/features/ | grep -v ".test." | grep "tool.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - grep -rn "initialExchanges" apps/web/src/features/ | grep tool.tsx retorna 5 matches (uma por tool)
    - grep "initialExchanges.map" em cada tool retorna match (seed lazy do estado)
    - grep "lastEx" em cada tool retorna match (restauração de seletores)
    - grep "handleNewConversation" em cada tool retorna match
    - grep "setExchanges\(\[\]\)" em cada tool retorna match (limpar estado no handleNewConversation)
    - grep "onNewConversation" em cada tool retorna match (prop adicionada)
    - grep "status.*complete.*as const" retorna match (exchanges históricos com status correto)
    - pnpm --filter web tsc --noEmit retorna exit 0 (todos os 5 tools + pages + route handler compilam limpos)
    - pnpm --filter web build retorna exit 0 (build de produção passa)
  </acceptance_criteria>

  <done>5 tool components aceitam initialExchanges e fazem seed do chat-thread + restauração de seletores no mount. handleNewConversation limpa o estado e é wired ao Topbar via onNewConversation prop. TypeScript e build de produção passam limpos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Fluxo completo da Phase 7:
    1. Plano 01: findConversationExchanges + deleteConversationExchanges no repository
    2. Plano 02: DELETE /api/conversations/[tool] + botão "Nova conversa" no Topbar com popover
    3. Plano 03: 5 pages com prefetch server-side de exchanges
    4. Plano 04 Tarefa 0: wiring Topbar ↔ tool components determinado
    5. Plano 04 Tarefa 1: 5 tool components com seed de exchanges + seletores + onNewConversation
  </what-built>

  <how-to-verify>
    1. Iniciar o servidor local: pnpm dev (na raiz do monorepo)

    2. HIST-03 — Histórico carrega automaticamente:
       a. Abrir /workspace (Formula) — verificar se exchanges anteriores (se houver) aparecem no chat-thread sem nenhuma ação
       b. Se não tiver histórico ainda: enviar 2-3 mensagens no Formula, fechar a aba, reabrir /workspace — as mensagens devem aparecer
       c. Repetir para /workspace/sql: enviar mensagem, fechar, reabrir — histórico deve aparecer
       d. Verificar que /workspace/file-analysis NÃO mostra histórico (permanece efêmero por D-07)

    3. HIST-03 — Restauração de seletores:
       a. No Formula, trocar plataforma para "Google Sheets" e dialeto para "EN-US", enviar uma mensagem
       b. Fechar e reabrir /workspace — verificar que os seletores mostram "Google Sheets" e "EN-US" (não os defaults "Excel" / "pt-BR")

    4. HIST-05 — Nova conversa:
       a. Abrir /workspace com histórico existente
       b. Clicar "Nova conversa" no topbar — deve abrir popover com "Apagar o histórico deste tool? Esta ação não pode ser desfeita."
       c. Clicar "Cancelar" — popover deve fechar, histórico permanece
       d. Clicar "Nova conversa" novamente → "Apagar histórico" — chat deve limpar
       e. Fechar e reabrir /workspace — deve abrir vazio (confirma hard delete)

    5. Acessibilidade básica:
       a. Com popover aberto, pressionar Esc — deve fechar sem deletar
       b. Verificar que foco retorna ao botão "Nova conversa" após fechar

    6. Estado de borda — primeiro uso:
       a. Se possível testar com conta nova (sem histórico): workspace deve abrir vazio com input pronto, sem mensagem especial nem spinner
  </how-to-verify>

  <resume-signal>
    Digite "aprovado" se todos os cenários funcionam corretamente, ou descreva os problemas encontrados para que possam ser corrigidos.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx (server) → tool component (client) | initialExchanges vem do servidor autenticado — dados do usuário correto |
| tool component → topbar → DELETE endpoint | toolKind é string prop do tool component — mesma string hardcoded na page |
| assistantPayload (JSON deserializado) → render | Cast de unknown para tipo de payload — sem sanitização HTML necessária pois o output panel já lida com tipos conhecidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Information Disclosure | initialExchanges seed no client component | accept | Dados já foram autorizados pelo repository (IDOR guard no Plano 01); passagem server→client component via RSC é segura; exchanges são do próprio usuário |
| T-07-11 | Tampering | assistantPayload cast (unknown → ToolPayload) | accept | Dados vêm do próprio banco do usuário (salvos pela Phase 6); cast não executa código — apenas tipagem TypeScript; output panels já lidam com dados malformados via null checks |
| T-07-12 | Information Disclosure | handleNewConversation via onNewConversation prop | accept | Callback não expõe dados — apenas limpa estado local do cliente; nenhum dado sensível transitado |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo instalado neste plano; slopcheck não aplicável |
</threat_model>

<verification>
1. pnpm --filter web tsc --noEmit retorna exit 0
2. pnpm --filter web build retorna exit 0
3. Abrir /workspace com histórico existente → exchanges aparecem sem ação do usuário
4. Seletores restauram do exchange mais recente
5. "Nova conversa" + confirmar → chat limpa e reload do workspace abre vazio
6. File Analysis não mostra histórico
</verification>

<success_criteria>
5 tool components renderizam histórico persistido automaticamente no mount, restauram seletores do exchange mais recente, e limpam o thread via "Nova conversa" com hard delete confirmado. pnpm build passa limpo. Checkpoint humano aprovado.
</success_criteria>

<output>
Criar `.planning/phases/07-frontend-history/07-04-SUMMARY.md` ao concluir.
</output>
