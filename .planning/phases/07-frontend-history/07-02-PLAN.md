---
phase: 07-frontend-history
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - apps/web/src/app/api/conversations/[tool]/route.ts
  - apps/web/src/components/app/topbar.tsx
autonomous: true
requirements:
  - HIST-05

must_haves:
  truths:
    - "DELETE /api/conversations/[tool] usa rota genérica por enum com IDOR guard (userId + toolKind) e sem cookie de sessão retorna 401 (D-11)"
    - "DELETE /api/conversations/[tool] com toolKind inválido (ex: 'file-analysis' — fora do histórico por privacidade, 'xpto') retorna 400 (D-07)"
    - "DELETE /api/conversations/formula autenticado apaga os exchanges do usuário autenticado e retorna { ok: true } com status 200"
    - "O botão 'Nova conversa' aparece no topbar quando toolKind prop está presente (D-06)"
    - "Clicar em 'Nova conversa' abre popover de confirmação com dois botões: 'Apagar histórico' (destructive) e 'Cancelar' — delete permanente exige confirmação (D-05)"
    - "Clicar 'Cancelar' ou pressionar Esc fecha o popover sem disparar DELETE"
    - "Clicar 'Apagar histórico' chama DELETE /api/conversations/[toolKind], invoca onNewConversation callback e fecha o popover"
  artifacts:
    - path: "apps/web/src/app/api/conversations/[tool]/route.ts"
      provides: "DELETE handler com auth + enum guard + IDOR"
      exports: ["DELETE"]
    - path: "apps/web/src/components/app/topbar.tsx"
      provides: "Topbar com prop toolKind opcional e botão Nova conversa"
      exports: ["Topbar"]
  key_links:
    - from: "apps/web/src/components/app/topbar.tsx"
      to: "apps/web/src/app/api/conversations/[tool]/route.ts"
      via: "fetch DELETE /api/conversations/[toolKind]"
      pattern: "fetch.*api/conversations"
    - from: "apps/web/src/features/formula/formula-tool.tsx"
      to: "apps/web/src/components/app/topbar.tsx"
      via: "prop onNewConversation callback"
      pattern: "onNewConversation"
---

<objective>
Criar o endpoint DELETE /api/conversations/[tool] (hard delete com auth + IDOR + enum guard) e adicionar o botão "Nova conversa" com popover de confirmação ao Topbar.

Purpose: Implementa HIST-05 — o usuário pode limpar o histórico de um tool individual. O route handler é a contraparte server-side; o Topbar é o ponto de entrada na UI. Ambos pertencem ao mesmo fluxo "Nova conversa" e não têm conflito de arquivos com o Plano 03 (pages).
Output: Route handler DELETE em `/api/conversations/[tool]/route.ts` e Topbar com `toolKind?: string` + `onNewConversation?: () => void` props e popover de confirmação acessível.
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-frontend-history/07-CONTEXT.md
@.planning/phases/07-frontend-history/07-PATTERNS.md
@.planning/phases/07-frontend-history/07-UI-SPEC.md
@.planning/phases/07-frontend-history/07-01-SUMMARY.md

<interfaces>
<!-- Contratos necessários para este plano. -->

De conversation-repository.ts (criado no Plano 01):
  export async function deleteConversationExchanges(
    userId: string,
    toolKind: string
  ): Promise<Prisma.BatchPayload | null>

De apps/web/src/server/auth/session.ts (padrão existente — extraído dos route handlers de formula/generate):
  export function getSessionFromCookieHeader(cookie: string | null): SessionUser | null

De apps/web/src/components/app/topbar.tsx (estado atual):
  export function Topbar({
    user,
    entitlement,
    supportLinks,
  }: {
    user: SessionUser;
    entitlement: UserEntitlement;
    supportLinks: SupportLinks;
  })
  // Estado interno existente: const [showAccountMenu, setShowAccountMenu] = useState(false)
  // Estrutura JSX: <header className="topbar"> > <div className="topbar-actions"> > <div className="account-menu-container">

Enum de toolKinds válidos (extraído dos route handlers existentes):
  const VALID_TOOL_KINDS = ["formula", "sql", "regex", "scripts", "template"] as const;
  // File Analysis é excluído por D-07 (histórico efêmero)

Padrão de autenticação em route handlers (formula/generate/route.ts):
  const user = getSessionFromCookieHeader(request.headers.get("cookie"));
  if (!user) {
    return NextResponse.json({ error: "Autenticacao obrigatoria." }, { status: 401 });
  }

Classes CSS existentes para reuso (globals.css):
  .ghost-button — border 1px solid var(--border), bg #fff, color var(--text), hover usa --primary
  .account-menu-container — position: relative
  .account-menu — position: absolute; top: calc(100% + 8px); right: 0; z-index: 10; width: 220px;
    border 1px solid var(--border); border-radius: 6px; background: var(--surface);
    box-shadow: 0 8px 24px rgb(15 23 42 / 12%); padding: 8px
  CSS token --destructive: #b42318
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar DELETE /api/conversations/[tool]/route.ts</name>
  <files>apps/web/src/app/api/conversations/[tool]/route.ts</files>

  <read_first>
    - apps/web/src/app/api/tools/formula/generate/route.ts — padrão de auth (getSessionFromCookieHeader), NextResponse, estrutura do handler; aplicar o mesmo padrão aqui
    - apps/web/src/server/tools/conversation-repository.ts — confirmar assinatura de deleteConversationExchanges após Plano 01
    - apps/web/src/server/auth/session.ts — verificar nome exato da função e tipo SessionUser
    - .planning/phases/06-persistence-layer/06-CONTEXT.md — D-09 confirma padrão /api/conversations/[tool]
  </read_first>

  <behavior>
    - DELETE sem cookie retorna 401 com body { error: "Autenticacao obrigatoria." }
    - DELETE com params.tool = "file-analysis" retorna 400 com body { error: "Tool invalido." }
    - DELETE com params.tool = "xpto" retorna 400 com body { error: "Tool invalido." }
    - DELETE com params.tool = "formula" e sessão válida chama deleteConversationExchanges(user.id, "formula") e retorna 200 { ok: true }
    - DELETE com params.tool = "sql" e sessão válida chama deleteConversationExchanges(user.id, "sql") e retorna 200 { ok: true }
    - Se deleteConversationExchanges retorna null (erro silencioso), o handler ainda retorna 200 { ok: true } — falha silenciosa não expõe erro ao cliente
  </behavior>

  <action>
    Criar o arquivo apps/web/src/app/api/conversations/[tool]/route.ts. O diretório [tool] ainda não existe — criá-lo.

    Estrutura do handler:
    1. Importar NextResponse de "next/server", getSessionFromCookieHeader de "@/server/auth/session", deleteConversationExchanges de "@/server/tools/conversation-repository".
    2. Declarar VALID_TOOL_KINDS como readonly tuple: ["formula", "sql", "regex", "scripts", "template"]. Declarar isValidToolKind(value: string): value is ToolKind.
    3. Exportar async function DELETE(request: Request, { params }: { params: Promise<{ tool: string }> }) — usar await params (Next.js 15 dynamic params são Promise).
    4. Auth guard: getSessionFromCookieHeader(request.headers.get("cookie")); retornar 401 se null.
    5. Enum guard: isValidToolKind(toolKind) — retornar 400 se inválido. toolKind vem de (await params).tool.
    6. Chamar deleteConversationExchanges(user.id, toolKind) — IDOR garantido no repository (userId + toolKind).
    7. Retornar NextResponse.json({ ok: true }, { status: 200 }) independente do resultado do delete (falha silenciosa — padrão D-10).
    8. Wrap geral em try/catch: retornar 500 { error: "Erro interno." } em caso de exceção inesperada.

    Não criar GET endpoint — D-02 confirma que prefetch server-side torna GET desnecessário nesta fase.
  </action>

  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && grep -n "export async function DELETE" "apps/web/src/app/api/conversations/[tool]/route.ts" && grep -n "status: 401" "apps/web/src/app/api/conversations/[tool]/route.ts" && grep -n "status: 400" "apps/web/src/app/api/conversations/[tool]/route.ts" && grep -n "VALID_TOOL_KINDS" "apps/web/src/app/api/conversations/[tool]/route.ts"</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo existe em apps/web/src/app/api/conversations/[tool]/route.ts
    - grep "export async function DELETE" retorna match
    - grep "status: 401" retorna match (auth guard)
    - grep "status: 400" retorna match (enum guard)
    - grep "VALID_TOOL_KINDS" inclui os 5 tools (formula, sql, regex, scripts, template) e NÃO inclui "file-analysis"
    - grep "deleteConversationExchanges" retorna match (função chamada)
    - pnpm --filter web tsc --noEmit retorna exit 0
  </acceptance_criteria>

  <done>DELETE /api/conversations/[tool] criado com auth 401, enum guard 400 para toolKinds inválidos, IDOR via repository, resposta 200 { ok: true } em sucesso, TypeScript compila limpo.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Adicionar botão "Nova conversa" com popover de confirmação ao Topbar</name>
  <files>apps/web/src/components/app/topbar.tsx</files>

  <read_first>
    - apps/web/src/components/app/topbar.tsx — leia o arquivo inteiro antes de editar; as props e o JSX existentes devem ser preservados integralmente
    - apps/web/src/styles/globals.css — confirmar nomes das classes .ghost-button, .account-menu-container, .account-menu, e token --destructive antes de usar
    - .planning/phases/07-frontend-history/07-UI-SPEC.md — spec de interação do popover (acessibilidade: role="dialog", aria-label, focus trap, Esc, focus return, aria-expanded, aria-haspopup="dialog")
    - .planning/phases/07-frontend-history/07-PATTERNS.md — estrutura JSX alvo do botão (copiar estrutura comentada lá)
  </read_first>

  <action>
    Modificar apps/web/src/components/app/topbar.tsx para:

    1. Adicionar duas props opcionais à assinatura do componente Topbar:
       - toolKind?: string — quando presente, ativa o botão "Nova conversa"
       - onNewConversation?: () => void — callback chamado após delete bem-sucedido para limpar o estado local do tool component

    2. Adicionar estado interno: const [showNewConvPopover, setShowNewConvPopover] = useState(false)

    3. Implementar handleDeleteHistory: função async que faz fetch(`/api/conversations/${toolKind}`, { method: "DELETE" }), chama onNewConversation?.(), e chama setShowNewConvPopover(false). Não await o fetch de forma bloqueante para UX — executar e fechar o popover.

    4. Implementar acessibilidade do popover via useEffect: fechar com Esc (keydown listener), fechar com click-outside (mousedown listener verificando se o click foi fora do container). Retornar foco ao trigger button após fechar (usando ref no botão trigger). Estes listeners devem ser registrados apenas quando showNewConvPopover for true e removidos no cleanup.

    5. No JSX, dentro de .topbar-actions e ANTES do .account-menu-container existente (para manter a conta do usuário à direita), renderizar condicionalmente quando toolKind está presente:
       - Container com className="account-menu-container" e ref para click-outside
       - Botão trigger com className="ghost-button", texto "Nova conversa", onClick toggle showNewConvPopover, aria-expanded={showNewConvPopover}, aria-haspopup="dialog", ref para retorno de foco
       - Quando showNewConvPopover: div com className="account-menu", role="dialog", aria-label="Confirmar exclusão do histórico"
         - Parágrafo de confirmação: "Apagar o histórico deste tool? Esta ação não pode ser desfeita." (Body 14px, margin 0, padding "4px 8px 8px")
         - Row de ações (display: flex, gap: 8px, padding: "0 4px 4px"):
           - Botão "Apagar histórico": ghost-button com style={{ color: "var(--destructive)", borderColor: "var(--destructive)" }}, onClick={handleDeleteHistory}
           - Botão "Cancelar": ghost-button, onClick={() => setShowNewConvPopover(false)}

    Não modificar o comportamento existente do account-menu (showAccountMenu / signOut). Não adicionar CSS novo — reutilizar .account-menu-container e .account-menu. Não usar window.confirm() — popover inline conforme UI-SPEC.
  </action>

  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && grep -n "toolKind" apps/web/src/components/app/topbar.tsx && grep -n "onNewConversation" apps/web/src/components/app/topbar.tsx && grep -n "Nova conversa" apps/web/src/components/app/topbar.tsx && grep -n "role=\"dialog\"" apps/web/src/components/app/topbar.tsx && grep -n "destructive" apps/web/src/components/app/topbar.tsx</automated>
  </verify>

  <acceptance_criteria>
    - grep "toolKind" retorna match na assinatura de props e no JSX condicional
    - grep "onNewConversation" retorna match na assinatura e na chamada handleDeleteHistory
    - grep "Nova conversa" retorna match no texto do botão
    - grep 'role="dialog"' retorna match no popover container
    - grep "destructive" retorna match no estilo do botão "Apagar histórico"
    - grep "aria-expanded" retorna match no botão trigger
    - grep "aria-haspopup" retorna match com valor "dialog"
    - grep "Apagar o histórico deste tool" retorna match no texto de confirmação
    - grep "Cancelar" retorna match no botão de cancelamento
    - O account-menu existente (showAccountMenu, signOut) permanece inalterado — grep "showAccountMenu" retorna as mesmas ocorrências de antes
    - pnpm --filter web tsc --noEmit retorna exit 0
  </acceptance_criteria>

  <done>Topbar aceita toolKind e onNewConversation props opcionais; quando toolKind presente, renderiza botão "Nova conversa" com popover de confirmação acessível (role="dialog", Esc, click-outside, focus return); "Apagar histórico" chama DELETE e onNewConversation; TypeScript compila limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → DELETE /api/conversations/[tool] | Requisição não autenticada ou com toolKind arbitrário pode chegar |
| browser → route handler | params.tool vem da URL — entrada não confiável |
| Topbar → route handler | toolKind é string literal passada como prop pelo page.tsx do servidor — confiável na origem, mas validado no server de qualquer forma |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Spoofing | DELETE /api/conversations/[tool] | mitigate | Auth guard: getSessionFromCookieHeader retorna null → 401 antes de qualquer operação de dados |
| T-07-05 | Information Disclosure / IDOR | DELETE handler → deleteConversationExchanges | mitigate | userId extraído da sessão (não da requisição); toolKind validado por enum; IDOR guard no repository (where: { userId, toolKind }) |
| T-07-06 | Tampering | params.tool (URL param) | mitigate | Enum guard: isValidToolKind(params.tool) → 400 se não for um dos 5 valores válidos; impede deleção de toolKinds não existentes ou de outros domínios |
| T-07-07 | Elevation of Privilege | Topbar onNewConversation callback | accept | Callback é fornecido pelo tool component — nenhuma lógica privilegiada; apenas limpa estado local no cliente |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo instalado neste plano — apenas código adicionado a arquivos existentes; slopcheck não aplicável |
</threat_model>

<verification>
1. pnpm --filter web tsc --noEmit passa sem erros após ambas as tarefas
2. curl -X DELETE http://localhost:3000/api/conversations/formula (sem cookie) retorna 401
3. curl -X DELETE http://localhost:3000/api/conversations/file-analysis (com cookie válido) retorna 400
4. Topbar renderizado com toolKind="formula" exibe botão "Nova conversa" no topbar
5. Popover de confirmação contém "Apagar histórico" (destructive) e "Cancelar"
</verification>

<success_criteria>
DELETE /api/conversations/[tool] retorna 401 sem auth, 400 com toolKind inválido, 200 { ok: true } para delete bem-sucedido. Topbar exibe botão "Nova conversa" quando toolKind presente, popover de confirmação acessível com focus trap + Esc + click-outside.
</success_criteria>

<output>
Criar `.planning/phases/07-frontend-history/07-02-SUMMARY.md` ao concluir.
</output>
