---
phase: 08-multi-turn-llm-context
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/web/src/app/api/tools/sql/generate/route.ts
  - apps/web/src/app/api/tools/regex/generate/route.ts
  - apps/web/src/app/api/tools/scripts/generate/route.ts
  - apps/web/src/app/api/tools/template/generate/route.ts
  - apps/web/tests/multi-turn-context.test.ts
autonomous: true
requirements: [MULTI-01, MULTI-02, MULTI-03]

must_haves:
  truths:
    - "Cada route handler lê o histórico via findConversationExchanges(user.id, toolKind) dentro do try, antes de resolve*Payload, e passa history adiante"
    - "O toolKind da leitura bate exatamente com o toolKind do saveConversationExchange existente (scripts usa \"script\" singular)"
    - "No template, a leitura do histórico acontece DEPOIS do Pro gate, dentro do try"
    - "Erro de leitura do histórico não quebra o fluxo: o LLM é chamado só com system+user (skip-on-error herdado do repository)"
    - "Trocar de tool não vaza contexto — a leitura é filtrada por user.id + toolKind"
  artifacts:
    - path: "apps/web/src/app/api/tools/sql/generate/route.ts"
      provides: "Leitura de history sql + passagem a resolveSqlPayload"
      contains: "findConversationExchanges(user.id, \"sql\")"
    - path: "apps/web/src/app/api/tools/scripts/generate/route.ts"
      provides: "Leitura de history script (singular) + passagem"
      contains: "findConversationExchanges(user.id, \"script\")"
    - path: "apps/web/tests/multi-turn-context.test.ts"
      provides: "Integração: history é lido por toolKind e passado ao stream; isolamento por tool"
      min_lines: 60
  key_links:
    - from: "apps/web/src/app/api/tools/sql/generate/route.ts"
      to: "findConversationExchanges"
      via: "leitura por user.id + toolKind antes de resolveSqlPayload"
      pattern: "findConversationExchanges\\(user\\.id,"
    - from: "route handlers"
      to: "resolve*Payload({ request, history })"
      via: "passagem do history lido"
      pattern: "history"
---

<objective>
Conectar os 4 route handlers (SQL, Regex, Scripts, Template) à leitura do histórico: após auth/quota (e Pro gate no template), dentro do `try` e antes de `resolve*Payload`, ler o histórico via `findConversationExchanges(user.id, toolKind)` e passá-lo ao stream resolver.

Espelha o padrão de `file-analysis/chat/route.ts` (lê history → passa ao stream). É a camada de wiring que fecha o multi-turn end-to-end e garante o isolamento por tool (MULTI-03) via o filtro `user.id + toolKind`.

Decisões encodadas:
- **toolKind exato por route** (deve bater com o `saveConversationExchange` existente): sql→`"sql"`, regex→`"regex"`, scripts→`"script"` (singular!), template→`"template"`.
- **Template:** ler history DEPOIS do Pro gate (linhas 19-23), dentro do try (D — discrição de ordem de leitura).
- **D-09:** skip-on-error já embutido no repository (`findConversationExchanges` retorna `[]` + `console.warn` em erro) — nenhum tratamento extra no route.
- **D-06:** `saveConversationExchange` continua salvando APÓS o stream, inalterado — sem duplicação do turno atual.

Purpose: entregar follow-ups funcionais (MULTI-01) com isolamento por tool (MULTI-03) e truncagem ativa (MULTI-02, herdada do Plano 01/02), validados por teste de integração.

Output: 4 routes lendo+passando history; suite de integração verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-turn-llm-context/08-CONTEXT.md
@.planning/phases/08-multi-turn-llm-context/08-PATTERNS.md
@.planning/phases/08-multi-turn-llm-context/08-02-SUMMARY.md

<interfaces>
<!-- API do repository (Phase 7) — usar diretamente. apps/web/src/server/tools/conversation-repository.ts:62-72 -->
findConversationExchanges(userId: string, toolKind: string): Promise<ConversationExchange[]>
// já filtra por { userId, toolKind }, ordena por createdAt asc, try/catch → return [] em erro (skip-on-error, D-09)

<!-- Assinaturas estendidas dos resolvers (Plano 02) -->
resolveSqlPayload({ request, history? })
resolveRegexPayload({ mode: "generate", request, history? })   // explain NÃO recebe history
resolveScriptPayload({ request, history? })
resolveTemplatePayload({ request, history? })

<!-- toolKind por route — DEVE bater com saveConversationExchange existente: -->
sql/generate/route.ts       → "sql"      (save em route:43-49)
regex/generate/route.ts     → "regex"    (save em route:41-47)
scripts/generate/route.ts   → "script"   (singular! save em route:42-49)
template/generate/route.ts  → "template" (Pro gate em route:18-23, ler history DEPOIS)

<!-- Estrutura atual do try em cada route: resolve*Payload → confirmToolUse → recordToolRequest → saveConversationExchange → stream -->
<!-- Padrão de teste: apps/web/tests/formula-api.test.ts (vitest, vi.hoisted, readEvents NDJSON, authedRequest) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire leitura de history nos 4 route handlers</name>
  <files>apps/web/src/app/api/tools/sql/generate/route.ts, apps/web/src/app/api/tools/regex/generate/route.ts, apps/web/src/app/api/tools/scripts/generate/route.ts, apps/web/src/app/api/tools/template/generate/route.ts</files>
  <read_first>
    - apps/web/src/app/api/tools/sql/generate/route.ts (estrutura auth→quota→try, linhas 11-57)
    - apps/web/src/app/api/tools/regex/generate/route.ts
    - apps/web/src/app/api/tools/scripts/generate/route.ts (toolKind "script" singular no save, linhas 24-49)
    - apps/web/src/app/api/tools/template/generate/route.ts (Pro gate linhas 18-23)
    - apps/web/src/app/api/tools/file-analysis/chat/route.ts (analog: lê history e passa ao stream, linhas 41-51)
    - apps/web/src/server/tools/conversation-repository.ts (findConversationExchanges, 62-72)
  </read_first>
  <action>
    Em cada um dos 4 routes, importar `findConversationExchanges` de `@/server/tools/conversation-repository` (já importam `saveConversationExchange` do mesmo módulo). Dentro do `try`, como PRIMEIRA linha antes de `resolve*Payload`, ler o histórico e passá-lo ao resolver:
    - `sql/generate/route.ts`: `const history = await findConversationExchanges(user.id, "sql");` → `resolveSqlPayload({ request: parsed.data, history })`.
    - `regex/generate/route.ts`: `const history = await findConversationExchanges(user.id, "regex");` → `resolveRegexPayload({ mode: "generate", request: parsed.data, history })`.
    - `scripts/generate/route.ts`: `const history = await findConversationExchanges(user.id, "script");` (SINGULAR — bate com o save existente) → `resolveScriptPayload({ request: parsed.data, history })`.
    - `template/generate/route.ts`: a leitura entra DENTRO do try, já APÓS o Pro gate existente (não mover o Pro gate): `const history = await findConversationExchanges(user.id, "template");` → `resolveTemplatePayload({ request: parsed.data, history })`.
    NÃO alterar: auth, parse Zod, quota reserve/confirm/release, recordToolRequest, saveConversationExchange (continua salvando após — D-06), nem o Pro gate do template. Nenhum try/catch extra para a leitura — o skip-on-error já está no repository (D-09).
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `sql/generate/route.ts` contém `findConversationExchanges(user.id, "sql")` antes de `resolveSqlPayload`.
    - `scripts/generate/route.ts` contém `findConversationExchanges(user.id, "script")` (singular, não "scripts").
    - `regex/generate/route.ts` passa `mode: "generate"` e `history` a `resolveRegexPayload`.
    - `template/generate/route.ts`: a chamada a `findConversationExchanges` aparece DEPOIS do bloco do Pro gate (`code: "pro_required"`).
    - `pnpm typecheck` sai com código 0.
  </acceptance_criteria>
  <done>Os 4 routes leem history por user.id+toolKind dentro do try e passam adiante; toolKind scripts é "script" singular; template lê após Pro gate; typecheck verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teste de integração — leitura por toolKind + isolamento por tool</name>
  <files>apps/web/tests/multi-turn-context.test.ts</files>
  <read_first>
    - apps/web/tests/formula-api.test.ts (padrão: vi.hoisted, vi.mock de quota, authedRequest, readEvents NDJSON)
    - apps/web/src/app/api/tools/sql/generate/route.ts (resultado da Task 1)
    - apps/web/src/app/api/tools/scripts/generate/route.ts (toolKind "script")
    - apps/web/src/server/tools/conversation-repository.ts (findConversationExchanges a mockar)
  </read_first>
  <behavior>
    - Test: POST autenticado em /api/tools/sql/generate chama findConversationExchanges com (user.id, "sql") exatamente uma vez.
    - Test: POST em /api/tools/scripts/generate chama findConversationExchanges com (user.id, "script") — SINGULAR (regressão do gotcha de isolamento).
    - Test: POST em /api/tools/template/generate (usuário Pro) chama findConversationExchanges com (user.id, "template").
    - Test (isolamento MULTI-03): chamar sql e depois scripts → os toolKind passados são distintos ("sql" vs "script"), provando que cada tool lê só seu próprio thread.
    - Test (skip-on-error D-09): se findConversationExchanges resolver para [], o route ainda responde 200 (fluxo não quebra).
  </behavior>
  <action>
    Criar `apps/web/tests/multi-turn-context.test.ts` espelhando `formula-api.test.ts`. Mockar `@/server/usage/quota-service` (reserve/confirm/release) e `@/server/tools/conversation-repository` via `vi.hoisted` + `vi.mock`, com `findConversationExchanges` como `vi.fn().mockResolvedValue([])` e `saveConversationExchange` como `vi.fn()`. Para o template, mockar também `@/server/billing/entitlements` `getUserEntitlement` retornando `{ plan: "pro", status: "active" }`. Importar os 4 POST handlers. Usar `authedRequest`/`createSessionToken` como no analog. Para cada tool, fazer um POST válido e assertar `findConversationExchanges` chamado com `expect.any(String)` (user id) e o toolKind literal correto. Incluir o teste de isolamento (sql vs script distintos) e o teste de 200 com history vazio. Como `OPENAI_API_KEY` não está setado no ambiente de teste, os streams usam o fixture path (determinístico) — focar as asserções na leitura do history e no status, não no conteúdo do LLM.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run tests/multi-turn-context.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - O teste assere `findConversationExchanges` chamado com `(expect.any(String), "sql")`, `(.., "regex")`, `(.., "script")`, `(.., "template")`.
    - Existe um teste nomeado para isolamento por tool que prova toolKinds distintos entre sql e scripts.
    - Existe um teste que assere status 200 quando history é `[]`.
    - `pnpm vitest run tests/multi-turn-context.test.ts` sai com código 0.
  </acceptance_criteria>
  <done>Suite de integração verde provando leitura por toolKind correto (incl. "script" singular), isolamento por tool, e fluxo não quebrado com history vazio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request autenticado → leitura de histórico | route lê exchanges escopados ao usuário da sessão |
| toolKind por route → thread isolado | filtro user.id+toolKind separa o contexto de cada tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Elevation of Privilege / IDOR (vazamento cross-user) | findConversationExchanges no route | mitigate | A leitura usa SEMPRE `user.id` da sessão (getSessionFromCookieHeader) como filtro — nunca um id vindo do body. O `where: { userId, toolKind }` no repository impede acesso a histórico de outro usuário. Coberto por asserção `expect.any(String)` + escopo de sessão. |
| T-08-08 | Information Disclosure (vazamento cross-tool, MULTI-03) | toolKind literal por route | mitigate | Cada route passa seu toolKind literal; scripts usa `"script"` singular para casar com o save. Teste de isolamento assere toolKinds distintos entre tools, garantindo que o thread de um tool nunca é injetado em outro. |
| T-08-09 | Denial of Service (falha de leitura derruba o fluxo) | findConversationExchanges | accept/mitigate | Skip-on-error embutido no repository (return [] + console.warn). Route não adiciona try/catch extra; teste assere 200 com history vazio. Risco de falha silenciosa de contexto é aceito (D-09 — pior caso: perde multi-turn naquela chamada, nunca quebra). |
| T-08-10 | Tampering (Pro gate contornado no template) | ordem de leitura no template route | mitigate | Leitura do history entra DEPOIS do Pro gate, dentro do try; acceptance_criteria verifica a ordem. Usuário não-Pro recebe 403 antes de qualquer leitura/LLM. |
</threat_model>

<verification>
- `pnpm typecheck` verde.
- `pnpm vitest run tests/multi-turn-context.test.ts` verde.
- Os 4 routes leem history por toolKind correto e passam aos resolvers.
- Isolamento por tool e skip-on-error cobertos por teste.
</verification>

<success_criteria>
- Follow-up multi-turn funcional end-to-end: history lido → injetado → LLM (MULTI-01).
- Truncagem ativa via Plano 01/02 (MULTI-02).
- Isolamento por tool via filtro user.id+toolKind, validado por teste (MULTI-03).
- `saveConversationExchange` e Pro gate inalterados.
</success_criteria>

<output>
Create `.planning/phases/08-multi-turn-llm-context/08-03-SUMMARY.md` when done
</output>
