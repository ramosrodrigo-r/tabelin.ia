---
phase: 02-freemium-billing-and-entitlements
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["prisma/schema.prisma", ".env.example", "apps/web/src/server/auth/session.ts", "apps/web/src/app/api/auth/[...all]/route.ts", "apps/web/src/server/usage/quota-service.ts", "apps/web/src/server/usage/quota-types.ts", "apps/web/src/server/billing/entitlements.ts", "apps/web/src/server/tools/formula-repository.ts", "apps/web/src/app/api/tools/formula/generate/route.ts", "apps/web/src/app/api/tools/formula/explain/route.ts", "packages/shared/src/billing/schema.ts", "packages/shared/src/index.ts", "apps/web/tests/quota-service.test.ts", "apps/web/tests/formula-api.test.ts", "apps/web/tests/auth-routes.test.ts"]
autonomous: true
requirements: [BILL-03, QUOT-01, QUOT-02, QUOT-03]
user_setup:
  - service: database
    why: "Quota and entitlement models require pushing Prisma schema changes to the local PostgreSQL database."
    commands:
      - "pnpm prisma:generate"
      - "pnpm exec prisma db push"
must_haves:
  truths:
    - "D-01/D-02: Tool usage is reserved before AI work but confirmed only after validated success; provider/validation failures release the reservation."
    - "D-03: Formula generate and explain both consume one `tool_use` for Free users."
    - "D-04: Usage ledger supports `tool_use`, `chat_message`, and future upload meters, but only formula `tool_use` is active in Phase 2."
    - "D-05: Active Pro users bypass user-facing quotas while internal abuse safeguards remain possible."
    - "D-06/D-07/D-08/D-09: Server responses expose enough quota state for inline block, simple message, direct Pro CTA, and last-use warning."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Usage ledger and entitlement persistence"
      contains: "model UsageLedger"
    - path: "apps/web/src/server/usage/quota-service.ts"
      provides: "Transactional reservation/confirmation/release service"
      contains: "reserveToolUse"
    - path: "apps/web/src/server/billing/entitlements.ts"
      provides: "Free/Pro entitlement resolver"
      contains: "getUserEntitlement"
    - path: "apps/web/src/app/api/tools/formula/generate/route.ts"
      provides: "Quota-gated formula generation route"
      contains: "reserveToolUse"
  key_links:
    - from: "apps/web/src/app/api/tools/formula/generate/route.ts"
      to: "apps/web/src/server/usage/quota-service.ts"
      via: "quota reservation before AI work"
      pattern: "reserveToolUse"
    - from: "apps/web/src/server/usage/quota-service.ts"
      to: "prisma/schema.prisma"
      via: "UsageLedger transaction"
      pattern: "usageLedger"
    - from: "apps/web/src/server/billing/entitlements.ts"
      to: "prisma/schema.prisma"
      via: "active Pro entitlement lookup"
      pattern: "Entitlement"
---

<objective>
Build the transactional quota and entitlement foundation for Phase 2.

Purpose: make Free limits and Pro bypass correct before checkout and final UI depend on them.
Output: Prisma usage/entitlement models, quota service, formula route enforcement, and tests for reservation/confirmation/release.
</objective>

<execution_context>
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-freemium-billing-and-entitlements/02-CONTEXT.md
@.planning/phases/02-freemium-billing-and-entitlements/02-RESEARCH.md
@.planning/phases/02-freemium-billing-and-entitlements/02-PATTERNS.md
</context>

<source_coverage>
Requirements: QUOT-01, QUOT-02, QUOT-03, BILL-03.
Decisions: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09.
</source_coverage>

<threat_model>
Assets: user identity, usage ledger, entitlement state, provider/API costs, formula prompts, generated outputs.
Attackers: authenticated users racing requests to bypass quotas, clients forging Pro state, users replaying reservation keys, scripts forcing failed provider calls to consume or avoid quota incorrectly.
Required mitigations: server-side entitlement checks only, transactional quota reservation with unique reservation keys, serializable transaction retry for write conflicts, explicit confirm/release states, no client-trusted quota counters, no quota consumption for provider/validation failures.
Security verification: tests must prove the fifth Free tool use is blocked, Pro bypass is server-side, failed provider/validation paths release reservations, and formula routes call quota reservation before AI work.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Align persisted auth identity with session user IDs</name>
  <files>apps/web/src/server/auth/session.ts, apps/web/src/app/api/auth/[...all]/route.ts, apps/web/tests/auth-routes.test.ts</files>
  <read_first>apps/web/src/server/auth/session.ts, apps/web/src/app/api/auth/[...all]/route.ts, apps/web/tests/auth-routes.test.ts, .planning/phases/02-freemium-billing-and-entitlements/02-RESEARCH.md</read_first>
  <action>Update the auth persistence flow so successful `sign-up/email` and `sign-in/email` create session tokens using the persisted Prisma `User.id`, not the current email-HMAC fallback ID. Keep a local-development fallback only when persistence is unavailable outside production. Ensure the `SessionUser` shape remains `{ id, email, name }` and all existing auth route tests still pass with updated expectations.</action>
  <verify>pnpm --filter web test -- auth-routes && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - `sign-up/email` response user id equals the mocked Prisma user id in auth tests.
    - `sign-in/email` response user id equals the persisted account user id in auth tests.
    - `createSessionUser` is not used to override a persisted Prisma id after successful DB auth.
    - Production persistence failure still returns HTTP 503 instead of issuing a fallback session.
  </acceptance_criteria>
  <done>Billing, entitlements, and usage rows can safely relate to the same user id stored in Prisma.</done>
</task>

<task type="auto">
  <name>Task 2: Add quota, entitlement, and shared billing contracts</name>
  <files>prisma/schema.prisma, packages/shared/src/billing/schema.ts, packages/shared/src/index.ts, apps/web/src/server/billing/entitlements.ts, apps/web/src/server/usage/quota-types.ts, apps/web/src/server/usage/quota-service.ts, apps/web/tests/quota-service.test.ts, .env.example</files>
  <read_first>prisma/schema.prisma, packages/shared/src/formula/schema.ts, apps/web/src/server/db/client.ts, .planning/phases/02-freemium-billing-and-entitlements/02-CONTEXT.md, .planning/phases/02-freemium-billing-and-entitlements/02-RESEARCH.md, .planning/phases/02-freemium-billing-and-entitlements/02-PATTERNS.md</read_first>
  <action>Extend Prisma with models for active entitlement state and usage ledger lifecycle. Use concrete enum/string values `free`, `pro`, `monthly`, `annual`, `tool_use`, `chat_message`, `upload_file`, `reserved`, `confirmed`, and `released` or equivalent constants exported from `packages/shared/src/billing/schema.ts`. Implement `getUserEntitlement`, `reserveToolUse`, `confirmToolUse`, and `releaseToolUse`. `reserveToolUse` must use an interactive Prisma transaction with serializable isolation and retry handling for write conflicts, count confirmed Free `tool_use` rows in the prior 12 hours, count active reservations if they consume capacity, block at 4, return a `lastFreeUse` signal when the reservation is the fourth use, and bypass user-facing limits for active Pro users. Include modeled constants for `chat_message` 10 per 30 days and upload limits 5 MB / 5 files per history without enforcing non-existent chat/upload features.</action>
  <verify>pnpm --filter web test -- quota-service && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `UsageLedger` and an entitlement model related to `User`.
    - `UsageLedger` has a unique reservation/idempotency key and an index that supports user + meter + status + createdAt lookup.
    - `reserveToolUse` returns allowed for the first 4 Free uses in a 12-hour window and blocked for the fifth.
    - `reserveToolUse` returns `lastFreeUse: true` or equivalent for the final allowed Free use.
    - `confirmToolUse` changes a reserved row to confirmed.
    - `releaseToolUse` changes a reserved row to released and does not count against later quota.
    - Active Pro entitlement bypasses the Free quota branch while preserving a technical priority/abuse flag field for later plans.
  </acceptance_criteria>
  <done>Server code has a reusable, transactional quota foundation for formula and future meters.</done>
</task>

<task type="auto">
  <name>Task 3: Gate formula generate/explain routes with quota reservations</name>
  <files>apps/web/src/app/api/tools/formula/generate/route.ts, apps/web/src/app/api/tools/formula/explain/route.ts, apps/web/src/server/tools/formula-repository.ts, apps/web/tests/formula-api.test.ts</files>
  <read_first>apps/web/src/app/api/tools/formula/generate/route.ts, apps/web/src/app/api/tools/formula/explain/route.ts, apps/web/src/server/tools/formula-repository.ts, apps/web/src/server/usage/quota-service.ts, apps/web/src/server/billing/entitlements.ts, packages/shared/src/billing/schema.ts, apps/web/tests/formula-api.test.ts</read_first>
  <action>Insert quota reservation after auth and request validation but before `resolveFormulaPayload(...)` in both formula routes. On quota block, return a typed JSON error such as `{ code: "quota_exceeded", meterKind: "tool_use", cta: "pro_checkout" }` with a status the client can distinguish from provider validation errors. On validated success, confirm the reservation and persist/link `ToolRequest` metadata. On provider throw, validation throw, or 5xx response path, release the reservation and return the existing Portuguese provider error. Keep generate and explain equal cost.</action>
  <verify>pnpm --filter web test -- formula-api && pnpm --filter web test -- quota-service && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - Formula generation route calls `reserveToolUse` before `resolveFormulaPayload`.
    - Formula explanation route calls `reserveToolUse` before `resolveFormulaPayload`.
    - Quota-blocked API response is not the generic `"Nao consegui validar a resposta."` provider error.
    - Successful generate and explain tests assert `confirmToolUse` is called.
    - Provider/validation failure tests assert `releaseToolUse` is called and no confirmed quota is consumed.
    - Existing unauthenticated and invalid request tests still pass.
  </acceptance_criteria>
  <done>Current Formula tool is protected by strict Free quotas and Pro bypass at the API boundary.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Push Prisma schema and regenerate client</name>
  <files>prisma/schema.prisma, package.json</files>
  <read_first>prisma/schema.prisma, package.json, apps/web/src/server/db/client.ts</read_first>
  <action>After all schema edits in this plan are complete, run `pnpm prisma:generate` and push the local development schema with `pnpm exec prisma db push`. If the non-TTY environment prompts for destructive changes, rerun with `pnpm exec prisma db push --accept-data-loss` only after confirming the diff is additive or locally disposable. Do not proceed to final verification until Prisma Client has been regenerated and the local database schema accepts the new models.</action>
  <verify>pnpm prisma:generate && pnpm exec prisma db push</verify>
  <acceptance_criteria>
    - `pnpm prisma:generate` exits 0.
    - `pnpm exec prisma db push` exits 0 against the configured local database.
    - Generated Prisma Client exposes the new usage and entitlement models to TypeScript.
  </acceptance_criteria>
  <done>Database schema and generated client match the Phase 2 quota models.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm prisma:generate` passes.
- [ ] `pnpm exec prisma db push` passes.
- [ ] `pnpm --filter web test -- auth-routes` passes.
- [ ] `pnpm --filter web test -- quota-service` passes.
- [ ] `pnpm --filter web test -- formula-api` passes.
- [ ] `pnpm --filter web typecheck` passes.
- [ ] `pnpm --filter web lint` passes.
</verification>

<success_criteria>
- QUOT-01 is enforced for formula generate/explain with 4 confirmed Free uses per 12 hours.
- QUOT-02 and QUOT-03 have modeled shared constants/ledger hooks but no premature chat/upload feature.
- BILL-03 Pro bypass works server-side for formula tool use subject to internal safeguards.
- D-01 through D-09 are represented in services, route behavior, and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-freemium-billing-and-entitlements/02-01-SUMMARY.md`.
</output>
