---
phase: 02-freemium-billing-and-entitlements
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["apps/web/src/app/(workspace)/workspace/page.tsx", "apps/web/src/components/app/topbar.tsx", "apps/web/src/features/formula/formula-tool.tsx", "apps/web/src/features/formula/components/formula-input-panel.tsx", "apps/web/src/features/formula/components/formula-output-panel.tsx", "apps/web/src/features/formula/hooks/use-formula-stream.ts", "apps/web/src/server/billing/entitlements.ts", "apps/web/src/server/usage/quota-service.ts", "apps/web/src/styles/globals.css", "apps/web/tests/formula-ui.test.tsx", "apps/web/tests/formula-api.test.ts", "apps/web/tests/e2e/billing.spec.ts", "apps/web/playwright.config.ts"]
autonomous: true
requirements: [QUOT-01, QUOT-02, QUOT-03, BILL-01, BILL-02, BILL-03, PRO-02, PRO-03]
user_setup:
  - service: support
    why: "Pro support links need destination values before production launch."
    env_vars:
      - name: PRO_SUPPORT_EMAIL
        source: "Priority support email address"
      - name: PRO_SUPPORT_WHATSAPP_URL
        source: "WhatsApp click-to-chat URL or business contact URL"
must_haves:
  truths:
    - "D-06/D-07/D-08/D-09: Free quota UX is inline, simple, direct-to-checkout, and only warns on the last remaining use."
    - "D-11: Blocked-state checkout defaults to monthly."
    - "D-14: Checkout return may show processing until webhook confirms Pro."
    - "D-16: Pro badge is compact in the topbar/account area and quota warnings/blocks disappear for Pro."
    - "D-17: Pro support paths are simple email and WhatsApp links in account/menu area."
    - "D-18: Priority is a technical flag plus discreet text, not a measurable speed promise."
    - "D-19: Revoked/expired entitlement downgrades to Free and surfaces inline on the next limited action."
  artifacts:
    - path: "apps/web/src/features/formula/formula-tool.tsx"
      provides: "Quota-aware Formula UI"
      contains: "quota"
    - path: "apps/web/src/features/formula/components/formula-input-panel.tsx"
      provides: "Inline block and last-use warning"
      contains: "Assinar Pro"
    - path: "apps/web/src/components/app/topbar.tsx"
      provides: "Compact Pro badge and support links"
      contains: "Pro"
    - path: "apps/web/tests/e2e/billing.spec.ts"
      provides: "Quota to checkout to Pro smoke path"
      contains: "webhook"
  key_links:
    - from: "apps/web/src/app/(workspace)/workspace/page.tsx"
      to: "apps/web/src/server/billing/entitlements.ts"
      via: "server-side entitlement props"
      pattern: "getUserEntitlement"
    - from: "apps/web/src/features/formula/hooks/use-formula-stream.ts"
      to: "apps/web/src/app/api/tools/formula/generate/route.ts"
      via: "typed quota-blocked response handling"
      pattern: "quota_exceeded"
    - from: "apps/web/src/features/formula/components/formula-input-panel.tsx"
      to: "apps/web/src/app/api/billing/checkout/route.ts"
      via: "direct monthly Pro checkout CTA"
      pattern: "/api/billing/checkout"
---

<objective>
Finish the Pro entitlement UX, support paths, priority state, and end-to-end billing verification.

Purpose: make Phase 2 demonstrable from Free quota exhaustion through checkout/webhook activation to Pro workspace state.
Output: quota-aware Formula UI, topbar Pro badge/support links, priority flag display, revoked-plan notice, and mocked E2E smoke coverage.
</objective>

<execution_context>
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-freemium-billing-and-entitlements/02-CONTEXT.md
@.planning/phases/02-freemium-billing-and-entitlements/02-RESEARCH.md
@.planning/phases/02-freemium-billing-and-entitlements/02-PATTERNS.md
@.planning/phases/02-freemium-billing-and-entitlements/02-01-SUMMARY.md
@.planning/phases/02-freemium-billing-and-entitlements/02-02-SUMMARY.md
</context>

<source_coverage>
Requirements: QUOT-01, QUOT-02, QUOT-03, BILL-01, BILL-02, BILL-03, PRO-02, PRO-03.
Decisions: D-06, D-07, D-08, D-09, D-11, D-14, D-16, D-17, D-18, D-19.
</source_coverage>

<threat_model>
Assets: client-visible entitlement state, checkout redirect URL, support contact URLs, priority flag, revoked-plan state.
Attackers: clients forging Pro UI state, users editing local state to bypass quota, malicious links in env-provided support URLs, stale browser state after entitlement revocation.
Required mitigations: server-side entitlement remains source of truth, quota APIs enforce limits regardless of UI, support URLs validated/configured server-side, UI re-checks state after quota errors/checkout return, no measurable speed claim for priority before queue support.
Security verification: tests must prove Pro UI alone cannot bypass quota API, support links render configured safe URLs, revoked entitlement leads back to Free behavior, and direct checkout CTA uses monthly by default.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Load and render Pro workspace state</name>
  <files>apps/web/src/app/(workspace)/workspace/page.tsx, apps/web/src/components/app/topbar.tsx, apps/web/src/server/billing/entitlements.ts, apps/web/src/styles/globals.css, apps/web/tests/formula-ui.test.tsx</files>
  <read_first>apps/web/src/app/(workspace)/workspace/page.tsx, apps/web/src/components/app/topbar.tsx, apps/web/src/server/billing/entitlements.ts, apps/web/src/styles/globals.css, .planning/phases/02-freemium-billing-and-entitlements/02-CONTEXT.md</read_first>
  <action>Resolve entitlement on the server in `/workspace` and pass a compact plan state to `Topbar` and `FormulaTool`. Render a small `Pro` badge in the topbar/account area when entitlement is active. Add account/menu support links for `PRO_SUPPORT_EMAIL` and `PRO_SUPPORT_WHATSAPP_URL`; links should be simple and not always-visible workspace buttons. Add discreet Pro priority text such as `Prioridade Pro ativa` only when the entitlement is active and priority flag is true. Do not add a recurring billing card to the main workspace.</action>
  <verify>pnpm --filter web test -- formula-ui && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - Workspace page calls entitlement resolver for the current user.
    - Active Pro state renders a compact topbar badge containing `Pro`.
    - Free state does not render the Pro badge.
    - Support links include configured email and WhatsApp destinations only for Pro/account support area.
    - Priority copy does not contain measurable speed claims such as `mais rapido`, `2x`, or guaranteed response time.
  </acceptance_criteria>
  <done>Users can see Pro state, support paths, and truthful priority affordance without workspace clutter.</done>
</task>

<task type="auto">
  <name>Task 2: Add inline quota warning, block, and checkout CTA to Formula UI</name>
  <files>apps/web/src/features/formula/formula-tool.tsx, apps/web/src/features/formula/hooks/use-formula-stream.ts, apps/web/src/features/formula/components/formula-input-panel.tsx, apps/web/src/features/formula/components/formula-output-panel.tsx, apps/web/src/styles/globals.css, apps/web/tests/formula-ui.test.tsx, apps/web/tests/formula-api.test.ts</files>
  <read_first>apps/web/src/features/formula/formula-tool.tsx, apps/web/src/features/formula/hooks/use-formula-stream.ts, apps/web/src/features/formula/components/formula-input-panel.tsx, apps/web/src/app/api/tools/formula/generate/route.ts, packages/shared/src/billing/schema.ts, .planning/phases/02-freemium-billing-and-entitlements/02-CONTEXT.md</read_first>
  <action>Extend `useFormulaStream` to distinguish `quota_exceeded` and last-use warning metadata from generic errors. In `FormulaTool` and `FormulaInputPanel`, show a subtle warning only when one Free use remains. When quota is blocked, disable generate/explain submit, keep the user in the formula panel, show simple copy without exact reset time, and render `Assinar Pro` CTA. The CTA must call `POST /api/billing/checkout` with `cycle: "monthly"` and redirect to the returned hosted checkout URL. For Pro users, do not render quota counter, last-use warning, or blocked state.</action>
  <verify>pnpm --filter web test -- formula-ui && pnpm --filter web test -- formula-api && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - Last-use warning appears only when server/client state says one Free use remains.
    - Quota block copy does not include an exact reset timestamp.
    - Blocked state disables generate and explain actions.
    - `Assinar Pro` CTA posts `{ "cycle": "monthly" }` to `/api/billing/checkout`.
    - Pro state suppresses Free quota warning and block UI.
    - Generic provider validation error still renders separately from quota block.
  </acceptance_criteria>
  <done>Free users get clear inline quota UX and direct monthly Pro checkout; Pro users see an unlocked tool.</done>
</task>

<task type="auto">
  <name>Task 3: Add revoked-plan notice, priority plumbing, and end-to-end billing smoke</name>
  <files>apps/web/src/server/billing/entitlements.ts, apps/web/src/server/usage/quota-service.ts, apps/web/src/features/formula/formula-tool.tsx, apps/web/tests/e2e/billing.spec.ts, apps/web/playwright.config.ts, apps/web/tests/formula-ui.test.tsx</files>
  <read_first>apps/web/src/server/billing/entitlements.ts, apps/web/src/server/usage/quota-service.ts, apps/web/src/app/api/billing/mercado-pago/webhook/route.ts, apps/web/src/features/formula/formula-tool.tsx, apps/web/tests/e2e/formula.spec.ts, .planning/phases/02-freemium-billing-and-entitlements/02-RESEARCH.md</read_first>
  <action>Add a server/client signal for recently revoked or expired Pro entitlement so the next limited action can show an inline notice and then proceed as Free. Ensure the quota service returns priority metadata for active Pro users without changing current synchronous processing. Add Playwright or integration smoke coverage with mocked Mercado Pago behavior: Free user consumes four formula uses, sees inline block, starts monthly checkout, return page shows processing, webhook fixture approves payment, workspace shows Pro badge/support links, formula action works without Free block, webhook fixture revokes payment, next limited action shows inline downgrade notice.</action>
  <verify>pnpm --filter web test -- formula-ui && pnpm --filter web exec playwright test tests/e2e/billing.spec.ts && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - Revoked/expired entitlement results in Free quota behavior on the next limited action.
    - Inline downgrade notice appears after revoked/expired plan state and does not use a global alarm banner.
    - Active Pro quota response includes a priority flag or equivalent metadata.
    - E2E smoke covers quota block, checkout start, webhook activation, Pro badge, support links, Pro bypass, webhook revocation, and downgrade notice.
    - The smoke test uses mocked Mercado Pago/provider responses and does not require real credentials.
  </acceptance_criteria>
  <done>Phase 2 works end to end from Free quota to Pro activation and back to Free on revocation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter web test -- quota-service` passes.
- [ ] `pnpm --filter web test -- billing-checkout` passes.
- [ ] `pnpm --filter web test -- mercado-pago-webhook` passes.
- [ ] `pnpm --filter web test -- formula-api` passes.
- [ ] `pnpm --filter web test -- formula-ui` passes.
- [ ] `pnpm --filter web exec playwright test tests/e2e/billing.spec.ts` passes.
- [ ] `pnpm --filter web typecheck` passes.
- [ ] `pnpm --filter web lint` passes.
- [ ] `pnpm --filter web build` passes.
</verification>

<success_criteria>
- Free users are blocked after 4 tool uses in a 12-hour window with inline upgrade UX.
- Chat and upload quota foundations are modeled without prematurely building those future features.
- Checkout can be started with Pix/card-capable hosted Mercado Pago flow.
- Webhook-confirmed Pro users see unlimited tool access, support contact paths, and priority state where available.
- Revoked or expired Pro users automatically return to Free behavior.
- D-06, D-07, D-08, D-09, D-11, D-14, D-16, D-17, D-18, and D-19 are visible in UX/tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-freemium-billing-and-entitlements/02-03-SUMMARY.md`.
</output>
