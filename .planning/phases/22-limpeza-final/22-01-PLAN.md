# Phase 22, Plan 1: Limpeza do Prisma, Correção do Linter, Dependências e Assets

Este plano descreve as tarefas e passos necessários para executar a limpeza final dos modelos de banco de dados, corrigir os problemas de lint ativos, remover as dependências não utilizadas e apagar assets obsoletos.

## Tasks

### Task 1: Limpeza do Prisma Schema e Migração
- **File**: `prisma/schema.prisma`
- **Changes**:
  - Excluir modelos: `ToolRequest`, `Entitlement`, `UsageLedger`, `BillingCheckout`, `UploadedFile`, `ChatMessage`, `PaymentEvent`.
  - No modelo `User`, remover os campos de relacionamento: `toolRequests`, `entitlements`, `usageLedger`, `billingCheckouts`, `uploadedFiles`.
- **Command**:
  - Rodar `pnpm prisma migrate dev --name remove_orphaned_models` na raiz para criar e aplicar a nova migration.

### Task 2: Correção dos Erros de Lint
- **Files & Changes**:
  - `apps/web/src/components/app/workspace-state-context.tsx`: Adicionar `void _exhaustive;` no bloco default do reducer.
  - `apps/web/src/features/unified-chat/components/table-grid-panel.tsx`:
    - Adicionar `void _exhaustive;` no bloco default.
    - Adicionar `void _removed;` após a desestruturação na remoção de coluna.
  - `apps/web/src/features/unified-chat/hooks/use-formula-engine.ts`: Renomear parâmetro `separator` para `_separator` em `resolveArgument`.
  - `apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts`: Alterar `finalMetadata: any` para `finalMetadata: UnifiedChatStreamMetadata | null = null;` e fazer cast seguro de payload.

### Task 3: Remoção de Dependências NPM
- **File**: `apps/web/package.json`
- **Changes**:
  - Remover dependências: `node-cron`, `recharts` e `@types/node-cron`.
- **Command**:
  - Executar `pnpm install` na raiz para atualizar o lockfile.

### Task 4: Deleção de Assets Órfãos
- **Command**:
  - Remover os arquivos:
    - `tabela-teste-ocr.png` (raiz)
    - `invalido.txt` (raiz)
    - `grande.csv` (raiz)
    - `multi-abas.xlsx` (raiz)
    - `apps/web/tests/fixtures/tabela-teste.png`
    - `apps/web/tests/fixtures/dados.csv`

### Task 5: Ajuste em Documentações e Env Example
- **Files & Changes**:
  - `README.md`: Reescrever a apresentação, focando exclusivamente na Planilha Viva com Chat (v3.0).
  - `.env.example`: Alterar título do bloco `# Billing` para `# Support`.

---

## Verification Plan

- `pnpm typecheck` deve rodar limpo.
- `pnpm lint` deve rodar limpo.
- `pnpm test` deve passar com 100% verde.
- `pnpm build` deve gerar a build de produção sem erros.
