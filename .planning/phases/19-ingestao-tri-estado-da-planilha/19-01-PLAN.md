---
phase: 19-ingestao-tri-estado-da-planilha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(workspace)/workspace/layout.tsx
  - apps/web/src/components/app/workspace-shell.tsx
files_created:
  - apps/web/src/components/app/workspace-state-context.tsx
  - apps/web/src/app/api/workspace/import/route.ts
  - apps/web/tests/workspace-import.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "WorkspaceStateContext compartilha colunas, linhas, título e histórico de undo/redo"
    - "A rota /api/workspace/import valida bytes, magic bytes, anti-zip-bomb e limita o parsing a 200 linhas e 26 colunas"
    - "A importação é efêmera, o arquivo bruto não é salvo em disco ou banco de dados"
  artifacts:
    - path: "apps/web/src/components/app/workspace-state-context.tsx"
      provides: "WorkspaceStateProvider e hooks useWorkspaceState"
    - path: "apps/web/src/app/api/workspace/import/route.ts"
      provides: "POST route handler com validações de bytes e parser CSV/XLSX"
  key_links:
    - from: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      to: "apps/web/src/components/app/workspace-state-context.tsx"
      via: "WorkspaceStateProvider"
      pattern: "WorkspaceStateProvider"
---

<objective>
Implementar a infraestrutura básica de estado compartilhado (Contexto React) e a rota do servidor para parsing seguro e efêmero de planilhas CSV/XLSX na Fase 19.

**Mudanças concretas:**
1. Criar `apps/web/src/components/app/workspace-state-context.tsx` que gerencia a planilha ativa (linhas, colunas, título) e histórico de undo/redo.
2. Atualizar `WorkspaceShell` e o layout `/workspace` para embrulhar toda a página com o novo provedor de contexto.
3. Criar a rota de API `apps/web/src/app/api/workspace/import/route.ts` que valida os bytes do arquivo (limite de tamanho, magic bytes, zip-bomb) e realiza o parse de até 200 linhas e 26 colunas, mapeando os tipos inferidos para os formatos do grid.
4. Criar a suíte de testes de integração `apps/web/tests/workspace-import.test.ts` para cobrir a autenticação, validação e sucesso de parsing da rota de importação.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar o WorkspaceStateContext e integrá-lo ao WorkspaceShell e layout</name>
  <read_first>
    - apps/web/src/components/app/workspace-shell.tsx
    - apps/web/src/app/(workspace)/workspace/layout.tsx
    - apps/web/src/features/unified-chat/lib/sample-spec.ts
  </read_first>
  <action>
    1. Criar o arquivo `apps/web/src/components/app/workspace-state-context.tsx`:
       - Definir `GridState = { rows: RowData[], columns: TableColumn[], title: string }`.
       - Implementar o redutor de histórico ou state hook com suporte a `past`, `present`, `future`.
       - Fornecer `spec` (derivado de `present`), `undo()`, `redo()`, `updateRows(newRows)`, `setSpec(newSpec)`, `resetToBlank()` e `resetToSeed()`.
       - Inicializar com `SAMPLE_SPEC` por padrão.
    2. Modificar `apps/web/src/components/app/workspace-shell.tsx` para aninhar o `WorkspaceStateProvider`.
    3. Modificar `apps/web/src/app/(workspace)/workspace/layout.tsx` para passar o estado compartilhado (nesta etapa o grid ainda recebe o spec padrão, mas já envolvido pelo Provider).
  </action>
  <verify>
    <automated>pnpm -r typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar a rota POST /api/workspace/import com validações e parser robusto</name>
  <read_first>
    - apps/web/src/server/extraction/dispatcher.ts
    - apps/web/src/server/file-analysis/file-parser.ts
  </read_first>
  <action>
    1. Criar o arquivo `apps/web/src/app/api/workspace/import/route.ts`:
       - Autenticação obrigatória (reaproveitar `getSessionFromCookieHeader`).
       - Validar upload multipart/form-data do arquivo `file`.
       - Ler o arquivo para Node Buffer e validar tamanho máximo (cap de 5 MB/25 MB).
       - Invocar validações de magic bytes e zip-bomb (`guardXlsxZip`).
       - Parsear o arquivo usando o parser do Sheets/XLSX ou CSV-parser.
       - Extrair até 200 linhas e 26 colunas.
       - Converter nomes de colunas para chaves seguras e mapear tipos:
         - `"numero"` -> `"number"`
         - `"data"` -> `"date"`
         - outros -> `"text"`
       - Retornar o `TableSpecPayload` estruturado com status 200.
  </action>
  <verify>
    <automated>pnpm -r typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Escrever testes automatizados de integração em workspace-import.test.ts</name>
  <read_first>
    - apps/web/tests/unified-route.test.ts
  </read_first>
  <action>
    1. Criar o arquivo `apps/web/tests/workspace-import.test.ts`:
       - Mockar sessões de usuário e o repository do Postgres.
       - Escrever casos de teste:
         - Rejeita requisições não autenticadas (401).
         - Rejeita requisições sem arquivo (400).
         - Suporta importação bem-sucedida de um CSV válido (retorna TableSpecPayload com 200).
         - Suporta importação bem-sucedida de um XLSX válido (retorna TableSpecPayload com 200).
         - Trunca linhas e colunas acima do cap (200 linhas e 26 colunas).
  </action>
  <verify>
    <automated>pnpm --filter web test workspace-import.test.ts</automated>
  </verify>
</task>

</tasks>
