---
phase: 17-desligar-monetiza-o-cota
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/app/topbar.tsx
  - apps/web/src/features/unified-chat/unified-chat-tool.tsx
  - apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts
  - apps/web/src/app/(workspace)/workspace/layout.tsx
  - apps/web/src/app/(workspace)/workspace/page.tsx
  - apps/web/src/styles/globals.css
  - apps/web/tests/topbar.test.tsx
  - apps/web/tests/unified-chat-tool.test.tsx
  - apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx
  - apps/web/tests/e2e/smoke.spec.ts
autonomous: true
requirements: [CLEAN-04]

must_haves:
  truths:
    - "Nenhum badge de plano Pro (Sparkles/.pro-badge), seção 'Suporte Pro', banner de limite (.quota-warning/.quota-blocked) ou CTA de upgrade aparece em qualquer tela"
    - "Topbar e UnifiedChatTool não recebem mais a prop entitlement"
    - "O hook use-unified-chat-stream não expõe quotaBlocked/proBlocked/lastFreeUse nem trata 403 pro_required/429 quota_exceeded/evento quota_warning"
    - "layout.tsx e page.tsx do workspace não chamam getCachedEntitlement"
    - "A grade, o RenderDispatcher, o ChatInput e o fluxo de anexo permanecem funcionais sem gating Pro"
  artifacts:
    - path: "apps/web/src/components/app/topbar.tsx"
      provides: "Topbar enxuta sem badge/Suporte Pro/prop entitlement"
      contains: "topbar-brand"
    - path: "apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts"
      provides: "Hook de stream sem estados/branches de cota/Pro"
      contains: "useUnifiedChatStream"
    - path: "apps/web/src/styles/globals.css"
      provides: "CSS sem classes de upsell"
      contains: "topbar"
  key_links:
    - from: "apps/web/src/app/(workspace)/workspace/layout.tsx"
      to: "@/components/app/topbar"
      via: "Topbar sem prop entitlement"
      pattern: "Topbar"
    - from: "apps/web/src/app/(workspace)/workspace/page.tsx"
      to: "@/features/unified-chat/unified-chat-tool"
      via: "UnifiedChatTool sem prop entitlement"
      pattern: "UnifiedChatTool"
---

<objective>
Remover toda a UI de upsell/monetização da superfície acessível (SC#3): badge Pro + Sparkles + seção "Suporte Pro" no Topbar; banners quota-warning/quota-blocked/proBlocked e gating de anexo Pro no UnifiedChatTool; estados e branches de cota/Pro no hook de stream; chamadas a getCachedEntitlement no layout/page do workspace; e as classes CSS órfãs. Editar os testes de componente (topbar, unified-chat-tool, layout) e o smoke e2e em lockstep para a suíte ficar verde no mesmo commit.

Purpose: SC#3 (zero UI de upsell) + parte de SC#4. Preserva o chat, a grade, o RenderDispatcher e o fluxo de anexo (Phase 19).
Output: UI enxuta sem monetização; testes de componente/e2e verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-desligar-monetiza-o-cota/17-RESEARCH.md
@.planning/phases/17-desligar-monetiza-o-cota/17-VALIDATION.md

<scope_exception>
DECISÃO DE ESCOPO (exceção documentada de SC#4):
NÃO deletar getCachedEntitlement/getUserEntitlement (request-cache.ts / entitlements.ts) — páginas de
tool da Phase 18 ainda importam. Esta tarefa apenas PARA DE CHAMAR getCachedEntitlement no layout/page
do workspace e remove a prop entitlement dos componentes da tela única. O símbolo sobrevive. NÃO tocar
em request-cache.ts nem entitlements.ts. supportLinks/getSupportLinks e NEXT_PUBLIC_PRO_SUPPORT_* ficam
para a Phase 18 — a Topbar ainda recebe supportLinks; só a SEÇÃO condicional "Suporte Pro" (isPro) sai.
</scope_exception>

<interfaces>
Topbar props atuais: { user, entitlement, supportLinks, toolKind?, onNewConversation? }.
Remover: prop entitlement, import UserEntitlement, const isPro, bloco span.pro-badge (Sparkles),
seção {isPro ? Suporte Pro : null}. Remover Sparkles do import lucide-react; remover Mail/MessageCircle
se ficarem sem uso. MANTER supportLinks, Sair, Nova conversa, Privacidade.

UnifiedChatTool props atuais: { entitlement, initialExchanges? }.
Remover: prop entitlement, import UserEntitlement, const isPro, a guarda
`if (pending || stream.quotaBlocked || stream.proBlocked) return` (deixar `if (pending) return`),
`if (!isPro) return` no onDrop, os 3 blocos JSX de upsell, disabled={stream.quotaBlocked} no ChatInput
e no AttachmentButton, e isPro={isPro} no AttachmentButton (se isPro for prop obrigatória do
AttachmentButton, passar isPro={true} — ler attachment-button.tsx antes; não invadir Phase 19).

use-unified-chat-stream: remover estados quotaBlocked/proBlocked/lastFreeUse (declarações + resets em
reset() e submit() + do objeto de retorno); branch 403 pro_required (ambos os ramos); branch 429
quota_exceeded; handler do evento quota_warning. MANTER branches 413/422, parse NDJSON e demais handlers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover badge/Suporte Pro do Topbar e estados/branches de cota/Pro do hook de stream (+ testes topbar)</name>
  <files>apps/web/src/components/app/topbar.tsx, apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts, apps/web/tests/topbar.test.tsx</files>
  <read_first>
    - apps/web/src/components/app/topbar.tsx
    - apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts
    - apps/web/tests/topbar.test.tsx
    - apps/web/src/server/support/support-config.ts
  </read_first>
  <action>
    topbar.tsx: remover import type UserEntitlement e a prop entitlement; remover Sparkles (e Mail/MessageCircle se ficarem sem uso) do import lucide-react; remover a const isPro; remover o bloco span.pro-badge; remover a seção condicional Suporte Pro do account-menu, mantendo o botão Sair. Manter a prop supportLinks; se ficar sem uso, verificar com typecheck e prefixar com `_` somente se o lint exigir.

    use-unified-chat-stream.ts: remover os estados quotaBlocked/proBlocked/lastFreeUse (useState, resets em reset() e submit(), e do objeto de retorno); remover o branch `if (response.status === 403)` inteiro (ambos os ramos pro_required); remover o branch `if (response.status === 429)`; remover o handler `if (event.type === "quota_warning")`. Manter os branches 413/422, o parse NDJSON e todos os demais handlers de evento.

    topbar.test.tsx: remover os testes que assertam "Suporte Pro"/badge Pro (`renders validated Pro support links for Pro users`, `does not render Pro support links for Free users` e similares que dependem de entitlement); ajustar os render(<Topbar .../>) restantes para não passar `entitlement`. Manter os testes de sessão/email/Privacidade/Nova conversa.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec prisma generate &gt;/dev/null 2&gt;&amp;1; pnpm --filter web test topbar 2&gt;&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -En "isPro|pro-badge|Suporte Pro|UserEntitlement" apps/web/src/components/app/topbar.tsx` retorna 0 matches
    - `grep -En "quotaBlocked|proBlocked|lastFreeUse|quota_warning|quota_exceeded|pro_required" apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts` retorna 0 matches
    - `grep -c "topbar-brand" apps/web/src/components/app/topbar.tsx` retorna >= 1 (Topbar preservada)
    - `pnpm --filter web test topbar` sai com código 0
  </acceptance_criteria>
  <done>Topbar e hook sem símbolos de cota/Pro; teste do topbar verde.</done>
</task>

<task type="auto">
  <name>Task 2: Remover blocos de upsell do UnifiedChatTool e desfazer prop entitlement no layout/page (+ CSS + testes)</name>
  <files>apps/web/src/features/unified-chat/unified-chat-tool.tsx, apps/web/src/components/app/attachment-button.tsx, apps/web/src/app/(workspace)/workspace/layout.tsx, apps/web/src/app/(workspace)/workspace/page.tsx, apps/web/src/styles/globals.css, apps/web/tests/unified-chat-tool.test.tsx, apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx, apps/web/tests/e2e/smoke.spec.ts</files>
  <read_first>
    - apps/web/src/features/unified-chat/unified-chat-tool.tsx
    - apps/web/src/components/app/attachment-button.tsx
    - apps/web/src/app/(workspace)/workspace/layout.tsx
    - apps/web/src/app/(workspace)/workspace/page.tsx
    - apps/web/src/styles/globals.css
    - apps/web/tests/unified-chat-tool.test.tsx
    - apps/web/src/app/(workspace)/workspace/__tests__/layout.test.tsx
    - apps/web/tests/e2e/smoke.spec.ts
  </read_first>
  <action>
    unified-chat-tool.tsx: remover import type UserEntitlement e a prop entitlement (manter initialExchanges); remover a const isPro; na guarda do submitPrompt trocar `if (pending || stream.quotaBlocked || stream.proBlocked) return` por `if (pending) return`; no onDrop remover `if (!isPro) return` (permitir drop sempre); remover os 3 blocos JSX de upsell (warning stream.lastFreeUse, stream.quotaBlocked, stream.proBlocked); no ChatInput remover `disabled={stream.quotaBlocked}`; no AttachmentButton remover `disabled={pending || stream.quotaBlocked}` (deixar `disabled={pending}`) e tratar `isPro` conforme a assinatura lida de attachment-button.tsx (se obrigatória, passar isPro={true}; senão remover a prop).

    layout.tsx: remover `getCachedEntitlement` do import (manter getCachedUser); remover a linha `const entitlement = await getCachedEntitlement(user.id);`; remover a prop entitlement do `<Topbar ... />`.
    page.tsx: remover `getCachedEntitlement` do import (manter getCachedUser); remover a linha entitlement; remover a prop entitlement do `<UnifiedChatTool ... />`. Manter getCachedUser se ainda usado; se a página deixar de usar user, simplificar mantendo o redirect/guard de auth do layout.

    globals.css: remover as regras `.quota-warning`, `.quota-blocked`, `.quota-blocked p` e `.pro-badge`. Manter o restante do CSS.

    unified-chat-tool.test.tsx: remover os fixtures/usos que dependem de entitlement (freeEntitlement/proEntitlement) — ajustar os render(<UnifiedChatTool .../>) para não passar entitlement; remover quaisquer asserts de quota-blocked/proBlocked/upsell; manter os testes de chat/streaming/anexo/render.
    layout.test.tsx: remover o mock getCachedEntitlement e o freeEntitlement; ajustar para o layout sem prop entitlement; manter os asserts de shell/Topbar/redirect.
    smoke.spec.ts (e2e): remover o describe `smoke: quota block após 4 uses` e o teste que clica "Assinar Pro" (asserts de upsell — SC#3); remover o `formulaLastFreeUseBody` e o evento `{ type: "quota_warning", lastFreeUse: true }` do fixture se ficarem órfãos. Manter os smoke tests IN (sessão, chat, render).

    NOTA DE GATE: as edições em tests/e2e/smoke.spec.ts NÃO são exercitadas por `pnpm -r test` (vitest.config.ts exclui tests/e2e/** e nenhum script test:e2e está ligado ao gate). A correctness dessa edição repousa em `pnpm -r typecheck` (cobre o TypeScript do spec) + os testes de componente vitest (unified-chat-tool.test.tsx, topbar.test.tsx) — as assertions Playwright NÃO rodam no gate de fase. Não adicionar passo de Playwright (ambiente pode não suportar).
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec prisma generate &gt;/dev/null 2&gt;&amp;1; pnpm --filter web test unified-chat-tool layout 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -En "isPro|quotaBlocked|proBlocked|stream.lastFreeUse|UserEntitlement" apps/web/src/features/unified-chat/unified-chat-tool.tsx` retorna 0 matches
    - `grep -En "getCachedEntitlement|entitlement" apps/web/src/app/(workspace)/workspace/layout.tsx apps/web/src/app/(workspace)/workspace/page.tsx` retorna 0 matches
    - `grep -En "quota-warning|quota-blocked|pro-badge" apps/web/src/styles/globals.css` retorna 0 matches
    - `grep -En "Assinar Pro|quota block|quota_warning" apps/web/tests/e2e/smoke.spec.ts` retorna 0 matches
    - `pnpm --filter web test unified-chat-tool layout` sai com código 0
  </acceptance_criteria>
  <done>UnifiedChatTool, layout/page, CSS e os 4 arquivos de teste sem UI/símbolos de upsell; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Servidor (layout SSR) → Topbar (client) | A prop entitlement deixa de cruzar; nenhuma decisão de acesso depende dela na UI |
| Resposta da API → hook de stream | O hook deixa de interpretar 403/429 de cota como estado de UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Elevation of Privilege | workspace/layout.tsx | mitigate | Remover getCachedEntitlement NÃO pode remover o guard de auth: preservar `getCachedUser()` + `if (!user) redirect("/sign-in")`. O teste layout.test.tsx (asserts de redirect) executa e prova o guard intacto. |
| T-17-05 | Information Disclosure | smoke.spec.ts / globals.css | mitigate | Remover CTAs de upgrade e classes de upsell elimina links residuais para rotas de billing deletadas (Plan 03), evitando erro ao usuário (SC#3/SC#4). |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado nesta fatia (apenas remoções de código/CSS). Audit de legitimidade não se aplica. |
</threat_model>

<verification>
- `cd apps/web && pnpm exec prisma generate` antes do typecheck/test
- `pnpm --filter web test topbar unified-chat-tool layout` verde
- grep dirigido: zero `isPro`/`pro-badge`/`quota-warning`/`quota-blocked`/`Suporte Pro`/`Assinar Pro` na superfície de UI editada
- layout.tsx mantém `getCachedUser` + redirect de auth
- NOTA: a edição de tests/e2e/smoke.spec.ts NÃO é coberta pelo gate vitest (tests/e2e/** excluído); correctness repousa em typecheck + vitest de componente, não em Playwright.
</verification>

<success_criteria>
- Zero UI de upsell/badge/limite em qualquer tela editada (SC#3)
- Topbar/UnifiedChatTool sem prop entitlement; layout/page sem getCachedEntitlement
- `pnpm --filter web test topbar unified-chat-tool layout` verde no mesmo commit (SC#5)
- getCachedEntitlement/getUserEntitlement preservados em request-cache.ts/entitlements.ts (exceção SC#4)
</success_criteria>

<output>
Create `.planning/phases/17-desligar-monetiza-o-cota/17-02-SUMMARY.md` when done
</output>
</content>
</invoke>
