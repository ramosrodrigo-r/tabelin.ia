---
phase: 17-desligar-monetiza-o-cota
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/server/billing/mercado-pago-client.ts
  - apps/web/src/server/billing/checkout-service.ts
  - apps/web/src/server/billing/webhook-service.ts
  - apps/web/src/server/billing/entitlements.ts
  - apps/web/src/app/api/billing/checkout/route.ts
  - apps/web/src/app/api/billing/mercado-pago/webhook/route.ts
  - apps/web/src/app/(billing)/billing/return/page.tsx
  - apps/web/package.json
  - apps/web/.env.example
  - apps/web/tests/billing-checkout.test.ts
  - apps/web/tests/mercado-pago-webhook.test.ts
  - apps/web/tests/e2e/billing.spec.ts
autonomous: true
requirements: [CLEAN-04]

must_haves:
  truths:
    - "POST /api/billing/checkout e POST /api/billing/mercado-pago/webhook nao existem mais (404 — route.ts removidos)"
    - "A pagina /billing/return e o route group (billing) nao existem mais"
    - "O SDK mercadopago e os servicos de provedor de pagamento (client/checkout/webhook) nao existem mais no codigo"
    - "activateProEntitlement/revokeProEntitlement foram removidos; getUserEntitlement permanece intacto"
    - "A dependencia mercadopago saiu de package.json e as 4 env vars MP/PRO_PRICE sairam de .env.example"
  artifacts:
    - path: "apps/web/src/server/billing/entitlements.ts"
      provides: "getUserEntitlement preservado, sem activate/revoke"
      contains: "getUserEntitlement"
    - path: "apps/web/package.json"
      provides: "manifest sem a dependencia mercadopago"
      contains: "dependencies"
  key_links:
    - from: "apps/web/src/server/billing/entitlements.ts"
      to: "@/server/db/client"
      via: "prisma.entitlement.findFirst (getUserEntitlement preservado para Phase 18)"
      pattern: "getUserEntitlement"
---

<objective>
Deletar inteira a camada de provedor de pagamento (puramente OUT, zero consumidor IN sobrevivente apos Plan 01): os 3 servicos Mercado Pago (mercado-pago-client.ts, checkout-service.ts, webhook-service.ts), as 2 rotas de API (api/billing/checkout, api/billing/mercado-pago/webhook) e a pagina de retorno ((billing)/billing/return/page.tsx — route group inteiro). Remover activateProEntitlement/revokeProEntitlement de entitlements.ts (sem consumidor apos deletar o webhook) PRESERVANDO getUserEntitlement (importado por paginas Phase 18). Remover a dep mercadopago de package.json e as 4 env vars MP/PRO_PRICE de .env.example (decisao de escopo: removivel e comprovavel ja em 17; NEXT_PUBLIC_PRO_SUPPORT_* fica para Phase 18). Deletar os 3 arquivos de teste de billing que exercitam so capacidade OUT.

Purpose: SC#1 (rotas de checkout/webhook/plano Pro retornam 404) + SC#4/SC#5. Resolve as Open Questions 1 e 2 da RESEARCH.md (remover dep+env+activate/revoke ja nesta fase para um commit auto-contido e comprovavel).
Output: superficie de pagamento fora do ar; suite verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-desligar-monetiza-o-cota/17-RESEARCH.md
@.planning/phases/17-desligar-monetiza-o-cota/17-VALIDATION.md

<scope_exception>
DECISAO DE ESCOPO (excecao documentada de SC#4):
PRESERVAR getUserEntitlement em apps/web/src/server/billing/entitlements.ts — 6 paginas de tool da Phase 18
(workspace/sql|ocr|regex|scripts|templates|file-analysis/page.tsx) e request-cache.ts ainda o importam.
NAO deletar o arquivo inteiro; remover SOMENTE activateProEntitlement e revokeProEntitlement (consumidos so
pelo webhook-service.ts, deletado nesta tarefa). A migration Prisma destrutiva (drop das tabelas
Entitlement/UsageLedger/BillingCheckout/PaymentEvent) e da Phase 22 (CLEAN-08) — NAO criar migration aqui;
as tabelas ficam orfas no schema. NAO deletar server/usage/quota-service.ts (Phase 18 — ver Plan 01).
</scope_exception>

<interfaces>
entitlements.ts exporta: getUserEntitlement(userId) [PRESERVAR], type ActivateProOptions [remover],
activateProEntitlement(options) [remover], revokeProEntitlement(userId) [remover]. Apos a edicao, os
imports remanescentes devem ser apenas import type UserEntitlement de @tabelin/shared e import prisma de
@/server/db/client. Remover PlanCycle do import se ficar sem uso.

Unico importador de webhook-service: api/billing/mercado-pago/webhook/route.ts (deletado).
Unico importador de checkout-service: api/billing/checkout/route.ts (deletado).
Unico importador de mercado-pago-client: checkout-service.ts e webhook-service.ts (deletados).
Unico importador de mercadopago (npm): mercado-pago-client.ts (deletado).
.env.example linhas a remover: MERCADO_PAGO_ACCESS_TOKEN, MERCADO_PAGO_WEBHOOK_SECRET,
PRO_MONTHLY_PRICE_BRL, PRO_ANNUAL_PRICE_BRL (e o cabecalho "# Billing" se ficar orfao). MANTER
NEXT_PUBLIC_PRO_SUPPORT_EMAIL e NEXT_PUBLIC_PRO_SUPPORT_WHATSAPP_URL (Phase 18).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deletar a camada de provedor de pagamento (servicos, rotas, pagina) e podar entitlements.ts</name>
  <files>apps/web/src/server/billing/mercado-pago-client.ts, apps/web/src/server/billing/checkout-service.ts, apps/web/src/server/billing/webhook-service.ts, apps/web/src/server/billing/entitlements.ts, apps/web/src/app/api/billing/checkout/route.ts, apps/web/src/app/api/billing/mercado-pago/webhook/route.ts, apps/web/src/app/(billing)/billing/return/page.tsx</files>
  <read_first>
    - apps/web/src/server/billing/entitlements.ts
    - apps/web/src/server/billing/webhook-service.ts
    - apps/web/src/app/api/billing/checkout/route.ts
    - apps/web/src/app/api/billing/mercado-pago/webhook/route.ts
    - apps/web/src/app/(billing)/billing/return/page.tsx
  </read_first>
  <action>
    Deletar com git rm: src/server/billing/mercado-pago-client.ts, src/server/billing/checkout-service.ts, src/server/billing/webhook-service.ts, src/app/api/billing/checkout/route.ts, src/app/api/billing/mercado-pago/webhook/route.ts, src/app/(billing)/billing/return/page.tsx. Se o route group (billing) ficar sem nenhum arquivo, remover o diretorio (billing) inteiro.

    Editar src/server/billing/entitlements.ts: remover type ActivateProOptions, activateProEntitlement e revokeProEntitlement. PRESERVAR getUserEntitlement integralmente. Remover PlanCycle do import se ficar sem uso; manter UserEntitlement e prisma.

    NAO criar migration Prisma. NAO tocar em quota-service.ts. NAO tocar nas rotas api/tools/*.
  </action>
  <verify>
    <automated>cd apps/web && (pnpm exec prisma generate > /dev/null 2>&1 || true); ls src/app/api/billing 2> /dev/null; grep -c getUserEntitlement src/server/billing/entitlements.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os 6 arquivos listados (3 servicos + 2 rotas + pagina de retorno) nao existem mais: ls em cada caminho falha
    - test ! -f apps/web/src/app/api/billing/checkout/route.ts e verdadeiro (SC#1)
    - test ! -f apps/web/src/app/api/billing/mercado-pago/webhook/route.ts e verdadeiro (SC#1)
    - grep -c getUserEntitlement apps/web/src/server/billing/entitlements.ts retorna >= 1 (simbolo preservado)
    - grep -En "activateProEntitlement|revokeProEntitlement" apps/web/src/server/billing/entitlements.ts retorna 0 matches
    - grep -rln "checkout-service|webhook-service|mercado-pago-client" apps/web/src retorna 0 matches (sem imports orfaos)
  </acceptance_criteria>
  <done>Camada de pagamento deletada; getUserEntitlement preservado; sem imports orfaos.</done>
</task>

<task type="auto">
  <name>Task 2: Remover a dependencia mercadopago e as env vars MP/PRO_PRICE; deletar os testes de billing OUT</name>
  <files>apps/web/package.json, apps/web/.env.example, apps/web/tests/billing-checkout.test.ts, apps/web/tests/mercado-pago-webhook.test.ts, apps/web/tests/e2e/billing.spec.ts</files>
  <read_first>
    - apps/web/package.json
    - apps/web/.env.example
    - apps/web/tests/billing-checkout.test.ts
    - apps/web/tests/mercado-pago-webhook.test.ts
    - apps/web/tests/e2e/billing.spec.ts
  </read_first>
  <action>
    package.json: remover a linha "mercadopago": "^3.0.0", de dependencies. Apos editar, rodar pnpm install na raiz para atualizar o lockfile (sem download novo — apenas remocao).

    .env.example: remover as linhas MERCADO_PAGO_ACCESS_TOKEN, MERCADO_PAGO_WEBHOOK_SECRET, PRO_MONTHLY_PRICE_BRL, PRO_ANNUAL_PRICE_BRL. Remover o cabecalho "# Billing" se ficar orfao. MANTER NEXT_PUBLIC_PRO_SUPPORT_EMAIL e NEXT_PUBLIC_PRO_SUPPORT_WHATSAPP_URL.

    Deletar com git rm: apps/web/tests/billing-checkout.test.ts, apps/web/tests/mercado-pago-webhook.test.ts, apps/web/tests/e2e/billing.spec.ts (exercitam so capacidade OUT: getBillingConfig/checkoutRequestSchema, processMercadoPagoWebhook, fluxo de billing/upsell e2e).
  </action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && grep -c mercadopago apps/web/package.json; grep -Ec "MERCADO_PAGO|PRO_MONTHLY_PRICE|PRO_ANNUAL_PRICE" apps/web/.env.example; pnpm -r typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c mercadopago apps/web/package.json retorna 0
    - grep -Ec "MERCADO_PAGO_ACCESS_TOKEN|MERCADO_PAGO_WEBHOOK_SECRET|PRO_MONTHLY_PRICE_BRL|PRO_ANNUAL_PRICE_BRL" apps/web/.env.example retorna 0
    - grep -Ec "NEXT_PUBLIC_PRO_SUPPORT_EMAIL" apps/web/.env.example retorna >= 1 (preservado para Phase 18)
    - Os 3 arquivos de teste de billing nao existem mais (ls falha em cada)
    - pnpm -r typecheck sai com codigo 0 (lockfile coerente apos pnpm install; sem imports orfaos)
  </acceptance_criteria>
  <done>mercadopago e env vars MP/PRO_PRICE removidos; testes de billing OUT deletados; typecheck verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet publica -> POST /api/billing/mercado-pago/webhook | Endpoint publico (assinatura HMAC). Ao deletar o route.ts, vira 404 — nenhum handler nao autenticado orfao permanece |
| Mercado Pago (painel externo) -> webhook configurado | Config vive fora do git; a rota deletada retorna 404 automaticamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Spoofing | webhook deletado / painel MP | accept | A rota e removida -> retorna 404; nenhum handler orfao. Recomendar ao operador desativar o webhook no painel Mercado Pago pos-deploy (higiene externa, nao bloqueia). Sem efeito de seguranca residual. |
| T-17-07 | Elevation of Privilege | api/billing/checkout (deletado) | mitigate | A rota e o servico saem juntos; acceptance criterion grep-verifica zero imports orfaos de checkout-service/webhook-service, garantindo que nenhum handler de pagamento reachable sobreviva. |
| T-17-08 | Tampering | entitlements.ts (preservar getUserEntitlement) | mitigate | Remover SO activate/revoke; getUserEntitlement permanece intacto (grep-verificado). A funcao de leitura usada pelas paginas Phase 18 nao e alterada — evita quebra de typecheck e remocao acidental de logica de acesso ainda viva. |
| T-17-SC | Tampering | remocao de dep mercadopago | mitigate | A operacao e REMOCAO de dependencia (nao instalacao) — sem superficie de supply-chain nova. pnpm install apenas atualiza o lockfile retirando o pacote; nenhum pacote novo [ASSUMED]/[SUS] entra. Sem checkpoint de legitimidade necessario. |
</threat_model>

<verification>
- cd apps/web && pnpm exec prisma generate antes do typecheck
- pnpm -r typecheck && pnpm -r test verdes (gate de fase — SC#5)
- grep -rln "checkout-service|webhook-service|mercado-pago-client|mercadopago" apps/web/src retorna zero
- grep -c getUserEntitlement apps/web/src/server/billing/entitlements.ts >= 1 (excecao SC#4 preservada)
- test ! -f apps/web/src/app/api/billing/checkout/route.ts (SC#1)
</verification>

<success_criteria>
- Rotas de checkout/webhook e pagina de retorno removidas (404) — SC#1
- Camada de provedor de pagamento e SDK mercadopago fora do codigo — SC#1/SC#4
- getUserEntitlement preservado; activate/revoke removidos (excecao SC#4 documentada)
- dep mercadopago e env vars MP/PRO_PRICE removidos; testes de billing OUT deletados
- pnpm -r typecheck && pnpm -r test verdes no mesmo commit (SC#5)
</success_criteria>

<output>
Create `.planning/phases/17-desligar-monetiza-o-cota/17-03-SUMMARY.md` when done
</output>
