---
phase: 17-desligar-monetiza-o-cota
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/api/chat/unified/route.ts
  - apps/web/tests/unified-route.test.ts
autonomous: true
requirements: [CLEAN-04]

must_haves:
  truths:
    - "POST /api/chat/unified retorna 200 e streama para qualquer usuário autenticado, sem 429 de cota nem 403 pro_required"
    - "A rota de chat não importa nem chama reserveToolUse/confirmToolUse/releaseToolUse"
    - "A rota de chat não importa getUserEntitlement nem define/chama ensureProUser"
    - "A checagem de sessão (401 sem cookie) permanece intacta na rota de chat"
    - "O streaming NDJSON (intent_detected → ... → complete) é byte-idêntico ao anterior, sem o evento quota_warning/lastFreeUse"
  artifacts:
    - path: "apps/web/src/app/api/chat/unified/route.ts"
      provides: "Rota de chat unificada sem gate de cota/entitlement, com auth e streaming preservados"
      contains: "getSessionFromCookieHeader"
    - path: "apps/web/tests/unified-route.test.ts"
      provides: "Suite da rota de chat sem mocks/asserts de cota/Pro, com asserts de streaming/intent preservados"
      contains: "intent_detected"
  key_links:
    - from: "apps/web/src/app/api/chat/unified/route.ts"
      to: "@/server/auth/session"
      via: "getSessionFromCookieHeader (auth preservada)"
      pattern: "getSessionFromCookieHeader"
---

<objective>
Desacoplar cirurgicamente o gate de cota/entitlement da rota de chat sobrevivente `POST /api/chat/unified` — o gate sai, o streaming fica (SC#2). Remover o pré-check `reserveToolUse`→429, o gate de anexo Pro→403, o gate `template`+Pro→403, e todas as ~8 chamadas `confirmToolUse`/`releaseToolUse`/`quotaCheck.lastFreeUse`, sem tocar em nenhuma linha de classificação de intent, switch-case de tool (Phase 18), ingestão de arquivo (Phase 19), loop de clarificação ou save/record de exchange. Editar `unified-route.test.ts` em lockstep para que a suíte permaneça verde no mesmo commit.

Purpose: SC#2 + SC#4 (símbolos de cota/Pro como gate na rota de chat → zero). É a fatia mais sensível da fase: a tentação de "deletar os tools enquanto estou aqui" pertence à Phase 18 e violaria a atomicidade (SC#5).
Output: Rota de chat sem gate; teste da rota verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-desligar-monetiza-o-cota/17-RESEARCH.md
@.planning/phases/17-desligar-monetiza-o-cota/17-VALIDATION.md

<scope_exception>
DECISÃO DE ESCOPO (exceção documentada de SC#4 — corrige a Blast Radius A da RESEARCH.md):
NÃO deletar `apps/web/src/server/usage/quota-service.ts` nesta fase. 10 rotas de tool de
propriedade da Phase 18 (`api/tools/formula|sql|regex|scripts|template|ocr|file-analysis/*`)
ainda importam `reserveToolUse`/`confirmToolUse`/`releaseToolUse`. Deletar o módulo agora
quebra `pnpm -r typecheck` dessas rotas, violando SC#5. O módulo é tratado como símbolo
compartilhado, exatamente como `getUserEntitlement`. SC#4 fala em "símbolos usados como gate
NA ROTA DE CHAT" — esta tarefa zera essas referências NA ROTA DE CHAT; o módulo em si sai na
Phase 18 junto com as rotas de tool. NÃO deletar quota-service.ts, NÃO deletar entitlements.ts,
NÃO tocar nas rotas `api/tools/*`.
</scope_exception>

<interfaces>
Da rota atual (apps/web/src/app/api/chat/unified/route.ts) — a forma do envelope de gate a remover:
- Pré-check: `const quotaCheck = await reserveToolUse(user.id, "unified", "generate")` + bloco `if (!quotaCheck.allowed) → 429 { code: "quota_exceeded", meterKind, cta: "pro_checkout" }`
- Gate de anexo: `if (file && !(await ensureProUser(user.id))) → 403 { code: "pro_required", feature: "attachment", cta: "pro_checkout" }`
- Gate template: `if (resolvedToolKind === "template" && !(await ensureProUser(user.id))) → 403`
- `ensureProUser(userId)` (função local, linhas ~254-257) — só usada pelos dois gates acima
- Pós-hooks: `await confirmToolUse(quotaCheck.reservationKey)` (em cada case + file_analysis/ocr), `await releaseToolUse(quotaCheck.reservationKey)` (needs_file early return, FILE_TOO_LARGE, extract fail, clarification path, catch)
- 2º argumento `quotaCheck.lastFreeUse` passado a `createFormulaEventStream`/`createSqlEventStream`/`createRegexEventStream`/`createScriptEventStream`/`createTemplateEventStream` (linhas ~513, 546, 579, 612, 640)

Assinaturas dos create*EventStream (server/ai/*-stream.ts) — o 2º param lastFreeUse é opcional;
ao remover o argumento a chamada vira `createXEventStream(payload, undefined, attachmentMeta)` OU
`createXEventStream(payload, false, attachmentMeta)`. NÃO alterar a assinatura da função (Phase 18);
apenas parar de passar `quotaCheck.lastFreeUse`. Verifique a assinatura real lendo o arquivo do stream
antes de decidir entre `undefined` e `false` (use o que o typecheck aceitar sem warning).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover o gate de cota/entitlement da rota de chat unificada</name>
  <files>apps/web/src/app/api/chat/unified/route.ts</files>
  <read_first>
    - apps/web/src/app/api/chat/unified/route.ts (arquivo a editar — ler inteiro)
    - apps/web/src/server/ai/formula-stream.ts (confirmar assinatura de createFormulaEventStream e o que passar no lugar de lastFreeUse)
    - apps/web/src/server/auth/session.ts (confirmar que getSessionFromCookieHeader permanece a fonte de auth — NÃO remover)
  </read_first>
  <action>
    Em `apps/web/src/app/api/chat/unified/route.ts`, fazer SOMENTE as remoções do gate, preservando auth, classificação, switch-cases, ingestão e save/record:

    1. Remover o import `import { getUserEntitlement } from "@/server/billing/entitlements";` (linha ~32).
    2. Remover o import `import { confirmToolUse, releaseToolUse, reserveToolUse } from "@/server/usage/quota-service";` (linha ~37).
    3. Remover a função local `ensureProUser` (linhas ~254-257).
    4. No corpo do POST, remover o bloco `if (file && !(await ensureProUser(user.id))) → 403 pro_required` (linhas ~370-375).
    5. Remover o bloco `const quotaCheck = await reserveToolUse(...)` + `if (!quotaCheck.allowed) → 429` (linhas ~377-383).
    6. No bloco de ingestão de arquivo: remover APENAS as duas chamadas `await releaseToolUse(quotaCheck.reservationKey);` (em FILE_TOO_LARGE ~389 e em extract fail ~396). PRESERVAR `extractContent`, `MAX_UPLOAD_BYTES`, validação de bytes e os returns 413/422 (território Phase 19).
    7. No early-return needs_file (`(file_analysis||ocr) && !file`): remover apenas `await releaseToolUse(quotaCheck.reservationKey);` (~433). Manter o `return responseFromStream(needsFileStream(...))`.
    8. No ramo file_analysis/ocr: remover `await confirmToolUse(quotaCheck.reservationKey);` (~453). Manter o recordToolRequest e o stream.
    9. Remover o gate `if (resolvedToolKind === "template" && !(await ensureProUser(user.id))) → 403` (linhas ~474-477).
    10. Em cada case do switch (formula/sql/regex/script/template): remover a chamada `await confirmToolUse(quotaCheck.reservationKey);` e remover o 2º argumento `quotaCheck.lastFreeUse` das chamadas `createXEventStream(payload, quotaCheck.lastFreeUse, attachmentMeta)` — passar `undefined` (ou `false`, conforme a assinatura aceitar) no lugar. NÃO remover resolveXPayload, recordToolRequest, saveConversationExchange nem prefixIntentEvent.
    11. No case unified_table: no clarification path remover `await releaseToolUse(quotaCheck.reservationKey);` (~653); no generation path remover `await confirmToolUse(quotaCheck.reservationKey);` (~687). Preservar TODO o resto do loop (askClarificationQuestion, buildTableSpec, conservativeClarTurnCount, save).
    12. No `catch` final: remover `await releaseToolUse(quotaCheck.reservationKey);` (~723). Manter o `console.error` e o `return ... 502`.

    PRESERVAR INTACTOS: `getSessionFromCookieHeader` + return 401 (auth, V2/V4 ASVS), `validatePrompt`, schemas Zod, `classifyIntent`, `INTENT_TO_TOOL_KIND`, o curto-circuito de clarificação, todos os switch-cases, `responseFromStream`, `prefixIntentEvent`, `createEventStream`. NÃO deletar nenhum case de tool (Phase 18). NÃO deletar quota-service.ts nem entitlements.ts.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec prisma generate >/dev/null 2>&1; grep -c "reserveToolUse\|confirmToolUse\|releaseToolUse\|ensureProUser\|getUserEntitlement\|quotaCheck" src/app/api/chat/unified/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "reserveToolUse|confirmToolUse|releaseToolUse|ensureProUser|getUserEntitlement|quotaCheck|quota_exceeded|pro_required" apps/web/src/app/api/chat/unified/route.ts` retorna 0 matches
    - `grep -c "getSessionFromCookieHeader" apps/web/src/app/api/chat/unified/route.ts` retorna >= 1 (auth preservada)
    - `grep -c "classifyIntent" apps/web/src/app/api/chat/unified/route.ts` retorna >= 1 (classificação preservada)
    - `grep -c "case \"formula\"\|case \"sql\"\|case \"unified_table\"" apps/web/src/app/api/chat/unified/route.ts` retorna >= 1 (switch-cases Phase 18 intactos)
    - `git -C apps/web diff --stat src/app/api/chat/unified/route.ts` mostra apenas remoções/edições pontuais (sem deleção de blocos de switch)
  </acceptance_criteria>
  <done>Rota de chat compila sem nenhum símbolo de cota/Pro; auth, classificação, switch-cases, ingestão e clarificação preservados.</done>
</task>

<task type="auto">
  <name>Task 2: Editar unified-route.test.ts para refletir a remoção do gate, mantendo asserts de streaming verdes</name>
  <files>apps/web/tests/unified-route.test.ts</files>
  <read_first>
    - apps/web/tests/unified-route.test.ts (arquivo a editar — ler inteiro)
    - apps/web/src/app/api/chat/unified/route.ts (estado já editado na Task 1 — fonte da verdade do novo comportamento)
  </read_first>
  <action>
    Editar `apps/web/tests/unified-route.test.ts` para casar com a rota sem gate. Remover o acoplamento a cota/Pro; manter integralmente os asserts de auth, classificação, intent_detected, streaming, save de histórico e o loop de clarificação (que agora NÃO debita cota):

    1. Remover `vi.mock("@/server/usage/quota-service", ...)` (linhas ~145-149) e `vi.mock("@/server/billing/entitlements", ...)` (linhas ~151-153).
    2. Em `routeMocks`, remover as entradas `reserveToolUse`, `confirmToolUse`, `releaseToolUse`, `getUserEntitlement`.
    3. Em ambos os `beforeEach`, remover as linhas `routeMocks.reserveToolUse.mockResolvedValue(...)`, `confirmToolUse...`, `releaseToolUse...`, `getUserEntitlement...`.
    4. Remover inteiros os testes que exercitam só o gate: `"returns quota 429 using the existing response shape"` (~285-302) e `"blocks free multipart attachments before reserving quota"` (~304-315).
    5. Nos testes que sobrevivem, remover toda asserção sobre `reserveToolUse`/`confirmToolUse`/`releaseToolUse`:
       - `"rejects unauthenticated requests"`: trocar `expect(routeMocks.reserveToolUse).not.toHaveBeenCalled()` por assert de que `classifyIntent` não foi chamado (já existe em outro teste) OU simplesmente remover essa linha mantendo o `expect(response.status).toBe(401)`.
       - `"rejects invalid prompts before provider work"`: remover a linha `expect(routeMocks.reserveToolUse).not.toHaveBeenCalled()`; manter o assert de status 400 e classifyIntent não chamado.
       - `"emits intent_detected as the first formula stream event..."`: remover `expect(routeMocks.reserveToolUse).toHaveBeenCalledWith(..., "unified", "generate")`; manter os asserts de intent/complete/findConversationExchanges/resolveFormulaPayload/saveConversationExchange.
       - `"returns needs_file..."`: remover `expect(routeMocks.releaseToolUse).toHaveBeenCalledWith("res_123")` e `expect(routeMocks.confirmToolUse).not.toHaveBeenCalled()`; manter os asserts de eventos e saveConversationExchange não chamado.
       - `"returns an ephemeral file_analysis payload..."`: remover o mock `routeMocks.getUserEntitlement.mockResolvedValue({plan:"pro"...})` e `expect(routeMocks.confirmToolUse).toHaveBeenCalledWith("res_123")`; manter os asserts de attachment_grounded/complete.
       - No describe `unified_table — clarification loop`: nos cenários A–G remover TODA asserção sobre `confirmToolUse`/`releaseToolUse` (ex.: `expect(routeMocks.releaseToolUse).toHaveBeenCalledOnce()`, `expect(routeMocks.confirmToolUse).not.toHaveBeenCalled()`). PRESERVAR os asserts de payload (table_clar_question / table_spec / turnIndex), askClarificationQuestion e buildTableSpec — o comportamento de roteamento NÃO mudou, só sumiu a cota.
       - No describe `regressão de misroute`: remover os mocks de quota no beforeEach e quaisquer asserts de confirm/release; manter os asserts de roteamento (unified_table permanece / não sequestra).
    6. NÃO criar testes novos. NÃO mockar quota-service nem entitlements (a rota não os importa mais).
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec prisma generate >/dev/null 2>&1; pnpm --filter web test unified-route 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "reserveToolUse|confirmToolUse|releaseToolUse|getUserEntitlement|quota-service|billing/entitlements" apps/web/tests/unified-route.test.ts` retorna 0 matches
    - `grep -c "intent_detected" apps/web/tests/unified-route.test.ts` retorna >= 1 (asserts de streaming preservados)
    - `grep -c "table_clar_question\|table_spec" apps/web/tests/unified-route.test.ts` retorna >= 1 (loop de clarificação preservado)
    - `pnpm --filter web test unified-route` sai com código 0
  </acceptance_criteria>
  <done>`pnpm --filter web test unified-route` verde sem nenhuma referência a cota/entitlement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente não autenticado → POST /api/chat/unified | Entrada não confiável; a sessão deve ser exigida ANTES de qualquer trabalho |
| Cliente autenticado → handler de streaming | Após auth, todo usuário tem acesso pleno ao chat (Pro removido — intencional) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Elevation of Privilege | apps/web/src/app/api/chat/unified/route.ts | mitigate | Ao remover o gate de cota/Pro, PRESERVAR `getSessionFromCookieHeader` + `return 401`. Acceptance criterion grep-verifica `getSessionFromCookieHeader` >= 1. Sem isso, a rota ficaria aberta a não autenticados. |
| T-17-02 | Tampering | unified-route.test.ts (regressão) | mitigate | O teste `"rejects unauthenticated requests"` (status 401) permanece e é executado por `pnpm --filter web test unified-route` — prova que a remoção do gate não removeu a checagem de auth. |
| T-17-03 | Information Disclosure | inputs do prompt/upload | accept | `validatePrompt`, schemas Zod e validação de bytes (MAX_UPLOAD_BYTES) permanecem (V5 ASVS); não são tocados nesta tarefa. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado nesta fatia (apenas remoções de código). Sem audit de legitimidade necessário aqui. |
</threat_model>

<verification>
- `cd apps/web && pnpm exec prisma generate` (evita falso-positivo `.prisma/client/default` em worktree)
- `pnpm --filter web test unified-route` verde
- `grep -rn "reserveToolUse\|confirmToolUse\|releaseToolUse\|ensureProUser\|getUserEntitlement\|quota_exceeded\|pro_required" apps/web/src/app/api/chat/unified/route.ts` retorna zero
- `grep -c "getSessionFromCookieHeader" apps/web/src/app/api/chat/unified/route.ts` >= 1 (auth intacta)
</verification>

<success_criteria>
- Rota de chat sem gate de cota/entitlement; auth, classificação, switch-cases, ingestão e clarificação preservados (SC#2, SC#4 para a rota de chat)
- `pnpm --filter web test unified-route` verde no mesmo commit (SC#5, bisseccionável)
- Nenhum arquivo fora de `route.ts` e `unified-route.test.ts` tocado (atomicidade)
</success_criteria>

<output>
Create `.planning/phases/17-desligar-monetiza-o-cota/17-01-SUMMARY.md` when done
</output>
