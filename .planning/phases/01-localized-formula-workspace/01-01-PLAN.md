---
phase: 01-localized-formula-workspace
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "pnpm-workspace.yaml", "apps/web/package.json", "apps/web/next.config.ts", "apps/web/tsconfig.json", "apps/web/src/app/layout.tsx", "apps/web/src/app/page.tsx", "apps/web/src/app/(auth)/sign-in/page.tsx", "apps/web/src/app/(auth)/sign-up/page.tsx", "apps/web/src/app/(auth)/reset-password/page.tsx", "apps/web/src/app/(workspace)/workspace/page.tsx", "apps/web/src/components/app/sidebar.tsx", "apps/web/src/components/app/topbar.tsx", "apps/web/src/server/auth/config.ts", "apps/web/src/server/db/client.ts", "apps/web/src/styles/globals.css", "apps/web/tests/auth.spec.ts", "prisma/schema.prisma", ".env.example", "docker-compose.yml", "README.md"]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, WORK-01, WORK-02, WORK-03]
user_setup:
  - service: transactional_email
    why: "Production password reset email delivery needs provider credentials; local/dev must work through a safe console/dev adapter."
    env_vars:
      - name: EMAIL_FROM
        source: "Chosen email provider sender address"
      - name: EMAIL_SERVER
        source: "Chosen email provider SMTP/API connection string, if Better Auth adapter requires it"
must_haves:
  truths:
    - "D-01/D-02: User can sign up with email/password through Better Auth."
    - "D-03: Signed-in session survives browser refresh and signed-out users cannot access the workspace."
    - "D-04: Password reset flow exists and works in local/dev without a production email provider."
    - "D-05/D-06/D-07: First signed-in screen is a quiet sidebar workspace with Formula active."
    - "D-08: Workspace shell exposes the shared tool-state regions needed by later formula UI work."
  artifacts:
    - path: "apps/web/src/server/auth/config.ts"
      provides: "Better Auth configuration"
      contains: "betterAuth"
    - path: "prisma/schema.prisma"
      provides: "User/session persistence schema"
      contains: "model User"
    - path: "apps/web/src/app/(workspace)/workspace/page.tsx"
      provides: "Authenticated workspace route"
      min_lines: 40
    - path: "apps/web/src/components/app/sidebar.tsx"
      provides: "Tool navigation shell"
      contains: "Formula"
  key_links:
    - from: "apps/web/src/app/(workspace)/workspace/page.tsx"
      to: "apps/web/src/server/auth/config.ts"
      via: "server-side session check"
      pattern: "auth"
    - from: "apps/web/src/server/auth/config.ts"
      to: "prisma/schema.prisma"
      via: "database-backed auth adapter"
      pattern: "prisma"
---

<objective>
Build the walking skeleton for Tabelin.IA: a running Next.js workspace with email/password auth, persistent sessions, password reset, and a restrained app shell.

Purpose: prove the full-stack foundation before formula AI work lands.
Output: scaffolded web app, auth routes, Postgres/Prisma schema, local dev services, and workspace navigation.
</objective>

<execution_context>
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-localized-formula-workspace/01-CONTEXT.md
@.planning/phases/01-localized-formula-workspace/01-RESEARCH.md
@.planning/phases/01-localized-formula-workspace/01-UI-SPEC.md
@.planning/phases/01-localized-formula-workspace/SKELETON.md
</context>

<source_coverage>
Requirements: AUTH-01, AUTH-02, AUTH-03, AUTH-04, WORK-01, WORK-02, WORK-03.
Decisions: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08.
</source_coverage>

<threat_model>
Assets: user email/password credentials, session cookies, reset tokens, workspace access state.
Attackers: unauthenticated web users, users attempting to access another user's workspace, scripts attempting CSRF/session theft.
Required mitigations: secure httpOnly session cookies, CSRF protection per Better Auth guidance, password hashing through auth framework, reset tokens that expire and cannot be reused, server-side workspace auth checks, no secrets in client bundles.
Security verification: tests must prove signed-out users are redirected, refresh preserves sessions, and reset routes do not leak whether an email exists beyond the framework's safe behavior.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the full-stack workspace skeleton</name>
  <files>package.json, pnpm-workspace.yaml, apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, apps/web/src/styles/globals.css, docker-compose.yml, .env.example, README.md</files>
  <read_first>.planning/research/STACK.md, .planning/phases/01-localized-formula-workspace/01-UI-SPEC.md, AGENTS.md</read_first>
  <action>Create a pnpm workspace with `apps/web` as a Next.js App Router TypeScript app. Configure Tailwind CSS, lint/typecheck/test scripts, `lucide-react`, and local Postgres via `docker-compose.yml`. Add `.env.example` with `DATABASE_URL`, auth secret/base URL values, and email reset env placeholders. The root `/` route must redirect signed-in users to `/workspace` and signed-out users to `/sign-in`.</action>
  <verify>pnpm install && pnpm --filter web typecheck && pnpm --filter web lint</verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` includes `apps/*` and `packages/*`.
    - `apps/web/package.json` contains scripts for `dev`, `build`, `lint`, `typecheck`, and `test`.
    - `.env.example` contains `DATABASE_URL` and auth secret/base URL entries.
    - `docker-compose.yml` defines a local Postgres service.
  </acceptance_criteria>
  <done>Next.js app installs, typechecks, lints, and has documented local Postgres configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement email/password auth with persistent sessions and reset</name>
  <files>prisma/schema.prisma, apps/web/src/server/db/client.ts, apps/web/src/server/auth/config.ts, apps/web/src/app/(auth)/sign-in/page.tsx, apps/web/src/app/(auth)/sign-up/page.tsx, apps/web/src/app/(auth)/reset-password/page.tsx, apps/web/tests/auth.spec.ts</files>
  <read_first>.planning/phases/01-localized-formula-workspace/01-CONTEXT.md, .planning/research/STACK.md, apps/web/package.json, .env.example</read_first>
  <action>Add Prisma models required by Better Auth for users, sessions/accounts, and reset flow support. Configure Better Auth email/password auth with a local/dev reset-link sender that logs or captures the link without requiring a production provider. Build sign-up, sign-in, sign-out, and reset pages with concise pt-BR copy. Add tests covering account creation, sign-in, sign-out, refresh session persistence, and reset request.</action>
  <verify>pnpm --filter web test -- auth && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - `apps/web/src/server/auth/config.ts` exports the auth configuration used by route handlers/pages.
    - `prisma/schema.prisma` contains durable user/session storage required by Better Auth.
    - Auth pages include fields for email/password and submit actions.
    - Tests cover sign-up, sign-in, sign-out, refresh, and reset request paths.
  </acceptance_criteria>
  <done>Users can create accounts, authenticate, keep sessions across refresh, sign out, and request password reset in local/dev.</done>
</task>

<task type="auto">
  <name>Task 3: Build the authenticated workspace shell</name>
  <files>apps/web/src/app/(workspace)/workspace/page.tsx, apps/web/src/components/app/sidebar.tsx, apps/web/src/components/app/topbar.tsx, apps/web/src/styles/globals.css, apps/web/tests/auth.spec.ts</files>
  <read_first>.planning/phases/01-localized-formula-workspace/01-UI-SPEC.md, .planning/phases/01-localized-formula-workspace/SKELETON.md, apps/web/src/server/auth/config.ts</read_first>
  <action>Create `/workspace` as the first signed-in screen. Use a dark restrained sidebar with Formula active and future tools disabled or clearly unavailable: Scripts, SQL, Regex, File Analysis, OCR. Add a topbar with product identity and sign-out. The main region should include stable placeholders for input, streaming output, assumptions/warnings, errors, and copy affordance so Plan 03 can replace placeholders without changing shell layout.</action>
  <verify>pnpm --filter web test -- auth && pnpm --filter web build</verify>
  <acceptance_criteria>
    - `/workspace` performs a server-side session check.
    - Sidebar text includes `Formula`, `Scripts`, `SQL`, `Regex`, `File Analysis`, and `OCR`.
    - Formula is visually active; later tools do not navigate to implemented behavior.
    - Shell includes stable regions for input, output, assumptions/warnings, error, and copy.
  </acceptance_criteria>
  <done>Signed-in users land in a task-focused Formula workspace and signed-out users cannot access it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm install` completes.
- [ ] `pnpm --filter web typecheck` passes.
- [ ] `pnpm --filter web lint` passes.
- [ ] `pnpm --filter web test -- auth` passes.
- [ ] `pnpm --filter web build` passes.
- [ ] Local README documents how to run Postgres and the web app.
</verification>

<success_criteria>
- AUTH-01, AUTH-02, AUTH-03, AUTH-04 are implemented.
- WORK-01 is implemented and WORK-02/WORK-03 have stable shell regions for Plan 03.
- No provider secret or session token is exposed to browser code.
- Walking skeleton records remain consistent with `SKELETON.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-localized-formula-workspace/01-01-SUMMARY.md`.
</output>
