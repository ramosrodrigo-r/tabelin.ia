---
phase: 01-localized-formula-workspace
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["packages/shared/package.json", "packages/shared/src/index.ts", "packages/shared/src/formula/platforms.ts", "packages/shared/src/formula/schema.ts", "packages/shared/src/formula/fixtures.ts", "apps/web/src/server/ai/openai-client.ts", "apps/web/src/server/ai/formula-prompts.ts", "apps/web/src/server/ai/formula-stream.ts", "apps/web/src/server/tools/formula-repository.ts", "apps/web/src/app/api/tools/formula/generate/route.ts", "apps/web/src/app/api/tools/formula/explain/route.ts", "apps/web/tests/formula-contract.test.ts", "apps/web/tests/formula-api.test.ts", "prisma/schema.prisma", ".env.example"]
autonomous: true
requirements: [FORM-01, FORM-02, FORM-03, FORM-04, FORM-05, WORK-02, WORK-04, RELY-01]
user_setup:
  - service: openai
    why: "Real formula generation requires a provider API key; automated tests must use mocked provider responses."
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard API key"
      - name: OPENAI_MODEL
        source: "Configured text model name; default documented in `.env.example`"
must_haves:
  truths:
    - "D-09: Formula generation rejects requests without an explicit platform."
    - "D-10: Formula generation rejects requests without an explicit formula language and separator contract."
    - "D-11: Validated output includes platform, formula language, separator, and assumptions."
    - "D-12: Explanation output is step-by-step Brazilian Portuguese."
    - "D-13: Golden fixtures cover `SE`, `PROCV`, `SOMASE`, and financial/operational examples."
    - "D-14/D-15: AI calls are server-side and final output validates before copy-ready rendering."
    - "D-17/D-18: Missing context produces assumptions/narrowing prompts and metadata is persisted without full history UI."
  artifacts:
    - path: "packages/shared/src/formula/schema.ts"
      provides: "Formula request and response schemas"
      contains: "z.object"
    - path: "apps/web/src/server/ai/formula-prompts.ts"
      provides: "Localized prompt builders"
      contains: "separator"
    - path: "apps/web/src/app/api/tools/formula/generate/route.ts"
      provides: "Formula generation route"
      exports: ["POST"]
    - path: "apps/web/src/app/api/tools/formula/explain/route.ts"
      provides: "Formula explanation route"
      exports: ["POST"]
    - path: "apps/web/src/server/tools/formula-repository.ts"
      provides: "Request metadata persistence"
      contains: "toolRequest"
  key_links:
    - from: "apps/web/src/app/api/tools/formula/generate/route.ts"
      to: "packages/shared/src/formula/schema.ts"
      via: "request and response validation"
      pattern: "Formula"
    - from: "apps/web/src/app/api/tools/formula/generate/route.ts"
      to: "apps/web/src/server/ai/formula-prompts.ts"
      via: "prompt builder call"
      pattern: "build.*Prompt"
    - from: "apps/web/src/server/tools/formula-repository.ts"
      to: "prisma/schema.prisma"
      via: "metadata persistence"
      pattern: "toolRequest"
---

<objective>
Implement the server-side formula assistant contract: platform/language selectors, prompt builders, structured schemas, streaming route handlers, and metadata persistence.

Purpose: make localized formula generation and explanation reliable before the UI depends on it.
Output: shared formula package, AI server modules, API routes, Prisma metadata model, and tests.
</objective>

<execution_context>
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-localized-formula-workspace/01-CONTEXT.md
@.planning/phases/01-localized-formula-workspace/01-RESEARCH.md
@.planning/phases/01-localized-formula-workspace/01-PATTERNS.md
@.planning/phases/01-localized-formula-workspace/01-01-SUMMARY.md
</context>

<source_coverage>
Requirements: FORM-01, FORM-02, FORM-03, FORM-04, FORM-05, WORK-02, WORK-04, RELY-01.
Decisions: D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-17, D-18.
</source_coverage>

<threat_model>
Assets: provider API key, user prompts, generated formulas, request metadata, future quota/accounting data.
Attackers: browser clients attempting to call provider directly, prompt-injection attempts, authenticated users trying to bypass validation, provider/network failures producing malformed output.
Required mitigations: server-only OpenAI client, authenticated route handlers, Zod validation on request and final response, schema-safe streaming event format, no raw provider key in client bundle, metadata persistence without unnecessary raw sensitive content.
Security verification: tests must fail if invalid platform/language values are accepted, if response schema validation is bypassed, or if `OPENAI_API_KEY` appears in client-facing code.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define shared formula platform and locale contracts</name>
  <files>packages/shared/package.json, packages/shared/src/index.ts, packages/shared/src/formula/platforms.ts, packages/shared/src/formula/schema.ts, packages/shared/src/formula/fixtures.ts, apps/web/tests/formula-contract.test.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-localized-formula-workspace/01-CONTEXT.md, .planning/phases/01-localized-formula-workspace/01-RESEARCH.md, apps/web/package.json</read_first>
  <action>Create a shared package exporting platform constants for `excel`, `google_sheets`, `airtable`, and `libreoffice_calc`; language constants for `pt-BR` and `en-US`; separator rules `;` and `,`; and Zod schemas for generate/explain requests and responses. Add fixtures for `SE`, `PROCV`, `SOMASE`, and at least one operational finance example. Tests must assert that platform/language are required and separators match language.</action>
  <verify>pnpm --filter web test -- formula-contract && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - `platforms.ts` exports all four required platform identifiers and display labels.
    - `schema.ts` rejects missing platform and missing formula language.
    - `fixtures.ts` contains `SE`, `PROCV`, `SOMASE`, and one financial/operational fixture.
    - Contract tests assert Portuguese uses `;` and English uses `,`.
  </acceptance_criteria>
  <done>Client and server can import one canonical formula contract.</done>
</task>

<task type="auto">
  <name>Task 2: Build prompt, provider, and validation modules</name>
  <files>apps/web/src/server/ai/openai-client.ts, apps/web/src/server/ai/formula-prompts.ts, apps/web/src/server/ai/formula-stream.ts, apps/web/tests/formula-contract.test.ts, .env.example</files>
  <read_first>.planning/research/STACK.md, .planning/research/PITFALLS.md, packages/shared/src/formula/schema.ts, packages/shared/src/formula/fixtures.ts</read_first>
  <action>Add a server-only OpenAI client factory that reads `OPENAI_API_KEY` and `OPENAI_MODEL` from env. Implement `buildFormulaGenerationPrompt` and `buildFormulaExplanationPrompt` so every prompt includes platform, formula language, separator, Brazilian Portuguese response style, and instruction to state assumptions instead of inventing sheet structure. Implement a stream adapter that emits typed events: `metadata`, `delta`, `warning`, `complete`, and `error`; only `complete` can carry validated copy-ready output.</action>
  <verify>pnpm --filter web test -- formula-contract && ! rg \"OPENAI_API_KEY\" apps/web/src/app apps/web/src/features apps/web/src/components --glob '!**/*.test.ts'</verify>
  <acceptance_criteria>
    - Prompt builder output includes platform, formula language, separator, and assumptions instructions.
    - `openai-client.ts` is imported only from server route/server modules.
    - Stream adapter has a final validation step against shared schemas.
    - `.env.example` documents `OPENAI_API_KEY` and `OPENAI_MODEL`.
  </acceptance_criteria>
  <done>Server modules can create localized AI requests without exposing secrets or unvalidated copy-ready output.</done>
</task>

<task type="auto">
  <name>Task 3: Add formula API routes and metadata persistence</name>
  <files>prisma/schema.prisma, apps/web/src/server/tools/formula-repository.ts, apps/web/src/app/api/tools/formula/generate/route.ts, apps/web/src/app/api/tools/formula/explain/route.ts, apps/web/tests/formula-api.test.ts</files>
  <read_first>apps/web/src/server/auth/config.ts, apps/web/src/server/db/client.ts, packages/shared/src/formula/schema.ts, apps/web/src/server/ai/formula-prompts.ts, apps/web/src/server/ai/formula-stream.ts</read_first>
  <action>Add a `ToolRequest` metadata model with user id, tool kind `formula`, mode `generate|explain`, platform, formula language, separator, status, latency, provider model, and timestamps. Implement authenticated `POST /api/tools/formula/generate` and `POST /api/tools/formula/explain` route handlers that validate requests, call prompt/stream modules, persist metadata, and return typed streaming responses. Use mocked provider responses in tests.</action>
  <verify>pnpm --filter web test -- formula-api && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - Unauthenticated formula API requests fail.
    - Invalid platform/language requests fail before provider calls.
    - Successful generate route returns metadata plus validated formula payload.
    - Successful explain route returns a pt-BR step-by-step explanation payload.
    - Metadata write happens for success and failure statuses without storing provider secrets.
  </acceptance_criteria>
  <done>Formula generation and explanation are available through authenticated, validated, server-only routes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter web test -- formula-contract` passes.
- [ ] `pnpm --filter web test -- formula-api` passes.
- [ ] `pnpm --filter web typecheck` passes.
- [ ] `pnpm --filter web lint` passes.
- [ ] `! rg "OPENAI_API_KEY" apps/web/src/app apps/web/src/features apps/web/src/components --glob '!**/*.test.ts'` proves provider keys are not referenced by client/app UI code.
</verification>

<success_criteria>
- FORM-01 through FORM-05 are represented in request/response schemas and route behavior.
- WORK-02 and WORK-04 have typed server states for loading, errors, assumptions, warnings, and validated completion.
- RELY-01 is enforced by Zod validation before copy-ready output.
- Route handlers preserve a clean future path to quota enforcement in Phase 2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-localized-formula-workspace/01-02-SUMMARY.md`.
</output>
