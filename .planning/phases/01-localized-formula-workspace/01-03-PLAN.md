---
phase: 01-localized-formula-workspace
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["apps/web/src/features/formula/formula-tool.tsx", "apps/web/src/features/formula/components/formula-input-panel.tsx", "apps/web/src/features/formula/components/formula-output-panel.tsx", "apps/web/src/features/formula/components/copy-button.tsx", "apps/web/src/features/formula/hooks/use-formula-stream.ts", "apps/web/src/app/(workspace)/workspace/page.tsx", "apps/web/src/components/app/sidebar.tsx", "apps/web/tests/formula-ui.test.tsx", "apps/web/tests/e2e/formula.spec.ts", "apps/web/playwright.config.ts", "apps/web/src/styles/globals.css"]
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, FORM-01, FORM-02, FORM-03, FORM-04, FORM-05, PERF-01, RELY-01]
user_setup: []
must_haves:
  truths:
    - "D-05/D-06/D-07: User lands in the usable workspace with Formula active and no marketing hero."
    - "D-08: Formula UI uses one shared interaction model for input, selectors, streaming output, assumptions/warnings, errors, and copy."
    - "D-09/D-10/D-11: User must choose platform and language, then output shows platform, language, separator, and assumptions."
    - "D-12: Formula explanation mode renders clear Brazilian Portuguese steps."
    - "D-13: UI/E2E fixtures exercise localized common formula cases."
    - "D-15/D-16/D-17: Streaming starts visibly within the performance budget and final copy-ready output is validated."
  artifacts:
    - path: "apps/web/src/features/formula/formula-tool.tsx"
      provides: "Integrated formula workspace UI"
      min_lines: 80
    - path: "apps/web/src/features/formula/hooks/use-formula-stream.ts"
      provides: "Client streaming state machine"
      contains: "getReader"
    - path: "apps/web/src/features/formula/components/copy-button.tsx"
      provides: "Copy feedback affordance"
      contains: "navigator.clipboard"
    - path: "apps/web/tests/e2e/formula.spec.ts"
      provides: "Formula MVP E2E coverage"
      contains: "2.5"
  key_links:
    - from: "apps/web/src/features/formula/hooks/use-formula-stream.ts"
      to: "/api/tools/formula/generate"
      via: "fetch streaming request"
      pattern: "/api/tools/formula/generate"
    - from: "apps/web/src/features/formula/formula-tool.tsx"
      to: "packages/shared/src/formula/schema.ts"
      via: "shared platform and language constants"
      pattern: "FORMULA"
    - from: "apps/web/src/app/(workspace)/workspace/page.tsx"
      to: "apps/web/src/features/formula/formula-tool.tsx"
      via: "workspace renders Formula tool"
      pattern: "FormulaTool"
---

<objective>
Implement the user-facing Formula workspace: localized generate/explain controls, streaming result UI, assumptions/warnings, copy feedback, and end-to-end verification.

Purpose: complete the Phase 1 MVP demo path for Brazilian spreadsheet users.
Output: Formula UI components, streaming hook, copied feedback, E2E tests, and performance checks.
</objective>

<execution_context>
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/amandab.doamaral/Mopi/tabelin.ia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-localized-formula-workspace/01-CONTEXT.md
@.planning/phases/01-localized-formula-workspace/01-UI-SPEC.md
@.planning/phases/01-localized-formula-workspace/01-PATTERNS.md
@.planning/phases/01-localized-formula-workspace/01-02-SUMMARY.md
</context>

<source_coverage>
Requirements: WORK-01, WORK-02, WORK-03, WORK-04, FORM-01, FORM-02, FORM-03, FORM-04, FORM-05, PERF-01, RELY-01.
Decisions: D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-15, D-16, D-17.
</source_coverage>

<threat_model>
Assets: user prompts and formulas in browser state, validated outputs, clipboard content, authenticated formula endpoints.
Attackers: authenticated users sending malformed requests, browser extensions observing clipboard, prompt content attempting to override schema/safety instructions, network failures causing partial output.
Required mitigations: client validation mirrors shared schemas, copy button copies only validated complete output, partial stream is visually distinct from copy-ready output, error states preserve input without exposing internals, all API calls use same-origin authenticated routes.
Security verification: tests must prove invalid selector state cannot submit, copy is disabled until validated completion, and formula routes are called through `/api/tools/formula/*` rather than provider URLs.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace shell placeholders with Formula generate/explain UI</name>
  <files>apps/web/src/features/formula/formula-tool.tsx, apps/web/src/features/formula/components/formula-input-panel.tsx, apps/web/src/app/(workspace)/workspace/page.tsx, apps/web/src/styles/globals.css, apps/web/tests/formula-ui.test.tsx</files>
  <read_first>.planning/phases/01-localized-formula-workspace/01-UI-SPEC.md, packages/shared/src/formula/platforms.ts, packages/shared/src/formula/schema.ts, apps/web/src/app/(workspace)/workspace/page.tsx</read_first>
  <action>Create the Formula tool as the real workspace body. Add mode tabs for `Gerar formula` and `Explicar formula`, required platform selector, required language selector, prompt/formula textarea, and submit actions. Defaults may preselect Excel and Portuguese (Brazil), but the selected values must remain visible and editable. Use compact controls and stable dimensions from UI-SPEC.</action>
  <verify>pnpm --filter web test -- formula-ui && pnpm --filter web typecheck</verify>
  <acceptance_criteria>
    - Workspace imports and renders `FormulaTool`.
    - UI contains all four platform labels.
    - UI contains both formula language options with separator indicators.
    - Generate and explain modes use the same input/output layout.
    - Submit is blocked with visible validation when required input is missing.
  </acceptance_criteria>
  <done>User can open Formula and prepare a localized generate or explain request.</done>
</task>

<task type="auto">
  <name>Task 2: Implement streaming output, assumptions/warnings, errors, and copy feedback</name>
  <files>apps/web/src/features/formula/hooks/use-formula-stream.ts, apps/web/src/features/formula/components/formula-output-panel.tsx, apps/web/src/features/formula/components/copy-button.tsx, apps/web/src/features/formula/formula-tool.tsx, apps/web/tests/formula-ui.test.tsx</files>
  <read_first>apps/web/src/app/api/tools/formula/generate/route.ts, apps/web/src/app/api/tools/formula/explain/route.ts, packages/shared/src/formula/schema.ts, .planning/phases/01-localized-formula-workspace/01-UI-SPEC.md</read_first>
  <action>Build a `useFormulaStream` hook that posts to generate or explain routes, parses typed stream events, tracks `idle|loading|streaming|complete|error`, and exposes draft text separately from validated result. Render metadata chips for platform, language, and separator. Render assumptions and warnings under the output. Implement `CopyButton` as an icon button that copies only validated formula/explanation content and switches to `Copiado`/check state immediately.</action>
  <verify>pnpm --filter web test -- formula-ui && pnpm --filter web lint</verify>
  <acceptance_criteria>
    - Output panel distinguishes streaming draft from validated complete result.
    - Metadata displays selected platform, formula language, and separator.
    - Assumptions/warnings render when present.
    - Copy button is disabled before validated completion and shows copied feedback after success.
    - Error state keeps the user's input visible and offers retry.
  </acceptance_criteria>
  <done>User sees streaming progress, trustworthy metadata, assumptions/warnings, and copy-ready validated output.</done>
</task>

<task type="auto">
  <name>Task 3: Add formula MVP E2E and performance verification</name>
  <files>apps/web/playwright.config.ts, apps/web/tests/e2e/formula.spec.ts, apps/web/tests/formula-ui.test.tsx, apps/web/src/features/formula/hooks/use-formula-stream.ts</files>
  <read_first>packages/shared/src/formula/fixtures.ts, apps/web/src/features/formula/formula-tool.tsx, apps/web/src/features/formula/components/copy-button.tsx, apps/web/package.json</read_first>
  <action>Add Playwright coverage for the strongest first demo: sign up/sign in, open Formula, select Excel + Portuguese (Brazil), ask `Quero somar a coluna B se a coluna C for Pago`, observe first visible streamed output within 2.5 seconds with mocked provider latency, see metadata/assumptions, copy the formula, then switch to explain mode and explain a pasted formula. Mock provider responses at the route boundary so tests are deterministic.</action>
  <verify>pnpm --filter web test -- formula-ui && pnpm --filter web exec playwright test tests/e2e/formula.spec.ts</verify>
  <acceptance_criteria>
    - E2E test covers sign-up/sign-in to workspace.
    - E2E test covers generation with Excel + Portuguese (Brazil) and `;` separator.
    - E2E test asserts first visible output before 2500ms under mocked normal latency.
    - E2E test covers copy feedback.
    - E2E test covers formula explanation in Brazilian Portuguese.
  </acceptance_criteria>
  <done>Phase 1 user demo path is covered by deterministic automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter web test -- formula-ui` passes.
- [ ] `pnpm --filter web exec playwright test tests/e2e/formula.spec.ts` passes.
- [ ] `pnpm --filter web typecheck` passes.
- [ ] `pnpm --filter web lint` passes.
- [ ] `pnpm --filter web build` passes.
- [ ] Playwright screenshot checks show no overlapping text on desktop and mobile viewports.
</verification>

<success_criteria>
- WORK-01 through WORK-04 are implemented in the visible workspace.
- FORM-01 through FORM-05 are implemented in the Formula UI.
- PERF-01 has an automated timing assertion for first visible streamed output under normal mocked latency.
- RELY-01 is reflected in UI behavior: copy-ready state only after validated output.
</success_criteria>

<output>
After completion, create `.planning/phases/01-localized-formula-workspace/01-03-SUMMARY.md`.
</output>
