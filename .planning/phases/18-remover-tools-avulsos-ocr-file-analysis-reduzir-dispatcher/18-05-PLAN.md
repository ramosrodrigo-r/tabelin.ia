---
phase: 18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher
plan: 05
type: execute
wave: 3
depends_on: ["18-04"]
files_modified:
  - apps/web/src/server/ai/intent-classifier.ts
  - packages/shared/src/unified-chat/schema.ts
  - apps/web/src/app/api/chat/unified/route.ts
  - apps/web/tests/intent-classifier.test.ts
autonomous: true
requirements: [CLEAN-06]

must_haves:
  truths:
    - "intent-classifier.ts classifica apenas em sheet_operation | qa | unknown"
    - "UNIFIED_INTENTS em schema.ts tem 3 valores (sheet_operation, qa, unknown), não 9"
    - "route.ts roteia para o branch correto com base no intent binário, sem fallback temporário do Plan 04"
    - "Os ~6-8 prompts PT representativos do eval binário (CLEAN-06/checkpoint 17->18) estão cobertos em intent-classifier.test.ts"
  artifacts:
    - path: "apps/web/src/server/ai/intent-classifier.ts"
      provides: "fixtureClassify binário + buildClassifierSystemPrompt binário"
    - path: "packages/shared/src/unified-chat/schema.ts"
      provides: "UNIFIED_INTENTS reduzido, OVERRIDE_INTENTS/FILE_DEPENDENT_INTENTS removidos ou reduzidos"
    - path: "apps/web/tests/intent-classifier.test.ts"
      provides: "suite reescrita para o eixo binário, com casos representativos do eval de 20 prompts"
  key_links:
    - from: "apps/web/src/app/api/chat/unified/route.ts"
      to: "apps/web/src/server/ai/intent-classifier.ts"
      via: "classifyIntent()"
      pattern: "classifyIntent"
---

<objective>
Executar o Bloco 6 do inventário de deleção da Phase 18 (CLEAN-06) — reduzir `intent-classifier.ts` e `packages/shared/src/unified-chat/schema.ts` do eixo de 9 intents para o eixo BINÁRIO definido em D-A1/A2 do RESEARCH.md: **`"sheet_operation"`** (operação estruturada na planilha — mutação, RF-03/Phase 20 implementa a EXECUÇÃO, esta fase só classifica) e **`"qa"`** (pergunta analítica, resposta em texto), mais `"unknown"` (pedido ambíguo, já existe).

**ATENÇÃO — Checkpoint 17→18 plantado aqui (STATE.md Blockers/Concerns):** este plano PLANTA os rótulos corretos do eixo binário "operação na planilha vs Q&A" e cobre 6-8 prompts PT representativos do eval de ~20 prompts ambíguos como casos de teste em `intent-classifier.test.ts`. A EXECUÇÃO completa do eval (UAT com os ~20 prompts) é Phase 20 — este plano apenas estabelece a base de regressão. Inclua no SUMMARY uma nota explícita: "Eval binário plantado nesta fase (6-8 casos); UAT completo (~20 prompts) é Phase 20".

**Mudanças concretas:**
1. `packages/shared/src/unified-chat/schema.ts`: `UNIFIED_INTENTS` passa de `["formula","sql","regex","script","template","file_analysis","ocr","tabela","unknown"]` para `["sheet_operation","qa","unknown"]`. `OVERRIDE_INTENTS` reduzido para `["sheet_operation","qa"]` (sem `"unknown"`, igual ao padrão atual). `FILE_DEPENDENT_INTENTS`/`fileDependentIntentSchema` — REMOVER (eram exclusivos de `file_analysis`/`ocr`, ambos saídos no Bloco 2/3/5).
2. `intent-classifier.ts`: `classifyFileIntent()` — REMOVER (só servia ocr/file_analysis). `fixtureClassify()` — reescrever a árvore de regex inteira para o binário (heurísticas novas: presença de verbos de mutação como "ordena"/"ordene"/"soma a coluna"/"cria uma coluna"/"preencha"/"remove linha"/"filtra" → `sheet_operation`; perguntas analíticas "qual"/"quantas"/"qual a média"/"quantos" → `qa`; ambíguo → `unknown`). `buildClassifierSystemPrompt()` — reescrever para o prompt binário (2 categorias + unknown).
3. `apps/web/src/app/api/chat/unified/route.ts`: `INTENT_TO_TOOL_KIND` — substituir pelo mapeamento binário (`sheet_operation` → branch de operação na planilha, `qa` → branch de Q&A, `unknown` → fallback). Substituir o fallback temporário do Plan 04 (`case "unified_table"`) pelo branch real `sheet_operation`/`qa` — ainda sem implementar o protocolo de mutação completo (RF-03/Phase 20), mas o roteamento agora usa os novos labels. Se a implementação completa de `sheet_operation` for trabalho de Phase 20, o branch pode retornar uma resposta de texto de "operação ainda não implementada" — desde que o ROTEAMENTO (classificação + dispatch) já use os labels corretos, conforme SC#3.
4. `apps/web/tests/intent-classifier.test.ts`: reescrever os asserts hardcoded para os 9 intents antigos. Incluir 6-8 casos representativos: (a) mutação clara ("ordena por data", "cria uma coluna de total" → `sheet_operation`), (b) Q&A clara ("qual a média da coluna Valor?", "quantas linhas têm valor acima de 1000?" → `qa`), (c) ambíguo "soma"-like ("some a coluna Valor" — fixar a expectativa de produto: classificar como `qa` por padrão dado que "somar X" tende a pedir um resultado, não criar fórmula — documentar a escolha no SUMMARY como decisão de produto a revisitar na UAT da Phase 20).

Purpose: SC#3 da Phase 18 — classificador reconhece só "operação na planilha" e "Q&A"; planta o eval binário do checkpoint 17→18.
Output: schema.ts/intent-classifier.ts/route.ts/intent-classifier.test.ts atualizados; `pnpm -r typecheck && pnpm -r test` verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-04-SUMMARY.md
</context>

<interfaces>
<!-- packages/shared/src/unified-chat/schema.ts — ANTES desta plan: -->
<!-- export const UNIFIED_INTENTS = ["formula","sql","regex","script","template","file_analysis","ocr","tabela","unknown"] as const; -->
<!-- export const OVERRIDE_INTENTS = ["formula","sql","regex","script","template","file_analysis","ocr","tabela"] as const; -->
<!-- export const FILE_DEPENDENT_INTENTS = ["file_analysis","ocr"] as const; -->
<!-- export const unifiedIntentSchema = z.enum(UNIFIED_INTENTS); -->
<!-- export const overrideIntentSchema = z.enum(OVERRIDE_INTENTS); -->
<!-- export const fileDependentIntentSchema = z.enum(FILE_DEPENDENT_INTENTS); -->
<!-- export const intentClassificationSchema = z.object({ intent: unifiedIntentSchema, confidence: z.enum(["high","low"]) }); -->

<!-- DEPOIS desta plan: -->
<!-- export const UNIFIED_INTENTS = ["sheet_operation", "qa", "unknown"] as const; -->
<!-- export const OVERRIDE_INTENTS = ["sheet_operation", "qa"] as const; -->
<!-- FILE_DEPENDENT_INTENTS/fileDependentIntentSchema: REMOVIDOS -->
<!-- intentClassificationSchema: mantém a forma { intent, confidence } -->

<!-- apps/web/src/server/ai/intent-classifier.ts — exports preservados (assinatura igual): -->
<!-- export type ClassifyIntentInput = { prompt: string; hasFile: boolean; lastIntent?: string | null; overrideIntent?: string | null }; -->
<!-- export async function classifyIntent(input: ClassifyIntentInput): Promise<IntentClassification> -->
<!-- classifyFileIntent: REMOVER. fixtureClassify/buildClassifierSystemPrompt: reescrever corpo, manter assinatura. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduzir UNIFIED_INTENTS/OVERRIDE_INTENTS em schema.ts e reescrever intent-classifier.ts para o eixo binário</name>
  <read_first>
    - packages/shared/src/unified-chat/schema.ts (linhas 1-40)
    - apps/web/src/server/ai/intent-classifier.ts (arquivo completo, 176 linhas)
    - apps/web/tests/intent-classifier.test.ts (arquivo completo)
    - .planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md (Bloco 6, "Validation Architecture" / "O Eval Binário do Classificador")
  </read_first>
  <behavior>
    - `fixtureClassify("ordena por data", false)` -> `{ intent: "sheet_operation", confidence: "high" }`
    - `fixtureClassify("cria uma coluna de total", false)` -> `{ intent: "sheet_operation", confidence: "high" }`
    - `fixtureClassify("preencha os valores faltantes", false)` -> `{ intent: "sheet_operation", confidence: "high" }`
    - `fixtureClassify("qual a média da coluna Valor?", false)` -> `{ intent: "qa", confidence: "high" }`
    - `fixtureClassify("quantas linhas têm valor acima de 1000?", false)` -> `{ intent: "qa", confidence: "high" }`
    - `fixtureClassify("some a coluna Valor", false)` -> `{ intent: "qa", confidence: "low" }` (decisão de produto documentada — "somar X" tende a Q&A; ambíguo, confidence "low")
    - `fixtureClassify("", false)` / texto < 3 chars -> `{ intent: "unknown", confidence: "low" }`
    - `fixtureClassify(prompt, true)` (com arquivo anexado) -> não usa mais `classifyFileIntent`; classifica normalmente pelo texto do prompt (anexo agora é apenas contexto de Q&A/sheet_operation, não um intent próprio)
  </behavior>
  <action>
    Em `packages/shared/src/unified-chat/schema.ts`:
    - `UNIFIED_INTENTS` -> `["sheet_operation", "qa", "unknown"] as const`
    - `OVERRIDE_INTENTS` -> `["sheet_operation", "qa"] as const`
    - Remover `FILE_DEPENDENT_INTENTS` e `fileDependentIntentSchema` (zero consumidor após Bloco 2/3/5 — confirmar via grep antes de remover; se `needsFilePayloadSchema` ainda referenciar `fileDependentIntentSchema`, tratar no Bloco 7/Plan 06, deixar nota no SUMMARY se houver dependência cruzada).
    - `intentClassificationSchema`/`unifiedIntentSchema`/`overrideIntentSchema` mantêm a forma, apenas o enum subjacente muda.

    Em `apps/web/src/server/ai/intent-classifier.ts`:
    - Remover `classifyFileIntent()`.
    - Reescrever `fixtureClassify()`: nova árvore de regex PT-BR binária. Padrões de `sheet_operation` (mutação): verbos como "ordena"/"ordene"/"organize"/"classificar por", "cria(r) uma coluna"/"adiciona(r) coluna", "preench(a/er)", "remov(a/er) linha"/"deleta(r) linha", "filtra(r)", "soma(r)"+"coluna" combinado com verbo de criação ("cria uma coluna de soma"). Padrões de `qa` (pergunta analítica): "qual"/"quais"/"quanto(s)"/"quantas", "média"/"soma total"/"maior"/"menor"/"total de", terminação de pergunta "?". Fallback: `unknown` para texto vazio/curto; para texto não vazio sem match claro, `{ intent: "qa", confidence: "low" }` (Q&A é o fallback mais seguro — não altera a grade).
    - Reescrever `buildClassifierSystemPrompt()`: prompt binário com as 3 categorias (`sheet_operation`, `qa`, `unknown`), descrições em PT-BR alinhadas ao novo eixo (operação estruturada na planilha aberta vs pergunta sobre os dados).
    - `classifyIntent()`: manter assinatura e fluxo (override/fixture/Structured Outputs/fallback) — apenas os valores de intent mudam.

    Reescreva `apps/web/tests/intent-classifier.test.ts`: substitua os asserts dos 9 intents antigos pelos 6-8 casos do bloco `<behavior>` acima. Mantenha os testes de "keeps intent as the first schema field", "short-circuits valid override intents" (ajustar valores para `sheet_operation`/`qa`), "rejects invalid override intents". Remova "classifies file prompts as analysis or OCR in fixture mode" (testava `classifyFileIntent`, removido).
  </action>
  <verify>
    <automated>pnpm exec prisma generate && pnpm --filter web test intent-classifier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "formula\|\"sql\"\|\"regex\"\|\"script\"\|\"template\"\|file_analysis\|\"ocr\"\|\"tabela\"" packages/shared/src/unified-chat/schema.ts` referindo a `UNIFIED_INTENTS`/`OVERRIDE_INTENTS` == 0 (outros usos legítimos, ex. em payloads removidos no Plan 06, são fora do escopo desta task — focar apenas nos arrays de intent)
    - `grep -c "classifyFileIntent\|FILE_DEPENDENT_INTENTS\|fileDependentIntentSchema" apps/web/src/server/ai/intent-classifier.ts packages/shared/src/unified-chat/schema.ts` == 0
    - `pnpm --filter web test intent-classifier.test.ts` exit 0 com >= 6 casos do eval binário passando
  </acceptance_criteria>
  <done>UNIFIED_INTENTS/OVERRIDE_INTENTS reduzidos a sheet_operation/qa/unknown; intent-classifier.ts binário; intent-classifier.test.ts cobre 6-8 prompts representativos do eval (nota no SUMMARY: UAT completo é Phase 20).</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar INTENT_TO_TOOL_KIND e dispatch em route.ts para o eixo binário; substituir fallback temporário do Plan 04</name>
  <read_first>
    - apps/web/src/app/api/chat/unified/route.ts (estado pós-Plan 04 — reler completo)
    - .planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-04-SUMMARY.md (localizar o fallback temporário documentado)
    - apps/web/tests/unified-route.test.ts (estado pós-Plan 04)
  </read_first>
  <action>
    Em `apps/web/src/app/api/chat/unified/route.ts`:
    - Atualizar o type `ResolvedToolKind`/`INTENT_TO_TOOL_KIND` (ou removê-lo se não fizer mais sentido com apenas 2-3 valores — simplificar para usar `classification.intent` diretamente como discriminador, já que `UnifiedIntent` agora é `"sheet_operation" | "qa" | "unknown"`).
    - Localizar o fallback temporário inserido no Plan 04 (`case "unified_table"` → resposta de texto provisória) e substituí-lo por dois branches reais: `"sheet_operation"` e `"qa"` (e `"unknown"` cai em um dos dois — recomendação: tratar `unknown` como `qa`, resposta de texto, nunca crash). Ambos os branches, NESTA FASE, podem retornar uma resposta de texto via `createEventStream` (ex.: "Recebi seu pedido. Implementação completa da operação na planilha chega na próxima fase.") — a IMPLEMENTAÇÃO REAL do protocolo de mutação (`sheet_operation`) e da resposta analítica (`qa`) é Phase 20 (CHAT-01..06). O objetivo desta task é o ROTEAMENTO correto (SC#3), não a feature completa.
    - Persistir `toolKind` em `saveConversationExchange` usando os novos labels (`"sheet_operation"`/`"qa"`) em vez de `"unified_table"`/`"formula"` etc — confirmar que `recordToolRequest`/`saveConversationExchange` aceitam string livre (já confirmado no RESEARCH.md — partição `userId+toolKind` é string livre).

    Ajustar `apps/web/tests/unified-route.test.ts`: os testes restantes (auth 401, prompt inválido 400) não dependem do dispatch específico — devem continuar passando. Adicionar (ou ajustar) 1-2 testes mínimos cobrindo o novo roteamento binário: prompt classificado como `sheet_operation` retorna `intent_detected` com `intent: "sheet_operation"`; prompt classificado como `qa` retorna `intent_detected` com `intent: "qa"`.
  </action>
  <verify>
    <automated>pnpm exec prisma generate && pnpm --filter web test unified-route.test.ts && pnpm -r typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "unified_table\|INTENT_TO_TOOL_KIND\[\"formula\"\]\|INTENT_TO_TOOL_KIND\[\"tabela\"\]" apps/web/src/app/api/chat/unified/route.ts` == 0
    - `grep -c "\"sheet_operation\"\|\"qa\"" apps/web/src/app/api/chat/unified/route.ts` >= 2
    - `pnpm --filter web test unified-route.test.ts` exit 0
    - `pnpm -r typecheck` exit 0
  </acceptance_criteria>
  <done>route.ts roteia por sheet_operation/qa/unknown sem fallback temporário; persistência usa os novos toolKind labels; unified-route.test.ts cobre o roteamento binário; typecheck verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → POST /api/chat/unified → classifyIntent() | Classificação determina o branch; fixtureClassify roda sem OPENAI_API_KEY (modo fixture do projeto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-13 | Tampering | `overrideIntentSchema`/`unifiedIntentSchema` reduzidos devem rejeitar valores antigos (`"sql"`, `"ocr"`, `"tabela"`) com 400 limpo | mitigate | `validateOptionalIntent` já usa `.safeParse()` — ao reduzir o enum, valores antigos automaticamente falham a validação e retornam 400; confirmado por teste em Task 2 |
| T-18-14 | Information Disclosure | `assistantPayload.kind` antigo no histórico (`formula`/`sql`/etc) causa erro de parse não tratado ao carregar `findConversationExchanges` | accept | Fora do escopo desta task — `findConversationExchanges`/`saveConversationExchange` operam sobre `toolKind` (string livre) e `assistantPayload` (JSON), não fazem `.parse()` estrito no schema reduzido nesta camada; o `RenderDispatcher` (Plan 06) trata o parse no client |
| T-18-15 | Denial of Service | Fallback binário (`qa`/`sheet_operation`) lança exceção para casos não cobertos | mitigate | Task 2 usa `createEventStream` com resposta de texto determinística para ambos os branches — nunca `throw` não capturado |
</threat_model>

<verification>
```bash
pnpm exec prisma generate
pnpm --filter web test intent-classifier.test.ts unified-route.test.ts
pnpm -r typecheck
grep -c "FILE_DEPENDENT_INTENTS\|fileDependentIntentSchema\|classifyFileIntent" apps/web/src/server/ai/intent-classifier.ts packages/shared/src/unified-chat/schema.ts
grep -n "UNIFIED_INTENTS\s*=\|OVERRIDE_INTENTS\s*=" packages/shared/src/unified-chat/schema.ts
```
</verification>

<success_criteria>
- UNIFIED_INTENTS/OVERRIDE_INTENTS reduzidos a sheet_operation/qa(/unknown); FILE_DEPENDENT_INTENTS removido.
- intent-classifier.ts classifica em binário; 6-8 prompts do eval (checkpoint 17->18) cobertos em intent-classifier.test.ts, com nota de que UAT completo (~20 prompts) é Phase 20.
- route.ts roteia por sheet_operation/qa/unknown sem fallback temporário do Plan 04; persistência usa os novos labels.
- `pnpm -r typecheck && pnpm -r test` verdes.
</success_criteria>

<output>
Create `.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-05-SUMMARY.md` when done
</output>
</content>
