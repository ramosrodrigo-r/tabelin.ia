---
phase: 18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/api/tools/formula/generate/route.ts (delete)
  - apps/web/src/app/api/tools/formula/explain/route.ts (delete)
  - apps/web/src/app/api/tools/sql/generate/route.ts (delete)
  - apps/web/src/app/api/tools/regex/generate/route.ts (delete)
  - apps/web/src/app/api/tools/regex/explain/route.ts (delete)
  - apps/web/src/app/api/tools/scripts/generate/route.ts (delete)
  - apps/web/src/app/api/tools/template/generate/route.ts (delete)
  - apps/web/src/app/(workspace)/workspace/sql/page.tsx (delete)
  - apps/web/src/app/(workspace)/workspace/regex/page.tsx (delete)
  - apps/web/src/app/(workspace)/workspace/scripts/page.tsx (delete)
  - apps/web/src/app/(workspace)/workspace/templates/page.tsx (delete)
  - apps/web/src/features/sql/ (delete dir)
  - apps/web/src/features/regex/ (delete dir)
  - apps/web/src/features/scripts/ (delete dir)
  - apps/web/src/features/template/ (delete dir)
  - apps/web/src/features/formula/ (delete dir, except copy-button.tsx decision)
  - apps/web/tests/formula-api.test.ts (delete)
  - apps/web/tests/formula-contract.test.ts (delete)
  - apps/web/tests/formula-ui.test.tsx (delete)
  - apps/web/tests/multi-turn-context.test.ts (delete)
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "Nenhuma rota /api/tools/{formula,sql,regex,scripts,template} responde mais"
    - "Nenhuma página /workspace/{sql,regex,scripts,templates} existe mais"
  artifacts:
    - path: "apps/web/src/app/api/tools/formula"
      provides: "removido"
    - path: "apps/web/src/app/api/tools/sql"
      provides: "removido"
    - path: "apps/web/src/app/api/tools/regex"
      provides: "removido"
    - path: "apps/web/src/app/api/tools/scripts"
      provides: "removido"
    - path: "apps/web/src/app/api/tools/template"
      provides: "removido"
  key_links: []
---

<objective>
Remover o Bloco 1 do inventário de deleção da Phase 18 (CLEAN-01, parte 1): as 5 rotas de API dos tools de texto avulsos (Fórmula/SQL/Regex/Scripts/Template), as páginas órfãs `/workspace/{sql,regex,scripts,templates}`, as `features/` dedicadas a esses 4 tools (sql/regex/scripts/template inteiros + formula com nuance no `copy-button.tsx`), e os testes que exercitam exclusivamente esse código.

Este plano NÃO toca em `apps/web/src/app/api/chat/unified/route.ts` (Bloco 5, Wave 2) nem em `apps/web/src/server/ai/*-stream.ts` (também Bloco 5) — esses arquivos ainda são importados pelo route.ts até a Wave 2. Apenas os pontos de ENTRADA (rotas próprias, páginas, features de UI) saem aqui.

Purpose: reduzir a superfície pública a zero para os 5 geradores de texto avulsos, conforme SC#1 da Phase 18 e D-§5.3/§5.7 do PRD.
Output: rotas/páginas/features/testes deletados; `pnpm -r typecheck && pnpm -r test` verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decidir destino do copy-button.tsx e remover rotas de API + páginas de workspace dos 4 tools sem nuance (sql/regex/scripts/template) + formula</name>
  <files>apps/web/src/app/api/tools/{sql,regex,scripts,template,formula}/**, apps/web/src/app/(workspace)/workspace/{sql,regex,scripts,templates,formula}/page.tsx, apps/web/src/features/formula/components/copy-button.tsx</files>
  <read_first>
    - apps/web/src/features/formula/components/copy-button.tsx
    - apps/web/src/features/unified-chat/components/render-dispatcher.tsx (linhas 1-35, import de CopyButton)
    - .planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md (Bloco 1, "Nota Formula")
  </read_first>
  <action>
    Primeiro, ler `copy-button.tsx`: se for um componente genérico (botão "copiar" que recebe `value: string` e `disabled: boolean`, sem nenhuma referência a fórmula/LLM), mova-o para `apps/web/src/components/app/copy-button.tsx` (criar o arquivo lá com o mesmo conteúdo, ajustando apenas o path de export se necessário) e atualize o import em `render-dispatcher.tsx` para `@/components/app/copy-button` — documente essa decisão no SUMMARY como "copy-button promovido a componente compartilhado". Se o componente tiver qualquer especificidade do gerador de fórmula (ex.: formatação de fórmula, referência a `FormulaCompletePayload`), NÃO mova — ele sai junto com `features/formula/` no Bloco 5/7 (deixe uma nota no SUMMARY para o plano da Wave 2 remover o import em `render-dispatcher.tsx` quando o `FormulaOutputPanel` branch sair).

    Depois, delete por completo:
    - `apps/web/src/app/api/tools/formula/generate/route.ts`
    - `apps/web/src/app/api/tools/formula/explain/route.ts`
    - `apps/web/src/app/api/tools/sql/generate/route.ts`
    - `apps/web/src/app/api/tools/regex/generate/route.ts`
    - `apps/web/src/app/api/tools/regex/explain/route.ts`
    - `apps/web/src/app/api/tools/scripts/generate/route.ts`
    - `apps/web/src/app/api/tools/template/generate/route.ts`
    - `apps/web/src/app/(workspace)/workspace/sql/page.tsx`
    - `apps/web/src/app/(workspace)/workspace/regex/page.tsx`
    - `apps/web/src/app/(workspace)/workspace/scripts/page.tsx`
    - `apps/web/src/app/(workspace)/workspace/templates/page.tsx`

    Remova os diretórios vazios resultantes em `apps/web/src/app/api/tools/{formula,sql,regex,scripts,template}/` se ficarem sem arquivos.
  </action>
  <verify>
    <automated>find apps/web/src/app/api/tools/formula apps/web/src/app/api/tools/sql apps/web/src/app/api/tools/regex apps/web/src/app/api/tools/scripts apps/web/src/app/api/tools/template -type f 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/web/src/app/api/tools/formula apps/web/src/app/api/tools/sql apps/web/src/app/api/tools/regex apps/web/src/app/api/tools/scripts apps/web/src/app/api/tools/template -type f 2>/dev/null | wc -l` == 0
    - `find apps/web/src/app/\(workspace\)/workspace/sql apps/web/src/app/\(workspace\)/workspace/regex apps/web/src/app/\(workspace\)/workspace/scripts apps/web/src/app/\(workspace\)/workspace/templates -type f 2>/dev/null | wc -l` == 0
    - Decisão sobre `copy-button.tsx` documentada no SUMMARY (movido ou marcado para Wave 2)
  </acceptance_criteria>
  <done>As 7 rotas de API e as 4 páginas de workspace não existem mais; copy-button.tsx tem destino decidido e documentado.</done>
</task>

<task type="auto">
  <name>Task 2: Remover features/ dedicadas (sql, regex, scripts, template, formula) e testes órfãos</name>
  <files>apps/web/src/features/{sql,regex,scripts,template,formula}/**, apps/web/tests/{formula-api,formula-contract,formula-ui,multi-turn-context}.test.ts(x)</files>
  <read_first>
    - apps/web/src/features/sql/, apps/web/src/features/regex/, apps/web/src/features/scripts/, apps/web/src/features/template/, apps/web/src/features/formula/ (listar conteúdo)
    - apps/web/tests/formula-api.test.ts, apps/web/tests/formula-contract.test.ts, apps/web/tests/formula-ui.test.tsx, apps/web/tests/multi-turn-context.test.ts
    - .planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md (Pitfall 4 — formula-contract.test.ts vs formula-engine.test.ts)
  </read_first>
  <action>
    Delete os diretórios inteiros:
    - `apps/web/src/features/sql/`
    - `apps/web/src/features/regex/`
    - `apps/web/src/features/scripts/`
    - `apps/web/src/features/template/`
    - `apps/web/src/features/formula/` (exceto `copy-button.tsx` se foi movido na Task 1 — deletar o arquivo original do diretório de qualquer forma após mover)

    Delete os testes órfãos:
    - `apps/web/tests/formula-api.test.ts` (testa `/api/tools/formula/*`, removidas na Task 1)
    - `apps/web/tests/formula-contract.test.ts` (confirme antes: testa `formulaGenerateRequestSchema`/`FORMULA_PLATFORMS`/`buildFormulaGenerationPrompt` — gerador LLM de fórmula, NÃO `use-formula-engine.ts`/grade — distinto de `formula-engine.test.ts` que PRESERVA)
    - `apps/web/tests/formula-ui.test.tsx` (testa `formula-tool.tsx`/`FormulaOutputPanel`, removidos)
    - `apps/web/tests/multi-turn-context.test.ts` (testa exclusivamente `/api/tools/{sql,scripts,regex,template}/generate`, todas removidas na Task 1 — confirme via grep que não cobre nenhum mecanismo genérico de `context-messages.ts`/`buildToolContextMessages` usado por outro caminho antes de deletar; se cobrir, extraia esses casos para `context-messages.test.ts` em vez de deletar tudo)

    Após as deleções, rode `pnpm exec prisma generate` (mitigação do falso-positivo de worktree) e então `pnpm -r typecheck && pnpm -r test` para confirmar que nada quebrou — qualquer erro de import remanescente apontando para `features/{sql,regex,scripts,template,formula}` ou para `/api/tools/{...}` deve ser corrigido nesta mesma tarefa (provavelmente nenhum, pois route.ts/render-dispatcher ainda importam desses módulos — se o typecheck falhar por isso, é esperado e será resolvido na Wave 2; documente no SUMMARY se esse for o caso e NÃO prossiga para "verde total" — apenas confirme que as falhas restantes são exclusivamente os imports de `route.ts`/`render-dispatcher.tsx` para `formula-stream`/`sql-stream`/etc e para `features/formula|sql|regex|scripts|template`, que são escopo da Wave 2).
  </action>
  <verify>
    <automated>pnpm exec prisma generate && find apps/web/src/features/sql apps/web/src/features/regex apps/web/src/features/scripts apps/web/src/features/template apps/web/src/features/formula -type f 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/web/src/features/sql apps/web/src/features/regex apps/web/src/features/scripts apps/web/src/features/template apps/web/src/features/formula -type f 2>/dev/null | wc -l` == 0
    - `find apps/web/tests -maxdepth 1 -name "formula-api.test.ts" -o -name "formula-contract.test.ts" -o -name "formula-ui.test.tsx" -o -name "multi-turn-context.test.ts" 2>/dev/null | wc -l` == 0
    - `pnpm exec prisma generate` exit 0
    - Resultado de `pnpm -r typecheck` documentado no SUMMARY: se vermelho, listar exatamente quais erros restam (esperado: imports de route.ts/render-dispatcher.tsx para módulos removidos, resolvidos na Wave 2)
  </acceptance_criteria>
  <done>Diretórios features/{sql,regex,scripts,template,formula} e os 4 testes órfãos não existem mais; estado de typecheck documentado (verde, ou vermelho apenas por imports de route.ts/render-dispatcher.tsx pendentes para Wave 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → /api/tools/* (removidas) | Rotas deletadas; qualquer requisição cai em 404 nativo do App Router |
| Browser → /workspace/{sql,regex,scripts,templates} (removidas) | Páginas deletadas; Next.js retorna 404 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | Rotas `/api/tools/{formula,sql,regex,scripts,template}/*` removidas | mitigate | Confirmar via `find`/build que os diretórios de rota não existem — Next.js App Router gera 404 automático para rota ausente, não exige redirect manual |
| T-18-02 | Tampering | Remoção acidental de `copy-button.tsx` consumido por `render-dispatcher.tsx` ainda ativo | mitigate | Task 1 lê `render-dispatcher.tsx` antes de decidir mover vs deletar; se mover, atualizar o import no mesmo commit para não quebrar o build |
| T-18-03 | Denial of Service | `multi-turn-context.test.ts` cobre mecanismo genérico além das rotas removidas | mitigate | Task 2 exige grep/leitura antes de deletar; se cobrir `buildToolContextMessages` de forma não duplicada em `context-messages.test.ts`, extrair casos em vez de apagar |
</threat_model>

<verification>
```bash
pnpm exec prisma generate
pnpm -r typecheck
pnpm --filter web test
find apps/web/src/app/api/tools/formula apps/web/src/app/api/tools/sql apps/web/src/app/api/tools/regex apps/web/src/app/api/tools/scripts apps/web/src/app/api/tools/template -type f 2>/dev/null | wc -l
find "apps/web/src/app/(workspace)/workspace/sql" "apps/web/src/app/(workspace)/workspace/regex" "apps/web/src/app/(workspace)/workspace/scripts" "apps/web/src/app/(workspace)/workspace/templates" -type f 2>/dev/null | wc -l
find apps/web/src/features/sql apps/web/src/features/regex apps/web/src/features/scripts apps/web/src/features/template apps/web/src/features/formula -type f 2>/dev/null | wc -l
```
</verification>

<success_criteria>
- Rotas API e páginas de workspace dos 5 tools de texto avulsos não existem mais (grep/find retorna 0).
- features/{sql,regex,scripts,template,formula} removidos; `copy-button.tsx` tem destino decidido e documentado.
- Testes órfãos (formula-api, formula-contract, formula-ui, multi-turn-context) deletados.
- `pnpm exec prisma generate` roda sem erro; estado de `pnpm -r typecheck`/`pnpm -r test` documentado no SUMMARY (verde ou vermelho-esperado-Wave-2).
</success_criteria>

<output>
Create `.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-01-SUMMARY.md` when done
</output>
</content>
