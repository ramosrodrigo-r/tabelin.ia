---
phase: 18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher
plan: 07
type: execute
wave: 5
depends_on: ["18-06"]
files_modified:
  - apps/web/src/features/unified-chat/unified-chat-tool.tsx
  - apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts
  - apps/web/src/features/unified-chat/components/session-context-selector.tsx (delete)
  - apps/web/src/server/ai/context-messages.ts
  - apps/web/tests/context-messages.test.ts
autonomous: true
requirements: [CLEAN-06, CLEAN-07]

must_haves:
  truths:
    - "unified-chat-tool.tsx não referencia mais platform/formulaLanguage/separator/sqlDialect/scriptType nem SessionContextSelector"
    - "intentFromPayload mapeia apenas table_spec->sheet_operation e qa_response->qa (binário)"
    - "Não há mais handlers de clarificação (handleAnswerClarification/handleSkipClarification/handleConfirmSpec) nem props onAnswer/onSkip/onConfirm/needsFile no RenderDispatcher"
    - "serializeAssistant em context-messages.ts só trata kinds que ainda existem (table_spec, qa_response) + default null"
    - "pnpm -r typecheck && pnpm -r test passam"
  artifacts:
    - path: "apps/web/src/features/unified-chat/unified-chat-tool.tsx"
      provides: "UnifiedChatTool reduzido ao eixo binário sheet_operation/qa, sem SessionContextSelector"
    - path: "apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts"
      provides: "SubmitUnifiedChatInput reduzido, sem campos de tool avulso e sem needsFile"
    - path: "apps/web/src/server/ai/context-messages.ts"
      provides: "serializeAssistant reduzido a table_spec/qa_response + default null"
  key_links:
    - from: "apps/web/src/features/unified-chat/unified-chat-tool.tsx"
      to: "apps/web/src/features/unified-chat/components/render-dispatcher.tsx"
      via: "<RenderDispatcher payload={...} />"
      pattern: "RenderDispatcher"
---

<objective>
Concluir o Bloco 7 do inventário de deleção da Phase 18 (finalização CLEAN-06 + CLEAN-07 no client): reduzir `unified-chat-tool.tsx` e `use-unified-chat-stream.ts` ao eixo binário `sheet_operation`/`qa`, removendo todo o estado/handlers exclusivos de tools avulsos (`platform`, `formulaLanguage`, `separator`, `sqlDialect`, `scriptType`) e de clarificação/geração-do-zero de tabela (`handleAnswerClarification`, `handleSkipClarification`, `handleConfirmSpec`, `needsFile`), deletar `session-context-selector.tsx`, e limpar `context-messages.ts` (`serializeAssistant`) dos cases de payloads removidos (sql/regex_generate/script/template/table_stub/table_clar_question/formula), preservando `table_spec` (grade) e adicionando `qa_response`.

**Decisão de coordenação com Plan 06:** o Plan 06 já removeu de `render-dispatcher.tsx` as props/cases que consumiam `onAnswer`/`onSkip`/`onConfirm`/`needsFile`. Este plano remove os USOS dessas props no chamador (`unified-chat-tool.tsx`) e os tipos correspondentes em `use-unified-chat-stream.ts`/`SubmitUnifiedChatInput`.

**`intentFromPayload` (binário):** `"table_spec"` -> `"sheet_operation"`, `"qa_response"` -> `"qa"`, demais/desconhecido -> `null`. Isso reflete `UnifiedIntent = "sheet_operation" | "qa" | "unknown"` (Plan 05) e `unifiedCompletePayloadSchema = union(tableSpecPayloadSchema, qaResponsePayloadSchema)` (Plan 06).

**`serializeAssistant` em context-messages.ts:** preservar `case "table_spec"` (ainda usado pela grade — histórico de spec confirmada continua relevante como contexto multi-turn) e adicionar `case "qa_response"` (serializa `[Resposta anterior]\n${content}`). Remover os cases `sql`, `regex_generate`, `script`, `template`, `table_stub`, `table_clar_question`, `formula` — todos órfãos (payloads desses kinds não são mais produzidos desde Plans 04-06; histórico antigo com esses kinds cai no `default: return null`, comportamento já existente e testado, D-09).

Purpose: SC#3/SC#4 da Phase 18 — UI do chat unificado e construção de contexto multi-turn refletem só o eixo "operação na planilha vs Q&A".
Output: unified-chat-tool.tsx/use-unified-chat-stream.ts/context-messages.ts reduzidos; session-context-selector.tsx deletado; `pnpm -r typecheck && pnpm -r test` verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-06-SUMMARY.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-05-SUMMARY.md
</context>

<interfaces>
<!-- packages/shared/src/unified-chat/schema.ts (pós Plan 05/06) — relevante para esta plan: -->
<!-- export type UnifiedIntent = "sheet_operation" | "qa" | "unknown" -->
<!-- export type OverrideIntent = "sheet_operation" | "qa" -->
<!-- export const unifiedCompletePayloadSchema = z.union([tableSpecPayloadSchema, qaResponsePayloadSchema]) -->
<!-- export type UnifiedCompletePayload = TableSpecPayload | QaResponsePayload -->
<!-- export type QaResponsePayload = { kind: "qa_response"; content: string } -->
<!-- export type TableSpecPayload = { kind: "table_spec"; title; columns; rowCount; format?; rows?; ... } -->
<!-- unifiedStreamEventSchema: SEM evento "needs_file" / "quota_warning" (removidos no Plan 06) -->

<!-- render-dispatcher.tsx (pós Plan 06) — RenderDispatcher props esperadas por este plano: -->
<!-- (status, draft, payload, metadata, warnings, error, attachmentMeta, onRetry) -->
<!-- onAnswer/onSkip/onConfirm/needsFile: REMOVIDOS da assinatura pelo Plan 06 -->
<!-- (confirmar exatamente no SUMMARY do Plan 06 antes de editar o chamador) -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduzir use-unified-chat-stream.ts (SubmitUnifiedChatInput binário, remover needsFile) e context-messages.ts (serializeAssistant binário)</name>
  <read_first>
    - apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts (arquivo completo)
    - apps/web/src/server/ai/context-messages.ts (arquivo completo)
    - apps/web/tests/context-messages.test.ts (arquivo completo)
    - packages/shared/src/unified-chat/schema.ts (pós Plan 06 — confirmar QaResponsePayload/TableSpecPayload/unifiedStreamEventSchema)
  </read_first>
  <behavior>
    - `serializeAssistant({ kind: "qa_response", content: "A média é 42." })` -> `"[Resposta anterior]\nA média é 42."`
    - `serializeAssistant({ kind: "table_spec", title: "Vendas", columns: [{name:"Data"},{name:"Valor"}], rowCount: 10 })` -> string contendo `"Vendas"` e `"Data, Valor"` (formato preservado igual ao atual)
    - `serializeAssistant({ kind: "sql", query: "SELECT 1", explanation: "x" })` -> `null` (kind removido, cai no default)
    - `serializeAssistant({ kind: "table_stub", originalPrompt: "x", message: "y" })` -> `null` (kind removido)
    - `serializeAssistant({ kind: "unknown_future_kind" })` -> `null` (default preservado, D-09)
  </behavior>
  <action>
    Em `apps/web/src/server/ai/context-messages.ts`, função `serializeAssistant`:
    - Remover os cases `"sql"`, `"regex_generate"`, `"script"`, `"template"`, `"table_stub"`, `"table_clar_question"`, `"formula"`.
    - Manter o case `"table_spec"` exatamente como está (extrai `title`/`columns`).
    - Adicionar `case "qa_response"`: extrair `content` (`typeof p.content === "string" ? p.content.trim() : ""`); se vazio retornar `null`; caso contrário retornar `` `[Resposta anterior]\n${content}` ``.
    - Manter `default: return null` (D-09, já existe).
    - Atualizar o comentário JSDoc da função (linha ~52-72) para refletir os 2 kinds atuais (`table_spec`, `qa_response`) em vez da lista de 7 kinds antiga.

    Em `apps/web/tests/context-messages.test.ts`: localizar os testes que exercitam `serializeAssistant` via `buildToolContextMessages` com payloads `sql`/`regex_generate`/`script`/`template`/`table_stub`/`table_clar_question`/`formula` — ajustar para os 5 casos do `<behavior>` acima (3 produzem string, 2 produzem null via default). Preservar os testes de `truncateHistory`/`buildMultiTurnSystemPrompt`/budget de tokens — não dependem do `kind`.

    Em `apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts`:
    - `SubmitUnifiedChatInput`: remover `platform`, `formulaLanguage`, `separator`, `sqlDialect`, `scriptType`, `overrideGenerate`, `specOverride` (exclusivos de tools avulsos/clarificação). Manter `prompt`, `file`, `overrideIntent` (agora `Exclude<UnifiedIntent, "unknown">` = `"sheet_operation" | "qa"`), `lastIntent`.
    - Remover o import de `FileDependentIntent` e o estado `needsFile`/`setNeedsFile` (e o branch `if (event.type === "needs_file")` em `handleLine`, e `if (event.payload.kind === "needs_file")` dentro do branch `complete` — `unifiedStreamEventSchema` pós-Plan 06 não emite mais `needs_file`).
    - Remover `FormulaLanguage`/`FormulaPlatform`/`ScriptType`/`SqlDialect` do import de `@tabelin/shared` (órfãos).
    - No corpo de `submit()`: remover os `fd.append(...)`/campos do JSON body para `platform`/`formulaLanguage`/`separator`/`sqlDialect`/`scriptType`/`overrideGenerate`/`specOverride`. Manter `prompt`, `overrideIntent`, `lastIntent`, `file`.
    - Remover `needsFile` do objeto retornado pelo hook.
  </action>
  <verify>
    <automated>pnpm exec prisma generate && pnpm --filter web exec vitest run context-messages.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case \"sql\"\|case \"regex_generate\"\|case \"script\"\|case \"template\"\|case \"table_stub\"\|case \"table_clar_question\"\|case \"formula\"" apps/web/src/server/ai/context-messages.ts` == 0
    - `grep -c "case \"qa_response\"\|case \"table_spec\"\|default:" apps/web/src/server/ai/context-messages.ts` >= 3
    - `grep -c "FileDependentIntent\|needsFile\|FormulaLanguage\|FormulaPlatform\|ScriptType\|SqlDialect\|overrideGenerate\|specOverride" apps/web/src/features/unified-chat/hooks/use-unified-chat-stream.ts` == 0
    - `pnpm --filter web exec vitest run context-messages.test.ts` exit 0
  </acceptance_criteria>
  <done>serializeAssistant reduzido a table_spec/qa_response/default; SubmitUnifiedChatInput binário sem campos de tools avulsos/clarificação; testes de context-messages atualizados e passando.</done>
</task>

<task type="auto">
  <name>Task 2: Reduzir unified-chat-tool.tsx (UnifiedContext, intentFromPayload, handlers de clarificação) e deletar session-context-selector.tsx</name>
  <read_first>
    - apps/web/src/features/unified-chat/unified-chat-tool.tsx (arquivo completo, já lido nesta sessão)
    - apps/web/src/features/unified-chat/components/session-context-selector.tsx
    - apps/web/src/features/unified-chat/components/render-dispatcher.tsx (estado pós-Plan 06 — confirmar props finais de RenderDispatcher)
    - apps/web/tests/unified-chat-tool.test.tsx (localizar testes que exercitam SessionContextSelector/clarificação/override de 9 intents)
  </read_first>
  <action>
    Delete `apps/web/src/features/unified-chat/components/session-context-selector.tsx`.

    Em `apps/web/src/features/unified-chat/unified-chat-tool.tsx`:
    - Remover o type `UnifiedContext` e a função `defaultContext()`. Remover o estado `context`/`setContext` e `submittedContext`/`setSubmittedContext` SE não houver mais nenhum uso após a remoção dos campos — caso `UnifiedExchange.context` ainda seja necessário para outro propósito (ex.: re-submit em `handleRetry`), simplificar para um objeto vazio `{}` ou remover o campo `context` de `UnifiedExchange`/`PersistedExchange` por completo (preferível — menos estado implícito).
    - Reescrever `intentFromPayload(payload)`: `case "table_spec": return "sheet_operation"`, `case "qa_response": return "qa"`, `default: return null`. Remover todos os outros cases (`formula`/`explanation`/`sql`/`regex_*`/`script`/`template`/`file_analysis`/`ocr`/`table_stub`/`table_clar_question`/`needs_file`).
    - `UnifiedExchange.needsFile: "file_analysis" | "ocr" | null` -> remover o campo inteiro (tipo `FileDependentIntent` não existe mais).
    - Remover `handleAnswerClarification`, `handleSkipClarification`, `handleConfirmSpec` (e o import de `TableSpecPayload` SE não usado em outro lugar — `TableSpecPayload` ainda é exportado por `@tabelin/shared`, mas se não for referenciado neste arquivo após a remoção dos handlers, remover o import).
    - `handleOverride`/`handleLiveOverride`: simplificar `overrideIntent: OverrideIntent` para o tipo binário `"sheet_operation" | "qa"` (já é o tipo de `OverrideIntent` pós-Plan 05 — sem mudança de assinatura, apenas confirmar que compila).
    - `submitPrompt`: remover `contextSnapshot` do parâmetro `options` e do corpo (não há mais `UnifiedContext` para snapshot). `submitInput` passa a ser `{ prompt: trimmed, file: fileSnapshot, overrideIntent: options.overrideIntent, lastIntent: resolvedLastIntent }` (campos `platform`/`formulaLanguage`/`separator`/`sqlDialect`/`scriptType` removidos, conforme `SubmitUnifiedChatInput` reduzido na Task 1).
    - `lastSubmitInputRef`/`handleRetry`: ajustar para o `submitInput` reduzido — `handleRetry` chama `submitPrompt(exchange.userText)` sem `contextSnapshot`.
    - No JSX: remover `<SessionContextSelector ... />` e a prop `options={...}` do `<ChatInput>` (ou passar `options={undefined}`/omitir a prop, conforme assinatura de `ChatInput` — verificar se `options` é opcional). Remover import de `SessionContextSelector`.
    - Nas duas instâncias de `<RenderDispatcher ... />`: remover as props `needsFile`, `onAnswer`, `onSkip`, `onConfirm` (Plan 06 já removeu da assinatura do componente — passar essas props agora seria erro de tipo). Manter `status`, `draft`, `payload`, `metadata`, `warnings`, `error`, `attachmentMeta`, `onRetry`.
    - Ajustar o texto do empty-state (linhas ~363-368): remover a menção a "fórmula PROCV"/"SELECT das vendas"/"análise ou OCR" — substituir por um texto alinhado ao eixo binário, ex.: "Escreva um pedido sobre a planilha aberta (ex.: \"ordena por data\", \"cria uma coluna de total\") ou faça uma pergunta sobre os dados (ex.: \"qual a média da coluna Valor?\")."
    - Ajustar o `placeholder` do `<ChatInput>` (linha ~444) de forma equivalente, removendo a menção a "fórmula, SQL, regex, script, análise ou tabela".

    Em `apps/web/tests/unified-chat-tool.test.tsx`: ajustar/remover testes que dependiam de `SessionContextSelector`, `handleAnswerClarification`/clarificação, `intentFromPayload` para os 9 kinds antigos, ou overrides para intents removidos (`sql`/`ocr`/`tabela`/etc). Manter os testes de fluxo geral (submit, retry, attachment) ajustando payloads de exemplo para `table_spec`/`qa_response`.

    Rode `pnpm exec prisma generate && pnpm -r typecheck && pnpm -r test`. Esta é a Wave 5 — a última que toca arquivos client do chat unificado; o estado FINAL deve ser verde (sem mais "vermelho esperado documentado"). Se restar erro de typecheck/test fora do escopo desta plan (ex.: testes em `smoke.spec.ts`/`unified-schema.test.ts` ainda referenciando kinds antigos — Bloco 8/Plan 08), documentar precisamente no SUMMARY com o arquivo e a linha, para o Plan 08 resolver.
  </action>
  <verify>
    <automated>pnpm exec prisma generate && pnpm -r typecheck && pnpm --filter web exec vitest run unified-chat-tool.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/web/src/features/unified-chat/components/session-context-selector.tsx 2>/dev/null | wc -l` == 0
    - `grep -c "UnifiedContext\|defaultContext\|SessionContextSelector\|handleAnswerClarification\|handleSkipClarification\|handleConfirmSpec\|FormulaLanguage\|FormulaPlatform\|ScriptType\|SqlDialect\|needsFile" apps/web/src/features/unified-chat/unified-chat-tool.tsx` == 0
    - `grep -A1 "function intentFromPayload" apps/web/src/features/unified-chat/unified-chat-tool.tsx | grep -c "table_spec\|qa_response"` >= 1
    - `pnpm --filter web exec vitest run unified-chat-tool.test.tsx` exit 0 (descontando a flaky NDJSON conhecida — rodar isolado se necessário)
    - SUMMARY documenta quaisquer erros remanescentes fora do escopo (smoke.spec.ts/unified-schema.test.ts) para o Plan 08
  </acceptance_criteria>
  <done>unified-chat-tool.tsx reduzido ao eixo binário sem context de tools avulsos nem handlers de clarificação; session-context-selector.tsx deletado; typecheck e testes do componente verdes (erros fora de escopo documentados para Plan 08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Histórico persistido (`PersistedExchange.assistantPayload`) → `intentFromPayload`/`serializeAssistant` | payloads antigos com `kind` removido não devem quebrar render nem context-building |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-19 | Information Disclosure | `intentFromPayload` recebe `kind` desconhecido de histórico antigo (`"formula"`, `"sql"`, etc) | mitigate | `default: return null` preservado — `exchange.intent` vira `null`, `IntentPill` não renderiza (já é o comportamento condicional existente: `{exchange.intent ? <IntentPill .../> : null}`) |
| T-18-20 | Tampering | `serializeAssistant` recebe payload antigo com `kind` removido durante reconstrução de contexto multi-turn | mitigate | `default: return null` preservado (D-09) — exchange é pulada na montagem de `historyMessages`, comportamento já testado |
| T-18-21 | Denial of Service | Remoção de `SubmitUnifiedChatInput.specOverride`/`overrideGenerate` quebra requests em voo de sessões antigas (FormData com campos órfãos) | accept | Campos extras em FormData/JSON são ignorados pelo backend (route.ts pós-Plan 05 não os lê); sem erro 500, apenas no-op — risco aceito, sessão antiga reflui ao recarregar a página |
</threat_model>

<verification>
```bash
pnpm exec prisma generate
pnpm -r typecheck
pnpm --filter web exec vitest run context-messages.test.ts unified-chat-tool.test.tsx
find apps/web/src/features/unified-chat/components/session-context-selector.tsx 2>/dev/null | wc -l
grep -c "UnifiedContext\|SessionContextSelector\|handleAnswerClarification" apps/web/src/features/unified-chat/unified-chat-tool.tsx
```
</verification>

<success_criteria>
- serializeAssistant reduzido a table_spec/qa_response/default(null); context-messages.test.ts atualizado e passando.
- use-unified-chat-stream.ts: SubmitUnifiedChatInput binário, sem needsFile/campos de tools avulsos.
- unified-chat-tool.tsx: sem UnifiedContext/SessionContextSelector/handlers de clarificação; intentFromPayload binário; RenderDispatcher chamado sem props removidas.
- session-context-selector.tsx deletado.
- `pnpm -r typecheck && pnpm -r test` verdes (ou erros remanescentes documentados para Plan 08, fora do escopo desta plan).
</success_criteria>

<output>
Create `.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-07-SUMMARY.md` when done
</output>
</content>
