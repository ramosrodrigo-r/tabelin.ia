---
phase: 18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/api/tools/file-analysis/chat/route.ts (delete)
  - apps/web/src/app/api/tools/file-analysis/upload/route.ts (delete)
  - apps/web/src/app/(workspace)/workspace/file-analysis/page.tsx (delete)
  - apps/web/src/features/file-analysis/ (delete dir)
  - apps/web/src/server/ai/file-chat-stream.ts (delete)
  - apps/web/src/server/file-analysis/file-repository.ts (delete)
  - apps/web/src/server/file-analysis/cleanup-job.ts (delete)
  - apps/web/src/instrumentation.ts (remove cleanup-job invocation)
autonomous: true
requirements: [CLEAN-03]

must_haves:
  truths:
    - "Nenhuma rota /api/tools/file-analysis/* responde mais"
    - "Nenhuma página /workspace/file-analysis existe mais"
    - "apps/web/src/server/file-analysis/file-parser.ts continua existindo e sendo usado por extraction/csv-xlsx-extractor.ts (PRESERVADO)"
  artifacts:
    - path: "apps/web/src/app/api/tools/file-analysis"
      provides: "removido"
    - path: "apps/web/src/server/file-analysis/file-parser.ts"
      provides: "PRESERVADO — usado por extraction/csv-xlsx-extractor.ts"
  key_links:
    - from: "apps/web/src/server/extraction/csv-xlsx-extractor.ts"
      to: "apps/web/src/server/file-analysis/file-parser.ts"
      via: "import parseFile"
      pattern: "parseFile"
---

<objective>
Remover o Bloco 3 do inventário de deleção da Phase 18 (CLEAN-03): File Analysis como tool separado — as 2 rotas `/api/tools/file-analysis/{chat,upload}`, a página `/workspace/file-analysis`, `features/file-analysis/` inteiro, `server/ai/file-chat-stream.ts`, e `server/file-analysis/{file-repository.ts,cleanup-job.ts}`.

**Decisão da Open Question A3/3 (resolvida nesta fase):** `apps/web/src/server/file-analysis/file-parser.ts` é PRESERVADO — confirmado por grep que `apps/web/src/server/extraction/csv-xlsx-extractor.ts` importa `parseFile` dele. É o caminho de ingestão CSV/XLSX usado pelo dispatcher genérico de anexos (`extraction/dispatcher.ts`, usado por `/api/chat/unified`), reaproveitado pela futura ingestão da planilha (DATA-03, Phase 19) — NÃO faz parte do "chat efêmero" do tool File Analysis. `apps/web/tests/file-parser.test.ts` testa exatamente `parseFile` — PRESERVADO também, sem modificação.

**Decisão da Open Question 2 (cleanup-job.ts):** confirmado que `cleanup-job.ts` é invocado apenas por `apps/web/src/instrumentation.ts` (hook nativo do Next.js, `register()`), não por cron/scheduler externo (sem `vercel.json`/registro de cron encontrado). Ao remover `cleanup-job.ts`, edite `instrumentation.ts` para remover a chamada — `register()` pode ficar com corpo vazio ou o arquivo `instrumentation.ts` inteiro pode ser removido se não tiver mais nenhuma outra responsabilidade (verificar antes).

Este plano NÃO toca `apps/web/src/app/api/chat/unified/route.ts` (`case "file_analysis"`/`fileAnalysisPayloadSchema` saem no Bloco 5, Wave 2) nem `render-dispatcher.tsx`/`unified-chat/schema.ts` (Bloco 6/7, Wave 3).

Purpose: SC#1/SC#2 da Phase 18 — File Analysis como tool sai por completo, ingestão CSV/XLSX genérica preservada.
Output: rota/página/feature/file-chat-stream/file-repository/cleanup-job removidos; file-parser.ts intacto; `pnpm -r typecheck && pnpm -r test` verdes (ou vermelho documentado restrito a route.ts/render-dispatcher.tsx).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover rotas, página e feature de File Analysis + file-chat-stream.ts</name>
  <files>apps/web/src/app/api/tools/file-analysis/**, apps/web/src/app/(workspace)/workspace/file-analysis/page.tsx, apps/web/src/features/file-analysis/**, apps/web/src/server/ai/file-chat-stream.ts</files>
  <read_first>
    - apps/web/src/app/api/tools/file-analysis/chat/route.ts
    - apps/web/src/app/api/tools/file-analysis/upload/route.ts
    - apps/web/src/app/(workspace)/workspace/file-analysis/page.tsx
    - apps/web/src/features/file-analysis/ (listar: file-analysis-tool.tsx, components/*, hooks/use-file-chat.ts, hooks/use-file-upload.ts)
    - apps/web/src/server/ai/file-chat-stream.ts
  </read_first>
  <action>
    Delete por completo:
    - `apps/web/src/app/api/tools/file-analysis/chat/route.ts`
    - `apps/web/src/app/api/tools/file-analysis/upload/route.ts` (e diretório `apps/web/src/app/api/tools/file-analysis/` se ficar vazio)
    - `apps/web/src/app/(workspace)/workspace/file-analysis/page.tsx`
    - `apps/web/src/features/file-analysis/` (diretório inteiro)
    - `apps/web/src/server/ai/file-chat-stream.ts`
  </action>
  <verify>
    <automated>find apps/web/src/app/api/tools/file-analysis apps/web/src/features/file-analysis apps/web/src/server/ai/file-chat-stream.ts -type f 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/web/src/app/api/tools/file-analysis apps/web/src/features/file-analysis apps/web/src/server/ai/file-chat-stream.ts -type f 2>/dev/null | wc -l` == 0
    - `find "apps/web/src/app/(workspace)/workspace/file-analysis" -type f 2>/dev/null | wc -l` == 0
  </acceptance_criteria>
  <done>Rotas, página, feature e file-chat-stream.ts removidos.</done>
</task>

<task type="auto">
  <name>Task 2: Remover file-repository.ts e cleanup-job.ts; editar instrumentation.ts; validar typecheck</name>
  <files>apps/web/src/server/file-analysis/file-repository.ts, apps/web/src/server/file-analysis/cleanup-job.ts, apps/web/src/instrumentation.ts</files>
  <read_first>
    - apps/web/src/server/file-analysis/file-repository.ts
    - apps/web/src/server/file-analysis/cleanup-job.ts
    - apps/web/src/server/file-analysis/file-parser.ts (NÃO MODIFICAR — confirmar que continua intacto)
    - apps/web/src/instrumentation.ts
    - apps/web/src/server/extraction/csv-xlsx-extractor.ts (confirmar import de parseFile de file-parser.ts)
  </read_first>
  <action>
    Delete `apps/web/src/server/file-analysis/file-repository.ts` e `apps/web/src/server/file-analysis/cleanup-job.ts`. Confirme que `apps/web/src/server/file-analysis/file-parser.ts` permanece — esse diretório agora deve conter SOMENTE `file-parser.ts`.

    Edite `apps/web/src/instrumentation.ts`: remova a chamada `startCleanupJob()` e o import dinâmico de `./server/file-analysis/cleanup-job`. Se `register()` ficar com corpo vazio e sem nenhuma outra responsabilidade, esvazie a função mas mantenha o export `register` (Next.js pode esperar o módulo `instrumentation.ts` existir mesmo vazio — NÃO delete o arquivo a menos que confirme via grep que nada referencia `instrumentation.ts`/`register` no `next.config`).

    Rode `pnpm exec prisma generate && pnpm -r typecheck`. **Resultado esperado:** o typecheck provavelmente FALHA porque `apps/web/src/app/api/chat/unified/route.ts` ainda importa `fileAnalysisPayloadSchema` (case "file_analysis" do Bloco 5, Wave 2) e `render-dispatcher.tsx` ainda importa `FileAnalysisPayload` (Bloco 7, Wave 3). Se o typecheck falhar EXCLUSIVAMENTE por esses dois símbolos, documente no SUMMARY como "vermelho esperado — resolvido na Wave 2/3" e não edite route.ts/render-dispatcher.tsx (propriedade de outros planos). Qualquer outro erro deve ser corrigido aqui.
  </action>
  <verify>
    <automated>pnpm exec prisma generate && pnpm --filter web test file-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/web/src/server/file-analysis -type f | wc -l` == 1 (apenas file-parser.ts)
    - `grep -c "cleanup-job\|startCleanupJob" apps/web/src/instrumentation.ts` == 0
    - `pnpm exec prisma generate` exit 0
    - SUMMARY documenta resultado de `pnpm -r typecheck`: lista de erros (esperado: apenas `fileAnalysisPayloadSchema` em route.ts e `FileAnalysisPayload` em render-dispatcher.tsx) ou "verde"
    - `pnpm --filter web test file-parser.test.ts` continua passando
  </acceptance_criteria>
  <done>file-repository.ts e cleanup-job.ts removidos; instrumentation.ts sem chamada a cleanup-job; file-parser.ts e seu teste intactos; estado de typecheck documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → /api/tools/file-analysis/{chat,upload} (removidas) | Rotas deletadas; 404 nativo |
| Browser → /workspace/file-analysis (removida) | Página deletada; 404 nativo |
| /api/chat/unified → extraction/dispatcher.ts → file-parser.ts | Caminho de ingestão CSV/XLSX preservado intacto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Information Disclosure | Rotas/página File Analysis removidas | mitigate | Confirmar via find que diretórios não existem; 404 nativo |
| T-18-08 | Tampering | Remover `file-parser.ts` por engano (mesmo diretório dos arquivos removidos) | mitigate | Task 2 lê e confirma `csv-xlsx-extractor.ts` antes; `file-parser.ts` fora de `files_modified` |
| T-18-09 | Denial of Service | `cleanup-job.ts` removido mas `instrumentation.ts` ainda referencia o módulo deletado, causando erro de import em runtime | mitigate | Task 2 edita `instrumentation.ts` no mesmo commit que deleta `cleanup-job.ts`; typecheck confirma ausência de import quebrado |
</threat_model>

<verification>
```bash
find apps/web/src/app/api/tools/file-analysis "apps/web/src/app/(workspace)/workspace/file-analysis" apps/web/src/features/file-analysis apps/web/src/server/ai/file-chat-stream.ts -type f 2>/dev/null | wc -l
find apps/web/src/server/file-analysis -type f
grep -c "cleanup-job" apps/web/src/instrumentation.ts
pnpm exec prisma generate
pnpm -r typecheck
pnpm --filter web test file-parser.test.ts
```
</verification>

<success_criteria>
- Rotas, página, feature, file-chat-stream.ts, file-repository.ts, cleanup-job.ts removidos.
- instrumentation.ts sem referência a cleanup-job; file-parser.ts e seu teste preservados e passando.
- Estado de `pnpm -r typecheck` documentado (verde ou vermelho restrito a route.ts/render-dispatcher.tsx, resolvido em waves seguintes).
</success_criteria>

<output>
Create `.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-03-SUMMARY.md` when done
</output>
</content>
