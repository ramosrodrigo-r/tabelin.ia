---
phase: 18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher
plan: 04
type: execute
wave: 2
depends_on: ["18-01", "18-02", "18-03"]
files_modified:
  - apps/web/src/app/api/chat/unified/route.ts
  - apps/web/src/server/ai/table-clarifier.ts (delete)
  - apps/web/src/server/ai/formula-stream.ts (delete)
  - apps/web/src/server/ai/sql-stream.ts (delete)
  - apps/web/src/server/ai/regex-stream.ts (delete)
  - apps/web/src/server/ai/scripts-stream.ts (delete)
  - apps/web/src/server/ai/template-stream.ts (delete)
  - apps/web/src/server/ai/destructive-classifier.ts (delete)
  - apps/web/src/server/ai/formula-prompts.ts (delete)
  - apps/web/src/server/tools/formula-repository.ts (delete)
  - apps/web/tests/table-clarifier.test.ts (delete)
autonomous: true
requirements: [CLEAN-07, CLEAN-01]

must_haves:
  truths:
    - "O case \"unified_table\" (stub/clarificação/confirmação de spec) não existe mais em route.ts"
    - "Os cases formula/sql/regex/script/template não existem mais em route.ts"
    - "O case file_analysis/ocr não existe mais em route.ts"
    - "table-clarifier.ts e os 5 *-stream.ts servidores não existem mais"
    - "table-clarifier, ClarificationCard, ConfirmationCard, TableIntentStub, table_clar_question, table_stub não têm mais referência em apps/web/src"
  artifacts:
    - path: "apps/web/src/app/api/chat/unified/route.ts"
      provides: "rota reduzida — sem branches de tools removidos nem geração de tabela do zero"
    - path: "apps/web/src/server/ai/table-clarifier.ts"
      provides: "removido"
  key_links: []
---

<objective>
Remover os Blocos 4 e 5 do inventário de deleção da Phase 18 — o coração da redução de `apps/web/src/app/api/chat/unified/route.ts` (CLEAN-01 parte 2 + CLEAN-07):

**Bloco 4 (CLEAN-07, decisão A2 = opção (a)):** remover por completo `apps/web/src/server/ai/table-clarifier.ts` (`askClarificationQuestion`, `buildTableSpec`, `injectCollectedSpecIntoPrompt`), o `case "unified_table"` inteiro do route.ts (clarificação + geração) e todos os helpers exclusivos do loop de clarificação (`countClarTurns`, `hasOpenTableClarification`, `mergeSpecFromHistory`, `promptLooksLikeNewTableRequest`, `conservativeClarTurnCount`, `resolveOverrideSpec`, `tableStubMessage`, `ConversationExchangeLike`, `MAX_CLAR_TURNS`, e o curto-circuito de clarificação no início do handler). **Decisão A2 = opção (a):** o intent `"tabela"`/`"unified_table"` é REMOVIDO do enum em si — isso acontece no Bloco 6 (Plan 05, Wave 3). Neste plano (Wave 2), o `INTENT_TO_TOOL_KIND["tabela"]`/`case "unified_table"` ainda existe no enum mas seu BRANCH de execução é removido — substitua o resultado por um fallback seguro dentro de `"sheet_operation"`/equivalente provisório (ver nota abaixo) para que `pnpm -r test` não quebre antes da Wave 3 reduzir o enum.

  **Nota de sequenciamento:** como `UNIFIED_INTENTS`/`INTENT_TO_TOOL_KIND` só são reduzidos no Bloco 6 (Wave 3), e este plano remove o branch de execução do `case "unified_table"`, é necessário decidir o que acontece SE `resolvedToolKind === "unified_table"` chegar ao switch nesta janela intermediária. Pitfall 1 do RESEARCH.md recomenda: tratar como fallback (resposta de texto curta explicando a limitação), nunca crashar. Implemente um `default`/fallback no switch que retorna uma resposta de texto simples via `createEventStream` (sem `table_spec`/`table_clar_question`/`table_stub`) — esse fallback é TEMPORÁRIO e será absorvido pelo branch Q&A binário criado no Bloco 6 (Wave 3, Plan 05). Documente esse fallback temporário no SUMMARY para o Plan 05 localizar e substituir.

**Bloco 5 (CLEAN-01 parte 2):** remover `case "formula"`, `case "sql"`, `case "regex"`, `case "script"`, `case "template"` do switch principal, e o bloco `if (resolvedToolKind === "file_analysis" || resolvedToolKind === "ocr")`. Remover todos os imports correspondentes (`createFormulaEventStream`, `resolveFormulaPayload`, `createRegexEventStream`, `resolveRegexPayload`, `createScriptEventStream`, `resolveScriptPayload`, `createSqlEventStream`, `resolveSqlPayload`, `createTemplateEventStream`, `resolveTemplatePayload`, `recordFormulaToolRequest`, `formulaGenerateRequestSchema`, `regexGenerateRequestSchema`, `scriptGenerateRequestSchema`, `sqlGenerateRequestSchema`, `templateGenerateRequestSchema`, `fileAnalysisPayloadSchema`, `ocrPayloadSchema`, `needsFileStream`/`FileDependentIntent` se órfãos). `recordToolRequest`/`saveConversationExchange`/`findConversationExchanges` ficam — usados pelo que resta.

Deletar os arquivos `server/ai/{formula,sql,regex,scripts,template}-stream.ts`, `server/ai/destructive-classifier.ts` (consumido só por sql-stream/scripts-stream), `server/ai/formula-prompts.ts` (consumido só por formula-stream, zero consumidores após Plan 01 deletar os testes formula-contract/formula-api), `server/tools/formula-repository.ts` (consumido só por route.ts e pelas rotas formula já removidas no Plan 01).

Purpose: SC#1, SC#3 (parcial), SC#5 da Phase 18 — route.ts reduzido ao que serve planilha + Q&A (binário completo na Wave 3).
Output: route.ts drasticamente reduzido (de 685 linhas para uma fração); 9 arquivos server/ai/server/tools deletados; `pnpm -r typecheck && pnpm -r test` verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-01-SUMMARY.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-02-SUMMARY.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-03-SUMMARY.md
</context>

<interfaces>
<!-- route.ts current structure (HEAD do repo, antes desta plan) -->
<!-- Helpers/types preservados (não tocar): -->
<!-- - getSessionFromCookieHeader, extractContent, recordToolRequest, findConversationExchanges, saveConversationExchange -->
<!-- - parseUnifiedRequest, validatePrompt, validateOptionalIntent, attachmentMetaFromContext -->
<!-- - createEventStream, prefixIntentEvent, responseFromStream, intentEvent -->
<!-- - classifyIntent (de intent-classifier.ts — reduzido na Wave 3, não aqui) -->

<!-- Helpers a REMOVER (exclusivos do loop de clarificação / unified_table): -->
<!-- countClarTurns, hasOpenTableClarification, mergeSpecFromHistory, promptLooksLikeNewTableRequest, -->
<!-- conservativeClarTurnCount, resolveOverrideSpec, tableStubMessage, ConversationExchangeLike, MAX_CLAR_TURNS, -->
<!-- needsFileStream (se ficar órfão após remover file_analysis/ocr) -->

<!-- UnifiedFields/DEFAULT_FIELDS (linhas ~44-77 do route.ts atual): -->
<!-- platform/formulaLanguage/separator/sqlDialect/scriptType são exclusivos dos tools removidos. -->
<!-- AVALIAR remoção destes campos do parsing (parseUnifiedRequest/readObjectFields/readFormString) -->
<!-- SE nenhum branch restante os consumir após esta plan — documentar decisão no SUMMARY. -->
<!-- Se a Wave 3 (unified-chat-tool.tsx) ainda enviar esses campos no body, manter os campos -->
<!-- no parsing (ignorados) é seguro; removê-los é cosmético — priorize NÃO quebrar a Wave 3 -->
<!-- sem coordenação. Recomendação: manter o parsing por ora, remover na Wave 3/4 se confirmado órfão. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remover table-clarifier.ts, case "unified_table" e helpers de clarificação do route.ts</name>
  <files>apps/web/src/app/api/chat/unified/route.ts, apps/web/src/server/ai/table-clarifier.ts (delete), apps/web/tests/table-clarifier.test.ts (delete), apps/web/tests/unified-route.test.ts</files>
  <read_first>
    - apps/web/src/app/api/chat/unified/route.ts (arquivo completo — 685 linhas)
    - apps/web/src/server/ai/table-clarifier.ts
    - apps/web/tests/table-clarifier.test.ts
    - apps/web/tests/unified-route.test.ts (describe "unified_table — clarification loop" e "unified_table — regressão de misroute", linhas ~398-690)
    - .planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md (Bloco 4, Pitfall 1, decisão A2)
  </read_first>
  <action>
    Em `apps/web/src/app/api/chat/unified/route.ts`:
    1. Remova o curto-circuito de clarificação no início do handler POST (bloco "CURTO-CIRCUITO DE CLARIFICAÇÃO" que força `resolvedToolKind = "unified_table"` baseado em `wantsTableOverride`/`hasOpenTableClarification`).
    2. Remova o `case "unified_table"` inteiro do switch (clarification path + generation path).
    3. Substitua por um fallback temporário: se `resolvedToolKind === "unified_table"` chegar ao switch, retorne uma resposta via `createEventStream` com `intentEvent(classification)` seguido de um evento `delta`/`complete` de texto simples (ex.: payload de texto explicando "ainda não suportado nesta versão — descreva o que quer fazer com a planilha aberta"). Use um `kind` de payload que JÁ exista no schema atual (ex.: reaproveitar uma estrutura mínima compatível com `unifiedCompletePayloadSchema` — se nenhuma variante textual existir ainda, use `tableStubPayloadSchema` temporariamente, JÁ QUE ele só será removido no Bloco 7/Wave 3 — documente isso explicitamente no SUMMARY como "fallback temporário a substituir no Plan 05/06"). Priorize não quebrar o build sobre elegância — este código é transitório.
    4. Remova os helpers exclusivos: `countClarTurns`, `hasOpenTableClarification`, `mergeSpecFromHistory`, `promptLooksLikeNewTableRequest`, `conservativeClarTurnCount`, `resolveOverrideSpec`, `tableStubMessage`, o type `ConversationExchangeLike`, e a constante `MAX_CLAR_TURNS` (se não usada em mais nada).
    5. Remova os imports `askClarificationQuestion`, `buildTableSpec` de `@/server/ai/table-clarifier`.

    Delete `apps/web/src/server/ai/table-clarifier.ts` e `apps/web/tests/table-clarifier.test.ts`.

    Edite `apps/web/tests/unified-route.test.ts`: remova os describes "unified_table — clarification loop" e "unified_table — regressão de misroute na clarificação" (cenários A-G) e o teste "returns a table_spec payload when clarTurnCount >= 2 (generation path)". Mantenha os testes restantes (auth, prompt inválido, formula/SQL/file_analysis — ainda existem nesta plan, removidos na Task 2).
  </action>
  <verify>
    <automated>pnpm exec prisma generate && find apps/web/src/server/ai/table-clarifier.ts apps/web/tests/table-clarifier.test.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "unified_table\|table-clarifier\|askClarificationQuestion\|buildTableSpec\|hasOpenTableClarification\|MAX_CLAR_TURNS\|ConversationExchangeLike" apps/web/src/app/api/chat/unified/route.ts` == 0 (exceto se mantido propositalmente no `INTENT_TO_TOOL_KIND`/tipo `ResolvedToolKind` — se mantido, documentar)
    - `find apps/web/src/server/ai/table-clarifier.ts apps/web/tests/table-clarifier.test.ts 2>/dev/null | wc -l` == 0
    - `grep -c "unified_table — clarification loop\|unified_table — regressão" apps/web/tests/unified-route.test.ts` == 0
  </acceptance_criteria>
  <done>case "unified_table" e table-clarifier.ts removidos; fallback temporário documentado; testes de clarificação removidos de unified-route.test.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Remover cases formula/sql/regex/script/template/file_analysis/ocr do route.ts + deletar os 9 arquivos server/ai e server/tools órfãos</name>
  <files>apps/web/src/app/api/chat/unified/route.ts, apps/web/src/server/ai/{formula,sql,regex,scripts,template}-stream.ts (delete), apps/web/src/server/ai/destructive-classifier.ts (delete), apps/web/src/server/ai/formula-prompts.ts (delete), apps/web/src/server/tools/formula-repository.ts (delete), apps/web/tests/unified-route.test.ts</files>
  <read_first>
    - apps/web/src/app/api/chat/unified/route.ts (após Task 1 — reler para pegar estado atualizado)
    - apps/web/src/server/ai/formula-stream.ts, sql-stream.ts, regex-stream.ts, scripts-stream.ts, template-stream.ts
    - apps/web/src/server/ai/destructive-classifier.ts
    - apps/web/src/server/ai/formula-prompts.ts
    - apps/web/src/server/tools/formula-repository.ts
    - apps/web/tests/unified-route.test.ts (testes restantes após Task 1)
  </read_first>
  <action>
    Em `apps/web/src/app/api/chat/unified/route.ts`:
    1. Remova `case "formula"`, `case "sql"`, `case "regex"`, `case "script"`, `case "template"` do switch principal.
    2. Remova o bloco `if (resolvedToolKind === "file_analysis" || resolvedToolKind === "ocr") { ... }` (incluindo o `ocrPayloadSchema.parse`/`fileAnalysisPayloadSchema.parse`).
    3. Remova o early-return `if ((classification.intent === "file_analysis" || classification.intent === "ocr") && !file) { return responseFromStream(needsFileStream(...)) }` e, se `needsFileStream`/`FileDependentIntent`/`needsFilePayloadSchema` ficarem sem outro uso no arquivo, remova-os também (confirmar via grep antes).
    4. Remova os imports órfãos: `createFormulaEventStream`, `resolveFormulaPayload`, `createRegexEventStream`, `resolveRegexPayload`, `createScriptEventStream`, `resolveScriptPayload`, `createSqlEventStream`, `resolveSqlPayload`, `createTemplateEventStream`, `resolveTemplatePayload`, `recordFormulaToolRequest`, `formulaGenerateRequestSchema`, `regexGenerateRequestSchema`, `scriptGenerateRequestSchema`, `sqlGenerateRequestSchema`, `templateGenerateRequestSchema`, `fileAnalysisPayloadSchema`, `ocrPayloadSchema`.
    5. Mantenha `recordToolRequest`, `saveConversationExchange`, `findConversationExchanges`, `extractContent`, `getSessionFromCookieHeader`, `classifyIntent`, `tableSpecPayloadSchema`/`TableSpecPayload`, `unifiedIntentSchema`/`overrideIntentSchema`.
    6. Confirme o `switch (resolvedToolKind)` resultante: se restar apenas o fallback temporário da Task 1 (caso `unified_table`), considere simplificar para `if (resolvedToolKind === "unified_table") { <fallback> }` sem switch — ajuste conforme o que sobrar; o objetivo é remover código morto, não preservar a forma do switch.

    Delete os arquivos:
    - `apps/web/src/server/ai/formula-stream.ts`
    - `apps/web/src/server/ai/sql-stream.ts`
    - `apps/web/src/server/ai/regex-stream.ts`
    - `apps/web/src/server/ai/scripts-stream.ts`
    - `apps/web/src/server/ai/template-stream.ts`
    - `apps/web/src/server/ai/destructive-classifier.ts`
    - `apps/web/src/server/ai/formula-prompts.ts`
    - `apps/web/src/server/tools/formula-repository.ts`

    Edite `apps/web/tests/unified-route.test.ts`: remova os testes "emits intent_detected as the first formula stream event...", "passes explicit formula context fields...", "overrideIntent forces SQL dispatch...", "returns needs_file without saving history", "returns an ephemeral file_analysis payload when a file is attached". Mantenha "rejects unauthenticated requests" e "rejects invalid prompts before provider work". Ajuste mocks (`vi.mock`) removendo os de `formula-stream`/`sql-stream`/`table-clarifier`/etc que não existem mais.

    Rode `pnpm exec prisma generate && pnpm -r typecheck && pnpm --filter web test unified-route.test.ts`. Corrija qualquer erro de import/símbolo remanescente. Em seguida rode `pnpm -r test` completo — se "corrupt NDJSON enters the error state" falhar, re-rode isolado (`pnpm --filter web test unified-chat-tool.test.tsx -t "corrupt NDJSON"`) e confirme que passa isolado antes de seguir (flaky conhecido, não é regressão desta plan).
  </action>
  <verify>
    <automated>pnpm exec prisma generate && pnpm --filter web test unified-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case \"formula\"\|case \"sql\"\|case \"regex\"\|case \"script\"\|case \"template\"\|resolvedToolKind === \"file_analysis\"\|resolvedToolKind === \"ocr\"" apps/web/src/app/api/chat/unified/route.ts` == 0
    - `grep -c "formula-stream\|sql-stream\|regex-stream\|scripts-stream\|template-stream\|destructive-classifier\|formula-prompts\|formula-repository" apps/web/src/app/api/chat/unified/route.ts` == 0
    - `find apps/web/src/server/ai/formula-stream.ts apps/web/src/server/ai/sql-stream.ts apps/web/src/server/ai/regex-stream.ts apps/web/src/server/ai/scripts-stream.ts apps/web/src/server/ai/template-stream.ts apps/web/src/server/ai/destructive-classifier.ts apps/web/src/server/ai/formula-prompts.ts apps/web/src/server/tools/formula-repository.ts 2>/dev/null | wc -l` == 0
    - `pnpm exec prisma generate && pnpm -r typecheck` exit 0
    - `pnpm --filter web test unified-route.test.ts` exit 0
  </acceptance_criteria>
  <done>route.ts sem branches dos 5 tools nem file_analysis/ocr; 8 arquivos server/ai+server/tools deletados; unified-route.test.ts ajustado e verde; pnpm -r typecheck verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → POST /api/chat/unified | Único ponto de entrada restante para o chat — auth (`getSessionFromCookieHeader`) preservada intacta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Tampering | Remover branch errado deixa `switch` inconsistente / símbolo usado por branch restante removido por engano | mitigate | Ler route.ts completo antes de editar (Task 1 read_first); typecheck após cada task confirma symbol graph |
| T-18-11 | Denial of Service | Fallback temporário do `case "unified_table"` lança exceção não tratada para um intent que ainda existe no enum até a Wave 3 | mitigate | Task 1 implementa fallback explícito via `createEventStream` (nunca `throw`/undefined), documentado para Plan 05 substituir |
| T-18-12 | Information Disclosure | Auth guard (401) e validação de prompt (400) preservados ao reduzir o handler | mitigate | Task 2 mantém `getSessionFromCookieHeader`/`validatePrompt`/`validateOptionalIntent` intactos; verificado por unified-route.test.ts |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote nesta plan — fase de remoção pura |
</threat_model>

<verification>
```bash
pnpm exec prisma generate
pnpm -r typecheck
pnpm --filter web test unified-route.test.ts
pnpm -r test
grep -c "unified_table\|table-clarifier\|formula-stream\|sql-stream\|regex-stream\|scripts-stream\|template-stream\|destructive-classifier\|formula-prompts\|formula-repository" apps/web/src/app/api/chat/unified/route.ts
find apps/web/src/server/ai/table-clarifier.ts apps/web/src/server/ai/formula-stream.ts apps/web/src/server/ai/sql-stream.ts apps/web/src/server/ai/regex-stream.ts apps/web/src/server/ai/scripts-stream.ts apps/web/src/server/ai/template-stream.ts apps/web/src/server/ai/destructive-classifier.ts apps/web/src/server/ai/formula-prompts.ts apps/web/src/server/tools/formula-repository.ts apps/web/tests/table-clarifier.test.ts -type f 2>/dev/null | wc -l
```
</verification>

<success_criteria>
- route.ts não contém mais nenhum dos branches/imports dos 5 tools, file_analysis/ocr, nem do loop de clarificação unified_table (com fallback temporário documentado e localizável para Wave 3).
- 9 arquivos (table-clarifier.ts + 5 *-stream.ts + destructive-classifier.ts + formula-prompts.ts + formula-repository.ts) e table-clarifier.test.ts deletados.
- unified-route.test.ts editado em lockstep, sem testes para branches removidos.
- `pnpm -r typecheck && pnpm -r test` verdes (NDJSON flaky tolerado se passar isolado).
</success_criteria>

<output>
Create `.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-04-SUMMARY.md` when done
</output>
</content>
