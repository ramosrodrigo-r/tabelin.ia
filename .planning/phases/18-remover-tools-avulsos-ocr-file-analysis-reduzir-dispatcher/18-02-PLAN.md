---
phase: 18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/api/tools/ocr/process/route.ts (delete)
  - apps/web/src/app/(workspace)/workspace/ocr/page.tsx (delete)
  - apps/web/src/features/ocr/ (delete dir)
  - apps/web/src/server/ai/ocr-processor.ts (delete)
  - packages/shared/src/ocr/schema.ts (delete)
  - packages/shared/src/ocr/fixtures.ts (delete)
  - packages/shared/src/index.ts (remove ocr exports)
autonomous: true
requirements: [CLEAN-02]

must_haves:
  truths:
    - "Nenhuma rota /api/tools/ocr/* responde mais"
    - "Nenhuma página /workspace/ocr existe mais"
    - "extraction/image-extractor.ts continua funcionando para anexos de imagem no chat unificado (PRESERVADO)"
  artifacts:
    - path: "apps/web/src/app/api/tools/ocr"
      provides: "removido"
    - path: "apps/web/src/server/ai/ocr-processor.ts"
      provides: "removido"
    - path: "apps/web/src/server/extraction/image-extractor.ts"
      provides: "PRESERVADO — dispatcher de anexos do chat unificado"
  key_links:
    - from: "apps/web/src/server/extraction/dispatcher.ts"
      to: "apps/web/src/server/extraction/image-extractor.ts"
      via: "import extractImage"
      pattern: "extractImage"
---

<objective>
Remover o Bloco 2 do inventário de deleção da Phase 18 (CLEAN-02): o OCR completo como tool dedicado — rota `/api/tools/ocr/process`, página `/workspace/ocr`, `features/ocr/` inteiro, `server/ai/ocr-processor.ts`, e os schemas/fixtures `packages/shared/src/ocr/*` (com seus exports em `packages/shared/src/index.ts`).

**Decisão da Open Question 1 (resolvida nesta fase):** `extraction/image-extractor.ts` é PRESERVADO — é parte do dispatcher genérico de extração de anexos (`extraction/dispatcher.ts`), usado por `/api/chat/unified` para qualquer anexo PNG/JPEG no contexto de Q&A. Isso é distinto do tool "OCR" dedicado (`/workspace/ocr`, fluxo "imagem→tabela TSV/CSV via ocr-processor.ts`), que sai por completo. Este plano NÃO toca `extraction/dispatcher.ts` nem `extraction/image-extractor.ts`.

Este plano NÃO toca `apps/web/src/app/api/chat/unified/route.ts` (o `case "ocr"`/`ocrPayloadSchema` saem no Bloco 5, Wave 2) nem `render-dispatcher.tsx`/`unified-chat/schema.ts` (Bloco 6/7, Wave 3). `ocrPayloadSchema`/`OcrPayload` ficam tecnicamente "órfãos de produtor" até a Wave 2/3, mas o schema/fixtures em `packages/shared/src/ocr/` são exclusivos deste tool e podem sair agora — desde que `pnpm -r typecheck` continue verde (route.ts ainda importa `ocrPayloadSchema` de `@tabelin/shared` — ver nota na Task 2).

Purpose: SC#1/SC#2 da Phase 18 — OCR como tool sai por completo, sem afetar extração genérica de anexos.
Output: rota/página/feature/processor/schemas de OCR deletados; image-extractor.ts intacto; `pnpm -r typecheck && pnpm -r test` verdes (ou vermelho documentado se `ocrPayloadSchema` ainda for consumido por route.ts — ver Task 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover rota, página, feature e processor de OCR</name>
  <files>apps/web/src/app/api/tools/ocr/**, apps/web/src/app/(workspace)/workspace/ocr/page.tsx, apps/web/src/features/ocr/**, apps/web/src/server/ai/ocr-processor.ts</files>
  <read_first>
    - apps/web/src/app/api/tools/ocr/process/route.ts
    - apps/web/src/app/(workspace)/workspace/ocr/page.tsx
    - apps/web/src/features/ocr/ (listar conteúdo: ocr-tool.tsx, components/image-upload-panel.tsx, components/ocr-result-panel.tsx, hooks/use-image-upload.ts)
    - apps/web/src/server/ai/ocr-processor.ts
    - apps/web/src/server/extraction/dispatcher.ts (confirmar que NÃO importa de ocr-processor.ts, apenas de image-extractor.ts)
  </read_first>
  <action>
    Delete por completo:
    - `apps/web/src/app/api/tools/ocr/process/route.ts` (e diretório `apps/web/src/app/api/tools/ocr/` se ficar vazio)
    - `apps/web/src/app/(workspace)/workspace/ocr/page.tsx`
    - `apps/web/src/features/ocr/` (diretório inteiro)
    - `apps/web/src/server/ai/ocr-processor.ts`

    Busque por assets de exemplo OCR em `apps/web/public/` e `apps/web/tests/fixtures/` com `find apps/web -iname "*ocr*"` (excluindo os arquivos já deletados e os schemas/fixtures de `packages/shared/src/ocr/`, tratados na Task 2) e delete quaisquer imagens/amostras exclusivas do tool OCR encontradas.
  </action>
  <verify>
    <automated>find apps/web/src/app/api/tools/ocr apps/web/src/features/ocr apps/web/src/server/ai/ocr-processor.ts -type f 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/web/src/app/api/tools/ocr apps/web/src/features/ocr -type f 2>/dev/null | wc -l` == 0
    - `find "apps/web/src/app/(workspace)/workspace/ocr" -type f 2>/dev/null | wc -l` == 0
    - `find apps/web/src/server/ai/ocr-processor.ts 2>/dev/null | wc -l` == 0
    - `apps/web/src/server/extraction/image-extractor.ts` continua existindo e `apps/web/src/server/extraction/dispatcher.ts` continua importando `extractImage` dele
  </acceptance_criteria>
  <done>Rota, página, feature e ocr-processor.ts removidos; image-extractor.ts/dispatcher.ts intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Remover schemas/fixtures OCR de packages/shared e validar estado de typecheck</name>
  <files>packages/shared/src/ocr/**, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/ocr/schema.ts
    - packages/shared/src/ocr/fixtures.ts
    - packages/shared/src/index.ts
    - apps/web/src/app/api/chat/unified/route.ts (grep por "ocrPayloadSchema"/"OcrPayload")
    - apps/web/src/features/unified-chat/components/render-dispatcher.tsx (grep por "OcrPayload")
  </read_first>
  <action>
    Delete `packages/shared/src/ocr/schema.ts` e `packages/shared/src/ocr/fixtures.ts`. Remova as linhas `export * from "./ocr/schema"` e `export * from "./ocr/fixtures"` de `packages/shared/src/index.ts`.

    Rode `pnpm exec prisma generate && pnpm -r typecheck`. **Resultado esperado:** o typecheck provavelmente FALHA porque `apps/web/src/app/api/chat/unified/route.ts` ainda importa `ocrPayloadSchema` de `@tabelin/shared` (case "ocr" do Bloco 5, Wave 2) e `render-dispatcher.tsx` ainda importa `OcrPayload` (Bloco 7, Wave 3).

    Se o typecheck falhar EXCLUSIVAMENTE por esses dois símbolos (`ocrPayloadSchema` em route.ts, `OcrPayload` em render-dispatcher.tsx), documente no SUMMARY como "vermelho esperado — resolvido na Wave 2 (route.ts) e Wave 3 (render-dispatcher.tsx)" e NÃO tente corrigir route.ts/render-dispatcher.tsx neste plano (eles são propriedade de outras plans/waves — editar agora criaria conflito de `files_modified` com os planos da Wave 2/3).

    Se o typecheck falhar por QUALQUER outro motivo não relacionado a `ocrPayloadSchema`/`OcrPayload`, investigue e corrija antes de finalizar.
  </action>
  <verify>
    <automated>find packages/shared/src/ocr -type f 2>/dev/null | wc -l && pnpm exec prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "export \* from \"./ocr/schema\"\|export \* from \"./ocr/fixtures\"" packages/shared/src/index.ts | wc -l` == 0
    - `find packages/shared/src/ocr -type f 2>/dev/null | wc -l` == 0
    - `pnpm exec prisma generate` exit 0
    - SUMMARY documenta o resultado exato de `pnpm -r typecheck`: lista de erros (esperado: apenas `ocrPayloadSchema` em route.ts e `OcrPayload` em render-dispatcher.tsx) ou "verde"
  </acceptance_criteria>
  <done>packages/shared/src/ocr/ removido e exports limpos; typecheck verde OU vermelho documentado restrito a route.ts (Wave 2) e render-dispatcher.tsx (Wave 3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → /api/tools/ocr/process (removida) | Rota deletada; 404 nativo |
| Browser → /workspace/ocr (removida) | Página deletada; 404 nativo |
| Browser → /api/chat/unified (anexo de imagem, preservado) | extraction/dispatcher.ts continua válido — Vision/extração genérica fica disponível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Information Disclosure | Rota/página OCR removidas | mitigate | Confirmar via find que diretórios não existem; Next.js gera 404 automático |
| T-18-05 | Tampering | Remover `image-extractor.ts` por engano (confundido com OCR) | mitigate | Task 1 lê `dispatcher.ts` antes de deletar; `image-extractor.ts` fica fora de `files_modified` deste plano |
| T-18-06 | Tampering | Remover `ocrPayloadSchema` quebra route.ts antes da Wave 2 remover o case "ocr" | accept | Vermelho esperado e documentado; route.ts é propriedade da Wave 2, que remove o import na mesma sessão de remoção do branch |
</threat_model>

<verification>
```bash
find apps/web/src/app/api/tools/ocr "apps/web/src/app/(workspace)/workspace/ocr" apps/web/src/features/ocr apps/web/src/server/ai/ocr-processor.ts packages/shared/src/ocr -type f 2>/dev/null | wc -l
grep -c "ocr" packages/shared/src/index.ts
pnpm exec prisma generate
pnpm -r typecheck
```
</verification>

<success_criteria>
- Rota, página, feature, processor e schemas/fixtures de OCR removidos (find retorna 0).
- `packages/shared/src/index.ts` sem exports de `./ocr/*`.
- `extraction/image-extractor.ts`/`extraction/dispatcher.ts` intactos e funcionais.
- Estado de `pnpm -r typecheck` documentado (verde ou vermelho restrito a route.ts/render-dispatcher.tsx, resolvido em waves seguintes).
</success_criteria>

<output>
Create `.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-02-SUMMARY.md` when done
</output>
</content>
