---
phase: 18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher
plan: 08
type: execute
wave: 6
depends_on: ["18-07"]
files_modified:
  - packages/shared/src/index.ts
  - packages/shared/src/formula/ (delete: fixtures.ts, platforms.ts, schema.ts)
  - packages/shared/src/sql/ (delete dir)
  - packages/shared/src/regex/ (delete dir)
  - packages/shared/src/scripts/ (delete dir)
  - packages/shared/src/template/ (delete dir)
  - packages/shared/src/file-analysis/ (delete dir)
  - apps/web/src/server/billing/entitlements.ts (delete)
  - apps/web/src/server/usage/quota-service.ts (delete)
  - apps/web/src/server/usage/quota-types.ts (delete)
  - apps/web/src/server/request-cache.ts (edit, remove getCachedEntitlement)
  - apps/web/tests/unified-schema.test.ts
  - apps/web/tests/quota-service.test.ts (delete)
  - apps/web/tests/e2e/smoke.spec.ts
  - apps/web/tests/e2e/formula.spec.ts (delete)
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-06, CLEAN-07]

must_haves:
  truths:
    - "packages/shared/src/index.ts não exporta mais formula/sql/regex/scripts/template/file-analysis"
    - "entitlements.ts/quota-service.ts/quota-types.ts não existem mais; request-cache.ts não exporta getCachedEntitlement"
    - "unified-schema.test.ts testa apenas os schemas vigentes (intentClassificationSchema binário, overrideIntentSchema binário, tableSpecPayloadSchema, qaResponsePayloadSchema, unifiedStreamEventSchema)"
    - "smoke.spec.ts cobre apenas fluxos ainda existentes (auth, chat unificado planilha+Q&A); formula.spec.ts removido"
    - "pnpm -r typecheck && pnpm -r test passam (suite completa, excetuando a flaky NDJSON conhecida)"
  artifacts:
    - path: "packages/shared/src/index.ts"
      provides: "exports reduzidos a billing? (verificar), unified-chat/schema, table/formula-locale"
    - path: "apps/web/tests/unified-schema.test.ts"
      provides: "suite reescrita para o eixo binário + table_spec/qa_response"
    - path: "apps/web/tests/e2e/smoke.spec.ts"
      provides: "smoke tests reduzidos a auth + chat unificado (planilha/Q&A)"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/unified-chat/schema.ts"
      via: "export * from"
      pattern: "unified-chat/schema"
---

<objective>
Concluir o Bloco 8 (final) do inventário de deleção da Phase 18 — limpeza de `packages/shared` (remover schemas/fixtures de formula/sql/regex/scripts/template/file-analysis, todos órfãos após Plans 01-07) e dos módulos de billing/quota órfãos (`entitlements.ts`, `quota-service.ts`, `quota-types.ts`, `getCachedEntitlement` em `request-cache.ts` — resolução da Assumption A4 do RESEARCH.md: os 6 "tool pages" que os consumiam foram TODOS removidos nos Plans 01-03), e reescrever os testes remanescentes que ainda referenciam o eixo de 9 intents/tools avulsos (`unified-schema.test.ts`, `apps/web/tests/e2e/smoke.spec.ts`, deletar `apps/web/tests/e2e/formula.spec.ts`).

**Resolução da Assumption A4 (RESEARCH.md):** `apps/web/src/server/billing/entitlements.ts` (`getUserEntitlement`), `apps/web/src/server/usage/quota-service.ts` (`reserveToolUse`/`confirmToolUse`/`releaseToolUse`) e `apps/web/src/server/usage/quota-types.ts` eram consumidos exclusivamente pelas 6 rotas `/api/tools/{formula,sql,regex,scripts,template}/generate` + `/api/tools/{ocr,file-analysis}/*` e pelas 8 páginas `/workspace/{formula,sql,regex,scripts,templates,ocr,file-analysis}` (`getCachedEntitlement`). Todas essas rotas/páginas foram removidas nos Plans 01-03 (Phase 18). Confirmar via grep (zero consumidores) e remover os 3 arquivos + a função `getCachedEntitlement` de `request-cache.ts` (preservar `getCachedUser`, usado por `workspace/layout.tsx`). `apps/web/tests/quota-service.test.ts` testa exclusivamente `quota-service.ts` — deletar junto. `FREE_QUOTAS`/`UserEntitlement`/`MeterKind`/`QuotaCheckResult`/etc em `packages/shared/src/billing/schema.ts` ficam órfãos de consumidor em `apps/web` mas PRESERVAR o arquivo `billing/schema.ts` em si (decisão: schemas de billing são candidatos a reuso futuro do AbacatePay — fora do escopo de deleção desta fase; apenas os consumidores em `apps/web/src/server/{billing,usage}/` saem). Confirmar no SUMMARY se `packages/shared/src/index.ts` ainda precisa exportar `./billing/schema` (provavelmente sim, mantém-se).

**packages/shared — schemas/fixtures de tools avulsos:** após Plans 01 (rotas/páginas/features) e 04 (route.ts/*-stream.ts) e 06 (unifiedCompletePayloadSchema reduzido), confirmar via grep que `packages/shared/src/{formula,sql,regex,scripts,template,file-analysis}/{schema,fixtures}.ts` e `formula/platforms.ts` têm ZERO consumidores em `apps/web/src` e `apps/web/tests`. Deletar os 6 diretórios/arquivos e remover as linhas `export * from` correspondentes em `packages/shared/src/index.ts`. `packages/shared/src/ocr/` já foi removido no Plan 02.

**Testes finais:**
- `apps/web/tests/unified-schema.test.ts`: reescrever para testar `intentClassificationSchema`/`overrideIntentSchema`/`unifiedIntentSchema` no eixo binário (`sheet_operation`/`qa`/`unknown`), `tableSpecPayloadSchema` (preservado), `qaResponsePayloadSchema` (novo, Plan 06), `unifiedStreamEventSchema` (sem `quota_warning`/`needs_file`). Remover os imports/describes de `fileAnalysisPayloadSchema`, `ocrPayloadSchema`, `tableStubPayloadSchema` e o bloco `try/require` "NOTE: ... Plan 02" (código morto de scaffold da Phase 13, já resolvido).
- `apps/web/tests/e2e/smoke.spec.ts`: remover os describes `"smoke: formula generation"`, `"smoke: multi-tools scripts/SQL/regex"`, `"smoke: OCR imagem -> tabela copiavel"`, `"smoke: chart sugestao e alternancia"` (testam tools/rotas removidas). Manter `"smoke: auth flow"`. `"smoke: file upload + chat"` e `"smoke: privacy cleanup"` — avaliar se testam o chat unificado com anexo CSV (preservado, ingestão genérica) ou o tool File Analysis removido; ajustar mocks de rota para `/api/chat/unified` se necessário, ou remover se exclusivos do tool removido (documentar decisão no SUMMARY).
- `apps/web/tests/e2e/formula.spec.ts`: deletar (testa exclusivamente `/workspace/formula`, removido no Plan 01).

Purpose: SC#1-5 da Phase 18 — fechar o inventário de deleção; `packages/shared` e a suite de testes refletem apenas o que sobrevive (planilha + Q&A + billing schemas preservados para uso futuro).
Output: packages/shared/src reduzido; entitlements/quota-service/quota-types removidos; request-cache.ts sem getCachedEntitlement; unified-schema.test.ts/smoke.spec.ts reescritos; formula.spec.ts/quota-service.test.ts deletados; `pnpm -r typecheck && pnpm -r test` verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-07-SUMMARY.md
@.planning/phases/17-quota-billing-pix-pivo/17-01-SUMMARY.md
@.planning/phases/17-quota-billing-pix-pivo/17-03-SUMMARY.md
</context>

<interfaces>
<!-- apps/web/src/server/request-cache.ts — ANTES: -->
<!-- export const getCachedUser = cache(...); -->
<!-- import { getUserEntitlement } from "./billing/entitlements"; -->
<!-- export const getCachedEntitlement = cache(getUserEntitlement); -->
<!-- DEPOIS: getCachedUser preservado; getCachedEntitlement + import removidos -->

<!-- packages/shared/src/unified-chat/schema.ts (pós Plan 06) — testado por unified-schema.test.ts: -->
<!-- export const UNIFIED_INTENTS = ["sheet_operation","qa","unknown"] -->
<!-- export const OVERRIDE_INTENTS = ["sheet_operation","qa"] -->
<!-- export const unifiedIntentSchema / overrideIntentSchema / intentClassificationSchema -->
<!-- export const tableSpecPayloadSchema / tableColumnSchema -->
<!-- export const qaResponsePayloadSchema -->
<!-- export const unifiedCompletePayloadSchema = union(tableSpecPayloadSchema, qaResponsePayloadSchema) -->
<!-- export const unifiedStreamEventSchema (sem quota_warning/needs_file) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remover entitlements/quota-service/quota-types + getCachedEntitlement; remover schemas/fixtures órfãos de packages/shared/src</name>
  <files>apps/web/src/server/billing/entitlements.ts (delete), apps/web/src/server/usage/quota-service.ts (delete), apps/web/src/server/usage/quota-types.ts (delete), apps/web/src/server/request-cache.ts, apps/web/tests/quota-service.test.ts (delete), packages/shared/src/index.ts, packages/shared/src/{formula,sql,regex,scripts,template,file-analysis}/** (delete)</files>
  <read_first>
    - apps/web/src/server/billing/entitlements.ts
    - apps/web/src/server/usage/quota-service.ts
    - apps/web/src/server/usage/quota-types.ts
    - apps/web/src/server/request-cache.ts
    - packages/shared/src/index.ts
    - packages/shared/src/billing/schema.ts (apenas confirmar exports usados por quota-types — NÃO modificar este arquivo)
  </read_first>
  <action>
    Confirme via grep que, após Plans 01-03, NENHUM arquivo em `apps/web/src` (fora dos próprios `entitlements.ts`/`quota-service.ts`/`quota-types.ts`/`request-cache.ts`) importa `getUserEntitlement`, `reserveToolUse`, `confirmToolUse`, `releaseToolUse`, `getCachedEntitlement`, ou tipos de `quota-types.ts`. Se algum consumidor inesperado aparecer, documente no SUMMARY e NÃO delete o arquivo correspondente (escopo desta fase não cobre esse consumidor).

    Delete `apps/web/src/server/billing/entitlements.ts`, `apps/web/src/server/usage/quota-service.ts`, `apps/web/src/server/usage/quota-types.ts` (e o diretório `apps/web/src/server/usage/` se ficar vazio; manter `apps/web/src/server/billing/` apenas se houver outro arquivo nele — verificar com `find`).

    Edite `apps/web/src/server/request-cache.ts`: remover `export const getCachedEntitlement = cache(getUserEntitlement);` e o import `import { getUserEntitlement } from "./billing/entitlements";`. Manter `getCachedUser` intacto.

    Delete `apps/web/tests/quota-service.test.ts`.

    Em `packages/shared/src/index.ts` e `packages/shared/src/`: delete os diretórios/arquivos `formula/fixtures.ts`, `formula/platforms.ts`, `formula/schema.ts` (e o diretório `formula/` inteiro se ficar vazio), `sql/`, `regex/`, `scripts/`, `template/`, `file-analysis/` (todos diretórios completos). Remova de `packages/shared/src/index.ts` as linhas:
    - `export * from "./formula/fixtures";`
    - `export * from "./formula/platforms";`
    - `export * from "./formula/schema";`
    - `export * from "./scripts/schema";`
    - `export * from "./scripts/fixtures";`
    - `export * from "./sql/schema";`
    - `export * from "./sql/fixtures";`
    - `export * from "./regex/schema";`
    - `export * from "./regex/fixtures";`
    - `export * from "./template/schema";`
    - `export * from "./template/fixtures";`
    - `export * from "./file-analysis/schema";`
    - `export * from "./file-analysis/fixtures";`

    Manter: `export * from "./billing/schema";`, `export * from "./unified-chat/schema";`, `export * from "./table/formula-locale";`. Confirmar via grep (`grep -rn "formula-locale\|table/" packages/shared/src/table`) se `table/formula-locale.ts` depende de algo em `formula/` que está sendo removido — se sim, documentar e NÃO remover esse arquivo de `formula/` (mover/copiar o necessário para `table/` ou manter o arquivo específico, documentando a exceção no SUMMARY).

    Rode `pnpm exec prisma generate && pnpm -r typecheck`.
  </action>
  <verify>
    <automated>pnpm exec prisma generate && pnpm -r typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/web/src/server/billing/entitlements.ts apps/web/src/server/usage/quota-service.ts apps/web/src/server/usage/quota-types.ts apps/web/tests/quota-service.test.ts 2>/dev/null | wc -l` == 0
    - `grep -c "getCachedEntitlement\|getUserEntitlement" apps/web/src/server/request-cache.ts` == 0
    - `grep -c "getCachedUser" apps/web/src/server/request-cache.ts` >= 1
    - `find packages/shared/src/sql packages/shared/src/regex packages/shared/src/scripts packages/shared/src/template packages/shared/src/file-analysis -type f 2>/dev/null | wc -l` == 0
    - `grep -c "\./formula/fixtures\|\./formula/platforms\|\./formula/schema\|\./sql/\|\./regex/\|\./scripts/\|\./template/\|\./file-analysis/" packages/shared/src/index.ts` == 0
    - `grep -c "\./billing/schema\|\./unified-chat/schema\|\./table/formula-locale" packages/shared/src/index.ts` >= 3
    - `pnpm exec prisma generate` exit 0
    - `pnpm -r typecheck` exit 0
  </acceptance_criteria>
  <done>entitlements.ts/quota-service.ts/quota-types.ts/quota-service.test.ts removidos; request-cache.ts sem getCachedEntitlement; packages/shared/src reduzido a billing/schema, unified-chat/schema, table/*; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever unified-schema.test.ts e e2e smoke.spec.ts; deletar formula.spec.ts; rodar suite completa</name>
  <files>apps/web/tests/unified-schema.test.ts, apps/web/tests/e2e/smoke.spec.ts, apps/web/tests/e2e/formula.spec.ts (delete)</files>
  <read_first>
    - apps/web/tests/unified-schema.test.ts (arquivo completo, 289 linhas)
    - apps/web/tests/e2e/smoke.spec.ts (arquivo completo)
    - apps/web/tests/e2e/formula.spec.ts (arquivo completo)
    - packages/shared/src/unified-chat/schema.ts (pós Plan 06 — confirmar exports finais)
    - .planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-05-SUMMARY.md (confirmar UNIFIED_INTENTS/OVERRIDE_INTENTS finais)
    - .planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-06-SUMMARY.md (confirmar qaResponsePayloadSchema/tableSpecPayloadSchema finais)
  </read_first>
  <action>
    Reescreva `apps/web/tests/unified-schema.test.ts`:
    - Remover o bloco `try { require("@tabelin/shared") ... } catch` (scaffold morto da Phase 13/Plan 02) e os imports de `fileAnalysisPayloadSchema`, `ocrPayloadSchema`, `tableStubPayloadSchema`.
    - Importar diretamente `intentClassificationSchema`, `unifiedIntentSchema`, `overrideIntentSchema`, `tableSpecPayloadSchema`, `tableColumnSchema`, `qaResponsePayloadSchema`, `unifiedCompletePayloadSchema`, `unifiedStreamEventSchema` de `@tabelin/shared`.
    - `describe("unified chat schemas")`: manter "keeps intent as the first classifier field". Adicionar/ajustar testes:
      - `unifiedIntentSchema.safeParse("sheet_operation").success === true`, `.safeParse("qa").success === true`, `.safeParse("formula").success === false` (intent antigo rejeitado).
      - `overrideIntentSchema.safeParse("sheet_operation").success === true`, `.safeParse("unknown").success === false` (override não inclui unknown, igual ao padrão pré-existente).
      - `tableSpecPayloadSchema.safeParse({ kind: "table_spec", title: "X", columns: [{name:"A",type:"string"}], rowCount: 1 }).success === true` (preservado).
      - `qaResponsePayloadSchema.safeParse({ kind: "qa_response", content: "resposta" }).success === true`; `.safeParse({ kind: "qa_response", content: "" }).success === false`.
      - `unifiedCompletePayloadSchema.safeParse({ kind: "table_spec", ... }).success === true` e `.safeParse({ kind: "qa_response", content: "x" }).success === true`; `.safeParse({ kind: "formula", formula: "=A1" }).success === false`.
      - `unifiedStreamEventSchema.safeParse({ type: "quota_warning", lastFreeUse: true }).success === false`; `.safeParse({ type: "needs_file", intent: "qa" }).success === false`.
    - Manter quaisquer outros describes/testes do arquivo que já testam `intentClassificationSchema`/eventos `intent_detected`/`delta`/`complete`/`error`/`warning`/`metadata`/`attachment_grounded` SE ainda válidos no schema reduzido — ajustar apenas os valores de `intent`/`kind` usados nos fixtures de teste para `sheet_operation`/`qa`/`table_spec`/`qa_response`.

    Reescreva `apps/web/tests/e2e/smoke.spec.ts`:
    - Remover os describes `"smoke: formula generation"`, `"smoke: multi-tools scripts/SQL/regex"`, `"smoke: OCR imagem -> tabela copiavel"`, `"smoke: chart sugestao e alternancia"`.
    - Manter `"smoke: auth flow"` sem alteração.
    - Para `"smoke: file upload + chat"` e `"smoke: privacy cleanup"`: ler o corpo de cada teste. Se o teste navega para `/workspace/file-analysis` (removido), reescrever para navegar ao chat unificado (`/workspace` ou rota equivalente — verificar layout atual) e fazer upload de CSV via o `AttachmentButton` do `UnifiedChatTool`, mockando `/api/chat/unified` (NDJSON com `attachment_grounded` + `complete`/`qa_response`). Se o teste for estruturalmente exclusivo do tool File Analysis removido (ex.: depende de UI/rota que não existe mais e não tem equivalente direto), DELETAR o describe e documentar no SUMMARY a decisão com justificativa (e referência ao SC correspondente — cobertura E2E completa do chat unificado é Phase 20/CHAT-*, não Phase 18).

    Delete `apps/web/tests/e2e/formula.spec.ts`.

    Rode a suite completa: `pnpm exec prisma generate && pnpm -r typecheck && pnpm -r test`. Esta é a ÚLTIMA wave da Phase 18 — o estado final deve ser verde, exceto a flaky NDJSON conhecida ("corrupt NDJSON enters the error state" em `unified-chat-tool.test.tsx`, que passa isolada). Se houver qualquer outro erro, corrija nesta task (é a última oportunidade da fase). Documente no SUMMARY o resultado final de `pnpm -r test` (contagem de suites/testes) e confirme zero referências residuais aos 9 intents antigos/tools removidos via grep global.
  </action>
  <verify>
    <automated>pnpm exec prisma generate && pnpm -r typecheck && pnpm -r test</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/web/tests/e2e/formula.spec.ts 2>/dev/null | wc -l` == 0
    - `grep -c "fileAnalysisPayloadSchema\|ocrPayloadSchema\|tableStubPayloadSchema\|require(\"@tabelin/shared\")" apps/web/tests/unified-schema.test.ts` == 0
    - `grep -c "qaResponsePayloadSchema\|tableSpecPayloadSchema" apps/web/tests/unified-schema.test.ts` >= 2
    - `grep -c "smoke: formula generation\|smoke: multi-tools scripts\|smoke: OCR imagem\|smoke: chart sugestao" apps/web/tests/e2e/smoke.spec.ts` == 0
    - `pnpm -r typecheck` exit 0
    - `pnpm -r test` exit 0 (ou apenas a flaky NDJSON documentada falhando na suite completa, passando isolada)
    - `grep -rln "formulaCompletePayloadSchema\|sqlGenerateResponseSchema\|regexCompletePayloadSchema\|scriptGenerateResponseSchema\|templateGenerateResponseSchema\|ocrPayloadSchema\|fileAnalysisPayloadSchema\|tableStubPayloadSchema\|tableClarQuestionPayloadSchema\|needsFilePayloadSchema\|FILE_DEPENDENT_INTENTS\|classifyFileIntent" apps/web packages/shared 2>/dev/null | wc -l` == 0
  </acceptance_criteria>
  <done>unified-schema.test.ts reescrito para o eixo binário + table_spec/qa_response; smoke.spec.ts reduzido aos fluxos vigentes; formula.spec.ts deletado; suite completa verde (flaky NDJSON documentada); grep global confirma zero referências residuais aos símbolos dos 9 intents antigos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `@tabelin/shared` package boundary | Reduzir exports não deve quebrar consumidores remanescentes (table-grid-panel, route.ts, render-dispatcher) |
| e2e smoke tests → app | Cobertura de regressão para os fluxos que sobrevivem (auth, chat unificado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-22 | Tampering | Remoção de `entitlements.ts`/`quota-service.ts` deixa rotas remanescentes (`/api/chat/unified`) sem qualquer limite de uso | accept | Fora do escopo da Phase 18 (D1-D6/PRD-MILESTONE não definem quota para o chat unificado nesta fase); RESEARCH.md documenta isso como decisão de Phase 17 (quota removida do unified chat) — esta plan apenas remove código órfão, não introduz a lacuna |
| T-18-23 | Repudiation | `packages/shared/src/index.ts` remove exports consumidos por código externo ao monorepo (se `@tabelin/shared` for publicado) | accept | Pacote não é publicado externamente (workspace interno pnpm) — confirmar via `package.json` `private: true` |
| T-18-24 | Denial of Service | Reescrita de `smoke.spec.ts` introduz teste que trava CI por mock incorreto de `/api/chat/unified` | mitigate | Task 2 reutiliza o padrão de mock NDJSON já estabelecido em `unified-route.test.ts`/testes existentes do chat unificado; rodar `pnpm -r test` antes de finalizar |
</threat_model>

<verification>
```bash
pnpm exec prisma generate
pnpm -r typecheck
pnpm -r test
find apps/web/src/server/billing apps/web/src/server/usage apps/web/tests/quota-service.test.ts apps/web/tests/e2e/formula.spec.ts 2>/dev/null
find packages/shared/src/sql packages/shared/src/regex packages/shared/src/scripts packages/shared/src/template packages/shared/src/file-analysis -type f 2>/dev/null | wc -l
grep -rln "formulaCompletePayloadSchema\|sqlGenerateResponseSchema\|regexCompletePayloadSchema\|scriptGenerateResponseSchema\|templateGenerateResponseSchema\|ocrPayloadSchema\|fileAnalysisPayloadSchema\|tableStubPayloadSchema\|FILE_DEPENDENT_INTENTS\|classifyFileIntent" apps/web packages/shared 2>/dev/null
```
</verification>

<success_criteria>
- entitlements.ts/quota-service.ts/quota-types.ts/quota-service.test.ts removidos; request-cache.ts preserva apenas getCachedUser.
- packages/shared/src reduzido a billing/schema (preservado), unified-chat/schema, table/*; index.ts sem exports órfãos.
- unified-schema.test.ts/smoke.spec.ts reescritos para o eixo binário; formula.spec.ts deletado.
- `pnpm -r typecheck && pnpm -r test` verdes (flaky NDJSON documentada).
- Grep global confirma zero referências residuais a qualquer símbolo dos 9 intents/tools removidos em toda a Phase 18 (SC#5).
</success_criteria>

<output>
Create `.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-08-SUMMARY.md` when done
</output>
</content>
