---
phase: 18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher
plan: 06
type: execute
wave: 4
depends_on: ["18-05"]
files_modified:
  - packages/shared/src/unified-chat/schema.ts
  - apps/web/src/features/unified-chat/components/render-dispatcher.tsx
  - apps/web/src/features/unified-chat/components/intent-pill.tsx
  - apps/web/src/features/unified-chat/components/clarification-card.tsx (delete)
  - apps/web/src/features/unified-chat/components/confirmation-card.tsx (delete)
  - apps/web/src/features/unified-chat/components/table-intent-stub.tsx (delete)
autonomous: true
requirements: [CLEAN-07, CLEAN-06]

must_haves:
  truths:
    - "render-dispatcher.tsx não tem mais cases para formula/sql/regex/script/template/table_clar_question/table_stub/file_analysis/ocr/needs_file"
    - "ClarificationCard, ConfirmationCard, TableIntentStub não existem mais"
    - "case table_spec continua renderizando TableGridPanel quando hasRows (estrutura da grade preservada)"
    - "unifiedCompletePayloadSchema só contém tableSpecPayloadSchema + um payload textual de Q&A/sheet_operation"
    - "intent-pill.tsx reduzido a 2-3 intents (sheet_operation/qa)"
  artifacts:
    - path: "apps/web/src/features/unified-chat/components/render-dispatcher.tsx"
      provides: "RenderDispatcher reduzido — table_spec (TableGridPanel) + payload textual Q&A/sheet_operation + default null"
    - path: "packages/shared/src/unified-chat/schema.ts"
      provides: "unifiedCompletePayloadSchema reduzido"
    - path: "apps/web/src/features/unified-chat/components/intent-pill.tsx"
      provides: "INTENT_LABELS/INTENT_ICONS/OVERRIDE_OPTIONS reduzidos a sheet_operation/qa"
  key_links:
    - from: "apps/web/src/features/unified-chat/components/render-dispatcher.tsx"
      to: "apps/web/src/features/unified-chat/components/table-grid-panel.tsx"
      via: "case \"table_spec\" -> <TableGridPanel spec={...} />"
      pattern: "TableGridPanel"
---

<objective>
Executar a parte client-side do Bloco 7 do inventário de deleção da Phase 18 (CLEAN-06 parte 2 + finalização do CLEAN-07): reduzir `packages/shared/src/unified-chat/schema.ts` (`unifiedCompletePayloadSchema`) e `apps/web/src/features/unified-chat/components/render-dispatcher.tsx` ao que serve "operação na planilha" (`table_spec` via `TableGridPanel`, PRESERVADO) + "Q&A" (novo payload textual simples), e deletar os 3 componentes exclusivos da geração de tabela do zero que sobraram do Bloco 4: `ClarificationCard`, `ConfirmationCard`, `TableIntentStub`.

**`packages/shared/src/unified-chat/schema.ts` — `unifiedCompletePayloadSchema`:** remover do union `formulaCompletePayloadSchema`, `sqlGenerateResponseSchema`, `regexCompletePayloadSchema`, `scriptGenerateResponseSchema`, `templateGenerateResponseSchema`, `fileAnalysisPayloadSchema`, `ocrPayloadSchema`, `tableStubPayloadSchema`, `tableClarQuestionPayloadSchema`, `needsFilePayloadSchema`. Manter `tableSpecPayloadSchema` (PRESERVAR — estrutura da grade, `TableColumn`/`TableSpecPayload` usados por `table-grid-panel.tsx`/`table-export.ts`). Adicionar um novo payload mínimo para Q&A — ex.: `qaResponsePayloadSchema = z.object({ kind: z.literal("qa_response"), content: z.string().trim().min(1) })` — esse é o formato textual que o branch `qa` do route.ts (Plan 05) usa em `saveConversationExchange`/`{ type: "complete", payload }`. Remover também `tableClarQuestionPayloadSchema`, `tableStubPayloadSchema`, `needsFilePayloadSchema`, `fileBackedPayloadMetadataSchema`, `fileAnalysisPayloadSchema`, `ocrPayloadSchema` (definições, não só do union) SE zero consumidor restar (confirmar via grep). `unifiedStreamEventSchema`: remover o evento `quota_warning` (cota já saiu na Phase 17 — confirmar zero emissor antes) e o evento `needs_file` (FILE_DEPENDENT_INTENTS removido no Plan 05).

**`render-dispatcher.tsx`:** remover os cases `"formula"`/`"explanation"`, `"sql"`, `"regex_generate"`/`"regex_explain"`, `"script"`, `"template"`, `"table_clar_question"`, `"table_stub"`, `"file_analysis"`, `"ocr"`, `"needs_file"`. Remover os componentes locais `FileBackedOutput` e `NeedsFileCard` (só usados pelos cases removidos). Remover imports: `FormulaOutputPanel`, `CopyButton` (se não migrado no Plan 01 — verificar SUMMARY do Plan 01), `RegexOutputPanel`, `ScriptsOutputPanel`, `SqlOutputPanel`, `TemplateOutputPanel`, `ClarificationCard`, `ConfirmationCard`, `TableIntentStub`, e os types `FileAnalysisPayload`, `FileDependentIntent`, `FormulaCompletePayload`, `FormulaMetadata`, `OcrPayload`, `RegexCompletePayload`, `RegexMetadata`, `ScriptGenerateResponse`, `ScriptMetadata`, `SqlGenerateResponse`, `SqlMetadata`, `TableClarQuestionPayload`, `TemplateGenerateResponse`, `TemplateMetadata`. **`case "table_spec"` PRESERVADO**: manter a renderização `hasRows -> <TableGridPanel spec={...} />`; o branch `else -> <ConfirmationCard ... />` sai (ConfirmationCard deletado) — se `hasRows` for `false`, retornar `null` (o gerador-do-zero que produzia `table_spec` sem rows não existe mais; `table_spec` só chega com rows). Adicionar um novo `case "qa_response"` que renderiza o texto via um card simples (reaproveitar o padrão de `StreamingCard`/`ErrorCard` já existentes — um `<div className="assistant-card">` com `<pre>{payload.content}</pre>`). Adicionar `default: return null` defensivo (Pitfall 2 — exchanges antigas com `kind` desconhecido não quebram o render).

**`intent-pill.tsx`:** `INTENT_LABELS`/`INTENT_ICONS`/`OVERRIDE_OPTIONS` reduzidos a `sheet_operation`/`qa` (e `unknown` em `INTENT_LABELS`/`INTENT_ICONS` se o tipo `UnifiedIntent` ainda incluir `unknown` — `OVERRIDE_OPTIONS` não inclui `unknown`, igual ao padrão atual). Escolher ícones/labels PT-BR adequados (ex.: `sheet_operation` -> "Planilha" com `FileSpreadsheet`/`Table` icon; `qa` -> "Pergunta" com ícone de texto/chat).

Purpose: SC#3/SC#4 da Phase 18 — render-dispatcher sem branches de tools removidos/geração-do-zero; schema reduzido ao que serve planilha + Q&A.
Output: schema.ts/render-dispatcher.tsx/intent-pill.tsx reduzidos; 3 componentes deletados; `pnpm -r typecheck` verde (testes de componente ajustados no Plan 07/08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-RESEARCH.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-05-SUMMARY.md
@.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-01-SUMMARY.md
</context>

<interfaces>
<!-- packages/shared/src/unified-chat/schema.ts — PRESERVAR sem alteração: -->
<!-- export const tableColumnSchema = z.object({ name, type, key?, formula?, width? }); -->
<!-- export const tableSpecPayloadSchema = z.object({ kind: z.literal("table_spec"), title, columns, rowCount, format?, rows?, formulaLanguage?, separator? }); -->
<!-- export type TableSpecPayload / TableColumn -->

<!-- NOVO payload Q&A a adicionar: -->
<!-- export const qaResponsePayloadSchema = z.object({ kind: z.literal("qa_response"), content: z.string().trim().min(1) }); -->
<!-- export type QaResponsePayload = z.infer<typeof qaResponsePayloadSchema>; -->

<!-- unifiedCompletePayloadSchema DEPOIS: -->
<!-- z.union([tableSpecPayloadSchema, qaResponsePayloadSchema]) -->

<!-- render-dispatcher.tsx — assinatura RenderDispatcher props PRESERVADA (mas needsFile/onAnswer/onSkip/onConfirm -->
<!-- tornam-se órfãos se não usados em mais nenhum case — remover da assinatura se confirmado sem uso; -->
<!-- Plan 07 ajusta unified-chat-tool.tsx que invoca RenderDispatcher, então a assinatura final -->
<!-- deve ser coordenada — documentar no SUMMARY quais props ficam/saem para o Plan 07 consumir). -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduzir unifiedCompletePayloadSchema/unifiedStreamEventSchema em schema.ts (novo qaResponsePayloadSchema)</name>
  <files>packages/shared/src/unified-chat/schema.ts, apps/web/tests/unified-schema.test.ts</files>
  <read_first>
    - packages/shared/src/unified-chat/schema.ts (arquivo completo)
    - apps/web/src/app/api/chat/unified/route.ts (pós-Plan 05 — confirmar formato exato do payload emitido pelo branch "qa")
    - .planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-05-SUMMARY.md
  </read_first>
  <behavior>
    - `qaResponsePayloadSchema.safeParse({ kind: "qa_response", content: "A média é 42." }).success` -> `true`
    - `qaResponsePayloadSchema.safeParse({ kind: "qa_response", content: "" }).success` -> `false` (min 1)
    - `unifiedCompletePayloadSchema.safeParse({ kind: "formula", ... }).success` -> `false` (formula removido do union)
    - `unifiedCompletePayloadSchema.safeParse({ kind: "table_spec", title: "X", columns: [...], rowCount: 1 }).success` -> `true` (preservado)
    - `unifiedStreamEventSchema.safeParse({ type: "quota_warning", lastFreeUse: false }).success` -> `false` (removido)
    - `unifiedStreamEventSchema.safeParse({ type: "needs_file", intent: "qa" }).success` -> `false` (removido)
  </behavior>
  <action>
    Em `packages/shared/src/unified-chat/schema.ts`:
    - Adicionar `qaResponsePayloadSchema` conforme `<interfaces>` (e exportar `QaResponsePayload` via `z.infer`).
    - Reduzir `unifiedCompletePayloadSchema` para `z.union([tableSpecPayloadSchema, qaResponsePayloadSchema])`.
    - Remover as definições `tableClarQuestionPayloadSchema`, `tableStubPayloadSchema`, `needsFilePayloadSchema`, `fileBackedPayloadMetadataSchema`, `fileAnalysisPayloadSchema`, `ocrPayloadSchema` SE zero consumidor restar fora deste arquivo (confirmar via grep em `apps/web/src` — após Plans 01-05, os únicos consumidores eram route.ts/render-dispatcher.tsx, ambos tratados nesta wave/plano).
    - Remover os imports no topo do arquivo: `formulaCompletePayloadSchema`, `regexCompletePayloadSchema`, `scriptGenerateResponseSchema`, `sqlGenerateResponseSchema`, `templateGenerateResponseSchema` (de `../formula/schema`, `../regex/schema`, etc — os arquivos `packages/shared/src/{formula,sql,regex,scripts,template,file-analysis}/schema.ts` em si ainda existem nesta wave, removidos no Plan 08/Bloco 8 se zero consumidor).
    - Em `unifiedStreamEventSchema`: remover o evento `quota_warning` (confirmar zero emissor via grep em route.ts pós-Plan 05 antes) e o evento `needs_file` (FILE_DEPENDENT_INTENTS já removido no Plan 05 — `fileDependentIntentSchema` não existe mais, então este evento referenciava um type removido).

    Escreva um pequeno teste inline ad-hoc (ou adicione a `unified-schema.test.ts` os 6 casos do `<behavior>` acima) ANTES de editar o schema (RED), confirme que falha, então edite o schema (GREEN). Os testes completos de `unified-schema.test.ts` (incluindo os describes "tableClarQuestionPayloadSchema"/"tableSpecPayloadSchema" antigos que referenciam Plan 02 da Phase 13) serão reconciliados no Plan 08 — nesta task, foque apenas nos 6 casos do `<behavior>` para não duplicar trabalho.
  </action>
  <verify>
    <automated>pnpm exec prisma generate && pnpm --filter web exec vitest run unified-schema.test.ts -t "qa_response\|quota_warning\|needs_file\|table_spec"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "formulaCompletePayloadSchema\|sqlGenerateResponseSchema\|regexCompletePayloadSchema\|scriptGenerateResponseSchema\|templateGenerateResponseSchema\|fileAnalysisPayloadSchema\|ocrPayloadSchema\|tableStubPayloadSchema\|tableClarQuestionPayloadSchema\|needsFilePayloadSchema" packages/shared/src/unified-chat/schema.ts` == 0
    - `grep -c "qaResponsePayloadSchema" packages/shared/src/unified-chat/schema.ts` >= 1
    - `grep -c "quota_warning" packages/shared/src/unified-chat/schema.ts` == 0
  </acceptance_criteria>
  <done>unifiedCompletePayloadSchema = union(tableSpecPayloadSchema, qaResponsePayloadSchema); quota_warning/needs_file removidos de unifiedStreamEventSchema; 6 testes do behavior passam.</done>
</task>

<task type="auto">
  <name>Task 2: Reduzir render-dispatcher.tsx e intent-pill.tsx; deletar ClarificationCard/ConfirmationCard/TableIntentStub</name>
  <files>apps/web/src/features/unified-chat/components/render-dispatcher.tsx, apps/web/src/features/unified-chat/components/intent-pill.tsx, apps/web/src/features/unified-chat/components/clarification-card.tsx (delete), apps/web/src/features/unified-chat/components/confirmation-card.tsx (delete), apps/web/src/features/unified-chat/components/table-intent-stub.tsx (delete)</files>
  <read_first>
    - apps/web/src/features/unified-chat/components/render-dispatcher.tsx (arquivo completo)
    - apps/web/src/features/unified-chat/components/intent-pill.tsx (arquivo completo)
    - apps/web/src/features/unified-chat/components/clarification-card.tsx
    - apps/web/src/features/unified-chat/components/confirmation-card.tsx
    - apps/web/src/features/unified-chat/components/table-intent-stub.tsx
    - apps/web/src/features/unified-chat/components/table-grid-panel.tsx (apenas a assinatura de props — PRESERVAR import)
  </read_first>
  <action>
    Delete `apps/web/src/features/unified-chat/components/clarification-card.tsx`, `confirmation-card.tsx`, `table-intent-stub.tsx`.

    Em `render-dispatcher.tsx`:
    - Remover os cases `"formula"`/`"explanation"`, `"sql"`, `"regex_generate"`/`"regex_explain"`, `"script"`, `"template"`, `"table_clar_question"`, `"table_stub"`, `"file_analysis"`, `"ocr"`, `"needs_file"`.
    - Remover os componentes locais `FileBackedOutput` e `NeedsFileCard`.
    - Remover os imports listados no `<objective>` (componentes de output panel removidos + tipos órfãos).
    - `case "table_spec"`: manter `hasRows -> <TableGridPanel spec={...} />`; se `!hasRows`, `return null`.
    - Adicionar `case "qa_response": return <div className="assistant-card" aria-label="Resposta"><div className="output-box" data-status="complete"><pre>{payload.content}</pre></div></div>` (ou reaproveitar `StreamingCard`/criar um componente local simples `QaResponseCard`).
    - Adicionar `default: return null;` ao final do switch (Pitfall 2 — defensivo contra `kind` desconhecido de histórico antigo).
    - Revisar a assinatura de `RenderDispatcher`: `needsFile`, `onAnswer`, `onSkip`, `onConfirm` ficam sem uso após remover os cases que os consumiam. Documentar no SUMMARY se devem ser removidos da assinatura aqui ou deixados para o Plan 07 (que edita `unified-chat-tool.tsx`, o chamador) — recomendação: REMOVER aqui (assinatura é responsabilidade deste arquivo) e o Plan 07 ajusta o chamador para não passar mais essas props.

    Em `intent-pill.tsx`:
    - `INTENT_LABELS`: reduzir para `{ sheet_operation: "Planilha", qa: "Pergunta", unknown: "Pergunta" }` (ou labels equivalentes PT-BR escolhidos).
    - `INTENT_ICONS`: reduzir para os mesmos 3 keys, escolhendo ícones de `lucide-react` já importados ou novos genéricos (ex.: `Table` para `sheet_operation`, `MessageCircleQuestion`/`HelpCircle` para `qa`/`unknown` — verificar disponibilidade no pacote `lucide-react` instalado).
    - `OVERRIDE_OPTIONS`: reduzir para `[{ intent: "sheet_operation", label: "Planilha", Icon: ... }, { intent: "qa", label: "Pergunta", Icon: ... }]`.
    - Remover imports de ícones não mais usados (`Braces`, `FileText`, `Image`, `LayoutTemplate`, `Regex`, `ScrollText`, `FileSpreadsheet` se substituído).

    Rode `pnpm exec prisma generate && pnpm -r typecheck`. Erros esperados nesta etapa: `unified-chat-tool.tsx`/`use-unified-chat-stream.ts` ainda referenciam tipos/props removidos (`FormulaLanguage`, `FormulaPlatform`, `SqlDialect`, `ScriptType`, `SessionContextSelector`, `handleAnswerClarification` etc) — esses são escopo do Plan 07 (Wave 5). Documente no SUMMARY a lista exata de erros restantes esperados para o Plan 07.
  </action>
  <verify>
    <automated>pnpm exec prisma generate && find apps/web/src/features/unified-chat/components/clarification-card.tsx apps/web/src/features/unified-chat/components/confirmation-card.tsx apps/web/src/features/unified-chat/components/table-intent-stub.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/web/src/features/unified-chat/components/clarification-card.tsx apps/web/src/features/unified-chat/components/confirmation-card.tsx apps/web/src/features/unified-chat/components/table-intent-stub.tsx 2>/dev/null | wc -l` == 0
    - `grep -c "FormulaOutputPanel\|SqlOutputPanel\|RegexOutputPanel\|ScriptsOutputPanel\|TemplateOutputPanel\|ClarificationCard\|ConfirmationCard\|TableIntentStub\|FileBackedOutput\|NeedsFileCard" apps/web/src/features/unified-chat/components/render-dispatcher.tsx` == 0
    - `grep -c "case \"table_clar_question\"\|case \"table_stub\"\|case \"file_analysis\"\|case \"ocr\"\|case \"needs_file\"\|case \"formula\"\|case \"sql\"\|case \"script\"\|case \"template\"\|case \"regex_generate\"" apps/web/src/features/unified-chat/components/render-dispatcher.tsx` == 0
    - `grep -c "case \"qa_response\"\|default:" apps/web/src/features/unified-chat/components/render-dispatcher.tsx` >= 2
    - `grep -c "\"formula\":\|\"sql\":\|\"regex\":\|\"script\":\|\"template\":\|\"file_analysis\":\|\"ocr\":\|\"tabela\":" apps/web/src/features/unified-chat/components/intent-pill.tsx` == 0
    - SUMMARY documenta lista de erros de typecheck esperados em `unified-chat-tool.tsx`/`use-unified-chat-stream.ts` para Plan 07
  </acceptance_criteria>
  <done>render-dispatcher.tsx reduzido a table_spec(TableGridPanel)/qa_response/default-null; intent-pill.tsx com 2-3 intents; 3 componentes deletados; erros de typecheck remanescentes documentados para Plan 07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Render de histórico (`initialExchanges`) → RenderDispatcher | Payloads antigos com `kind` removido do union não devem quebrar o render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-16 | Information Disclosure | `switch (payload.kind)` sem `default` em TS pode reclamar exhaustiveness, e em runtime `kind` desconhecido (histórico antigo) renderiza nada silenciosamente | mitigate | Task 2 adiciona `default: return null` explícito — comportamento documentado (Pitfall 2), não regressão |
| T-18-17 | Tampering | `qaResponsePayloadSchema` novo precisa rejeitar `content` vazio (min 1) para evitar card vazio renderizado | mitigate | Task 1 inclui `.min(1)` e teste correspondente |
| T-18-18 | Information Disclosure | `unifiedCompletePayloadSchema.parse()` em algum carregamento de histórico falha com `kind` antigo (`formula`/`sql`/etc) — Pitfall 2 | accept (nesta plan) | Carregamento de `initialExchanges` em `unified-chat-tool.tsx` é tratado no Plan 07 (`.safeParse()` com fallback); aqui apenas o schema é reduzido — typecheck local valida apenas este arquivo |
</threat_model>

<verification>
```bash
pnpm exec prisma generate
pnpm -r typecheck
pnpm --filter web exec vitest run unified-schema.test.ts -t "qa_response|quota_warning|needs_file|table_spec"
find apps/web/src/features/unified-chat/components/clarification-card.tsx apps/web/src/features/unified-chat/components/confirmation-card.tsx apps/web/src/features/unified-chat/components/table-intent-stub.tsx 2>/dev/null | wc -l
grep -c "FormulaOutputPanel\|SqlOutputPanel\|RegexOutputPanel\|ScriptsOutputPanel\|TemplateOutputPanel\|ClarificationCard\|ConfirmationCard\|TableIntentStub" apps/web/src/features/unified-chat/components/render-dispatcher.tsx
```
</verification>

<success_criteria>
- unifiedCompletePayloadSchema reduzido a tableSpecPayloadSchema + qaResponsePayloadSchema; quota_warning/needs_file removidos de unifiedStreamEventSchema.
- render-dispatcher.tsx sem cases/imports de tools removidos/geração-do-zero; table_spec preserva TableGridPanel; default null defensivo.
- intent-pill.tsx reduzido a sheet_operation/qa(/unknown).
- 3 componentes (ClarificationCard/ConfirmationCard/TableIntentStub) deletados.
- Typecheck restante documentado para Plan 07 (unified-chat-tool.tsx/use-unified-chat-stream.ts).
</success_criteria>

<output>
Create `.planning/phases/18-remover-tools-avulsos-ocr-file-analysis-reduzir-dispatcher/18-06-SUMMARY.md` when done
</output>
</content>
