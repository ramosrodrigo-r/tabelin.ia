---
phase: 03-multi-tool-generation-suite
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/web/src/server/ai/scripts-stream.ts
  - apps/web/src/server/ai/sql-stream.ts
  - apps/web/src/server/ai/regex-stream.ts
  - apps/web/src/server/ai/template-stream.ts
  - apps/web/src/app/api/tools/scripts/generate/route.ts
  - apps/web/src/app/api/tools/sql/generate/route.ts
  - apps/web/src/app/api/tools/regex/generate/route.ts
  - apps/web/src/app/api/tools/regex/explain/route.ts
  - apps/web/src/app/api/tools/template/generate/route.ts
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
  - SQL-01
  - SQL-02
  - REGX-01
  - REGX-02
  - SAFE-01
  - PRO-01

must_haves:
  truths:
    - "POST /api/tools/scripts/generate retorna NDJSON stream com events typed para script"
    - "POST /api/tools/sql/generate retorna NDJSON stream com events typed para sql"
    - "POST /api/tools/regex/generate e /explain retornam NDJSON stream com events typed para regex"
    - "POST /api/tools/template/generate retorna 403 com code: pro_required para usuários Free"
    - "Todos os route handlers retornam 401 sem sessão, 400 com input inválido, 429 com quota_exceeded"
    - "Scripts e SQL com código destrutivo têm isDestructive: true no payload complete"
    - "Sem OPENAI_API_KEY todos os endpoints retornam fixtures determinísticas"
  artifacts:
    - path: "apps/web/src/server/ai/scripts-stream.ts"
      provides: "resolveScriptPayload + createScriptEventStream"
      exports: ["resolveScriptPayload", "createScriptEventStream"]
    - path: "apps/web/src/server/ai/sql-stream.ts"
      provides: "resolveSqlPayload + createSqlEventStream"
      exports: ["resolveSqlPayload", "createSqlEventStream"]
    - path: "apps/web/src/server/ai/regex-stream.ts"
      provides: "resolveRegexPayload + createRegexEventStream"
      exports: ["resolveRegexPayload", "createRegexEventStream"]
    - path: "apps/web/src/server/ai/template-stream.ts"
      provides: "resolveTemplatePayload + createTemplateEventStream"
      exports: ["resolveTemplatePayload", "createTemplateEventStream"]
    - path: "apps/web/src/app/api/tools/scripts/generate/route.ts"
      provides: "POST handler para geração de scripts"
      contains: "reserveToolUse"
    - path: "apps/web/src/app/api/tools/template/generate/route.ts"
      provides: "POST handler com Pro gate"
      contains: "pro_required"
  key_links:
    - from: "apps/web/src/app/api/tools/scripts/generate/route.ts"
      to: "apps/web/src/server/usage/quota-service"
      via: "reserveToolUse(user.id, 'script', 'generate')"
      pattern: "reserveToolUse.*script"
    - from: "apps/web/src/server/ai/scripts-stream.ts"
      to: "apps/web/src/server/ai/destructive-classifier.ts"
      via: "classifyDestructive(code, 'script')"
      pattern: "classifyDestructive"
    - from: "apps/web/src/app/api/tools/template/generate/route.ts"
      to: "apps/web/src/server/billing/entitlements.ts"
      via: "getUserEntitlement(user.id) — retornar 403 se não Pro"
      pattern: "pro_required"
---

<objective>
Implementar os 4 stream services server-only e os 5 route handlers de API para as novas ferramentas.

Purpose: Os componentes de UI do plano 03-03 dependem dos endpoints `/api/tools/{tool}/generate` (e `/explain` para regex). Esses endpoints devem seguir exatamente o padrão auth → parse → quota → AI → confirm → record → stream do formula tool.

Output:
- 4 arquivos em apps/web/src/server/ai/ (scripts, sql, regex, template streams)
- 5 route handlers em apps/web/src/app/api/tools/ (scripts/generate, sql/generate, regex/generate, regex/explain, template/generate)
- template/generate com Pro gate (403 para Free users)
- Fixtures determinísticas quando OPENAI_API_KEY ausente
- classifyDestructive integrado em scripts e sql streams
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-multi-tool-generation-suite/03-CONTEXT.md
@.planning/phases/03-multi-tool-generation-suite/03-RESEARCH.md
@.planning/phases/03-multi-tool-generation-suite/03-PATTERNS.md
@.planning/phases/03-multi-tool-generation-suite/03-01-SUMMARY.md

<interfaces>
<!-- Interfaces chave extraídas do codebase para o executor -->

De apps/web/src/app/api/tools/formula/generate/route.ts (padrão a replicar):
```typescript
// Estrutura invariante de TODOS os novos route handlers:
// auth → parse → reserveToolUse → resolvePayload → confirmToolUse → recordToolRequest → stream

export async function POST(request: Request) {
  const user = getSessionFromCookieHeader(request.headers.get("cookie"));
  if (!user) return NextResponse.json({ error: "Autenticacao obrigatoria." }, { status: 401 });

  const startedAt = performance.now();
  const body = await request.json().catch(() => null);
  const parsed = xyzRequestSchema.safeParse(body);
  if (!parsed.success) return NextResponse.json({ error: "Pedido invalido.", issues: parsed.error.issues }, { status: 400 });

  const quotaCheck = await reserveToolUse(user.id, "toolKind", "mode");
  if (!quotaCheck.allowed) return NextResponse.json({ code: "quota_exceeded", meterKind: quotaCheck.meterKind, cta: "pro_checkout" }, { status: 429 });

  try {
    const payload = await resolveXxxPayload({ request: parsed.data });
    await confirmToolUse(quotaCheck.reservationKey);
    await recordToolRequest({ userId: user.id, toolKind: "xxx", mode: "generate", dialect: parsed.data.xxx, status: "success", latencyMs: Math.round(performance.now() - startedAt), providerModel: payload.metadata.providerModel });
    return new Response(createXxxEventStream(payload, quotaCheck.lastFreeUse), {
      headers: { "content-type": "application/x-ndjson; charset=utf-8", "cache-control": "no-store" }
    });
  } catch {
    await releaseToolUse(quotaCheck.reservationKey);
    return NextResponse.json({ error: "Nao consegui validar a resposta." }, { status: 502 });
  }
}
```

De apps/web/src/server/ai/formula-stream.ts (padrão createEventStream a replicar):
```typescript
export function createFormulaEventStream(payload: FormulaCompletePayload, lastFreeUse?: boolean) {
  const encoder = new TextEncoder();
  const events: FormulaStreamEvent[] = [
    { type: "metadata", metadata: payload.metadata },
    ...payload.warnings.map((warning): FormulaStreamEvent => ({ type: "warning", warning })),
    ...(lastFreeUse ? [{ type: "quota_warning" as const, lastFreeUse: true }] : []),
    ...splitForStreaming(payload).map((text): FormulaStreamEvent => ({ type: "delta", text })),
    { type: "complete", payload }
  ];
  return new ReadableStream<Uint8Array>({ async start(controller) {
    for (const event of events) { controller.enqueue(encoder.encode(`${JSON.stringify(event)}\n`)); await new Promise((resolve) => setTimeout(resolve, 5)); }
    controller.close();
  }});
}
```

De apps/web/src/server/billing/entitlements.ts (Pro gate para template):
```typescript
// getUserEntitlement retorna UserEntitlement com .plan e .status
export async function getUserEntitlement(userId: string): Promise<UserEntitlement>
// Pro check: entitlement.plan === "pro" && entitlement.status === "active"
```

De packages/shared (tipos exportados pelo plano 03-01):
```typescript
// Scripts
ScriptGenerateRequest, ScriptGenerateResponse, ScriptStreamEvent, scriptGenerateRequestSchema, scriptStreamEventSchema
SCRIPT_FIXTURES: ScriptGenerateResponse[]

// SQL
SqlGenerateRequest, SqlGenerateResponse, SqlStreamEvent, sqlGenerateRequestSchema, sqlStreamEventSchema
SQL_FIXTURES: SqlGenerateResponse[]

// Regex
RegexGenerateRequest, RegexExplainRequest, RegexCompletePayload, RegexStreamEvent, regexGenerateRequestSchema, regexExplainRequestSchema, regexStreamEventSchema
REGEX_GENERATE_FIXTURES: RegexGenerateResponse[]

// Template
TemplateGenerateRequest, TemplateGenerateResponse, TemplateStreamEvent, templateGenerateRequestSchema, templateStreamEventSchema
TEMPLATE_FIXTURES: TemplateGenerateResponse[]
```

De apps/web/src/server/ai/destructive-classifier.ts (criado no 03-01):
```typescript
export function classifyDestructive(code: string, toolKind: "sql" | "script"): boolean
export function getDestructiveMessage(code: string, toolKind: "sql" | "script"): string
```

De apps/web/src/server/tools/tool-repository.ts (criado no 03-01):
```typescript
export async function recordToolRequest(input: {
  userId: string; toolKind: string; mode: string; dialect?: string;
  status: "success" | "failure"; latencyMs?: number; providerModel?: string;
}): Promise<ToolRequest | null>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar server AI streams para scripts, SQL, regex e template</name>
  <read_first>
    - apps/web/src/server/ai/formula-stream.ts (padrão exato a replicar — ler linha por linha)
    - apps/web/src/server/ai/openai-client.ts (ver como getOpenAIModel() é chamado)
    - apps/web/src/server/ai/destructive-classifier.ts (importar classifyDestructive e getDestructiveMessage)
    - .planning/phases/03-multi-tool-generation-suite/03-PATTERNS.md (seção scripts-stream.ts)
  </read_first>
  <files>
    apps/web/src/server/ai/scripts-stream.ts,
    apps/web/src/server/ai/sql-stream.ts,
    apps/web/src/server/ai/regex-stream.ts,
    apps/web/src/server/ai/template-stream.ts
  </files>
  <action>
**CRIAR apps/web/src/server/ai/scripts-stream.ts:**

```typescript
import "server-only";

import {
  type ScriptGenerateRequest,
  type ScriptGenerateResponse,
  type ScriptStreamEvent,
  SCRIPT_FIXTURES,
  scriptGenerateResponseSchema
} from "@tabelin/shared";

import { classifyDestructive, getDestructiveMessage } from "./destructive-classifier";
import { getOpenAIModel } from "./openai-client";

export async function resolveScriptPayload(input: {
  request: ScriptGenerateRequest;
}): Promise<ScriptGenerateResponse> {
  const { request } = input;

  // Sem OPENAI_API_KEY: retornar fixture determinística correspondente ao scriptType
  if (!process.env.OPENAI_API_KEY) {
    const fixture = SCRIPT_FIXTURES.find((f) => f.metadata.scriptType === request.scriptType) ?? SCRIPT_FIXTURES[0];
    const isDestructive = classifyDestructive(fixture.code, "script");
    return scriptGenerateResponseSchema.parse({
      ...fixture,
      isDestructive,
      metadata: { ...fixture.metadata, scriptType: request.scriptType, isDestructive, providerModel: "deterministic-fixture" }
    });
  }

  // Com OPENAI_API_KEY: construir prompt e chamar AI
  const { default: OpenAI } = await import("openai");
  const client = new OpenAI();

  const scriptTypeLabels: Record<string, string> = {
    vba: "VBA para Microsoft Excel",
    apps_script: "Google Apps Script para Google Sheets",
    airtable_script: "Airtable Scripting para Airtable"
  };

  const completion = await client.chat.completions.create({
    model: getOpenAIModel(),
    messages: [
      {
        role: "system",
        content: `Voce e um especialista em automacao de planilhas. Gere ${scriptTypeLabels[request.scriptType]} em resposta ao pedido em portugues do usuario. Responda APENAS com JSON valido no formato: {"code": "...codigo completo...", "explanation": "...explicacao em portugues...", "assumptions": ["..."], "warnings": [], "isDestructive": false}`
      },
      {
        role: "user",
        content: request.prompt
      }
    ],
    response_format: { type: "json_object" }
  });

  const raw = JSON.parse(completion.choices[0]?.message?.content ?? "{}");
  const code = String(raw.code ?? "");

  // Classificador determinístico como fallback/validação do campo isDestructive do AI
  const isDestructiveByPattern = classifyDestructive(code, "script");
  const isDestructive = Boolean(raw.isDestructive) || isDestructiveByPattern;

  return scriptGenerateResponseSchema.parse({
    kind: "script",
    code,
    explanation: String(raw.explanation ?? ""),
    assumptions: Array.isArray(raw.assumptions) ? raw.assumptions : [],
    warnings: Array.isArray(raw.warnings) ? raw.warnings : [],
    isDestructive,
    metadata: {
      mode: "generate",
      scriptType: request.scriptType,
      isDestructive,
      providerModel: getOpenAIModel()
    }
  });
}

function splitScriptForStreaming(payload: ScriptGenerateResponse): string[] {
  return [payload.code, "\n", payload.explanation];
}

export function createScriptEventStream(payload: ScriptGenerateResponse, lastFreeUse?: boolean) {
  const encoder = new TextEncoder();
  const events: ScriptStreamEvent[] = [
    { type: "metadata", metadata: payload.metadata },
    ...payload.warnings.map((warning): ScriptStreamEvent => ({ type: "warning", warning })),
    ...(lastFreeUse ? [{ type: "quota_warning" as const, lastFreeUse: true }] : []),
    ...splitScriptForStreaming(payload).map((text): ScriptStreamEvent => ({ type: "delta", text })),
    { type: "complete", payload }
  ];

  return new ReadableStream<Uint8Array>({
    async start(controller) {
      for (const event of events) {
        controller.enqueue(encoder.encode(`${JSON.stringify(event)}\n`));
        await new Promise((resolve) => setTimeout(resolve, 5));
      }
      controller.close();
    }
  });
}
```

**CRIAR apps/web/src/server/ai/sql-stream.ts:**

```typescript
import "server-only";

import {
  type SqlGenerateRequest,
  type SqlGenerateResponse,
  type SqlStreamEvent,
  SQL_FIXTURES,
  sqlGenerateResponseSchema
} from "@tabelin/shared";

import { classifyDestructive, getDestructiveMessage } from "./destructive-classifier";
import { getOpenAIModel } from "./openai-client";

export async function resolveSqlPayload(input: {
  request: SqlGenerateRequest;
}): Promise<SqlGenerateResponse> {
  const { request } = input;

  if (!process.env.OPENAI_API_KEY) {
    const fixture = SQL_FIXTURES.find((f) => f.metadata.dialect === request.dialect) ?? SQL_FIXTURES[0];
    const isDestructive = classifyDestructive(fixture.query, "sql");
    return sqlGenerateResponseSchema.parse({
      ...fixture,
      isDestructive,
      metadata: { ...fixture.metadata, dialect: request.dialect, isDestructive, providerModel: "deterministic-fixture" }
    });
  }

  const { default: OpenAI } = await import("openai");
  const client = new OpenAI();

  const completion = await client.chat.completions.create({
    model: getOpenAIModel(),
    messages: [
      {
        role: "system",
        content: `Voce e um especialista em SQL. Gere uma consulta ${request.dialect.toUpperCase()} em resposta ao pedido em portugues. Responda APENAS com JSON valido: {"query": "...SQL completo...", "explanation": "...explicacao em portugues...", "assumptions": ["..."], "warnings": [], "isDestructive": false}`
      },
      { role: "user", content: request.prompt }
    ],
    response_format: { type: "json_object" }
  });

  const raw = JSON.parse(completion.choices[0]?.message?.content ?? "{}");
  const query = String(raw.query ?? "");

  const isDestructiveByPattern = classifyDestructive(query, "sql");
  const isDestructive = Boolean(raw.isDestructive) || isDestructiveByPattern;

  return sqlGenerateResponseSchema.parse({
    kind: "sql",
    query,
    explanation: String(raw.explanation ?? ""),
    assumptions: Array.isArray(raw.assumptions) ? raw.assumptions : [],
    warnings: Array.isArray(raw.warnings) ? raw.warnings : [],
    isDestructive,
    metadata: {
      mode: "generate",
      dialect: request.dialect,
      isDestructive,
      providerModel: getOpenAIModel()
    }
  });
}

function splitSqlForStreaming(payload: SqlGenerateResponse): string[] {
  return [payload.query, "\n", payload.explanation];
}

export function createSqlEventStream(payload: SqlGenerateResponse, lastFreeUse?: boolean) {
  const encoder = new TextEncoder();
  const events: SqlStreamEvent[] = [
    { type: "metadata", metadata: payload.metadata },
    ...payload.warnings.map((warning): SqlStreamEvent => ({ type: "warning", warning })),
    ...(lastFreeUse ? [{ type: "quota_warning" as const, lastFreeUse: true }] : []),
    ...splitSqlForStreaming(payload).map((text): SqlStreamEvent => ({ type: "delta", text })),
    { type: "complete", payload }
  ];

  return new ReadableStream<Uint8Array>({
    async start(controller) {
      for (const event of events) {
        controller.enqueue(encoder.encode(`${JSON.stringify(event)}\n`));
        await new Promise((resolve) => setTimeout(resolve, 5));
      }
      controller.close();
    }
  });
}
```

**CRIAR apps/web/src/server/ai/regex-stream.ts:**

```typescript
import "server-only";

import {
  type RegexCompletePayload,
  type RegexExplainRequest,
  type RegexGenerateRequest,
  type RegexStreamEvent,
  REGEX_EXPLAIN_FIXTURES,
  REGEX_GENERATE_FIXTURES,
  regexCompletePayloadSchema
} from "@tabelin/shared";

import { getOpenAIModel } from "./openai-client";

type RegexModeInput =
  | { mode: "generate"; request: RegexGenerateRequest }
  | { mode: "explain"; request: RegexExplainRequest };

export async function resolveRegexPayload(input: RegexModeInput): Promise<RegexCompletePayload> {
  if (!process.env.OPENAI_API_KEY) {
    if (input.mode === "generate") {
      return regexCompletePayloadSchema.parse({
        ...REGEX_GENERATE_FIXTURES[0],
        metadata: { mode: "generate", providerModel: "deterministic-fixture" }
      });
    }
    return regexCompletePayloadSchema.parse({
      ...REGEX_EXPLAIN_FIXTURES[0],
      pattern: input.request.pattern,
      metadata: { mode: "explain", providerModel: "deterministic-fixture" }
    });
  }

  const { default: OpenAI } = await import("openai");
  const client = new OpenAI();

  if (input.mode === "generate") {
    const completion = await client.chat.completions.create({
      model: getOpenAIModel(),
      messages: [
        {
          role: "system",
          content: 'Voce e um especialista em expressoes regulares. Gere uma regex em resposta ao pedido em portugues. Responda APENAS com JSON: {"pattern": "...regex...", "explanation": "...explicacao em portugues...", "examples": ["..."], "assumptions": [], "warnings": []}'
        },
        { role: "user", content: input.request.prompt }
      ],
      response_format: { type: "json_object" }
    });

    const raw = JSON.parse(completion.choices[0]?.message?.content ?? "{}");
    return regexCompletePayloadSchema.parse({
      kind: "regex_generate",
      pattern: String(raw.pattern ?? ""),
      explanation: String(raw.explanation ?? ""),
      examples: Array.isArray(raw.examples) ? raw.examples : [],
      assumptions: Array.isArray(raw.assumptions) ? raw.assumptions : [],
      warnings: Array.isArray(raw.warnings) ? raw.warnings : [],
      metadata: { mode: "generate", providerModel: getOpenAIModel() }
    });
  }

  // mode === "explain"
  const completion = await client.chat.completions.create({
    model: getOpenAIModel(),
    messages: [
      {
        role: "system",
        content: 'Voce e um especialista em expressoes regulares. Explique a regex fornecida passo a passo em portugues. Responda APENAS com JSON: {"steps": ["passo 1", "passo 2", ...], "assumptions": [], "warnings": []}'
      },
      { role: "user", content: `Explique esta regex: ${input.request.pattern}` }
    ],
    response_format: { type: "json_object" }
  });

  const raw = JSON.parse(completion.choices[0]?.message?.content ?? "{}");
  return regexCompletePayloadSchema.parse({
    kind: "regex_explain",
    pattern: input.request.pattern,
    steps: Array.isArray(raw.steps) ? raw.steps : ["Nao foi possivel explicar a expressao."],
    assumptions: Array.isArray(raw.assumptions) ? raw.assumptions : [],
    warnings: Array.isArray(raw.warnings) ? raw.warnings : [],
    metadata: { mode: "explain", providerModel: getOpenAIModel() }
  });
}

function splitRegexForStreaming(payload: RegexCompletePayload): string[] {
  if (payload.kind === "regex_generate") {
    return [payload.pattern, "\n", payload.explanation];
  }
  return [payload.pattern, "\n", ...payload.steps.map((step, i) => `${i + 1}. ${step}\n`)];
}

export function createRegexEventStream(payload: RegexCompletePayload, lastFreeUse?: boolean) {
  const encoder = new TextEncoder();
  const events: RegexStreamEvent[] = [
    { type: "metadata", metadata: payload.metadata },
    ...payload.warnings.map((warning): RegexStreamEvent => ({ type: "warning", warning })),
    ...(lastFreeUse ? [{ type: "quota_warning" as const, lastFreeUse: true }] : []),
    ...splitRegexForStreaming(payload).map((text): RegexStreamEvent => ({ type: "delta", text })),
    { type: "complete", payload }
  ];

  return new ReadableStream<Uint8Array>({
    async start(controller) {
      for (const event of events) {
        controller.enqueue(encoder.encode(`${JSON.stringify(event)}\n`));
        await new Promise((resolve) => setTimeout(resolve, 5));
      }
      controller.close();
    }
  });
}
```

**CRIAR apps/web/src/server/ai/template-stream.ts:**

```typescript
import "server-only";

import {
  type TemplateGenerateRequest,
  type TemplateGenerateResponse,
  type TemplateStreamEvent,
  TEMPLATE_FIXTURES,
  templateGenerateResponseSchema
} from "@tabelin/shared";

import { getOpenAIModel } from "./openai-client";

export async function resolveTemplatePayload(input: {
  request: TemplateGenerateRequest;
}): Promise<TemplateGenerateResponse> {
  const { request } = input;

  if (!process.env.OPENAI_API_KEY) {
    return templateGenerateResponseSchema.parse({
      ...TEMPLATE_FIXTURES[0],
      metadata: { mode: "generate", providerModel: "deterministic-fixture" }
    });
  }

  const { default: OpenAI } = await import("openai");
  const client = new OpenAI();

  const completion = await client.chat.completions.create({
    model: getOpenAIModel(),
    messages: [
      {
        role: "system",
        content: 'Voce e um especialista em planilhas Excel pt-BR. Gere um template de planilha estruturado em resposta ao pedido do usuario. Entregue em Markdown formatado com cabecalhos, colunas sugeridas com tipos, e formulas de referencia no estilo Excel pt-BR (separador ponto-e-virgula). Responda APENAS com JSON: {"output": "...markdown completo...", "explanation": "...descricao em portugues...", "assumptions": [], "warnings": []}'
      },
      { role: "user", content: request.prompt }
    ],
    response_format: { type: "json_object" }
  });

  const raw = JSON.parse(completion.choices[0]?.message?.content ?? "{}");
  return templateGenerateResponseSchema.parse({
    kind: "template",
    output: String(raw.output ?? ""),
    explanation: String(raw.explanation ?? ""),
    assumptions: Array.isArray(raw.assumptions) ? raw.assumptions : [],
    warnings: Array.isArray(raw.warnings) ? raw.warnings : [],
    metadata: { mode: "generate", providerModel: getOpenAIModel() }
  });
}

function splitTemplateForStreaming(payload: TemplateGenerateResponse): string[] {
  return [payload.output, "\n", payload.explanation];
}

export function createTemplateEventStream(payload: TemplateGenerateResponse, lastFreeUse?: boolean) {
  const encoder = new TextEncoder();
  const events: TemplateStreamEvent[] = [
    { type: "metadata", metadata: payload.metadata },
    ...payload.warnings.map((warning): TemplateStreamEvent => ({ type: "warning", warning })),
    ...(lastFreeUse ? [{ type: "quota_warning" as const, lastFreeUse: true }] : []),
    ...splitTemplateForStreaming(payload).map((text): TemplateStreamEvent => ({ type: "delta", text })),
    { type: "complete", payload }
  ];

  return new ReadableStream<Uint8Array>({
    async start(controller) {
      for (const event of events) {
        controller.enqueue(encoder.encode(`${JSON.stringify(event)}\n`));
        await new Promise((resolve) => setTimeout(resolve, 5));
      }
      controller.close();
    }
  });
}
```
</action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && npx tsc --project apps/web/tsconfig.json --noEmit 2>&1 | grep -E "scripts-stream|sql-stream|regex-stream|template-stream" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/server/ai/scripts-stream.ts existe e contém "export async function resolveScriptPayload" e "export function createScriptEventStream"
    - apps/web/src/server/ai/sql-stream.ts existe e contém "export async function resolveSqlPayload" e "export function createSqlEventStream"
    - apps/web/src/server/ai/regex-stream.ts existe e contém "export async function resolveRegexPayload" e "export function createRegexEventStream"
    - apps/web/src/server/ai/template-stream.ts existe e contém "export async function resolveTemplatePayload" e "export function createTemplateEventStream"
    - Todos os 4 arquivos contêm "import \"server-only\""
    - scripts-stream.ts e sql-stream.ts contêm "classifyDestructive"
    - Todos os 4 arquivos contêm "OPENAI_API_KEY" (fallback para fixtures)
    - npx tsc --project apps/web/tsconfig.json --noEmit não retorna erros nos arquivos listados
  </acceptance_criteria>
  <done>4 server AI streams criados. Cada um implementa resolveXxxPayload (com fixture fallback + OpenAI com JSON response_format) e createXxxEventStream (mesmo padrão NDJSON da formula). scripts-stream e sql-stream integram classifyDestructive.</done>
</task>

<task type="auto">
  <name>Task 2: Criar route handlers de API para scripts, SQL, regex e template</name>
  <read_first>
    - apps/web/src/app/api/tools/formula/generate/route.ts (padrão exato a replicar — estrutura invariante)
    - apps/web/src/app/api/tools/formula/explain/route.ts (padrão do explain route para regex/explain)
    - apps/web/src/server/billing/entitlements.ts (getUserEntitlement — Pro gate para template)
    - .planning/phases/03-multi-tool-generation-suite/03-PATTERNS.md (seção template/generate route e padrão Pro gate)
  </read_first>
  <files>
    apps/web/src/app/api/tools/scripts/generate/route.ts,
    apps/web/src/app/api/tools/sql/generate/route.ts,
    apps/web/src/app/api/tools/regex/generate/route.ts,
    apps/web/src/app/api/tools/regex/explain/route.ts,
    apps/web/src/app/api/tools/template/generate/route.ts
  </files>
  <action>
**CRIAR apps/web/src/app/api/tools/scripts/generate/route.ts:**

```typescript
import { NextResponse } from "next/server";

import { scriptGenerateRequestSchema } from "@tabelin/shared";

import { createScriptEventStream, resolveScriptPayload } from "@/server/ai/scripts-stream";
import { getSessionFromCookieHeader } from "@/server/auth/session";
import { recordToolRequest } from "@/server/tools/tool-repository";
import { confirmToolUse, releaseToolUse, reserveToolUse } from "@/server/usage/quota-service";

export async function POST(request: Request) {
  const user = getSessionFromCookieHeader(request.headers.get("cookie"));
  if (!user) {
    return NextResponse.json({ error: "Autenticacao obrigatoria." }, { status: 401 });
  }

  const startedAt = performance.now();
  const body = await request.json().catch(() => null);
  const parsed = scriptGenerateRequestSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json({ error: "Pedido de script invalido.", issues: parsed.error.issues }, { status: 400 });
  }

  const quotaCheck = await reserveToolUse(user.id, "script", "generate");
  if (!quotaCheck.allowed) {
    return NextResponse.json({ code: "quota_exceeded", meterKind: quotaCheck.meterKind, cta: "pro_checkout" }, { status: 429 });
  }

  try {
    const payload = await resolveScriptPayload({ request: parsed.data });
    await confirmToolUse(quotaCheck.reservationKey);
    await recordToolRequest({
      userId: user.id,
      toolKind: "script",
      mode: "generate",
      dialect: parsed.data.scriptType,
      status: "success",
      latencyMs: Math.round(performance.now() - startedAt),
      providerModel: payload.metadata.providerModel
    });
    return new Response(createScriptEventStream(payload, quotaCheck.lastFreeUse), {
      headers: { "content-type": "application/x-ndjson; charset=utf-8", "cache-control": "no-store" }
    });
  } catch {
    await releaseToolUse(quotaCheck.reservationKey);
    return NextResponse.json({ error: "Nao consegui validar a resposta." }, { status: 502 });
  }
}
```

**CRIAR apps/web/src/app/api/tools/sql/generate/route.ts:**

```typescript
import { NextResponse } from "next/server";

import { sqlGenerateRequestSchema } from "@tabelin/shared";

import { createSqlEventStream, resolveSqlPayload } from "@/server/ai/sql-stream";
import { getSessionFromCookieHeader } from "@/server/auth/session";
import { recordToolRequest } from "@/server/tools/tool-repository";
import { confirmToolUse, releaseToolUse, reserveToolUse } from "@/server/usage/quota-service";

export async function POST(request: Request) {
  const user = getSessionFromCookieHeader(request.headers.get("cookie"));
  if (!user) {
    return NextResponse.json({ error: "Autenticacao obrigatoria." }, { status: 401 });
  }

  const startedAt = performance.now();
  const body = await request.json().catch(() => null);
  const parsed = sqlGenerateRequestSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json({ error: "Pedido de SQL invalido.", issues: parsed.error.issues }, { status: 400 });
  }

  const quotaCheck = await reserveToolUse(user.id, "sql", "generate");
  if (!quotaCheck.allowed) {
    return NextResponse.json({ code: "quota_exceeded", meterKind: quotaCheck.meterKind, cta: "pro_checkout" }, { status: 429 });
  }

  try {
    const payload = await resolveSqlPayload({ request: parsed.data });
    await confirmToolUse(quotaCheck.reservationKey);
    await recordToolRequest({
      userId: user.id,
      toolKind: "sql",
      mode: "generate",
      dialect: parsed.data.dialect,
      status: "success",
      latencyMs: Math.round(performance.now() - startedAt),
      providerModel: payload.metadata.providerModel
    });
    return new Response(createSqlEventStream(payload, quotaCheck.lastFreeUse), {
      headers: { "content-type": "application/x-ndjson; charset=utf-8", "cache-control": "no-store" }
    });
  } catch {
    await releaseToolUse(quotaCheck.reservationKey);
    return NextResponse.json({ error: "Nao consegui validar a resposta." }, { status: 502 });
  }
}
```

**CRIAR apps/web/src/app/api/tools/regex/generate/route.ts:**

```typescript
import { NextResponse } from "next/server";

import { regexGenerateRequestSchema } from "@tabelin/shared";

import { createRegexEventStream, resolveRegexPayload } from "@/server/ai/regex-stream";
import { getSessionFromCookieHeader } from "@/server/auth/session";
import { recordToolRequest } from "@/server/tools/tool-repository";
import { confirmToolUse, releaseToolUse, reserveToolUse } from "@/server/usage/quota-service";

export async function POST(request: Request) {
  const user = getSessionFromCookieHeader(request.headers.get("cookie"));
  if (!user) {
    return NextResponse.json({ error: "Autenticacao obrigatoria." }, { status: 401 });
  }

  const startedAt = performance.now();
  const body = await request.json().catch(() => null);
  const parsed = regexGenerateRequestSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json({ error: "Pedido de regex invalido.", issues: parsed.error.issues }, { status: 400 });
  }

  const quotaCheck = await reserveToolUse(user.id, "regex", "generate");
  if (!quotaCheck.allowed) {
    return NextResponse.json({ code: "quota_exceeded", meterKind: quotaCheck.meterKind, cta: "pro_checkout" }, { status: 429 });
  }

  try {
    const payload = await resolveRegexPayload({ mode: "generate", request: parsed.data });
    await confirmToolUse(quotaCheck.reservationKey);
    await recordToolRequest({
      userId: user.id,
      toolKind: "regex",
      mode: "generate",
      status: "success",
      latencyMs: Math.round(performance.now() - startedAt),
      providerModel: payload.metadata.providerModel
    });
    return new Response(createRegexEventStream(payload, quotaCheck.lastFreeUse), {
      headers: { "content-type": "application/x-ndjson; charset=utf-8", "cache-control": "no-store" }
    });
  } catch {
    await releaseToolUse(quotaCheck.reservationKey);
    return NextResponse.json({ error: "Nao consegui validar a resposta." }, { status: 502 });
  }
}
```

**CRIAR apps/web/src/app/api/tools/regex/explain/route.ts:**

```typescript
import { NextResponse } from "next/server";

import { regexExplainRequestSchema } from "@tabelin/shared";

import { createRegexEventStream, resolveRegexPayload } from "@/server/ai/regex-stream";
import { getSessionFromCookieHeader } from "@/server/auth/session";
import { recordToolRequest } from "@/server/tools/tool-repository";
import { confirmToolUse, releaseToolUse, reserveToolUse } from "@/server/usage/quota-service";

export async function POST(request: Request) {
  const user = getSessionFromCookieHeader(request.headers.get("cookie"));
  if (!user) {
    return NextResponse.json({ error: "Autenticacao obrigatoria." }, { status: 401 });
  }

  const startedAt = performance.now();
  const body = await request.json().catch(() => null);
  const parsed = regexExplainRequestSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json({ error: "Pedido de explicacao de regex invalido.", issues: parsed.error.issues }, { status: 400 });
  }

  const quotaCheck = await reserveToolUse(user.id, "regex", "explain");
  if (!quotaCheck.allowed) {
    return NextResponse.json({ code: "quota_exceeded", meterKind: quotaCheck.meterKind, cta: "pro_checkout" }, { status: 429 });
  }

  try {
    const payload = await resolveRegexPayload({ mode: "explain", request: parsed.data });
    await confirmToolUse(quotaCheck.reservationKey);
    await recordToolRequest({
      userId: user.id,
      toolKind: "regex",
      mode: "explain",
      status: "success",
      latencyMs: Math.round(performance.now() - startedAt),
      providerModel: payload.metadata.providerModel
    });
    return new Response(createRegexEventStream(payload, quotaCheck.lastFreeUse), {
      headers: { "content-type": "application/x-ndjson; charset=utf-8", "cache-control": "no-store" }
    });
  } catch {
    await releaseToolUse(quotaCheck.reservationKey);
    return NextResponse.json({ error: "Nao consegui validar a resposta." }, { status: 502 });
  }
}
```

**CRIAR apps/web/src/app/api/tools/template/generate/route.ts (com Pro gate):**

```typescript
import { NextResponse } from "next/server";

import { templateGenerateRequestSchema } from "@tabelin/shared";

import { createTemplateEventStream, resolveTemplatePayload } from "@/server/ai/template-stream";
import { getSessionFromCookieHeader } from "@/server/auth/session";
import { getUserEntitlement } from "@/server/billing/entitlements";
import { recordToolRequest } from "@/server/tools/tool-repository";
import { confirmToolUse, releaseToolUse, reserveToolUse } from "@/server/usage/quota-service";

export async function POST(request: Request) {
  const user = getSessionFromCookieHeader(request.headers.get("cookie"));
  if (!user) {
    return NextResponse.json({ error: "Autenticacao obrigatoria." }, { status: 401 });
  }

  // Pro gate: verificar entitlement ANTES de reservar quota (per Pitfall 6 do RESEARCH.md)
  const entitlement = await getUserEntitlement(user.id);
  const isPro = entitlement.plan === "pro" && entitlement.status === "active";
  if (!isPro) {
    return NextResponse.json({ code: "pro_required", cta: "pro_checkout" }, { status: 403 });
  }

  const startedAt = performance.now();
  const body = await request.json().catch(() => null);
  const parsed = templateGenerateRequestSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json({ error: "Pedido de template invalido.", issues: parsed.error.issues }, { status: 400 });
  }

  const quotaCheck = await reserveToolUse(user.id, "template", "generate");
  if (!quotaCheck.allowed) {
    return NextResponse.json({ code: "quota_exceeded", meterKind: quotaCheck.meterKind, cta: "pro_checkout" }, { status: 429 });
  }

  try {
    const payload = await resolveTemplatePayload({ request: parsed.data });
    await confirmToolUse(quotaCheck.reservationKey);
    await recordToolRequest({
      userId: user.id,
      toolKind: "template",
      mode: "generate",
      status: "success",
      latencyMs: Math.round(performance.now() - startedAt),
      providerModel: payload.metadata.providerModel
    });
    return new Response(createTemplateEventStream(payload, quotaCheck.lastFreeUse), {
      headers: { "content-type": "application/x-ndjson; charset=utf-8", "cache-control": "no-store" }
    });
  } catch {
    await releaseToolUse(quotaCheck.reservationKey);
    return NextResponse.json({ error: "Nao consegui validar a resposta." }, { status: 502 });
  }
}
```
</action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && npx tsc --project apps/web/tsconfig.json --noEmit 2>&1 | grep -E "api/tools/(scripts|sql|regex|template)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Todos os 5 route files existem: scripts/generate, sql/generate, regex/generate, regex/explain, template/generate
    - Cada route file contém "export async function POST"
    - Cada route file contém "getSessionFromCookieHeader" (auth check)
    - Cada route file contém "reserveToolUse" (quota enforcement)
    - Cada route file contém "releaseToolUse" (no catch block)
    - template/generate/route.ts contém "pro_required"
    - template/generate/route.ts contém "getUserEntitlement"
    - template/generate/route.ts verifica isPro ANTES de reserveToolUse (ordem do código: getUserEntitlement antes de reserveToolUse)
    - npx tsc --project apps/web/tsconfig.json --noEmit não retorna erros nos arquivos listados
  </acceptance_criteria>
  <done>5 route handlers criados seguindo o padrão invariante auth→parse→quota→AI→confirm→record→stream. template/generate inclui Pro gate que retorna 403 com code: pro_required para Free users, verificado antes de qualquer reserva de quota.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API route handlers | Input não confiável: prompt do usuário pode conter tentativas de prompt injection |
| API → OpenAI | Prompt construído no servidor — input do usuário é passado como `content` do user message, não como instrução de sistema |
| API → getUserEntitlement | Entitlement verificado no servidor a cada request — não confia em claims do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Todos os route handlers — Zod safeParse | mitigate | `scriptGenerateRequestSchema.safeParse(body)` valida todos os campos antes de qualquer processamento; input inválido retorna 400 sem reservar quota |
| T-03-02-02 | Tampering | scripts/sql stream — prompt injection via `request.prompt` | mitigate | Prompt do usuário é passado como `role: "user"` content, separado das instruções de sistema (`role: "system"`). Modelo não executa o código gerado — apenas retorna texto |
| T-03-02-03 | Elevation of Privilege | template/generate route — Pro gate bypass | mitigate | `getUserEntitlement(user.id)` chamado no servidor antes de `reserveToolUse` — retorna 403 com `code: "pro_required"` se não Pro. Client-side checks são suplementares, não substitutos |
| T-03-02-04 | Denial of Service | Todos os routes — quota bypass via requests paralelas | accept | `reserveToolUse` usa `isolationLevel: Serializable` (herdado do quota-service da fase 2) — race conditions já cobertas. Abuse-safeguards existentes se aplicam |
| T-03-02-05 | Information Disclosure | server AI streams — import "server-only" | mitigate | `import "server-only"` em todos os 4 streams + destructive-classifier garante que lógica AI, prompts e API keys não sejam bundled para o cliente via importação acidental |
| T-03-02-06 | Tampering | SQL/Script output — isDestructive classificação | mitigate | classifyDestructive usa OR entre sinal do AI (campo `isDestructive` na resposta JSON) e pattern matching determinístico — falso negativo no AI é compensado pelo classifier, garantindo que operações destrutivas conhecidas sempre disparem o banner |
</threat_model>

<verification>
```bash
# Verificar todos os route handlers foram criados
ls /home/rodrigo/tabelin.ia/apps/web/src/app/api/tools/scripts/generate/route.ts
ls /home/rodrigo/tabelin.ia/apps/web/src/app/api/tools/sql/generate/route.ts
ls /home/rodrigo/tabelin.ia/apps/web/src/app/api/tools/regex/generate/route.ts
ls /home/rodrigo/tabelin.ia/apps/web/src/app/api/tools/regex/explain/route.ts
ls /home/rodrigo/tabelin.ia/apps/web/src/app/api/tools/template/generate/route.ts

# Verificar Pro gate no template
grep "pro_required" /home/rodrigo/tabelin.ia/apps/web/src/app/api/tools/template/generate/route.ts

# Verificar classifyDestructive nos streams
grep "classifyDestructive" /home/rodrigo/tabelin.ia/apps/web/src/server/ai/scripts-stream.ts
grep "classifyDestructive" /home/rodrigo/tabelin.ia/apps/web/src/server/ai/sql-stream.ts

# TypeScript sem erros
cd /home/rodrigo/tabelin.ia && npx tsc --project apps/web/tsconfig.json --noEmit 2>&1 | tail -5
```
</verification>

<success_criteria>
- 4 server AI streams criados com resolveXxxPayload (fixture fallback + OpenAI com JSON response_format) e createXxxEventStream (padrão NDJSON)
- 5 route handlers criados seguindo padrão auth → parse → quota → AI → confirm → record → stream
- template/generate retorna 403 com code: pro_required para usuários não-Pro (verificado antes de qualquer quota reservation)
- scripts-stream e sql-stream integram classifyDestructive como OR com o campo isDestructive do AI
- TypeScript compila sem erros em todos os novos arquivos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-multi-tool-generation-suite/03-02-SUMMARY.md`
</output>
