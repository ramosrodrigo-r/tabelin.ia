---
phase: 03-multi-tool-generation-suite
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - packages/shared/src/scripts/schema.ts
  - packages/shared/src/scripts/fixtures.ts
  - packages/shared/src/sql/schema.ts
  - packages/shared/src/sql/fixtures.ts
  - packages/shared/src/regex/schema.ts
  - packages/shared/src/regex/fixtures.ts
  - packages/shared/src/template/schema.ts
  - packages/shared/src/template/fixtures.ts
  - packages/shared/src/index.ts
  - apps/web/src/server/tools/tool-repository.ts
  - apps/web/src/server/ai/destructive-classifier.ts
  - apps/web/src/components/app/sidebar.tsx
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
  - SQL-01
  - SQL-02
  - REGX-01
  - REGX-02
  - SAFE-01
  - PRO-01

must_haves:
  truths:
    - "prisma/schema.prisma tem formulaLanguage String? e separator String? (nullable)"
    - "npx prisma db push executa sem erro após a mudança de schema"
    - "packages/shared exporta ScriptType, ScriptGenerateRequest, ScriptStreamEvent e fixtures para scripts"
    - "packages/shared exporta SqlDialect, SqlGenerateRequest, SqlStreamEvent e fixtures para SQL"
    - "packages/shared exporta RegexGenerateRequest, RegexExplainRequest, RegexStreamEvent e fixtures para regex"
    - "packages/shared exporta TemplateGenerateRequest, TemplateStreamEvent e fixtures para templates"
    - "tool-repository.ts exporta recordToolRequest que aceita toolKind genérico e persiste sem erro"
    - "destructive-classifier.ts exporta classifyDestructive(code, toolKind) retornando boolean"
    - "sidebar.tsx usa usePathname() para active state dinâmico e lista Scripts, SQL, Regex, Templates como links ativos"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "ToolRequest com formulaLanguage String? e separator String?"
      contains: "formulaLanguage String?"
    - path: "packages/shared/src/scripts/schema.ts"
      provides: "Contratos Zod para ferramenta Scripts"
      exports: ["scriptTypeSchema", "scriptGenerateRequestSchema", "scriptStreamEventSchema", "ScriptType", "ScriptGenerateRequest"]
    - path: "packages/shared/src/sql/schema.ts"
      provides: "Contratos Zod para ferramenta SQL"
      exports: ["sqlDialectSchema", "sqlGenerateRequestSchema", "sqlStreamEventSchema", "SqlDialect"]
    - path: "packages/shared/src/regex/schema.ts"
      provides: "Contratos Zod para ferramenta Regex (gerar + explicar)"
      exports: ["regexGenerateRequestSchema", "regexExplainRequestSchema", "regexStreamEventSchema"]
    - path: "packages/shared/src/template/schema.ts"
      provides: "Contratos Zod para ferramenta Templates Pro"
      exports: ["templateGenerateRequestSchema", "templateStreamEventSchema"]
    - path: "apps/web/src/server/tools/tool-repository.ts"
      provides: "Repositório genérico de ToolRequest para tools não-formula"
      exports: ["recordToolRequest"]
    - path: "apps/web/src/server/ai/destructive-classifier.ts"
      provides: "Classificação de operações destrutivas por pattern matching"
      exports: ["classifyDestructive"]
    - path: "apps/web/src/components/app/sidebar.tsx"
      provides: "Sidebar com active state dinâmico e novos links ativos"
      contains: "usePathname"
  key_links:
    - from: "packages/shared/src/scripts/schema.ts"
      to: "apps/web/src/app/api/tools/scripts/generate/route.ts"
      via: "import scriptGenerateRequestSchema from @tabelin/shared"
      pattern: "scriptGenerateRequestSchema"
    - from: "apps/web/src/server/ai/destructive-classifier.ts"
      to: "apps/web/src/server/ai/scripts-stream.ts"
      via: "classifyDestructive(code, 'script')"
      pattern: "classifyDestructive"
    - from: "prisma/schema.prisma"
      to: "apps/web/src/server/tools/tool-repository.ts"
      via: "prisma.toolRequest.create com formulaLanguage null"
      pattern: "formulaLanguage.*null"
---

<objective>
Estabelecer toda a fundação de contratos, schema e utilitários que os planos 03-02 e 03-03 consumirão.

Purpose: Os route handlers e componentes de UI dependem de schemas Zod tipados, do repositório genérico e do classificador de destrutividade. Esses artefatos devem existir antes de qualquer implementação de ferramenta. O schema Prisma deve ser atualizado e aplicado PRIMEIRO para evitar erros de constraint NOT NULL nos novos tools.

Output:
- prisma/schema.prisma com campos nullable + db push aplicado
- 4 módulos de schema Zod em packages/shared (scripts, sql, regex, template)
- 4 arquivos de fixtures em packages/shared
- packages/shared/src/index.ts exportando tudo
- apps/web/src/server/tools/tool-repository.ts (repositório genérico)
- apps/web/src/server/ai/destructive-classifier.ts (classificação de destrutividade)
- apps/web/src/components/app/sidebar.tsx atualizado com links ativos e active state dinâmico
</objective>

<execution_context>
@/home/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@/home/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-multi-tool-generation-suite/03-CONTEXT.md
@.planning/phases/03-multi-tool-generation-suite/03-RESEARCH.md
@.planning/phases/03-multi-tool-generation-suite/03-PATTERNS.md

<interfaces>
<!-- Interfaces extraídas do codebase para referência do executor -->

De packages/shared/src/formula/schema.ts:
```typescript
// Padrão de schema a replicar para cada nova ferramenta
export const formulaStreamEventSchema = z.discriminatedUnion("type", [
  z.object({ type: z.literal("metadata"), metadata: formulaMetadataSchema }),
  z.object({ type: z.literal("delta"), text: z.string() }),
  z.object({ type: z.literal("warning"), warning: z.string() }),
  z.object({ type: z.literal("quota_warning"), lastFreeUse: z.boolean() }),
  z.object({ type: z.literal("complete"), payload: formulaCompletePayloadSchema }),
  z.object({ type: z.literal("error"), message: z.string() })
]);
```

De prisma/schema.prisma (estado atual):
```prisma
model ToolRequest {
  formulaLanguage String   // MUDAR para String?
  separator       String   // MUDAR para String?
}
```

De apps/web/src/server/tools/formula-repository.ts:
```typescript
export async function recordFormulaToolRequest(input: {
  userId: string;
  metadata: FormulaMetadata;
  status: ToolRequestStatus;
  latencyMs?: number;
}) {
  try {
    return await prisma.toolRequest.create({ data: { ... } });
  } catch {
    console.warn("Formula metadata persistence skipped.");
    return null;
  }
}
```

De apps/web/src/components/app/sidebar.tsx (estado atual):
```typescript
// sidebar.tsx NÃO tem "use client" — precisa adicionar
// navItems tem Scripts, SQL, Regex como disabled: true — ativar
// active state é estático (data-active={item.active}) — tornar dinâmico via usePathname()
const navItems: NavItem[] = [
  { label: "Formula",      icon: FileSpreadsheet, href: "/workspace",          active: true },
  { label: "Scripts",      icon: Braces,          disabled: true },
  { label: "SQL",          icon: ScrollText,       disabled: true },
  { label: "Regex",        icon: Regex,            disabled: true },
  { label: "File Analysis",icon: FileText,         disabled: true },
  { label: "OCR",          icon: Image,            disabled: true }
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [BLOCKING] Atualizar Prisma schema e aplicar db push</name>
  <read_first>
    - prisma/schema.prisma (ler o estado atual dos campos formulaLanguage e separator)
    - apps/web/src/server/tools/formula-repository.ts (ver como formula usa esses campos — para não quebrar)
  </read_first>
  <files>prisma/schema.prisma, apps/web/src/server/tools/tool-repository.ts</files>
  <behavior>
    - Test 1: prisma.toolRequest.create com formulaLanguage: null deve não lançar erro de constraint
    - Test 2: prisma.toolRequest.create com formulaLanguage: "pt-BR" (existente formula) deve continuar funcionando
  </behavior>
  <action>
**Passo 1 — Modificar prisma/schema.prisma:**

Localizar o model ToolRequest e alterar exatamente estas duas linhas:
```
ANTES: formulaLanguage String
DEPOIS: formulaLanguage String?

ANTES: separator       String
DEPOIS: separator       String?
```

O restante do model ToolRequest não muda. A mudança de String para String? é backward-compatible (PostgreSQL permite NULL em colunas existentes).

**Passo 2 — Executar db push [BLOCKING]:**
```bash
cd /home/rodrigo/tabelin.ia && npx prisma db push
```
Confirmar que o output contém "Your database is now in sync with your Prisma schema." Se falhar, NÃO prosseguir com as demais tasks — o erro de constraint NOT NULL travará todos os novos tools.

**Passo 3 — Criar apps/web/src/server/tools/tool-repository.ts:**

Criar o arquivo com este conteúdo exato:
```typescript
import { prisma } from "@/server/db/client";

export type GenericToolRequestStatus = "success" | "failure";

export async function recordToolRequest(input: {
  userId: string;
  toolKind: string;
  mode: string;
  dialect?: string;  // scriptType, sqlDialect, etc.
  status: GenericToolRequestStatus;
  latencyMs?: number;
  providerModel?: string;
}) {
  try {
    return await prisma.toolRequest.create({
      data: {
        userId: input.userId,
        toolKind: input.toolKind,
        mode: input.mode,
        platform: input.dialect ?? "",
        formulaLanguage: null,   // null para tools não-formula (campo agora String?)
        separator: null,          // idem
        status: input.status,
        latencyMs: input.latencyMs,
        providerModel: input.providerModel
      }
    });
  } catch {
    console.warn("Tool request persistence skipped.");
    return null;
  }
}
```
</action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && npx prisma db push --dry-run 2>&1 | grep -E "(in sync|Drift)" || echo "PUSH_NEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - grep "String?" prisma/schema.prisma retorna duas linhas (formulaLanguage e separator)
    - grep "formulaLanguage String" prisma/schema.prisma (sem ?) retorna 0 resultados
    - apps/web/src/server/tools/tool-repository.ts existe e contém "export async function recordToolRequest"
    - apps/web/src/server/tools/tool-repository.ts contém "formulaLanguage: null"
    - npx prisma db push retorna "Your database is now in sync with your Prisma schema" sem erros
  </acceptance_criteria>
  <done>Schema Prisma atualizado com campos nullable e aplicado ao banco. tool-repository.ts criado com recordToolRequest genérico.</done>
</task>

<task type="auto">
  <name>Task 2: Criar schemas Zod e fixtures em packages/shared para scripts, SQL, regex e template</name>
  <read_first>
    - packages/shared/src/formula/schema.ts (padrão exato a replicar)
    - packages/shared/src/formula/fixtures.ts (padrão de fixtures)
    - packages/shared/src/formula/platforms.ts (padrão de constantes as const)
    - packages/shared/src/index.ts (barrel a estender)
    - .planning/phases/03-multi-tool-generation-suite/03-RESEARCH.md (Pattern 1: Shared Zod Contract)
  </read_first>
  <files>
    packages/shared/src/scripts/schema.ts,
    packages/shared/src/scripts/fixtures.ts,
    packages/shared/src/sql/schema.ts,
    packages/shared/src/sql/fixtures.ts,
    packages/shared/src/regex/schema.ts,
    packages/shared/src/regex/fixtures.ts,
    packages/shared/src/template/schema.ts,
    packages/shared/src/template/fixtures.ts,
    packages/shared/src/index.ts
  </files>
  <action>
**CRIAR packages/shared/src/scripts/schema.ts:**
```typescript
import { z } from "zod";

export const SCRIPT_TYPES = [
  { id: "vba",             label: "VBA",            highlightLang: "vba" },
  { id: "apps_script",     label: "Apps Script",    highlightLang: "javascript" },
  { id: "airtable_script", label: "Airtable Script",highlightLang: "javascript" }
] as const;

export const SCRIPT_TYPE_IDS = SCRIPT_TYPES.map((s) => s.id) as [
  "vba", "apps_script", "airtable_script"
];
export type ScriptType = (typeof SCRIPT_TYPE_IDS)[number];
export const scriptTypeSchema = z.enum(SCRIPT_TYPE_IDS);

export const scriptGenerateRequestSchema = z.object({
  scriptType: scriptTypeSchema,
  prompt: z.string().trim().min(3, "Descreva a automacao antes de gerar.")
});

export const scriptMetadataSchema = z.object({
  mode: z.literal("generate"),
  scriptType: scriptTypeSchema,
  isDestructive: z.boolean().default(false),
  providerModel: z.string().optional()
});

export const scriptGenerateResponseSchema = z.object({
  kind: z.literal("script"),
  code: z.string().trim().min(1),
  explanation: z.string().trim().min(1),
  assumptions: z.array(z.string()).default([]),
  warnings: z.array(z.string()).default([]),
  isDestructive: z.boolean().default(false),
  metadata: scriptMetadataSchema
});

export const scriptStreamEventSchema = z.discriminatedUnion("type", [
  z.object({ type: z.literal("metadata"), metadata: scriptMetadataSchema }),
  z.object({ type: z.literal("delta"), text: z.string() }),
  z.object({ type: z.literal("warning"), warning: z.string() }),
  z.object({ type: z.literal("quota_warning"), lastFreeUse: z.boolean() }),
  z.object({ type: z.literal("complete"), payload: scriptGenerateResponseSchema }),
  z.object({ type: z.literal("error"), message: z.string() })
]);

export type ScriptGenerateRequest = z.infer<typeof scriptGenerateRequestSchema>;
export type ScriptMetadata = z.infer<typeof scriptMetadataSchema>;
export type ScriptGenerateResponse = z.infer<typeof scriptGenerateResponseSchema>;
export type ScriptStreamEvent = z.infer<typeof scriptStreamEventSchema>;
```

**CRIAR packages/shared/src/scripts/fixtures.ts:**
```typescript
import type { ScriptGenerateResponse } from "./schema";

export const SCRIPT_FIXTURES: ScriptGenerateResponse[] = [
  {
    kind: "script",
    code: 'Sub CopiarDados()\n  Sheets("Vendas").Range("A:D").Copy Destination:=Sheets("Relatorio").Range("A1")\nEnd Sub',
    explanation: "Copia o intervalo A:D da aba Vendas para a aba Relatorio a partir da celula A1.",
    assumptions: ["As abas Vendas e Relatorio existem na pasta de trabalho."],
    warnings: [],
    isDestructive: false,
    metadata: { mode: "generate", scriptType: "vba", isDestructive: false, providerModel: "fixture" }
  },
  {
    kind: "script",
    code: 'function calcularTotal() {\n  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();\n  const dados = sheet.getRange("B2:B100").getValues();\n  const total = dados.reduce((acc, row) => acc + (row[0] || 0), 0);\n  sheet.getRange("B101").setValue(total);\n}',
    explanation: "Calcula a soma de todos os valores da coluna B e escreve o resultado em B101.",
    assumptions: ["A planilha ativa contém valores numéricos na coluna B, linhas 2 a 100."],
    warnings: [],
    isDestructive: false,
    metadata: { mode: "generate", scriptType: "apps_script", isDestructive: false, providerModel: "fixture" }
  }
];
```

**CRIAR packages/shared/src/sql/schema.ts:**
```typescript
import { z } from "zod";

export const SQL_DIALECTS = [
  { id: "postgresql", label: "PostgreSQL" },
  { id: "mysql",      label: "MySQL" },
  { id: "sqlserver",  label: "SQL Server" },
  { id: "oracle",     label: "Oracle" },
  { id: "bigquery",   label: "BigQuery" }
] as const;

export const SQL_DIALECT_IDS = SQL_DIALECTS.map((d) => d.id) as [
  "postgresql", "mysql", "sqlserver", "oracle", "bigquery"
];
export type SqlDialect = (typeof SQL_DIALECT_IDS)[number];
export const sqlDialectSchema = z.enum(SQL_DIALECT_IDS);

export const sqlGenerateRequestSchema = z.object({
  dialect: sqlDialectSchema,
  prompt: z.string().trim().min(3, "Descreva a consulta antes de gerar.")
});

export const sqlMetadataSchema = z.object({
  mode: z.literal("generate"),
  dialect: sqlDialectSchema,
  isDestructive: z.boolean().default(false),
  providerModel: z.string().optional()
});

export const sqlGenerateResponseSchema = z.object({
  kind: z.literal("sql"),
  query: z.string().trim().min(1),
  explanation: z.string().trim().min(1),
  assumptions: z.array(z.string()).default([]),
  warnings: z.array(z.string()).default([]),
  isDestructive: z.boolean().default(false),
  metadata: sqlMetadataSchema
});

export const sqlStreamEventSchema = z.discriminatedUnion("type", [
  z.object({ type: z.literal("metadata"), metadata: sqlMetadataSchema }),
  z.object({ type: z.literal("delta"), text: z.string() }),
  z.object({ type: z.literal("warning"), warning: z.string() }),
  z.object({ type: z.literal("quota_warning"), lastFreeUse: z.boolean() }),
  z.object({ type: z.literal("complete"), payload: sqlGenerateResponseSchema }),
  z.object({ type: z.literal("error"), message: z.string() })
]);

export type SqlGenerateRequest = z.infer<typeof sqlGenerateRequestSchema>;
export type SqlMetadata = z.infer<typeof sqlMetadataSchema>;
export type SqlGenerateResponse = z.infer<typeof sqlGenerateResponseSchema>;
export type SqlStreamEvent = z.infer<typeof sqlStreamEventSchema>;
```

**CRIAR packages/shared/src/sql/fixtures.ts:**
```typescript
import type { SqlGenerateResponse } from "./schema";

export const SQL_FIXTURES: SqlGenerateResponse[] = [
  {
    kind: "sql",
    query: "SELECT cliente_id, COUNT(*) as total_compras\nFROM pedidos\nWHERE data_pedido >= NOW() - INTERVAL '30 days'\nGROUP BY cliente_id\nHAVING COUNT(*) > 3\nORDER BY total_compras DESC;",
    explanation: "Lista clientes com mais de 3 compras nos últimos 30 dias, ordenados pelo total de compras.",
    assumptions: ["A tabela pedidos existe com colunas cliente_id e data_pedido.", "O banco de dados usa PostgreSQL com suporte a INTERVAL."],
    warnings: [],
    isDestructive: false,
    metadata: { mode: "generate", dialect: "postgresql", isDestructive: false, providerModel: "fixture" }
  }
];
```

**CRIAR packages/shared/src/regex/schema.ts:**
```typescript
import { z } from "zod";

export const regexGenerateRequestSchema = z.object({
  prompt: z.string().trim().min(3, "Descreva o padrao antes de gerar.")
});

export const regexExplainRequestSchema = z.object({
  pattern: z.string().trim().min(1, "Cole uma expressao regular antes de explicar.")
});

export const regexMetadataSchema = z.object({
  mode: z.enum(["generate", "explain"]),
  providerModel: z.string().optional()
});

export const regexGenerateResponseSchema = z.object({
  kind: z.literal("regex_generate"),
  pattern: z.string().trim().min(1),
  explanation: z.string().trim().min(1),
  examples: z.array(z.string()).default([]),
  assumptions: z.array(z.string()).default([]),
  warnings: z.array(z.string()).default([]),
  metadata: regexMetadataSchema.extend({ mode: z.literal("generate") })
});

export const regexExplainResponseSchema = z.object({
  kind: z.literal("regex_explain"),
  pattern: z.string().trim().min(1),
  steps: z.array(z.string().trim().min(1)).min(1),
  assumptions: z.array(z.string()).default([]),
  warnings: z.array(z.string()).default([]),
  metadata: regexMetadataSchema.extend({ mode: z.literal("explain") })
});

export const regexCompletePayloadSchema = z.discriminatedUnion("kind", [
  regexGenerateResponseSchema,
  regexExplainResponseSchema
]);

export const regexStreamEventSchema = z.discriminatedUnion("type", [
  z.object({ type: z.literal("metadata"), metadata: regexMetadataSchema }),
  z.object({ type: z.literal("delta"), text: z.string() }),
  z.object({ type: z.literal("warning"), warning: z.string() }),
  z.object({ type: z.literal("quota_warning"), lastFreeUse: z.boolean() }),
  z.object({ type: z.literal("complete"), payload: regexCompletePayloadSchema }),
  z.object({ type: z.literal("error"), message: z.string() })
]);

export type RegexGenerateRequest = z.infer<typeof regexGenerateRequestSchema>;
export type RegexExplainRequest = z.infer<typeof regexExplainRequestSchema>;
export type RegexMetadata = z.infer<typeof regexMetadataSchema>;
export type RegexGenerateResponse = z.infer<typeof regexGenerateResponseSchema>;
export type RegexExplainResponse = z.infer<typeof regexExplainResponseSchema>;
export type RegexCompletePayload = z.infer<typeof regexCompletePayloadSchema>;
export type RegexStreamEvent = z.infer<typeof regexStreamEventSchema>;
```

**CRIAR packages/shared/src/regex/fixtures.ts:**
```typescript
import type { RegexGenerateResponse, RegexExplainResponse } from "./schema";

export const REGEX_GENERATE_FIXTURES: RegexGenerateResponse[] = [
  {
    kind: "regex_generate",
    pattern: "^\\d{3}\\.\\d{3}\\.\\d{3}-\\d{2}$",
    explanation: "Valida um CPF no formato com pontos e traço: 000.000.000-00.",
    examples: ["123.456.789-09", "000.000.000-00"],
    assumptions: ["O CPF já está formatado com pontos e traço antes da validação."],
    warnings: [],
    metadata: { mode: "generate", providerModel: "fixture" }
  }
];

export const REGEX_EXPLAIN_FIXTURES: RegexExplainResponse[] = [
  {
    kind: "regex_explain",
    pattern: "^\\d{3}\\.\\d{3}\\.\\d{3}-\\d{2}$",
    steps: [
      "^ ancora no início da string.",
      "\\d{3} casa exatamente 3 dígitos numéricos.",
      "\\. casa um ponto literal (escapado).",
      "\\d{3} repete o grupo de 3 dígitos.",
      "\\. outro ponto literal.",
      "\\d{3} terceiro grupo de 3 dígitos.",
      "- casa o traço literal.",
      "\\d{2} casa exatamente 2 dígitos (dígito verificador).",
      "$ ancora no fim da string."
    ],
    assumptions: [],
    warnings: [],
    metadata: { mode: "explain", providerModel: "fixture" }
  }
];
```

**CRIAR packages/shared/src/template/schema.ts:**
```typescript
import { z } from "zod";

export const templateGenerateRequestSchema = z.object({
  prompt: z.string().trim().min(3, "Descreva o tipo de planilha antes de gerar.")
});

export const templateMetadataSchema = z.object({
  mode: z.literal("generate"),
  providerModel: z.string().optional()
});

export const templateGenerateResponseSchema = z.object({
  kind: z.literal("template"),
  output: z.string().trim().min(1),  // Markdown ou CSV copy-ready
  explanation: z.string().trim().min(1),
  assumptions: z.array(z.string()).default([]),
  warnings: z.array(z.string()).default([]),
  metadata: templateMetadataSchema
});

export const templateStreamEventSchema = z.discriminatedUnion("type", [
  z.object({ type: z.literal("metadata"), metadata: templateMetadataSchema }),
  z.object({ type: z.literal("delta"), text: z.string() }),
  z.object({ type: z.literal("warning"), warning: z.string() }),
  z.object({ type: z.literal("quota_warning"), lastFreeUse: z.boolean() }),
  z.object({ type: z.literal("complete"), payload: templateGenerateResponseSchema }),
  z.object({ type: z.literal("error"), message: z.string() })
]);

export type TemplateGenerateRequest = z.infer<typeof templateGenerateRequestSchema>;
export type TemplateMetadata = z.infer<typeof templateMetadataSchema>;
export type TemplateGenerateResponse = z.infer<typeof templateGenerateResponseSchema>;
export type TemplateStreamEvent = z.infer<typeof templateStreamEventSchema>;
```

**CRIAR packages/shared/src/template/fixtures.ts:**
```typescript
import type { TemplateGenerateResponse } from "./schema";

export const TEMPLATE_FIXTURES: TemplateGenerateResponse[] = [
  {
    kind: "template",
    output: "# Controle de Gastos Mensais\n\n| Data | Categoria | Descricao | Valor (R$) | Tipo |\n|------|-----------|-----------|------------|------|\n| 01/01/2025 | Alimentacao | Supermercado | 350,00 | Saida |\n\n**Total do mes:** =SOMA(D2:D100)\n**Total por categoria:** =SOMASE(B:B;\"Alimentacao\";D:D)",
    explanation: "Template de controle de gastos com colunas de data, categoria, descrição, valor e tipo. Inclui fórmulas de total mensal e subtotal por categoria no estilo Excel pt-BR.",
    assumptions: ["Usa separador de ponto-e-vírgula para fórmulas pt-BR.", "Valores em reais (R$) com vírgula decimal."],
    warnings: [],
    metadata: { mode: "generate", providerModel: "fixture" }
  }
];
```

**MODIFICAR packages/shared/src/index.ts** — adicionar exports para todos os novos módulos:
```typescript
export * from "./formula/fixtures";
export * from "./formula/platforms";
export * from "./formula/schema";
export * from "./billing/schema";
// Fase 3: novos tools
export * from "./scripts/schema";
export * from "./scripts/fixtures";
export * from "./sql/schema";
export * from "./sql/fixtures";
export * from "./regex/schema";
export * from "./regex/fixtures";
export * from "./template/schema";
export * from "./template/fixtures";
```
</action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && npx tsc --project packages/shared/tsconfig.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/scripts/schema.ts existe e contém "export const SCRIPT_TYPES"
    - packages/shared/src/sql/schema.ts existe e contém "export const SQL_DIALECTS"
    - packages/shared/src/regex/schema.ts existe e contém "regexCompletePayloadSchema"
    - packages/shared/src/template/schema.ts existe e contém "templateGenerateRequestSchema"
    - packages/shared/src/index.ts contém "export * from \"./scripts/schema\""
    - packages/shared/src/index.ts contém "export * from \"./sql/schema\""
    - packages/shared/src/index.ts contém "export * from \"./regex/schema\""
    - packages/shared/src/index.ts contém "export * from \"./template/schema\""
    - npx tsc --project packages/shared/tsconfig.json --noEmit termina sem erros de tipo
  </acceptance_criteria>
  <done>8 arquivos novos criados em packages/shared (4 schemas + 4 fixtures). index.ts atualizado com todos os exports. TypeScript compila sem erros.</done>
</task>

<task type="auto">
  <name>Task 3: Criar destructive-classifier e atualizar sidebar com active state dinâmico</name>
  <read_first>
    - apps/web/src/components/app/sidebar.tsx (estado atual — verificar imports existentes e estrutura)
    - .planning/phases/03-multi-tool-generation-suite/03-CONTEXT.md (D-08, D-09 — padrões destrutivos)
    - .planning/phases/03-multi-tool-generation-suite/03-RESEARCH.md (Pattern 3: Safety Classification)
    - .planning/phases/03-multi-tool-generation-suite/03-PATTERNS.md (seção sidebar e destructive-classifier)
  </read_first>
  <files>
    apps/web/src/server/ai/destructive-classifier.ts,
    apps/web/src/components/app/sidebar.tsx
  </files>
  <action>
**CRIAR apps/web/src/server/ai/destructive-classifier.ts:**

```typescript
import "server-only";

// SQL: padrões destrutivos conforme D-08 do CONTEXT.md
// DROP, TRUNCATE, DELETE sem WHERE, UPDATE sem WHERE
const SQL_DESTRUCTIVE_PATTERNS = [
  /\bDROP\b/i,
  /\bTRUNCATE\b/i,
  // DELETE sem WHERE: presença de DELETE sem nenhum WHERE na mesma string
  /\bDELETE\b/i,  // será complementado pelo campo isDestructive da resposta AI
  /\bUPDATE\b(?![^;]*\bWHERE\b)/i
] as const;

// Padrão robusto para DELETE: verificar ausência de WHERE após DELETE
// Usa abordagem de dois passos: match DELETE + verificar se há WHERE na instrução
function hasSqlDestructiveDelete(code: string): boolean {
  // Dividir por ponto-e-vírgula para analisar cada instrução separadamente
  const statements = code.split(/;/);
  return statements.some((stmt) => {
    const trimmed = stmt.trim().toUpperCase();
    return trimmed.startsWith("DELETE") && !trimmed.includes("WHERE");
  });
}

// Scripts (VBA, Apps Script, Airtable): padrões destrutivos conforme D-09 do CONTEXT.md
const SCRIPT_DESTRUCTIVE_PATTERNS = [
  /\bDeleteFile\b/i,
  /\bKill\b/i,
  /\.Rows\.Delete\b/i,
  /DriveApp\.remove/i,
  /\.deleteSheet\b/i,
  /\.deleteRecord\b/i,
  /\.deleteRecords\b/i
] as const;

/**
 * Classifica se um código gerado contém operações destrutivas.
 * Chamado no servidor após geração, antes de montar o event stream.
 * O campo isDestructive da resposta AI é o sinal primário;
 * este classificador é o fallback determinístico.
 */
export function classifyDestructive(code: string, toolKind: "sql" | "script"): boolean {
  if (toolKind === "sql") {
    // Verificar DROP, TRUNCATE, UPDATE sem WHERE
    const hasOtherDestructive = [
      /\bDROP\b/i,
      /\bTRUNCATE\b/i,
      /\bUPDATE\b(?![^;]*\bWHERE\b)/i
    ].some((pattern) => pattern.test(code));

    // Verificar DELETE sem WHERE com análise por instrução
    return hasOtherDestructive || hasSqlDestructiveDelete(code);
  }

  // toolKind === "script"
  return SCRIPT_DESTRUCTIVE_PATTERNS.some((pattern) => pattern.test(code));
}

/**
 * Retorna a mensagem de warning específica para o tipo de operação destrutiva detectada.
 * Mensagens conforme UI-SPEC.md Safety Warning Contract.
 */
export function getDestructiveMessage(code: string, toolKind: "sql" | "script"): string {
  if (toolKind === "sql") {
    if (/\bDROP\b/i.test(code) || /\bTRUNCATE\b/i.test(code)) {
      return "Este script apaga dados permanentemente. Faca um backup antes de executar.";
    }
    if (hasSqlDestructiveDelete(code)) {
      return "DELETE sem WHERE apaga todas as linhas da tabela. Verifique a clausula WHERE antes de executar.";
    }
    if (/\bUPDATE\b(?![^;]*\bWHERE\b)/i.test(code)) {
      return "UPDATE sem WHERE altera todos os registros. Adicione uma clausula WHERE para limitar o impacto.";
    }
    return "Esta operacao pode modificar ou apagar dados permanentemente. Revise antes de executar.";
  }

  // script
  if (/\bDeleteFile\b/i.test(code) || /\bKill\b/i.test(code)) {
    return "Este script exclui arquivos do sistema. Confirme os caminhos antes de executar.";
  }
  return "Esta operacao remove dados da planilha ou base permanentemente. Nao pode ser desfeita.";
}
```

**MODIFICAR apps/web/src/components/app/sidebar.tsx:**

Substituir o conteúdo completo do arquivo por:
```typescript
"use client";

import type { LucideIcon } from "lucide-react";
import { Braces, FileSpreadsheet, FileText, Image, LayoutTemplate, Regex, ScrollText } from "lucide-react";
import Link from "next/link";
import { usePathname } from "next/navigation";

type NavItem =
  | { label: string; icon: LucideIcon; href: string; disabled?: never }
  | { label: string; icon: LucideIcon; disabled: true; href?: never };

const navItems: NavItem[] = [
  { label: "Formula",      icon: FileSpreadsheet, href: "/workspace" },
  { label: "Scripts",      icon: Braces,          href: "/workspace/scripts" },
  { label: "SQL",          icon: ScrollText,       href: "/workspace/sql" },
  { label: "Regex",        icon: Regex,            href: "/workspace/regex" },
  { label: "Templates",    icon: LayoutTemplate,   href: "/workspace/templates" },
  { label: "File Analysis",icon: FileText,         disabled: true },
  { label: "OCR",          icon: Image,            disabled: true }
];

export function Sidebar() {
  const pathname = usePathname();

  return (
    <aside className="sidebar" aria-label="Ferramentas">
      <div className="sidebar-brand">
        <strong>Tabelin.IA</strong>
        <span>Planilhas, codigo e dados</span>
      </div>
      <nav className="sidebar-nav">
        {navItems.map((item) => {
          const Icon = item.icon;
          const content = (
            <>
              <Icon aria-hidden size={18} />
              <span>{item.label}</span>
            </>
          );

          if (item.disabled) {
            return (
              <span className="nav-item" data-disabled="true" key={item.label} aria-disabled="true">
                {content}
              </span>
            );
          }

          // Active state dinâmico: exact match para /workspace (Formula),
          // startsWith para sub-rotas das demais ferramentas
          const isActive =
            item.href === "/workspace"
              ? pathname === "/workspace"
              : pathname === item.href || pathname.startsWith(item.href + "/");

          return (
            <Link className="nav-item" data-active={isActive} href={item.href} key={item.label}>
              {content}
            </Link>
          );
        })}
      </nav>
    </aside>
  );
}
```

**IMPORTANTE sobre o active state de /workspace:**
- `/workspace` deve ser exato (não startsWith), pois `/workspace/scripts` não deve ativar "Formula".
- `/workspace/scripts` ativa com startsWith(`/workspace/scripts/`) ou exact match.
</action>
  <verify>
    <automated>cd /home/rodrigo/tabelin.ia && npx tsc --project apps/web/tsconfig.json --noEmit 2>&1 | grep -E "destructive-classifier|sidebar" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/server/ai/destructive-classifier.ts existe e contém "export function classifyDestructive"
    - apps/web/src/server/ai/destructive-classifier.ts contém "export function getDestructiveMessage"
    - apps/web/src/server/ai/destructive-classifier.ts contém "import \"server-only\""
    - apps/web/src/components/app/sidebar.tsx contém "\"use client\""
    - apps/web/src/components/app/sidebar.tsx contém "usePathname"
    - apps/web/src/components/app/sidebar.tsx contém "LayoutTemplate"
    - apps/web/src/components/app/sidebar.tsx contém "/workspace/scripts"
    - apps/web/src/components/app/sidebar.tsx contém "/workspace/sql"
    - apps/web/src/components/app/sidebar.tsx contém "/workspace/regex"
    - apps/web/src/components/app/sidebar.tsx contém "/workspace/templates"
    - grep "disabled: true" apps/web/src/components/app/sidebar.tsx | wc -l retorna 2 (File Analysis e OCR ainda disabled)
    - npx tsc --project apps/web/tsconfig.json --noEmit não retorna erros relacionados a sidebar.tsx ou destructive-classifier.ts
  </acceptance_criteria>
  <done>destructive-classifier.ts criado com classifyDestructive e getDestructiveMessage. sidebar.tsx atualizado com "use client", usePathname, LayoutTemplate importado, e links ativos para scripts/sql/regex/templates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared package → API routes | Schemas Zod são importados tanto pelo client quanto pelo server — devem ser seguros como contratos, não executar lógica server-only |
| Sidebar (client) → rotas de tool | O estado de nav é puramente visual; autenticação real é feita pelos route handlers e Server Components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | destructive-classifier.ts — pattern matching | mitigate | Classificador usa regex determinístico sobre o código gerado; o campo `isDestructive` da resposta AI é o sinal primário. Pattern matching é fallback — resultados são OR'd para cobertura máxima (falso positivo é aceitável, falso negativo não é) |
| T-03-01-02 | Elevation of Privilege | sidebar.tsx — active state | accept | Active state é visual (CSS `data-active`); não controla acesso. Acesso real é verificado nos Server Components e route handlers de cada ferramenta |
| T-03-01-03 | Tampering | packages/shared schemas — Zod safeParse | mitigate | Todos os schemas usam `.safeParse()` nos route handlers antes de qualquer processamento; inputs inválidos retornam 400 antes de reservar quota |
| T-03-01-04 | Information Disclosure | destructive-classifier.ts — import "server-only" | mitigate | `import "server-only"` garante que o módulo não seja bundled para o cliente. Next.js lança erro de build se houver importação client-side acidental |
</threat_model>

<verification>
```bash
# Verificar schema Prisma atualizado
grep "String?" /home/rodrigo/tabelin.ia/prisma/schema.prisma | grep -E "formulaLanguage|separator"

# Verificar exports do shared package
grep "scripts\|sql\|regex\|template" /home/rodrigo/tabelin.ia/packages/shared/src/index.ts

# Verificar TypeScript compila sem erros
cd /home/rodrigo/tabelin.ia && npx tsc --project packages/shared/tsconfig.json --noEmit

# Verificar sidebar usa usePathname
grep "usePathname" /home/rodrigo/tabelin.ia/apps/web/src/components/app/sidebar.tsx

# Verificar destructive-classifier existe
ls /home/rodrigo/tabelin.ia/apps/web/src/server/ai/destructive-classifier.ts
```
</verification>

<success_criteria>
- prisma/schema.prisma tem `formulaLanguage String?` e `separator String?` e db push aplicado sem erro
- 8 arquivos novos em packages/shared (4 schemas + 4 fixtures) com TypeScript sem erros de tipo
- packages/shared/src/index.ts exporta todos os novos módulos
- tool-repository.ts criado com recordToolRequest genérico que aceita null para campos formula-específicos
- destructive-classifier.ts criado com classifyDestructive e getDestructiveMessage, protegido por server-only
- sidebar.tsx atualizado: "use client", usePathname para active state dinâmico, LayoutTemplate importado, 5 novos links ativos (Formula, Scripts, SQL, Regex, Templates), File Analysis e OCR ainda disabled
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-multi-tool-generation-suite/03-01-SUMMARY.md`
</output>
